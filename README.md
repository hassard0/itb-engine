# ITB Engine — Information-Theoretic Bootstrap for Quantum Gravity

**A research platform that constrains the space of possible quantum-gravity theories by imposing every consistency condition we can encode — amplitude positivity, causality, holographic-information bounds, the swampland, anomaly flow, black-hole thermodynamics — then confronts the survivors with *real experimental data* and asks what's left.**

> **Status:** v2.450 research loop · **local and remote Linux validation current** · **[Research Report II](docs/results/2026-07-02-v2.428-RESEARCH-REPORT-II.md) = current synthesis** · **UV completion: string/heterotic LEADING but NOT unique -- CDT + asymptotic safety also reach the candidate (only LQG excluded); low-energy EFT can't discriminate them** · **38 theoretical constraints (+4 ingested-data), rigor-tagged, opt-in extensions** · **14 framework encoders** · **10 Wilson coefficients** · **9 observables** · **561 indexed result notes**
>
> Honest by construction: most constraint prefactors are O(1) placeholders ("the right streets, the wrong house numbers"). A dedicated **realism program** stress-tests every headline claim against that toy-prefactor uncertainty, so the repo distinguishes *robust conclusions* from *artifacts*. Read every claim below with that in mind — and see [Honest limitations](#honest-limitations).

## Findings

**> Read [docs/FINDINGS.md](docs/FINDINGS.md) for the clean, current summary of what the engine has found** — the single near-unique candidate quantum-gravity EFT, its most robust predictions (matter-gravity locking, ghost-safety, string-like towers, near-Planckian cutoff), the decisive **CMB-S4** make-or-break test, and the honest limitations. The full live-verified profile is the [**v2.402 capstone**](docs/results/2026-07-01-v2.402-qnm-candidate-profile-refreshed.md).

The reverse-chronological research log below (newest first) is a per-cycle **changelog**, not a summary — start with FINDINGS.md, then dive into any cycle's dated note under `docs/results/`.

**Start here:** [**v2.450 the candidate, written out: the explicit low-energy Lagrangian as a concrete field theory**](docs/results/2026-07-02-v2.450-qnm-explicit-lagrangian.md) (capstone -- after ~560 notes the program had never assembled the candidate in ONE place; writes it down as metric + two scalars + matter: L/sqrt(-g) = (M_Pl^2/2)R [Einstein-Hilbert = PRESUPPOSED graviton, not carved] + g_R2 R^2 [~0.19, rigorous>0, scalaron phi = inflation+dark-energy] + g_R3 R^3 [~0.09] + g_R4 R^4 [>=0.042 opt-in tower] + g_C C^2 (Weyl^2) [~0.19, >=0 rigorous by Hofman-Maldacena, ghost above cutoff + BH-entropy] + theta g_R2_parity R^R-tilde [~0.06, data-selected, axion theta = cosmic birefringence = the heterotic model-independent axion] + matter dim-8 tower g_4 O_4+g_6 O_6+g_8 O_8+g_10 O_10 [g_4~0.53 DOMINANT, drives ~2/3 of constraints + caps gravity <=40%] + Lambda_vac [g_Lambda<=g_R2 opt-in] + ~3D dark hidden sector [unprobed]; SCALES: UV cutoff Lambda_species~0.72 M_Pl (Dvali, N~1.6 species) + scalaron mass M~3e13 GeV (fixed by A_s~2.1e-9 = the one solidly dimensionful number); each term carries its rigor tier (most RIGOROUS incl the matter tower + curvature keystone, vacuum=sourced_proxy, parity=the single data-selected magnitude); candidate feasible vs all 42 constraints (verified); a matter-dominant near-Planckian ghost-safe string-like EFT with TWO cosmological scalars of opposite parity whose gravity is forced-into-existence + capped by its matter; honest scope -- a SYNTHESIS/presentation artifact (no new physics), the operator structure is SCHEMATIC (R^2/R^3/C^2 + matter O_4..O_10 are operator CLASSES not a fully index-contracted canonicalized basis, O_k generic dim-(8-14) matter self-interactions not a specified multiplet), all g_i O(1)-toy except the noted rigorous bounds, specific VALUES = the Chebyshev point carrying the constructed-point assumptions (a=c,g_6=g_8,g_R3_parity=0), the 2 scales are the genuine anchors (cutoff toy-normalized, scalaron mass from the REAL A_s), feasibility is a real engine check; 5/5 checks) · [**v2.449 the falsification tracker: what would KILL the candidate (the actionable dual of the consilience)**](docs/results/2026-07-02-v2.449-qnm-falsification-tracker.md) (v2.446 catalogued what AGREES on the candidate; this builds the DUAL = a single actionable map of what would DISPROVE it -- for each prediction the observable + kill-threshold + current status; 10 predictions, 9 near-term falsifiers, 7 favorable/consistent, 0 currently excluded: birefringence beta>0 [kill beta<=0/wrong-handedness, ~3.6sig hint favorable], dark-energy w>-1 thawing [kill genuine phantom w<-1 or wa>0, DESI hints w0>-1 watch the CPL phantom-past], inflation r~0.004 [kill r>>0.01, consistent r<0.036], n_s~0.964 [kill far from 0.965, matches Planck], matter dominance g_4 [kill CMB-S4 sees pure single-field, open], the g_R2 OVER-DETERMINATION [kill LiteBIRD-plateau-but-DESI-phantom or vice versa = the SHARPEST single falsifier], GW speed=c/no dispersion [consistent GW170817], massless graviton [consistent], R^2-bearing UV completion [established LQG-excluded/type-II-disfavored], + primordial marked NON-discriminating (a primordial null does NOT kill it = plateau-class, v2.444/2.447 => falsification effort belongs on late-time+joint-consistency fronts NOT primordial); the 2 most decisive near-term tests = the birefringence HANDEDNESS + the DESI-LiteBIRD g_R2 CO-OCCURRENCE; the practical companion to near-uniqueness -- consilience says many principles converge here, the tracker says here's exactly how to prove it wrong; honest scope -- a CONSOLIDATION/accounting artifact NOT a new result (re-organizes v2.421/2.442/2.430/2.411 + adds explicit thresholds+status), predictions carry original tiers (birefringence a ~3.6sig HINT not detection, w>-1 the refined-dS proxy + the DESI CPL phantom-past is a real tension honestly tracked, r/n_s plateau-CLASS = confirm the class not uniquely the candidate, g_4 O(1)-toy, kill-thresholds approximate illustrative levels not computed exclusion contours), value is organizational+honest not stronger than the underlying results; 5/5 checks) · [**v2.448 the candidate's cosmological field content: TWO distinct scalars of opposite parity -- a parity-even scalaron (DE/inflation) + a parity-odd axion (birefringence)**](docs/results/2026-07-02-v2.448-qnm-two-scalars.md) (structural clarification the program left implicit; it kept attributing DE/inflation to 'the g_R2 scalaron' + birefringence to 'the parity coupling' without making explicit these are DIFFERENT FIELDS -- by PARITY: R^2 (g_R2) is parity-EVEN => via f(R)->scalar-tensor duality it propagates a spin-0 SCALARON phi = the inflaton (early, Starobinsky plateau) AND the dark-energy field (late, R^2 plateau w>-1); R^R-tilde (g_R2_parity) is parity-ODD + topological alone so to be dynamical it multiplies a pseudoscalar AXION theta (gravitational Chern-Simons = the heterotic model-independent axion) whose slow roll today rotates CMB polarization => birefringence; phi + theta have OPPOSITE parity => CANNOT be the same field => the candidate's late universe is a TWO-FIELD system; they coexist consistently (small measured beta~0.3deg => small axion excursion x coupling => rho_theta far SUBDOMINANT to the scalaron DE, phi dominates rho_DE + sets w, theta is a light subdominant birefringence field); RESOLVES an implicit ambiguity -- the early 'dark-energy axion' (v1.46-47) vs the 'g_R2 scalaron dark energy' (v2.422-425): in the current candidate the DARK ENERGY = the parity-even scalaron + the AXION = a SEPARATE parity-odd field for birefringence NOT the bulk DE; the two cosmological keystones (g_R2, g_R2_parity, each over-determined v2.442-443) map to TWO physically distinct fields; honest scope -- the field IDs are STANDARD EFT facts (R^2<->scalaron f(R) duality, theta R^R-tilde<->axion), the robust theorem-level content = opposite parity => distinct fields, the 'axion subdominant' consistency is ORDER-OF-MAGNITUDE from small beta (NOT a computed 2-field cosmology, the axion potential/mass is NOT computed + model-dependent so subdominant-quintessence-vs-DM-vs-radiation is open), coupling hierarchy 0.06<0.19 used as plausibility (O(1)-toy), adds NO constraint/observable = a structural clarification updating the v1.46-47 assignment; 5/5 checks) · [**v2.447 closing the primordial-observability question: the candidate has NO cosmological-collider signal, so its inflation is observationally generic Starobinsky**](docs/results/2026-07-02-v2.447-qnm-cosmological-collider.md) (fresh probe that CLOSES a question; the cosmological collider (Arkani-Hamed-Maldacena 2015) reads off m~H particles during inflation via oscillatory squeezed non-Gaussianity, needing m in a window ~(0.1-few)H (signal exp(-pi m/H)-suppressed for m>>H); the candidate's inflationary states are both OUT of the window -- the swampland tower is at m/H~625-125,000 (species scale ~0.8 M_Pl descending to ~0.004 M_Pl over the scalaron roll vs H~6e-6 M_Pl, FAR too heavy, exp(-pi m/H)~0) + the model-independent axion (the parity) is nearly massless (shift-symmetry protected, m/H~1e-3, FAR too light, a spectator not an oscillatory m~H signature) => NO state in the collider window => NO cosmological-collider signal; combined with the chiral GW Planckian-suppressed (Pi~1e-6, v2.444) + the scalar (n_s,r) plateau-CLASS (v2.442), this CLOSES the primordial-observability question -- the candidate's inflation is at every achievable precision indistinguishable from generic single-field Starobinsky => ALL candidate-specific discrimination is LATE-TIME (cosmic birefringence for parity + dark-energy w for the g_R2 plateau) + the multi-front over-determination cross-checks (v2.442-443); the early universe confirms the plateau CLASS not THIS candidate = a clean honest closure telling the observational program where NOT to look (primordial) + where the leverage IS (late-time), concentrating the falsification effort; honest scope -- order-of-magnitude observability argument (not a computed non-Gaussianity), toy species-scale tower + standard Starobinsky H, robust content = the huge separation from the window (survives large uncertainty, tower >100x above even at its lightest), exp(-pi m/H) is the standard collider Boltzmann factor, 'axion ~massless during inflation' assumes unbroken shift symmetry at the inflationary scale (mild model-dependent caveat), a NEGATIVE observability result completing the primordial-suppression picture; 5/5 checks) · [**v2.446 the convergent consistency backbone (CONSILIENCE): the candidate is the single point where SEVEN independent areas of theoretical physics + six measurements all agree**](docs/results/2026-07-02-v2.446-qnm-consilience.md) (META-result stepping up from the recent 'principle X agrees on the candidate' cycles instead of adding an 8th in the same shape; grouping the engine's 42 constraints by INDEPENDENT physical principle => the candidate is the intersection of SEVEN distinct theoretical-consistency areas + SIX measurements: (1) S-matrix analyticity/unitarity [14 constraints, all rigorous], (2) causality/CEMZ [4], (3) holography/conformal-collider [Hofman-Maldacena + CFT bootstrap + subadditivity, 3], (4) swampland/QG [WGC+distance+species+RFC, 5], (5) black-hole thermodynamics [GSL+quantum-focusing+Bekenstein+Wald, 5], (6) anomalies/topology [3], (7) EFT self-consistency [2] + observational data [birefringence + GW speed/dispersion + sub-mm + LIGO x2, 6]; these areas are GENUINELY INDEPENDENT (positivity doesn't imply causality, causality doesn't imply the swampland, the swampland doesn't imply BH-entropy positivity, none imply anomaly matching) yet the candidate satisfies ALL at one point => the near-uniqueness is a CONSILIENCE (convergence of independent lines like a scientific fact confirmed independently) NOT the output of one rule that might be wrong; even the RIGOROUS core alone (19 zero-toy constraints) spans THREE of the 7 areas (analyticity+causality+holography) so the multi-principle convergence is NOT an artifact of the conjectural tiers; recent dream-arc additions slot in as pillars (BH-entropy v2.445 = thermodynamics, ESC v2.440 = swampland refinement, amplitude tower = analyticity); honest headline = not 'the candidate is right' but 'the unique point consistent with 7 independent theoretical areas + 6 measurements at once', force scales with the NUMBER of independent principles + falsifiable by ANY ONE; honest scope -- the 7-area grouping is a physically-motivated organization NOT a theorem about independence (known interconnections exist: causality<->positivity via dispersion, WGC<->BH-entropy via Cheung-Liu-Remmen = the v2.445 link), only analyticity/causality/holography are source-exact rigorous (swampland/thermo/anomaly/EFT-validity are sourced_proxy + data carry measurement caveats incl the ~3.6sigma birefringence), robust sub-claim = the rigorous core spans 3 independent areas (verified), 'consilience' is an EPISTEMIC reframe not new physics; 5/5 checks) · [**v2.445 a new thermodynamic sector: the candidate's neutral (Schwarzschild) BH entropy shift is positive, and causality + the black-hole second law AGREE on its Weyl^2 sign**](docs/results/2026-07-02-v2.445-qnm-neutral-bh-entropy.md) (NEW axis outside the cosmology/UV arc; higher-derivative corrections shift a BH's entropy at fixed mass + the SIGN is tied to EFT consistency (Cheung-Liu-Remmen/Goon-Penco/Reall-Santos); v2.378 covered the CHARGED-EXTREMAL case (positive shift <=> WGC), the NEUTRAL Schwarzschild sector is untouched + is controlled by the Weyl^2 coupling g_C (Ricci-flat => R^2/Ricci^2 vanish => leading Wald correction ~ +g_C); ENGINE FACT (verified) -- g_C>=0 is RIGOROUSLY forced (g_C<0 violates the Hofman-Maldacena conformal-collider wedge, g_C=0 marginal, g_C>0 feasible) INDEPENDENT of the a=c constructed-point assumption (which fixes only the VALUE g_C=g_R2=0.19 not the sign); so TWO independent principles converge on the Weyl^2 sign -- CAUSALITY/conformal-collider forces g_C>=0 (verified) + the BLACK-HOLE SECOND LAW (Reall-Santos/CLR entropy positivity, Delta S~+g_C) wants g_C>=0, both SAME sign + the candidate sits on it (g_C>0) => the candidate predicts a POSITIVE Schwarzschild entropy shift, extending the extremal-charged WGC positivity (v2.378) to the neutral sector + a genuine cross-consistency between 2 independent consistency principles on ONE coupling => adds a THERMODYNAMIC pillar to the rigorous backbone (positivity/causality/bootstrap/swampland) all mutually consistent on the same point; honest scope -- the g_C>=0 positivity is a RIGOROUS engine result (independent of a=c, only the VALUE uses a=c) but 'Delta S~+g_C' is SIGN-LEVEL not a computed coefficient (uses the standard Ricci-flat Wald result, the g_C<->Weyl^2-Wald-coeff identification is a defensible assumption), robust claim = the SIGN CONVERGENCE not a computed entropy value, magnitude Planck-suppressed (a consistency statement not an observable), whether second-law positivity is STRICT vs a strong conjecture is itself debated; 5/5 checks) · [**v2.444 computing the v2.443 crux: the chiral-GW amplitude is Planckian-suppressed (Pi~1e-6), UNOBSERVABLE -- an HONEST NEGATIVE tempering the fingerprint**](docs/results/2026-07-02-v2.444-qnm-chiral-gw-amplitude.md) (v2.443 flagged the chiral-GW AMPLITUDE as uncomputed/'plausibly suppressed'; COMPUTE it (order-of-magnitude) + resolve the crux honestly -- Chern-Simons GW chirality scales Pi~H_inf/M_CS (weak regime); the engine's parity coupling g_R2_parity~0.06 is an O(1) Planck-scale coefficient of the dim-4 R^R-tilde operator so M_CS is PLANCKIAN (~M_Pl/sqrt(g_R2_parity)~4 M_Pl), and with H_inf~6e-6 M_Pl (v2.441) => Pi~1.6e-6 = ~4.8 ORDERS below the CMB tensor-parity threshold (~0.1); the only route to Pi~O(1) = the EXPONENTIAL-AMPLIFICATION regime (one helicity tachyonically produced) needs a super-Planckian axion velocity / sub-Planckian CS scale the candidate's O(1) Planck-scale coupling doesn't provide; HONEST VERDICT -- the primordial chirality is REAL in sign (v2.443 stands as a sign/structure statement) but UNOBSERVABLE in amplitude under the engine's normalization, so it does NOT break the plateau-class degeneracy at the CMB after all => the candidate's inflation is degenerate with generic Starobinsky in BOTH the scalar (n_s,r) AND tensor-parity (Pi) channels at achievable precision (LiteBIRD tests the CLASS), and the candidate's parity is observable via its LATE-TIME birefringence + the over-determination cross-checks NOT any primordial-inflationary observable; tempers v2.443 from 'candidate-specific observable' -> 'candidate-specific in principle but Planckian-suppressed hence unobservable' = an honest negative keeping the claims calibrated; honest scope -- ORDER-OF-MAGNITUDE (Pi~H/M_CS is the correct PARAMETRIC scaling but the O(1) prefactor/k-dependence uncomputed + the g_R2_parity->M_CS map is a normalization ASSUMPTION), robust content = the parametric Planckian-suppression (survives O(1)-O(100) prefactor uncertainty), the escape hatch (sub-Planckian f_a<<M_Pl or amplification -> larger Pi) is OUTSIDE the engine's O(1) Planck-scale setup so this is a negative ON THE ENGINE'S NORMALIZATION not a theorem for every parity completion; 5/5 checks) · [**v2.443 the candidate-specific inflationary fingerprint: CHIRAL primordial gravitational waves from the parity coupling (breaks the plateau-class degeneracy in the TENSOR sector, over-determines the parity keystone)**](docs/results/2026-07-02-v2.443-qnm-chiral-gw.md) (DREAM -- v2.442 flagged the scalar (n_s~0.964,r~0.004) as plateau-CLASS = degenerate with generic Starobinsky, so is there a candidate-SPECIFIC inflationary signature? YES, in the TENSOR PARITY: the candidate's parity coupling g_R2_parity (gravitational Chern-Simons R^R-tilde = the heterotic model-independent axion, v2.434) is time-dependent during inflation (the axion rolls), and a time-dependent grav-CS term gives the two graviton helicities DIFFERENT amplitudes = a CHIRAL primordial GW spectrum Pi=(P_R-P_L)/(P_R+P_L)!=0 with sign(Pi)=sign(g_R2_parity)=the CMB-birefringence handedness; generic Starobinsky (no parity) is parity-SYMMETRIC (Pi=0) => a nonzero Pi (chiral primordial GWs, observable as parity-odd CMB tensor TB/EB + net circular polarization of the primordial GW background) is candidate-SPECIFIC + BREAKS the plateau-class degeneracy where (n_s,r) can't; TWO consequences -- (1) the discriminant (n_s,r) lacks lives in the tensor parity (ties inflation front to parity front), (2) the SAME g_R2_parity drives BOTH CMB birefringence AND chiral primordial GWs so the PARITY keystone is OVER-DETERMINED (birefringence + chiral-GW, locked-sign check), exactly like g_R2 (DESI-w + LiteBIRD-r, v2.442) => BOTH cosmological keystones (g_R2, g_R2_parity) now doubly-measured + the single parity coupling unifies THREE signatures (late-time birefringence + heterotic-axion UV origin + early-time chiral GWs, all one handedness); honest scope -- the MECHANISM is standard (Lue-Wang-Kamionkowski 1999, Jackiw-Pi CS-modified-gravity, grav leptogenesis) + the SIGN/existence is robust BUT the chirality AMPLITUDE (=observability) is NOT computed + is the crux (depends on g_R2_parity's toy magnitude + the axion velocity/CS scale, grav-CS chirality is generically SUPPRESSED + carries a known high-k one-helicity instability the EFT treats perturbatively) so a large observable Pi is NOT established (could be far below LiteBIRD sensitivity), robust claim = the SIGN + over-determination STRUCTURE not detectability, 'breaks the degeneracy' is in-principle contingent on amplitude; 5/5 checks) · [**v2.442 the four-experiment / three-keystone unified verdict: LiteBIRD r and DESI w both test the g_R2 keystone => the candidate is OVER-DETERMINED (a built-in consistency cross-check)**](docs/results/2026-07-02-v2.442-qnm-four-front-verdict.md) (DREAM consolidating v2.441's 4th front; unifying all four reveals FOUR experiments but only THREE keystone couplings -- CMB-birefringence<-g_R2_parity (beta>0), CMB-S4<-g_4 (matter signal), DESI/Euclid<-g_R2 (w>-1 plateau), LiteBIRD<-g_R2 (r~0.004,n_s~0.964 Starobinsky plateau) -- because g_R2 drives BOTH the dark-energy plateau AND inflation, so g_R2 is OVER-DETERMINED (tested by 2 independent experiments); the SHARP new falsifier -- fronts 3+4 are LOCKED by the single g_R2>0 keystone so w>-1 and r~0.004 must CO-OCCUR (same R^2 scalaron gives late+early plateaus): if LiteBIRD sees the plateau (r~0.004) but DESI sees phantom (w<-1) or vice versa the single-scalaron story is FALSIFIED even though each front alone looks fine, a joint (r~0.004 AND w>-1) confirms g_R2 TWICE; the inflation front's payoff = it turns the g_R2 keystone (rigorously forced v2.417, caps gravity v2.412, leading UV-completion discriminant v2.434-440) into a DOUBLY-MEASURED internally-checkable prediction => the candidate's cosmological history (R^2 inflation -> R^2 dark energy) is a SINGLE falsifiable arc not two claims; still 1 of 2^3=8 sign-patterns (maximally falsifiable per v2.430) + the 4th experiment adds the internal g_R2 cross-check; honest scope -- a STRUCTURAL falsifiability-accounting cycle not a new numerical prediction, uses the SIGN structure (robust) not magnitudes (beta/r/w O(1)-toy/plateau-class, r~0.004 = the Starobinsky value ANY positive-plateau model shares, w>-1 = refined-dS proxy sourced_proxy-tier), 'over-determined' = 2 experiments probe the same coupling (a real cross-check) but does NOT make r,w numerically predict each other (same sign/plateau-class signature at 2 epochs, not a computed r(w); no unified potential computed); 5/5 checks) · [**v2.441 the candidate's Starobinsky inflation SURVIVES its own swampland tower: a fourth falsifiable front (r~0.004, LiteBIRD), self-consistent, on the g_R2 keystone**](docs/results/2026-07-02-v2.441-qnm-inflation-survives-tower.md) (DREAM connecting 3 never-met parts -- the inflation sector (g_R2 = the Starobinsky scalaron, engine obs v1.86) + the swampland tower (v2.440) + the Swampland Distance Conjecture; the famous swampland-vs-inflation TENSION = a large-field inflaton drags an infinite tower down (SDC m~M_Pl e^{-alpha phi}), if the tower falls below the inflationary Hubble H the single-field description breaks; TEST -- the candidate's inflation is Starobinsky (g_R2>0 => R^2 plateau, N=55 => n_s=0.964, r=0.0040, H_inf~6e-6 M_Pl, scalaron range phi~5.3 M_Pl), its OWN tower starts at the species scale ~0.8 M_Pl (v2.440) + descends via SDC (alpha~1 string / 1.2 KK) to ~4e-3 / ~1.4e-3 M_Pl by end-of-inflation = 648x / 227x ABOVE H, reaching H only at phi~10-12 M_Pl = >2x the field range => the tower is NEVER excited => single-field Starobinsky is SELF-CONSISTENT for the candidate; consequences -- (1) the swampland-inflation tension is RESOLVED in the candidate's favour (>2x field-range margin), (2) n_s~0.964 + r~0.004 is ROBUST (dead-center Planck sweet spot + squarely in the LiteBIRD r-window ~2030s) = a FOURTH near-term falsifiable front beyond the v2.430 three, on the SAME g_R2 keystone that drives the dark-energy plateau (v2.422-425) => inflation+dark-energy tied to ONE coupling, (3) links early universe to the UV (the heterotic-string XOR KK tower sits only ~100x above the inflationary scale); the candidate now has a COHERENT cosmological history on the single g_R2 scalaron -- R^2 inflation (swampland-safe, LiteBIRD) -> R^2 dark-energy plateau today (w>-1, DESI); honest scope -- n_s,r set by N not g_R2's MAGNITUDE (robust = positive-g_R2 gives the plateau/sweet-spot, forced by v2.417), the SDC is a CONJECTURE + alpha/species-scale are idealized/proxy so tower masses are order-of-magnitude (robust = tower stays well above H with >2x margin, survives alpha+proxy uncertainty), r~0.004 is the plateau-CLASS value so a LiteBIRD r~0.004 detection confirms the class incl the candidate NOT uniquely it, the inflation<->DE link is structural (same operator 2 cutoffs) not a computed unified potential; 5/5 checks) · [**v2.440 the Emergent String Conjecture sharpens the candidate's UV completion into a clean geometric dichotomy: a heterotic-string tower XOR a Planckian-KK decompactification**](docs/results/2026-07-02-v2.440-qnm-emergent-string.md) (DREAMING -- the UV picture had stalled at a low-energy DEGENERACY (string~CDT~AS, v2.436) + a tower whose existence the amplitude confirms but whose type it can't see (v2.437-438); bring in a NEW principle the engine hadn't used = the swampland EMERGENT STRING CONJECTURE (Lee-Lerche-Weigand 2019): EVERY infinite-distance limit in a consistent QG's moduli space is either a DECOMPACTIFICATION (KK tower) or an EMERGENT heterotic-STRING limit -- no third option; applied to the candidate's multi-state tower (v2.438) it FORCES a DICHOTOMY -- the tower is a heterotic-string tower (=> the string completion of v2.434) XOR a KK tower (=> decompactification to extra dimensions), sharper than the degeneracy (selects the two GEOMETRIC towers); CONCRETE consequence on the KK branch -- the candidate's tower/species scale ~0.8 M_Pl (Planckian) => extra-dimension size R~1/Lambda~1e-34 m = PLANCKIAN not large, ~30 orders below submm gravity tests (Eot-Wash ~5e-5 m) => submm-INVISIBLE, exactly consistent with the candidate satisfying the engine's sub-mm constraint (had it needed LARGE extra dims submm would constrain it; it doesn't) => the candidate is NOT the large-extra-dimensions (ADD) scenario; both ESC branches Planck-scale so string-vs-KK stays the UV frontier (v2.437) but now as a SHARP swampland-motivated geometric dichotomy not an open degeneracy -- reconnects v2.438 (a tower exists) to v2.437 (its type is the frontier) via a real principle + predicts any extra-dimensional branch is Planckian/submm-invisible; honest scope -- the ESC is a swampland CONJECTURE (sourced_proxy, not proven) that PRESUPPOSES the string landscape so it refines the STRING branch (v2.434) NOT engine-excludes non-string CDT/AS (outside its scope), the ~0.8 M_Pl tower scale is the toy Dvali species proxy so 1e-34 m is order-of-magnitude (robust content = only vastly-sub-submm/Planckian-not-large), the ESC's 'string' = heterotic MATCHES v2.434 (a consistency not a derivation); 5/5 checks) · [**v2.439 the two layers of solving quantum gravity: the program lives entirely in Layer 2 (which EFT) and PRESUPPOSES Layer 1 (is gravity quantum)**](docs/results/2026-07-02-v2.439-qnm-two-layers.md) (the most foundational other path; VERIFIED engine fact -- every coupling the engine carves is HIGHER-DERIVATIVE (R^2, Weyl^2, R^3, R^4, parity terms, matter dim-8+, vacuum energy) while the Einstein-Hilbert term R (the graviton kinetic term / Newton's constant) is NOT a coupling (lowest carved = R^2), so the whole program carves higher-derivative CORRECTIONS on top of a quantum graviton it takes as GIVEN = it PRESUPPOSES gravity is quantum; so 'solving QG' has TWO LAYERS + the program occupies ONE: LAYER 1 = is gravity quantum (does it carry a graviton)? = the PREMISE, empirical/table-top -- classical collapse (parameter-free Diosi-Penrose) already EXCLUDED ~14 orders (v1.90, weakly favours quantum), the DECISIVE near-term test = BMV (gravitationally-induced entanglement of 2 mesoscopic masses; a classical LOCC field CAN'T entangle -> positive = gravity carries quantum d.o.f., ~2030s); LAYER 2 = which higher-derivative EFT given a quantum graviton = the ENTIRE program (candidate + correlated 2030 verdict + heterotic completion); the layers are OBSERVATIONALLY SEPARATE -- BMV probes the LEADING/Newtonian graviton sector the engine presupposes + does NOT carve, the candidate's correlated signature (birefringence + CMB-S4 matter + dark-energy w) probes the HIGHER-DERIVATIVE couplings it DOES carve, so the candidate is agnostic to BMV + vice versa; the complete solve needs BOTH -- BMV (is it quantum) + the correlated cosmological signature (which EFT); the program has finished all of Layer 2's low-energy work, Layer 1 awaits table-top tests; honest scope -- a FOUNDATIONAL FRAMING grounded in 1 verified engine fact + standard results (no new computation), the BMV argument is standard Bose-Marletto-Vedral but itself DEBATED (whether the mediator must be quantum is contested, 'decisive'=leading probe not settled), Diosi-Penrose excludes ONE classical model not all, 'separate' holds at leading order; 4/4 checks) · [**v2.438 the low-energy amplitude DOES infer part of the UV: the candidate's Hankel saturation shows a multi-state tower (not a single resonance)**](docs/results/2026-07-02-v2.438-qnm-spectral-inference.md) (bold swing on the S-matrix/spectral-density angle, SAFE via the bound the engine already has; the matter dispersion bound g_6^2<=g_4 g_8 is Cauchy-Schwarz on the positive spectral density (couplings = its moments): SATURATED (s=g_6^2/(g_4 g_8)=1) iff a SINGLE massive resonance, INTERIOR (s<1) iff MULTI-state (a tower); the candidate has s=0.756 INTERIOR => its implied UV is a MULTI-STATE tower NOT a single resonance, and every viable completion is similarly interior (string 0.80, asymptotic_safety 0.75, cdt 0.83); TWO consequences -- (1) a genuine PARTIAL UV inference: amplitude positivity ESTABLISHES the candidate's UV is a tower (spread spectral density), ruling out a single-resonance completion => the candidate is 'stringy' = has many states (confirming from interior-saturation the tower v2.375 asserted from log-convexity); (2) it still does NOT discriminate the completions (all interior s~0.75-0.84 within the O(1) spread, and the saturation RANKS them differently from the parity-even distance -- closest by saturation = asymptotic safety, by distance = string -- so no single low-energy measure picks a winner, reinforcing the v2.436 degeneracy); NET sharpens the low-energy UV reach -- it CAN confirm a tower EXISTS (multi-state, s<1) but CANNOT see its TYPE (Regge/KK/fixed-point) = the tower's existence is a low-energy fact, its character is the UV frontier; honest scope -- the saturation=single/interior=multi reading is the exact moment-problem interpretation (rigorous in structure) BUT the candidate's g_6=g_8=0.4 is a Chebyshev ARTIFACT (v2.392) so the precise 0.756 is an artifact-point not a prediction, robust content = only s<1 (interior=>multi-state) which holds across the region + all frameworks, per-framework s + rankings are encoder-level, 'multi-state' inferred from the density's support not a computed spectrum; 5/5 checks) · [**v2.437 the asymptotic-safety discriminator FAILS at low energy: the species scale can't tell a fixed point from a string -- the UV degeneracy is robust, the genuine discriminator is the tower SPECTRUM**](docs/results/2026-07-02-v2.437-qnm-uv-discriminator.md) (bold swing on a genuinely different solve-path = asymptotic safety (UV fixed point, NO tower) vs string/CDT (fundamental scale + tower); the natural discriminator = the SPECIES SCALE (bare fixed point should be UV-complete with no sub-Planckian cutoff, N->1); HONEST NEGATIVE -- it FAILS: computing N=1+nu(|g_R2|+|g_C|+|g_R3|) on each framework, string (N=1.70), asymptotic_safety (N=1.50) + cdt (N=1.74) ALL have towers N~1.5-1.7 (viable spread only 0.24) because the species scale is built from the LOW-ENERGY curvature couplings the convergent completions SHARE, the only N=1 (no-tower) point is pure_gr which lacks the required R^2 + is far/excluded; so even this semi-UV quantity is a low-energy quantity and CANNOT break the degeneracy; SHARPENS v2.436 -- the UV-completion degeneracy (string~CDT~AS at low energy) is ROBUST, not even the species scale separates them; the genuine discriminator is the TOWER SPECTRUM itself -- Regge trajectory (m^2 linear in spin => string) vs Kaluza-Klein/decompactification (=> extra dimensions) vs no-tower-metric-fixed-point (=> asymptotic safety) = a genuinely UV/high-energy observable (resonances near the cutoff) NOT accessible to low-energy amplitude-carving; so the OTHER paths ARE discriminable but only by REACHING the UV -> tells us precisely what a QG-SOLVING experiment must measure (the spectrum at the cutoff, not more low-energy Wilson coefficients) = closes the honest loop on the UV question, the low-energy program has extracted all it can + the remaining discrimination is a distinct harder UV measurement; honest scope -- toy Dvali species proxy + schematic encoders (N values illustrative, robust content = all>1 + clustered), 'AS should have N->1' is the idealized bare-fixed-point expectation (the engine's AS encoder has R^2/R^3 -> N=1.5, itself the point), 'tower spectrum discriminates' is standard QG phenomenology not an engine computation; a negative + constructive reading; 5/5 checks) · [**v2.436 other paths to solve: the candidate is a UV-convergence point (string, CDT, asymptotic safety all viable), NOT string-unique**](docs/results/2026-07-02-v2.436-qnm-uv-tournament.md) (user: consider OTHER paths + bold swings; the program + the v2.433-435 arc assumed convergence on STRING/heterotic -- this runs the full UV-completion tournament (each framework's low-energy point vs the candidate's rigorous cage + parity-deformation reachability): FOUR paradigms reach the candidate (in cage + separated only by cosmic birefringence = a parity-conserving version): string_tree_eft (parity-even dist 0.067, closest), CDT (0.085, NEAR-TIE), asymptotic_safety (0.196, competitive), pure_gr (0.803, far + lacks the rigorously-required R^2); ONLY lqg_induced is EXCLUDED (graviton positivity + CFT bootstrap); so the candidate is NOT string-unique -- among the R^2-bearing UV completions it requires, string is closest+best-motivated (its model-independent axion = the cleanest parity story) but CDT + asymptotic safety are genuinely competitive alternatives the LOW-ENERGY EFT CANNOT rule out; the candidate is a CONVERGENCE POINT of multiple UV completions + discriminating string/CDT/AS needs genuine UV/QG data (a string tower vs a discrete-spacetime continuum limit vs a UV fixed point) which amplitude-carving doesn't access; the honest BROADENING (other viable solve-paths = CDT + asymptotic safety not only string) + TEMPERS v2.434 ('it IS heterotic' -> 'heterotic/string is the LEADING UV completion among several the low-energy data can't yet separate'); sharpens what a QG-SOLVING measurement must do -- not just confirm the candidate (2030 signature) but REACH the UV to discriminate; honest scope -- frameworks are the engine's schematic O(1) ENCODERS not computed spectra, distances toy, the string-CDT gap (0.067 vs 0.085) is WITHIN the O(1) encoder uncertainty (=why 'not unique'), TEMPERS but does NOT overturn v2.434 (heterotic still best-motivated via the axion, 'not type II' stands); 5/5 checks) · [**v2.435 the axion-universality consequence: if the parity is the heterotic model-independent axion it is DETERMINED not free -- heterotic PREDICTS cosmic birefringence, candidate approaches parameter-free**](docs/results/2026-07-02-v2.435-qnm-axion-universality.md) (following v2.434; (a) GROUNDING the heterotic ID -- the candidate's curvature+parity signs are heterotic-consistent: g_R2>0 (positive Gauss-Bonnet alpha' term), g_R3>0, the curvature moment tower g_R3^2<=g_R2 g_R4 satisfied (feasible g_R4>=0.042), parity g_R2_parity=0.06>0 positive handedness matching CMB beta>0 + the axion's R^R-tilde sign; (b) the UNIVERSALITY CONSEQUENCE -- the heterotic MODEL-INDEPENDENT axion (B-field) couples to R^R-tilde with a UNIVERSAL coefficient fixed by Green-Schwarz/the gravitational anomaly (SAME in every heterotic compactification), so if the candidate's parity IS this axion (natural reading) the parity is DETERMINED by universal geometric data NOT free -- a major parameter reduction since the parity was the candidate's ONE data-selected rigor-uncageable dof (the 'single residual toy' v2.418); consequences: (i) heterotic PREDICTS cosmic-birefringence beta given the string scale (a birefringence measurement tests a PREDICTION not a fit), (ii) the candidate's free continuous params collapse toward the rigor-caged parity-even shape + the string scale => approaches a (near-)parameter-free heterotic prediction = the strongest solve-toward statement; the whole arc CONVERGES -- the ONE coupling the rigor cage can't force + the empirical verdict tests + the UV embedding sources (the parity) is exactly what heterotic universality FIXES; honest scope -- curvature-consistency engine-checked but universality is a PHYSICS ARGUMENT not a computation, CONTINGENT on the parity being the MODEL-INDEPENDENT (universal) axion (heterotic also has model-DEPENDENT geometric axions = non-universal), 'predicts beta' is structural (the MAGNITUDE needs the string scale + axion decay constant, dimensionful inputs the engine lacks), '(near-)parameter-free' aspirational (the shape is CAGED not a point, scale+compactification remain, so the param count DROPS not to literally zero); 4/4 checks) · [**v2.434 THE BOLD DECISION: the candidate's UV completion is HETEROTIC string theory (not type II), and the parity coupling IS the heterotic axion**](docs/results/2026-07-02-v2.434-qnm-heterotic-identification.md) (the deepest solve-path #4 = identify the UV completion; making the specific falsifiable call of WHICH string via real string facts + the engine; TWO independent arguments converge on HETEROTIC: (1) the candidate rigorously requires a nonzero R^2 term -- tree-level type II has an R^4-ONLY leading curvature correction (no R^2/R^3) while heterotic has an R^2 (Gauss-Bonnet) at alpha', and the engine shows that GIVEN the candidate's cubic-curvature g_R3 + parity, setting g_R2=0 is EXCLUDED by source-exact bounds (graviton_forward_positivity + cemz_causality + cross_sector_efthedron) so an R^4-only type-II curvature sector is rigorously incompatible => candidate is R^2-bearing = heterotic-pattern; (2) the parity coupling IS the heterotic axion -- the candidate's one data-selected rigor-uncageable dof (parity g_R2_parity, v2.431/2.420) is a gravitational-Chern-Simons R^R-tilde term, and the heterotic string UNIQUELY carries the model-independent axion (B-field) whose Green-Schwarz R^R-tilde coupling is exactly such a term, so the deformation from the parity-conserving string point (v2.433) to the candidate is natively the heterotic axion; DECISION: candidate = a heterotic compactification's tree-level EFT + its model-independent axion, parity = the axion; names a specific UV completion (heterotic not type II) + identifies the physical origin of the candidate's ONE free coupling, turning #4 into a specific falsifiable target (a heterotic vacuum's R^2:R^3:R^4 + axionic R^R-tilde vs the cage), and ties to the empirical verdict -- a cosmic-birefringence detection = a detection of the heterotic axion; honest scope -- a STRUCTURAL identification from textbook string facts + the engine NOT a heterotic compactification computation (the real matching is the remaining work, now with a specific target), R2=0-excluded is source-exact (v2.411 caveat), the g_R2->R^2/g_R3->R^3/parity->axionic-R^R-tilde mapping is a defensible basis identification, type II DISFAVORED not excluded (loop/flux-induced R^2 could mimic; the clean statement is tree-level), parity=axion well-motivated but coefficient toy; 5/5 checks) · [**v2.433 a concrete UV-embedding lead (option #4): the candidate is string tree-EFT + the data-required parity deformation**](docs/results/2026-07-02-v2.433-qnm-string-deformation.md) (option #4 -- embed the candidate in a UV-complete theory, the deepest solve-path -- given a concrete LEAD not just scoping; locating the candidate vs the engine's string tree-level EFT framework (which HAS a UV completion, string theory): the string couplings (g_4=0.50,g_6=0.40,g_8=0.40,g_R2=0.20,g_R3=0.15,g_R2_parity=0.00) are FEASIBLE under the rigorous core AND the effective rigorous cage (v2.431), are rejected by the full theory+data stack by EXACTLY ONE constraint (cosmic_birefringence_data, because parity=0), sit within parity-even distance 0.067 of the candidate, and differ materially ONLY in the parity coupling (0.00 vs 0.06); so string tree-EFT IS precisely the parity-conserving rival (v2.420) and THE CANDIDATE = STRING TREE-EFT + THE DATA-REQUIRED PARITY DEFORMATION -- a small parity deformation of a UV-completable theory, so it can inherit string theory's UV completion up to the parity turn-on, and the concrete #4 task becomes 'find a string compactification whose low-energy EFT is string-tree-like AND carries the axionic / gravitational-Chern-Simons parity term generating g_R2_parity' (specific, not open-ended); dovetails with the whole program -- the candidate's ONE data-selected rigor-uncageable dof (the parity, v2.431/2.420) is exactly the deformation carrying string tree-EFT to the candidate, so the rigor cage + the empirical verdict + the UV embedding all point at the SAME single object = the parity coupling; honest scope -- 'string tree-EFT' is the ENGINE's O(1) framework PROXY not a specific compactification's computed spectrum so this is a genuine LEAD not a proof, actually establishing #4 still needs the real string-theory computation (this SCOPES it to a specific target), distance uses toy couplings, 'rejected only by birefringence' contingent on the ~3.6sigma hint; 5/5 checks) · [**v2.432 closing the 'what would solve it' investigation: an honest over-determination negative + the quantified state of the five options**](docs/results/2026-07-02-v2.432-qnm-solve-synthesis.md) (two parts; (a) HONEST NEGATIVE -- tested whether rigor + the front correlations OVER-DETERMINE the candidate (2 measurements predict the 3rd = the strongest falsifiability); it does NOT -- conditioning the island on the matter g_4 + dark-energy g_R2 fronts narrows the parity range only ~1.1x because each front is pinned by its OWN data not cross-predicted, so the fronts are correlated (v2.429) but NOT over-determined and the strongest falsifier stays the CONJUNCTION (v2.430, 1/8); (b) SYNTHESIS -- the quantified state of the 5 solve-options answering the user's question: #1 empirical DECISION-READY (correlated signature->verdict table, awaits ~2030 data), #2 rigor AT ITS CEILING (cages the parity-even shape but provably cannot force the parity since a parity-conserving theory is rigorously fine v2.420 -> residual = dark g_8 scale + data-selected parity), #3 real-normalization BLOCKED (needs a real amplitude/matching computation), #4 UV-embedding OPEN RESEARCH (needs a string-vacuum match), #5 write-up DONE (Report II); bottom line -- the two IN-ENGINE options are at their ceiling (candidate maximally testable + maximally rigor-forced), further progress needs the ~2030 DATA or external inputs = the honest terminus of the autonomous program, the candidate's fate now a matter of MEASUREMENT not further carving; honest scope -- over-determination is a sampled conditional-width in one direction (robust content = close-to-1 no strong cross-prediction), the 5-option state is a synthesis with honest status tags not new computation for #3/#4/#5; 5/5 checks) · [**v2.431 how much does rigor force? the rigorous core cages the candidate's parity-even sector to tight windows**](docs/results/2026-07-02-v2.431-qnm-rigorous-cage.md) (option #2 rigor-forces-it, quantified; for each candidate coupling scan up/down (others fixed) under the EFFECTIVE rigorous core (source-exact + implied, NO proxies/data) + record which constraint binds each edge: g_4 [0.40,0.63], g_6 [0.28,0.46], g_R2 [0.11,0.37] all BOTH EDGES RIGOROUS (boxed, factor ~1.5-3 around the candidate); g_R3 (<=0.16) + parity (<=0.26) capped ABOVE by rigor; only g_8 upper-unbounded = the observationally-DARK direction (v2.381); so rigor forces the parity-even SHAPE into a tight cage, and the irreducible residual freedom is exactly 3 understood things -- (i) g_8's dark upper scale, (ii) the free LOWER ends of g_R3+parity (a parity-conserving cubic-curvature-minimal theory is rigorously fine, DATA=birefringence selects the nonzero values = the v2.420 rival + the fundamental reason rigor can't force the FULL candidate), (iii) the overall scale/upper-cuts closed by EFT-validity+data; stronger+more precise than 'rigor gives a loose family' (v2.419 used the BARE core) -- once rigorous CONSEQUENCES are included the parity-even sector is caged and what's open is precisely the physically-understood dark scale + data-selected parity, so rigor pins the SHAPE + data pins the SCALE+PARITY = the quantified ceiling of #2; honest scope -- 1-D slice scan (others at candidate, not the full 6-D region), robust content = WHICH edges rigorous-vs-free not exact widths, source-exact-in-form (v2.411 caveat, headline-robust v2.427), g_8 unbounded within the scan cap 2.0; 5/5 checks) · [**v2.430 the 2030 verdict table: the candidate is confirmed by exactly one of eight joint-outcome patterns**](docs/results/2026-07-02-v2.430-qnm-verdict-table.md) (continuing option #1 -- turns the correlated signature v2.429 into an actionable DECISION RULE; the 3 measurements each binary -- parity (birefringence beta>0 vs 0), matter (CMB-S4 tension vs slow-roll), dark energy (w in candidate band vs far off) = 2^3=8 joint patterns; because the candidate REQUIRES the correlated triple (re-verified: parity=0 + weak-matter each INFEASIBLE), it is confirmed by exactly ONE pattern (P=1,M=1,D=1) and NOT confirmed by the other 7 => MAXIMALLY FALSIFIABLE; the two nearest alternatives cleanly separated -- beta=0 with the rest present selects the parity-conserving RIVAL (re-verified feasible when birefringence null), all-null (0,0,0) is standard cosmology/no-new-physics; the CORRELATION makes partial patterns decisive -- parity WITHOUT matter tension (P=1,M=0) breaks the keystone correlation + KILLS the candidate, a discrimination independent single-probe tests can't make; so ~2030 status is a clean decision-ready verdict (1/8 confirms, 6/8 don't, 2 runner-up theories in distinct patterns) = the concrete 'how we'll know' + the furthest an EFT-carving program pushes toward settling the theory short of the data; honest scope -- decision-support MAP not a likelihood/Bayes posterior (needs error models + toy-map magnitudes), coarse binaries, thresholds toy, parity axis birefringence-hint-contingent, '1/8' counts patterns not probability mass; 5/5 checks) · [**v2.429 the correlated make-or-break signature: the candidate's three near-term fronts are one linked smoking gun**](docs/results/2026-07-02-v2.429-qnm-correlated-signature.md) (acting on option #1 from the 'what would actually solve QG' discussion -- sharpen the EMPIRICAL test, the real resolution; the v2.421 portfolio LISTED 3 fronts, this shows they are LINKED (independent signals can be explained away, a correlated pattern can't); sampling the feasible island: the 3 front-drivers -- g_4 (CMB-S4 matter, [0.33,0.63]), g_R2 (dark-energy w, [0.084,0.248]), g_R2_parity (CMB birefringence, [0.047,0.089]) -- are (1) ALL REQUIRED NONZERO (each feasible min strictly positive), (2) POSITIVELY CORRELATED (g_4-g_R2 +0.42, g_R2-parity +0.44, g_4-parity +0.34, because g_4/g_R2 are the matter-dominance keystones + parity is capped by the g_R2 sector), (3) INDISPENSABLE (parity=0, small g_4, small g_R2 each INFEASIBLE with the rest at candidate values); so the decisive test is the CONJUNCTION -- large inflationary matter coupling AND nonzero positive parity AND bounded dark-energy w, all correlated -- far more decisive than any single front (a rival producing one signal by accident doesn't reproduce the triple; any single null kills the candidate); reframes the candidate as ONE correlated make-or-break signature ~2030 data can confirm/falsify as a PATTERN = the sharpest least-evadeable test, the most direct autonomous step toward the empirical solve; honest scope -- fronts linked at the COUPLING level (rigorous: matter dominance + parity cap) but the coupling->observable maps are O(1)-toy so robust content = 'all 3 required + positively correlated' not the precise observable correlation, correlations moderate (island tight) but the CONJUNCTION is a correlation-magnitude-independent feasibility fact, 'data requires parity' contingent on the ~3.6sigma birefringence hint (else parity front detaches, v2.420); 5/5 checks) · [**v2.428 Research Report II: a rigor-tiered candidate quantum-gravity EFT and its dark-energy sector**](docs/results/2026-07-02-v2.428-RESEARCH-REPORT-II.md) (a publication-style SYNTHESIS of the whole program as of v2.427, superseding the v2.0 research report which predated the candidate arc + de-toying + CC sector by ~350 cycles; ROADMAP thrust #5; sections: (1) method = swampland-complete carving + the first-class rigor tiering (19 rigorous / 17 sourced_proxy / 6 data); (2) the candidate (matter-dominant, near-Planckian, ghost-safe, string-like, globally-unique); (3) WHAT IS RIGOROUS = the de-toying arc -- LQG excluded (prefactor-independent), matter-dominance ceiling + BH decay implied, leading curvature forced by matter x cubic-curvature, rigor=family/data=point, single residual toy = the rigorously-capped+data-pinned parity magnitude; (4) the conjectural CC/dark-energy sector (admits DE / selects dS / predicts w>~-1, inflation-to-dark-energy on the keystone g_R2); (5) falsifiability = 3 independent fronts (parity CMB+GW, matter CMB-S4, dark energy DESI/Euclid); (6) honest limits (all magnitudes O(1)-toy, 3 assumptions, dark hidden sector, birefringence-hint-contingent, blocked ringdown map); anchor facts re-verified against the engine at v2.428 (tiers 19/17/6, candidate feasible, LQG excluded, WGC+Wald implied); a synthesis -- no new physics, consolidates verified results; full suite green 2712) · [**v2.427 the rigorous core is robust to its own simplified prefactor: the LQG exclusion is alpha-independent, the matter->curvature floor stable-to-tighter**](docs/results/2026-07-01-v2.427-qnm-rigorous-prefactor-audit.md) (the de-toying arc tagged bounds 'rigorous' = source-exact in FORM, some carrying a simplified O(1) prefactor; the clearest is cross_sector_efthedron (g_8 g_R2 >= alpha g_6 g_R3, alpha~1.1 a docstring-flagged 'motivated guess'), one of the bounds that excludes LQG (v2.411) + forces matter->curvature (v2.417) -- so DO the rigorous headlines depend on that toy alpha? varying alpha 0.5-3.0: (1) the LQG EXCLUSION IS ALPHA-INDEPENDENT -- excluded at every alpha, and at alpha<=0.8 cross_sector isn't even in LQG's kill-set so the OTHER rigorous bounds (graviton_forward_positivity, cft_flat_space) exclude it regardless = the flagship zero-toy exclusion does NOT rest on the prefactor; (2) the candidate is O(1)-robust (feasible for alpha in [0.5,2.0], fails only at alpha~3 well beyond O(1)); (3) the matter->curvature g_R2 floor is stable at 0.108 for alpha<=1.1 and only RISES/tightens for larger alpha, so 'matter forces nonzero leading curvature' is robust + only strengthened; so even the RIGOROUS tier's one simplified O(1) prefactor does NOT drive its headlines (the FORM carries the results, the coefficient doesn't), validating 'rigorous = source-exact in form' operationally and CLOSING the last honest gap in the de-toying story (toy confined to proxy/data tiers v2.412-419 AND the rigorous tier's own prefactors shown not to drive conclusions); honest scope -- audits ONE prefactor (the load-bearing one) not exhaustively all, alpha-independence is via redundancy with the other source-exact-in-form bounds, alpha~3 failure expected; 5/5 checks) · [**v2.426 RIGOROUS EXTENSION (MT): the matter moment tower's next rung -- source-exact positivity predicts g_10 >= g_8^2/g_6**](docs/results/2026-07-01-v2.426-qnm-matter-tower.md) (a rigorous low-risk core extension in the 'make it real' direction (ROADMAP thrust #2); the engine had the leading matter Hankel bound g_6^2<=g_4 g_8 but stopped at g_8 while the curvature sector already had its next rung (g_R3^2<=g_R2 g_R4, v2.375); this adds the next MATTER rung -- treating the matter coefficients (g_4,g_6,g_8,g_10) as consecutive moments mu_k of the positive spectral density, the Hankel matrix is PSD so the adjacent 2x2 minor gives g_8^2 <= g_6 g_10 (pure Cauchy-Schwarz on a positive measure, RIGOROUS); wired opt-in (include_matter_tower), both new constraints (scalar_positivity_g10 + the rung) tagged RIGOROUS and land in the source-exact core (zero toy input); RESULT -- for the candidate (g_6=g_8=0.4) the rung FORCES g_10 >= g_8^2/g_6 = 0.4, a rigorous PREDICTION (lower bound) of the next currently-absent matter coefficient purely from positivity, and the full stack pins g_10 to [0.4,0.6] (rigorous Hankel floor + EFT-validity ceiling); so the 'infinite matter tower' (v2.375/381) is made concrete by one more rigorous rung matching the curvature tower, extending the de-toying rigorous core with a real source-exact bound not a toy proxy; honest scope -- source-exact IN FORM (v2.411 caveat; the coeff<->moment identification is the standard dispersive-tower convention, prefactors=1 as the leading rung), the floor is a LOWER bound not a determination of g_10, the 0.6 upper edge is EFT-validity, opt-in so default/frameworks/goldens untouched; 5/5 checks) · [**v2.425 CC-ARC CAPSTONE: the candidate's dark-energy profile in one verified artifact**](docs/results/2026-07-01-v2.425-qnm-cc-capstone.md) (ties the three CC slices into one integrated verified profile + folds the dark-energy w into the falsification portfolio; natural close of the CC arc, the v2.415 rigor-ledger analogue; the candidate's dark-energy profile: ADMITS a positive cosmological constant g_Lambda in [0, g_R2~0.193] (CC1, keystone g_R2 that drives inflation also caps the dark energy), SELECTS dS/Minkowski over anti-de Sitter (CC2, bounded tower forbids the small-|Lambda| AdS vacuum -> AdS branch empty), PREDICTS w >~ -1 (CC3, flat Starobinsky plateau -> true-CC-to-mild-quintessence, full dS-conjecture-slope quintessence w~-0.8 data-disfavored, DESI/Euclid w(z) = 3rd falsification front); so the sector gives a coherent + falsifiable dark-energy story = an inflation-to-dark-energy arc on the SINGLE keystone scalaron g_R2 (inflation high-scale v1.86, dark energy low-scale bounded by the same g_R2); entire sector CONJECTURAL + honestly tiered (all CC constraints sourced_proxy) + dimensionless so it is the vacuum-energy STRUCTURE not the CC MAGNITUDE problem; honest scope -- a CONSOLIDATION (re-runs CC1-3, no new physics), every claim inherits its slice caveats (O(1) proxies, illustrative w, 'selects dS' O(1)-robust-not-theorem), opt-in so no prior result changes; a rigorous future refinement NOTED not attempted (conformal-anomaly-induced vacuum energy ~ a=g_R2, deferred to avoid mis-encoding); 5/5 checks) · [**v2.424 CORE EXTENSION CC3: the dark-energy equation of state -- the candidate predicts w >~ -1, testable by DESI/Euclid**](docs/results/2026-07-01-v2.424-qnm-cc-equation-of-state.md) (third CC slice = the OBSERVABLE readout of the sector; the refined dS conjecture admits positive vacuum energy two ways -- the TACHYONIC route (static vacuum, w=-1, a true cosmological constant, the CC1 route) or the SLOPE route (rolling scalar, quintessence, w=-1+(2/3)(1/2)(V'/V)^2 > -1); a full dS-conjecture O(1) slope (c~0.8) gives w~-0.79 = ~8sigma from observed w=-1.03+/-0.03 so FULL quintessence is DISFAVORED by data; the candidate's dark energy rides the FLAT Starobinsky R^2 scalaron plateau (g_R2, v1.86) -> w~-1, so the candidate predicts w in [-1, ~-0.98] (static to MILDLY quintessent), consistent with data + on the same side as the 2024 DESI evolving-dark-energy hint (w0 slightly > -1); this places the candidate at the SAME plateau-vs-swampland TENSION v2.410 flagged for inflation (flat plateau wants w=-1 static, dS-conjecture slope wants w>-1 rolling); adds a THIRD observable falsification front (after CMB birefringence + CMB-S4 inflation) = the dark-energy equation of state w(z), testable NOW by DESI + soon by Euclid; honest scope -- an observable-READOUT analysis not a new hard constraint, the specific w numbers are illustrative order-of-magnitude (V'/V needs the potential+scale), robust content is qualitative (static->w=-1, slope->w>-1, full O(1) slope data-disfavored, flat plateau near w=-1), refined dS conjectural (sector sourced_proxy), dynamics-readout NOT the CC magnitude problem; 5/5 checks) · [**v2.423 CORE EXTENSION CC2: the AdS distance conjecture selects de Sitter -- the candidate admits dS/Minkowski but NOT anti-de Sitter**](docs/results/2026-07-01-v2.423-qnm-cc-ads-distance.md) (second CC slice; adds the AdS distance conjecture (Lust-Palti-Vafa 2019) as a FLOOR on the vacuum-energy magnitude set by the species-scale tower -- |g_Lambda| >= c_AdS/N (alpha=1/2), since a small |Lambda| would need a parametrically light tower the candidate lacks; opt-in, tagged sourced_proxy; RESULT at c_AdS=1, candidate N=1.566 -> AdS floor |g_Lambda|>=0.639 but EFT-validity/complexity caps |g_Lambda|<=~0.60, so the floor EXCEEDS the ceiling and the AdS branch is EMPTY -- no anti-de Sitter vacuum admissible; combined with CC1's refined-dS ceiling (g_Lambda<=g_R2~0.193) the candidate's admissible vacuum-energy window collapses to the dS/Minkowski branch [0,0.193]; so the two swampland conjectures + EFT-validity SELECT de Sitter -- the candidate 'wants to be de Sitter', its bounded tower (the same string-like tower v2.375) forbids the small-|Lambda| AdS vacuum and drives it to positive/zero vacuum energy; O(1)-ROBUST (holds for c_AdS >~ 0.94, the threshold where the AdS floor drops to the EFT ceiling) not knife-edge; net CC arc -- candidate ACCOMMODATES dark energy (CC1) + bounded above by inflaton curvature (CC1) + pushed off AdS by its tower (CC2) = a coherent conjectural dark-energy story; honest scope -- both CC constraints CONJECTURAL proxies (sourced_proxy), 'no AdS' is an O(1)-robust STRUCTURAL statement not a theorem, g_Lambda dimensionless so this is dS-vs-AdS ADMISSIBILITY not the CC MAGNITUDE (10^-120), N uses g_C=0; 5/5 checks; full suite green 2693; CC3 next = quintessence-vs-constant) · [**v2.422 CORE EXTENSION CC1: the cosmological-constant / dark-energy sector**](docs/results/2026-07-01-v2.422-qnm-cc-sector.md) (the engine had no vacuum-energy coupling -- the one big QG problem it never touched; this first slice of a new core sector (user-chosen direction) adds a dimensionless vacuum-energy parameter g_Lambda (>0 de Sitter, <0 anti-de Sitter, 0 Minkowski) + its first swampland constraint the refined de Sitter conjecture (Ooguri-Palti-Shiu-Vafa 2018), wired OPT-IN (build_stack(include_cc_sector=True)) so the entire existing stack/frameworks/goldens are UNTOUCHED (g_R4-tower precedent), and tagged sourced_proxy by the rigor registry -- the de-toying arc's honest tiering auto-files this conjectural swampland statement in the proxy layer NOT the source-exact core; physics -- a positive vacuum energy at a potential extremum satisfies the refined dS conjecture only if the potential is sufficiently CONCAVE (tachyonic 2nd condition), and the candidate's dark energy rides the Starobinsky R^2 scalaron (g_R2, v1.86) whose plateau is concave, so the condition maps to g_Lambda <= g_R2/c_dS; RESULT -- the candidate ADMITS a positive cosmological constant, allowed g_Lambda window [-0.602, +0.192] (upper edge +0.192 = refined-dS bound ~g_R2 = dark energy capped by scalaron curvature; lower edge -0.602 = EFT-validity/complexity = |vacuum energy| can't exceed the cutoff), modest AdS+Minkowski consistent; so the SAME g_R2 that drives inflation (v1.86) + anchors the theory as keystone (v2.396) also caps the dark energy the candidate can carry = a genuine new curvature<->CC cross-link, consistent with the refined dS conjecture; honest scope -- a FIRST-PROXY of a CONJECTURAL statement (tagged sourced_proxy accordingly), the V''/V->g_R2 map is order-of-magnitude (robust content = 'positive Lambda bounded by scalaron curvature', c_dS is O(1)), g_Lambda dimensionless so this does NOT address the CC MAGNITUDE problem (10^-120) only the STRUCTURAL admissibility, opt-in so no prior result changes; 6/6 checks; full suite green 2688 passed; CC2 next = AdS-distance/species relation) · [**v2.421 the falsification portfolio: the concrete decision map for confirming or killing the candidate**](docs/results/2026-07-01-v2.421-qnm-falsification-portfolio.md) (assembling the program into one actionable artifact -- for each observable: what it measures, the candidate's prediction, the rival it distinguishes, the RIGOR TIER, and timeline: [1] CMB cosmic birefringence (LiteBIRD/CMB-S4 ~2028-30) = THE live discriminator (candidate-vs-parity-conserving-rival reduces to it, v2.420; parity SIGN rigorous+data, magnitude data-pinned under a rigorous ceiling v2.418; beta->0 selects the rival); [2] CMB-S4 inflation (~2030) = make-or-break MATTER test, parity-independent (matter dominance forces large g_4 -> >10sigma tension w/ slow-roll v2.395; structure rigorous, map toy); [3] GW birefringence (LISA/ET ~2035) = SECOND independent parity channel probing the GRAVITON sector vs CMB's photon sector (de-risks single-measurement dependence + agreement/disagreement tests the single-parity-origin assumption; candidate nonzero chirality ~4x below cap v2.387, rival zero); [4] sub-mm = consistency check (must screen); [5] amplitude/causality structure = the RIGOROUS backbone already in hand (LQG + extreme archetypes excluded, zero toy, v2.411/2.420); NET actionable picture -- the candidate is falsifiable on TWO INDEPENDENT FRONTS (parity: CMB now + GW later; matter: CMB-S4 inflation) atop a rigorous backbone, so NOT hostage to one measurement (parity has redundant probes in different sectors, matter test orthogonal); honest scope -- a CONSOLIDATION/decision-map not new physics (assembles v2.387/395/408/418/420 with tiers; only live computation = re-confirming the rigorous LQG exclusion), parity SIGN + ceiling rigorous but every MAGNITUDE toy, birefringence 'hint' ~3.6sigma not a detection, timelines approximate; 5/5 checks; NOTE: candidate program saturating -- flagged to user) · [**v2.420 the alternatives map: which consistent-QG archetypes rival the candidate, and exactly what rejects each**](docs/results/2026-07-01-v2.420-qnm-archetype-alternatives.md) (pivoting from 'is the candidate rigorous' to 'what are the consistent ALTERNATIVES + what rejects each'; evaluating 5 qualitatively distinct archetypes vs the rigorous core + full theory+data stack: A candidate -> SURVIVES; B parity-conserving (parity=0) -> RIGOROUSLY CONSISTENT, rejected by cosmic_birefringence_data ALONE; C curvature-minimal -> rejected by anomaly(toy)+SDC(toy)+data (not a clean exclusion); D curvature-heavy (g_R2 large) -> RIGOROUSLY EXCLUDED (cft_flat_space + left_handed_graviton positivity); E matter-light -> RIGOROUSLY EXCLUDED (graviton_mixed + cubic_graviton_matter positivity); so the space of consistent QG has clean structure -- 'too much gravity' (D) and 'too little matter' (E) are rigorously excluded by source-exact amplitude positivity (consistency bounds the gravity/matter balance from BOTH sides, zero toy), while the HEADLINE is that B (parity-conserving) is rigorously consistent + separated from the candidate by exactly ONE thing = the cosmic-birefringence datum, so B is the candidate's single LIVE RIVAL (a perfectly consistent parity-conserving QG that returns as viable if the ~3.6sigma birefringence hint fades -- matching the Z2 mirror v2.406 + load-bearing-datum v2.408); forward-looking NET: the candidate-vs-alternative question reduces to 'is cosmic birefringence real?', every other archetype settled independent of it; honest scope -- representative single points (archetype CLASSES separated by the stated causes, not exhaustive clustering; D/E somewhat extreme so 'rigorously excluded' = these points are), B's rejection contingent on the birefringence hint being real, C mixes toy+data; 5/5 checks) · [**v2.419 what the rigorous core alone determines: a broad FAMILY, not the candidate -- data does the final collapse**](docs/results/2026-07-01-v2.419-qnm-rigorous-family.md) (completing the rigorous-core arc: does rigor ALONE pin the candidate or only a family? walking the rigorous-core feasible set vs the full-stack island -- rigor gives a family ~5.6x looser per dimension (geom-mean stddev 0.213 vs 0.038), and within it the candidate's defining features are NOT forced: g_R2 can be ~0 (no curvature), parity can be 0 (parity-conserving), g_8 can be large; the full stack (swampland/observable tiers + cosmic-birefringence datum) collapses it to the tight ~3-dim island pinning g_R2 in [0.08,0.25], parity in [0.047,0.089] = the candidate region; so the honest scope of 'what rigor establishes about QG' is bounded -- the source-exact core delivers the framework EXCLUSIONS (LQG) + amplitude STRUCTURE (positivity relations, the matter x cubic-curvature forcing WHEN those sectors are present) but leaves a broad family and does NOT select the candidate; the candidate's coordinates are picked by DATA + the swampland/observable model layers -- rigor determines the RULES + BOUNDARIES, data determines the POINT; the single-candidate headline (v2.406) is a rigor+data+model result exactly as the rigor ledger (v2.415) tiered it, now shown at the feasible-SET geometry level; honest scope -- walk-based extent (lower bounds on the family, at least this broad), the 'g_R2~0/parity~0 allowed' qualitative content robust, and this is the honest COMPLEMENT to the de-toying arc NOT a weakening (LQG-excluded / matter-dominance-ceiling / forcing-lattice all still hold -- they are EXCLUSION + CONDITIONAL-structure claims, exactly what a family-carving core provides); 5/5 checks) · [**v2.418 even the 'one residual toy' is mostly rigorous+data: the parity magnitude is rigorously CAPPED and DATA-pinned; the toy only tightens the upper edge**](docs/results/2026-07-01-v2.418-qnm-parity-window.md) (the de-toying arc concluded the candidate's single residual toy = the parity-magnitude coefficient (anomaly-inflow rho); this dissects it by scanning g_R2_parity (other couplings at candidate values) under nested stacks: rigorous+implied (no data/toy) -> g_R2_parity in [0.0, 0.267] = a RIGOROUS CEILING via left_handed_graviton_positivity (amplitude positivity caps how parity-violating the theory can be, candidate ~4x below it per v2.387); + birefringence DATA (no toy) -> [0.048, 0.152] (data pins the lower edge + most of the upper); + toy anomaly-inflow (full) -> [0.048, 0.078] (the toy's ONLY effect = tightening the upper edge to sqrt(rho*g_4*g_R2)=0.078); crucially the candidate value 0.06 is feasible on the rigorous ceiling + data ALONE (lies in [0.048,0.152] WITHOUT the toy), so the toy does NOT determine any candidate value; combined with the parity SIGN/chirality being rigorous+data (v2.386/364), the candidate's parity coupling rests on rigorous positivity (ceiling+sign) + real data (birefringence), the lone toy playing a minor non-value-determining role; so the whole arc's bottom line STRENGTHENS -- the candidate QG EFT is essentially RIGOROUS + DATA throughout (matter-gravity source-exact v2.411-417, parity magnitude rigorously bounded + data-pinned), the one toy-FORM constraint sets no candidate value, only sharpens a bound the birefringence datum already imposes; honest scope -- 1D slice windows (qualitative ceiling/data/toy-edge decomposition robust), the 'data' pin is itself birefringence-hint-contingent (~3.6sigma, v2.408/329) so 'data-pinned' = pinned by that measurement not the toy, and this does NOT make rho real -- it shows the candidate value doesn't depend on it; 5/5 checks) · [**v2.417 the definitive feasibility lattice: the leading curvature coupling is forced by the matter x cubic-curvature pair (resolving both prior attributions)**](docs/results/2026-07-01-v2.417-qnm-curvature-forcing-lattice.md) (v2.414 said 'matter sources gravity' [wrong -- matter alone allows g_R2->0], v2.416 corrected to 'g_R3/parity force g_R2 within-gravity' [also imprecise -- g_R3 alone is infeasible w/o matter, parity doesn't force g_R2]; this enumerates ALL 7 sector-presence patterns over {matter, cubic g_R3, parity} on the effective rigorous+implied core: matter only -> g_R2 free (~0 OK); cubic-only / parity-only / cubic+parity-no-matter -> INFEASIBLE (each REQUIRES matter to exist); matter+g_R3 -> FORCES g_R2>=0.108 (via graviton_forward_positivity + cross_sector_efthedron); matter+parity -> g_R2 free (parity does NOT force it); so the SOLE rigorous forcer of the leading curvature term = the matter x cubic-curvature pair, a genuine two-sector amplitude-positivity bound -- NEITHER 'matter sources gravity' NOR 'gravity self-forces'; complete lattice => no further correction possible; physically satisfying -- an EFT with BOTH matter self-interactions AND a cubic-curvature (Riemann^3) coupling cannot omit the leading R^2 term (positivity ties the three), while matter-only or matter+parity can; the candidate inherits its g_R2>=0.108 floor from its matter x g_R3 content; honest scope -- lattice at the constructed magnitudes (qualitative which-forces-which pattern robust, floor 0.108 at those magnitudes), REFINES v2.416 (its retraction of 'matter sources gravity' stands, this pins the exact mechanism); 5/5 checks) · [**v2.416 SELF-CORRECTION: 'matter sources gravity' (v2.393/v2.414) was mis-attributed -- the rigorous g_R2 floor is forced by the higher-curvature (g_R3) and parity couplings, not matter**](docs/results/2026-07-01-v2.416-qnm-gravity-self-forcing.md) (while probing whether amplitude positivity pins g_R2 to a matter-determined window, the rigorous lower bound came out CONSTANT 0.108 independent of matter scale -- a red flag; isolating overturns v2.414: (b) matter but g_R3=0 & parity=0 -> g_R2 floor 0.0, (e) matter + g_R2=0.001 FEASIBLE, so matter alone forces NOTHING; (f) g_R3=0.09 alone at g_R2->0 VIOLATES graviton positivity, (g) parity=0.06 alone VIOLATES parity-decomposed positivity; so v2.414 held g_R3=0.09 + parity=0.06 fixed and mistook THEIR floor for a matter effect -- corrected: matter (g_4,g_6,g_8) alone does NOT rigorously force the leading curvature coupling, what forces g_R2>0 is the gravitational sector's OWN higher structure (cubic curvature g_R3 + parity, each via amplitude positivity), so the correct statement is 'the higher-curvature/parity couplings force the leading curvature term -- gravity is SELF-forcing' NOT 'matter sources gravity'; the apparent matter->gravity link came from the toy anomaly g_4*g_6=g_R2^2 which v2.414 itself showed is a removable 4D artifact; RETRACTS the v2.414 headline + corrects the v2.415 ledger row, everything else in the de-toying arc STANDS (LQG excluded, WGC ceiling, BH decay, a/c wedge, towers, harmless toys); honest scope -- found by isolating the forcing variable (direct reproducible feasibility fact not a sampled estimate), the candidate's g_R2>0 IS still rigorous (it HAS g_R3+parity) only the ATTRIBUTION to matter was wrong, and whether matter sources gravity via matter loops generating R^2 is a separate true RG statement the engine doesn't encode; a failure caught + reported (the honest trail is the point); 5/5 checks) · [**v2.415 CONSOLIDATION CAPSTONE: the rigor-annotated candidate profile -- which headline claims are rigorous, which need the one residual toy, which need data**](docs/results/2026-07-01-v2.415-qnm-rigor-ledger.md) (ties the de-toying arc v2.411-414 into ONE verified per-claim ledger; each testable tier re-verified live: RIGOROUS -- LQG excluded (cft_flat_space+graviton_fwd_pos+efthedron), matter-sources-gravity (g_R2>=0.108 given matter), string-like moment towers (dispersion positivity), Euler-vs-Weyl^2 a/c wedge (Hofman-Maldacena), L/R graviton chirality bounds (parity-decomposed positivity); RIGOROUS-IMPLIED -- matter dominance's WGC ceiling, extremal-BH decay, near-Planckian species cutoff/ghost-safety (all forced by the rigorous core); TOY/DATA remainder confined to exactly 3 things -- the parity MAGNITUDE (one coefficient, anomaly-inflow rho + birefringence datum), the observable MAGNITUDES (toy maps e.g. g_4<->inflaton behind CMB-S4), and the exact region SIZE; so 8 structural claims rigorous/implied, and the settled+verified one-line answer to 'is the engine a toy?' is NO -- its matter-gravity content is source-exact, the residual toy is one parity coefficient the cosmic-birefringence datum fixes anyway; honest scope -- tiers re-verified where testable or inherited from the cited cycle, 'rigorous'=source-exact in FORM (v2.411 caveat), 'rigorous_implied'=empirically forced over the sampled feasible region (v2.412 caveat, encoding-dependent), a consolidation introducing no new physics; 5/5 checks) · [**v2.414 ENGINE IMPROVEMENT (de-toying step 3, anomaly capstone): matter-sources-gravity is RIGOROUS, not anomaly-dependent -- the only genuine toy left is one parity coefficient**](docs/results/2026-07-01-v2.414-qnm-anomaly-detoy.md) (the anomaly sector was the last load-bearing toy; it splits into anomaly_cancellation (g_4*g_6=g_R2^2, matter<->curvature -- a self-described 'toy 4D form', and 4D has NO pure gravitational anomaly [Alvarez-Gaume-Witten live in 4k+2 dims] so it is a TOY ARTIFACT) and generalized_anomaly_inflow (parity<=rho*g_4*g_R2 -- conceptually-real anomaly-inflow physics per Harlow TASI 2022, toy coefficient rho); the DECISIVE test: does matter-sources-gravity (v2.393, the headline the artifact was credited with) depend on it? NO -- fixing matter and lowering g_R2->0 is excluded by the RIGOROUS source-exact core ALONE (graviton_forward_positivity + cross_sector_efthedron + cemz force g_R2>=0.108, and adding the toy anomaly only nudges the floor to 0.114, +6%), so a theory with matter but vanishing leading curvature violates cross-sector AMPLITUDE POSITIVITY with or without the anomaly = matter-sources-gravity is RIGOROUS; so the anomaly de-toys cleanly -- the matter<->curvature STRUCTURE is rigorous (toy anomaly_cancellation a removable 4D artifact), and the ONLY residual genuine toy is the parity-MAGNITUDE inflow coefficient which governs exactly the birefringence-contingent parity sector (v2.408/329); this CLOSES the de-toying arc (v2.411-414): the candidate's ENTIRE matter-gravity content is rigorous -- LQG excluded (v2.411), matter-dominance ceiling + BH decay implied (v2.412), 2 most speculative proxies harmless (v2.413), matter-sources-gravity rigorous (v2.414) -- the toy chased from the whole stack down to one parity coefficient; honest scope -- g_R2 floor is on the single matter-fixed ray (mechanism general), 'toy artifact' is a physics judgement + the constraint's self-description (the matter->curvature CONCLUSION is rigorously true w/ an independent matter-loop basis, only the anomaly DERIVATION was toy/redundant), did NOT remove anomaly_cancellation (would shift region 1.36x + rebake goldens) so it is removable-in-principle, 'one residual toy' scoped to the matter-gravity+parity Wilson sector; 5/5 checks) · [**v2.413 ENGINE IMPROVEMENT (de-toying step 2): the candidate's real toy-dependence is the anomaly sector alone -- the two most speculative proxies (complexity, SDC) are harmless**](docs/results/2026-07-01-v2.413-qnm-toy-dependence-map.md) (v2.412 localized the genuine toy cuts to 4 constraints; this asks WHICH the candidate actually depends on -- drop each from the full stack, measure how much the local region opens: complexity_cutoff 1.11x + swampland_distance_conjecture 1.11x = NEARLY REDUNDANT/HARMLESS (the two MOST speculative proxies -- an explicit research-grade complexity conjecture with arbitrary weighted-L2 form/C_max=1.5, and the aspect-ratio SDC proxy v2.410 flagged -- barely shape the result, and the scale window is IDENTICAL [0.786,1.070] with/without them; the upper edge is set by the anomaly not complexity), while anomaly_cancellation 1.36x + generalized_anomaly_inflow 2.43x = LOAD-BEARING and cosmic_birefringence 7.36x = real DATA (v2.408); so DOUBLY constructive -- RETIRES two speculative worries (candidate robust to dropping BOTH the complexity conjecture AND the imperfect swampland-distance proxy, so neither the v2.410 field-space gap nor the complexity bound threatens the result) and PINPOINTS the single remaining toy family that matters = the ANOMALY sector; engine now tags HARMLESS_SPECULATIVE (complexity, SDC) + LOAD_BEARING_TOY (anomaly sector) in stack.py so the next de-toying cycle has exactly ONE target; honest scope -- LOCAL leverage (constructed +/-0.15), 'harmless' = low leverage + unchanged scale window + constructed still feasible (NOT that the SDC/complexity forms stop being speculative -- the candidate is just robust to them), birefringence 7.36x is real data not toy; an engine change + the leverage map justifying it, does NOT de-toy the anomaly but isolates it; 5/5 checks) · [**v2.412 ENGINE IMPROVEMENT (de-toying step 1): half the proxy constraints are IMPLIED by the rigorous core -- matter dominance's ceiling and extremal-BH decay are secretly rigorous, not toy**](docs/results/2026-07-01-v2.412-qnm-rigorous-implied.md) (executing the de-toying program by DETERMINING true rigor, not relabelling -- sample the rigorous-core-feasible region, and for each of the 23 sourced_proxy/data constraints measure the fraction of those points that also satisfy it; fraction~1.0 means the source-exact amplitude/causality core ALREADY forces it so its toy prefactor is irrelevant + its conclusion is secretly rigorous; RESULT: 11 of 23 are redundant (effective rigorous core 19->30) -- KEY: weak_gravity_conjecture (g_R2<=sqrt(g_4), the KEYSTONE ceiling of MATTER DOMINANCE v2.389) is implied=1.00 so 'gravity is bounded by matter' is RIGOROUS not toy, and wald_entropy_positivity (Delta S_ext>=0, extremal-BLACK-HOLE DECAY / WGC-as-entropy v2.378) is implied=1.00 so 'extremal black holes decay' is rigorous; also implied: GSL, quantum focusing, holographic subadditivity, t'Hooft anomaly matching, causality/EFT-validity/species -- i.e. source-exact positivity ALREADY enforces the whole swampland/entropy layer over the feasible region (a concrete positivity-implies-swampland instance); the engine now exposes effective_rigorous_stack(); and it SHARPENS the de-toying frontier -- only a handful of constraints add genuine info beyond the rigorous core (complexity 0.002, swampland-distance 0.22, anomaly 0.26, cosmic-birefringence 0.32) so the remaining TOY input is localized to FOUR constraints, not smeared across the stack; honest scope -- 'implied' is EMPIRICAL redundancy over the sampled rigorous-feasible region (n~11k, strong evidence not a global proof), 2 of the 11 (submm-screened-mode, gw_speed) are redundant only because trivially-satisfied/non-binding (flagged), and 'rigorous-implied' inherits the source-exact-in-FORM caveat = 'forced by the source-exact bounds as encoded'; a genuine engine change (IMPLIED_BY_RIGOROUS + effective_rigorous_stack in stack.py) that PROVES which results never needed toy input + localizes those that do; 5/5 checks) · [**v2.411 ENGINE IMPROVEMENT: a first-class rigor classification, and the real (zero-toy) result it isolates -- the source-exact amplitude/causality core alone excludes LQG-induced gravity**](docs/results/2026-07-01-v2.411-qnm-rigorous-core.md) (a genuine ENGINE change responding to the 'it's a toy' critique -- the toy-ness was never uniform, so this makes the distinction FIRST-CLASS: experiments/stack.py now has a RIGOR registry + rigor_of()/filter_by_rigor()/rigorous_core_stack(), tagging all 42 constraints -- 19 RIGOROUS (source-exact amplitude positivity/causality/bootstrap: Adams-Nicolis-Rattazzi 2006, Caron-Huot et al 2021/2024, CEMZ 2014, Hofman-Maldacena 2008, Arkani-Hamed EFThedron), 17 SOURCED_PROXY (real conjectures via toy O(1) proxy forms: WGC, swampland, anomaly, entropy bounds), 6 DATA (real measurements via O(1) observable maps); running the central logic on the RIGOROUS core ALONE (19 bounds, no toy input): the constructed candidate is feasible, it carves a nontrivial region (locally 46x looser than the full stack since the swampland/data tiers do most of the SIZE carving), and it EXCLUDES LQG-induced gravity -- a genuine ZERO-TOY statement that LQG's low-energy couplings violate graviton_forward_positivity + cross_sector_efthedron + cft_flat_space_bound -- while pure GR/string/asymptotic-safety/CDT survive the rigorous core; so the engine's real content is now explicit + separable -- (1) LQG excluded by amplitude/causality with NO toy input, (2) the candidate's amplitude STRUCTURE (matter positivity, g_6^2<=g_4 g_8 dispersion tower, a/c wedge, parity-decomposed L/R graviton bounds) is rigorous -- while the toy-ness is confined to the region SIZE (swampland proxies) + observable MAGNITUDES (data maps), both now TAGGED so any result states its rigor tier instead of a blanket 'toy' caveat; the engine is NOT a toy, it is a rigorous amplitude/causality carver whose swampland+observable layers wear toy prefactors, now first-class; concrete de-toying path laid out (move SOURCED_PROXY->rigorous one at a time by replacing toy forms with literature-exact inequalities: WGC convex-hull bound, field-range SDC, Dvali species count); honest scope -- the tiering is a defensible JUDGEMENT keyed to citations not a theorem ('rigorous' = source-exact in FORM, units/prefactors may still be Lambda=1-simplified), the LQG exclusion rests on the LQG framework ENCODING (uncertain model of its couplings) so it is 'LQG as encoded violates source-exact positivity' not a theorem about all loop quantum gravity, and this makes the toy-vs-real boundary explicit -- it does not itself make the prefactors real; 5/5 checks) · [**v2.410 SWING (honest negative / engine limitation): the candidate's Starobinsky inflation faces the field-range swampland tension the engine's SDC cannot see**](docs/results/2026-07-01-v2.410-qnm-swampland-field-range-gap.md) (a self-critical big swing -- stress-test the candidate for an INTERNAL inconsistency: it satisfies the engine's SDC (v2.383) AND its keystone g_R2 is the Starobinsky R^2 inflaton (v1.86), but Starobinsky needs a SUPER-Planckian field range (Delta_phi~5.3 M_Pl over N~55) which the REAL field-space SDC disfavors -- the physical SDC puts a tower at m~M_Pl exp(-c Delta_phi)~0.005 M_Pl during inflation, far below the candidate's own species-scale cutoff (0.72 M_Pl v2.394), so the EFT breaks down mid-inflation (the known large-field-inflation-vs-swampland tension); YET the candidate PASSES the engine SDC, because that SDC is a toy ASPECT-RATIO proxy (max|g|/min|g|<=20) that bounds coupling HIERARCHIES not field-space DISTANCES -- so the engine encodes the swampland's amplitude/hierarchy SHADOW not its field-space GEOMETRY (the SDC's actual content), and the candidate passes a version of the conjecture the physical field-range SDC would flag; a genuine previously-unstated GAP in the 'swampland-complete' headline (v2.373) -- the carving intersects the swampland's coupling-space projections not its moduli-space distances, so 'swampland-complete' should read 'swampland-COUPLING-complete'; the honest counterweight to the stability run (v2.405-407: robust+converged WITHIN what it encodes, but what it encodes of the swampland is the coupling face not the geometry); constructive next step = a field-range/moduli-distance constraint tied to the species scale (a well-posed core extension that would let the engine ADJUDICATE the Starobinsky-vs-swampland question it can currently only pose); honest scope -- Delta_phi~5.3 M_Pl imported (standard Starobinsky, not engine-computed), the SDC c~1 and whether Starobinsky ACTUALLY violates the SDC is genuinely DEBATED so 'tension' = the known unsettled issue NOT a definitive violation (candidate NOT ruled out), the ROBUST engine-grounded content is the LIMITATION (the SDC is literally a Wilson-coefficient aspect-ratio with no field variable); no new prediction, a gap-identification swing; 5/5 checks) · [**v2.409 SWING: the candidate does not predict a fixed shear viscosity -- eta/s is a modulus-band, KSS-saturated at a=c, KSS-violating if c>a**](docs/results/2026-07-01-v2.409-qnm-eta-s-modulus.md) (a cross-era big swing connecting the recent c-a modulus to a FAMOUS real observable -- the shear viscosity-to-entropy ratio eta/s and the Kovtun-Son-Starinets bound, relevant to quark-gluon-plasma / holographic transport; the early program (v1.72) reported a FIXED eta/s (~0.81 x KSS) via the Gauss-Bonnet holographic relation 4pi(eta/s)=a/c, but the recent arc showed a=c is an ASSUMPTION and c-a a free modulus (v2.398/399), so eta/s is NOT fixed -- it is a BAND: eta/s/KSS = a/c, so across the HM wedge a/c in [1/3,31/18] the candidate spans eta/s in [0.33,1.72] x KSS; at the holographic a=c point (the constructed-theory assumption) eta/s = 1.00 x KSS EXACTLY (KSS SATURATED), and the KSS-VIOLATING region eta/s<1/4pi opens precisely when c>a (Weyl^2-dominant) -- the celebrated Gauss-Bonnet KSS violation requiring the nonzero c-a the resolved basis permits; so the transport prediction is honest+structured (saturates KSS at its assumed holographic point, consistent-with-but-not-requiring a KSS-violating fluid, the violation size a conformal-collider-bounded modulus); directly REFINES the early v1.72 fixed value = it was the a=c assumption in disguise, the same lesson as v2.400 (Weyl^2-sector predictions carry the c-a modulus) applied to a NEW real observable, tying the c-a arc to relativistic-heavy-ion phenomenology; honest scope -- 4pi(eta/s)=a/c is the HOLOGRAPHIC Gauss-Bonnet result so 'the candidate's eta/s' is CONDITIONAL on the candidate having an AdS/CFT dual (a strong unproven premise; no dual -> no transport interpretation), the a/c band is the engine's HM-only bound and the REAL GB causality bound (Brigante 2008: >=0.64 x KSS) is TIGHTER so the band is looser than a full analysis, the robust content is 'eta/s is a band saturating KSS at a=c, sub-KSS only for c>a' not the exact edges; a cross-era refinement, no new engine datum; 5/5 checks) · [**v2.408 SWING: the candidate's data-pinning hangs on one measurement -- cosmic birefringence is the only load-bearing datum**](docs/results/2026-07-01-v2.408-qnm-data-leverage.md) (a fresh big swing -- v2.373 gave the AGGREGATE data leverage (~1.7x), this asks the PER-constraint question: of the four ingested measurements (cosmic birefringence, Eot-Wash sub-mm, GW speed, GW dispersion), which pin the candidate? drop each, measure how much the local feasible region grows: dropping cosmic birefringence opens the region 7.4x, dropping any of the other three opens it 1.0x (no change) -- sub-mm is satisfied throughout the local region (the theory SCREENS v2.354, so it mandates screening as a qualitative FEATURE but never bites inside the region), GW speed/dispersion are frequency-suppressed and non-binding (v2.358); so ONLY cosmic birefringence actively CARVES the candidate = the single load-bearing datum; the complete honest picture is TWO-SIDED -- the candidate's STRUCTURE is robust on four independent axes (family/basis/adversarial/prefactor, v2.405-407) but its DATA-pinning is birefringence-CONTINGENT, resting on one ~3.6-sigma hint (v2.329); not a tension -- the swampland-complete theory carving does most of the work (data adds only ~1.7x aggregate v2.373) and the one binding datum is birefringence, which pins the parity magnitude AND breaks the Z2 handedness mirror to select a unique candidate (v2.406); so 'the one experiment to watch' is unambiguous -- cosmic birefringence (LiteBIRD/CMB-S4) is the empirical lifeline, and CMB-S4's independent matter-sector test (v2.395, large-g_4) is the SECOND orthogonal probe; honest scope -- LOCAL leverage (7.4x local, dominance robust), 'sub-mm opens 1.0x' = doesn't shrink the region NOT irrelevant (mandates screening, satisfied throughout so leverage~0 but role real), GW's genuinely non-binding, quantifies the pervasive birefringence-contingent caveat; 5/5 checks) · [**v2.407 SWING: the swampland-complete carving converges -- the feasible region is near-saturated, so the ~1e-5 predictive volume is a converged answer**](docs/results/2026-07-01-v2.407-qnm-carving-convergence.md) (a fresh meta-question -- is the ~1e-5 predictive region (v2.373) SATURATED (carving converged) or still actively shrinking (an overestimate)? precompute the per-point per-constraint satisfaction matrix on a local box (constructed +/-0.15) and average the cumulative-feasible-fraction curve over 300 RANDOM constraint orders (removing stack-order bias): the local feasible fraction falls 1.0 -> 0.0043 after all 42 constraints, and the order-averaged marginal shrinkage per constraint TAPERS ~18x (first-third 0.0555 vs last-third 0.0030) -- so the intersection is NEAR-SATURATED: strong amplitude/swampland conditions carve the region early, later ones largely re-cut already-excluded volume; so the ~1e-5 region is a CONVERGED property of the swampland-complete intersection not an artifact of an incomplete constraint list (adding more conditions of the SAME KIND would move it little); complements v2.406 (single global island) + v2.405 (prefactor-robust) -- the carving produces ONE region of a CONVERGED size INDEPENDENT of the toy prefactors, three independent senses the consistency-driven candidate is a stable output of the method not a fragile coincidence; honest limit -- convergence is w.r.t. the CURRENT KIND of constraint, a genuinely NEW physical condition (real ringdown rank-3 map, resolved matter-operator basis) could still cut further = exactly the basis-refinement frontier (v2.397); honest scope -- LOCAL box (local saturation, supports but doesn't re-measure the global ~1e-5), ORDER-AVERAGED (not a stack-order artifact but 'last-third adds little' is averaged), 'converged' = diminishing returns of the same-kind condition not a proof the true region equals ~1e-5, satisfaction matrix exact + finite-sample percent noise; 4/4 checks) · [**v2.406 SWING: the candidate is globally unique -- a multi-start search finds a single connected feasible island, no disconnected alternatives**](docs/results/2026-07-01-v2.406-qnm-global-uniqueness.md) (a genuinely new question -- is the candidate THE unique consistent theory or one of several disconnected consistency islands? v2.322 showed no NAMED framework fits, v2.383 showed LOCAL connectivity; this tests GLOBAL uniqueness with a multi-start search -- from 40 random starts across the a-priori O(1) box, greedily descend the total constraint violation to feasibility: 38/40 reach feasibility (the violation landscape FUNNELS toward the region = a single convex-ish basin, not a rugged multi-island landscape), every feasible endpoint lands within ~0.18 (max ~0.32) of the constructed point in a single UNIMODAL cluster, and EVERY endpoint has positive parity handedness (g_R2_parity>0) -- the cosmic-birefringence data broke the theory-only Z2 mirror symmetry (v2.364) so the second opposite-handedness island that exists WITHOUT data is absent WITH it; so the consistency-carved region is a SINGLE connected island and the candidate is GLOBALLY unique -- not merely 'no named framework fits' (v2.322, 14 frameworks) but 'no disconnected alternative consistent theory exists anywhere in the searched O(1) box' (up to the tightly-bounded moduli); the strongest form of the headline -- the swampland-complete carving leaves ESSENTIALLY ONE region so the consistency-driven candidate is a genuine near-unique prediction, the data's role sharp (selects one of two handedness mirror-islands); the 38/40 funnel also explains why the max-margin center is well-defined+stable (v2.361, single interior center); honest scope -- multi-start greedy descent is a HEURISTIC global search (strong evidence not proof; a tiny distant island could be missed), confined to the a-priori box (larger couplings excluded by scale-rigidity v2.390 anyway), couplings clipped>=0, single-handedness is a WITH-DATA statement (theory-only = two mirror islands), 6-coupling base so moduli not in the island count; upgrades uniqueness from framework-exclusion to global-search connectivity, no new datum; 4/4 checks) · [**v2.405 SWING (new robustness axis): the central claim is empirically robust to O(1) toy-prefactor variation -- validating the 'structure robust, numbers toy' caveat**](docs/results/2026-07-01-v2.405-qnm-prefactor-sensitivity.md) (a genuinely new robustness axis -- every result carries a honest-scope caveat 'structure robust, numbers toy' that had been ASSERTED but never EMPIRICALLY TESTED; tests it on the central claim (v2.322: constructed feasible AND no named framework fits theory+data) by re-running it under 0.5x-2.0x variation of five tunable prefactors (cemz_kappa, anomaly_rho, cubic_kappa, cft_alpha, efthedron_alpha) one at a time = 25 variations via build_stack(prefactors=...); the central claim is ROBUST -- 'no named framework fits theory+data' survives ALL 25 (never breaks), the feasible region is non-empty in ALL 25 (never collapses), full unique-feasibility survives 88% (22/25); the 3 failures (anomaly_rho x0.5, cft_alpha x0.5, efthedron_alpha x2.0) are POINT SHIFTS not collapses -- the FIXED constructed point falls outside a rescaled window but the region stays non-empty (a re-centered point is feasible) and matter dominance still holds there; so the qualitative result (nonempty consistency-carved region excluding every named framework) does NOT depend on the specific toy prefactors, only the constructed point's location does, exactly as claimed; closes the robustness theme on FOUR independent axes -- FAMILY (v2.372), BASIS (c-a v2.398-401), ADVERSARIAL (v2.391), and now PREFACTOR (here) -- all survived; the pervasive 'toy numbers' caveat is now precise+honest = specific values move with the prefactors (verified) not that the structure is fragile (falsified); honest scope -- only the CANONICAL-sourced prefactors varied (several constraints hardcode c_anom/tolerance/WGC-alpha/kappa_beta, not varied), ONE-AT-A-TIME 0.5x-2.0x (joint variation not scanned), region-nonempty at failures is a short-walk existence check, tests the central QUALITATIVE claim not that downstream numbers (0.72 M_Pl, 17.6 sigma) are stable (they explicitly move, the point); a meta-validation of the honesty caveat, no new datum; 5/5 checks) · [**v2.404 SWING: the candidate's three unpinned directions form a dark hidden sector -- its testable content is lower-dimensional than its parameter space**](docs/results/2026-07-01-v2.404-qnm-hidden-sector.md) (completes the audit -- the three couplings the theory does NOT pin (g_8 matter top-moment v2.381, g_C Euler-vs-Weyl^2 c-a split v2.399, g_R3_parity parity-odd cubic v2.403): are any OBSERVABLE? perturb each, check whether any constraint it moves is a DATA constraint vs a theory bound -- essentially none: g_8 -> 5 constraints ALL theory (DARK), g_R3_parity -> 5 ALL theory (DARK), g_C -> only gw_dispersion_bound (data but negligible v2.358); so the theory's genuine freedom lives in a ~3-dimensional HIDDEN SECTOR no observational channel probes (matter Regge edge, Euler-Weyl^2 split, parity-odd cubic); sharpens v2.380 (~2.5 observable directions) -- ~5+3 consistent parameters but testable content is lower-dimensional with three unmeasurable directions, observation pins the theory only up to this hidden sector; CORRECTS a qualitative aside in v2.403 -- the 'second cubic-order parity observable' does NOT exist in the engine's channel set (g_R3_parity is dark), so a nonzero parity-odd cubic is invisible and the anomaly-CLOSED relation tying it to the observable quadratic is the only indirect route; honest picture from the audit -- the candidate's ROBUST predictions (matter-gravity locking, ghost-safety, string-like towers, CMB-S4 on g_4) are genuine + choice-independent, wrapped around a small hidden sector of unobservable moduli fixed by ASSUMPTION (equal moments, a=c, no cubic parity) that observation cannot reach; honest scope -- 'dark' = no data constraint IN THE ENGINE'S channel set (not a theorem no observable exists; a richer theory could probe them), 'hidden sector' = unprobed by the catalogued channels (v2.380/381 usage), g_C 'essentially dark' a judgement (one negligible GW touch); an observability audit, no new datum, corrects v2.403; 5/5 checks) · [**v2.403 SWING: g_R3_parity=0 is a third constructed-point assumption -- the minimal parity sector is a choice, and it hides a second parity observable**](docs/results/2026-07-01-v2.403-qnm-third-assumption-parity-cubic.md) (continues the basis-structure/assumption audit -- the constructed theory sets the parity-odd CUBIC g_R3_parity=0 (parity only in the quadratic); forced or an assumption? an ASSUMPTION -- activating g_R3_parity as an independent axis its feasible range is [-0.034,0.035] with 0 INTERIOR (bounded both ends by generalized_anomaly_inflow), so 'parity only in the quadratic' is a CHOICE (the anomaly-SATURATED choice v2.370, cubic suppressed); the anomaly-CLOSED variant (v2.371) instead prefers a NONZERO g_R3_parity=r g_R2_parity~0.028 which is feasible (well inside) -- so the two anomaly variants of the v2.377 fork are literally two points on this axis, saturated(0) vs closed(0.028), distinguished only by whether the second anomaly-matching condition is imposed; adds a THIRD entry to the candidate's ASSUMPTION tier (v2.402): a=c (v2.399), g_6=g_8 (v2.392), g_R3_parity=0 (this) -- the systematic lesson that the max-margin (Chebyshev) point's apparent simplicity (holographic a=c, equal matter moments, minimal parity) is largely ASSUMPTION while the genuine predictions are the ROBUST-tier structural ones (matter dominance, ghost-safety, string-like towers, near-Planckian cutoff); physical consequence -- g_R3_parity!=0 sources a SECOND cubic-order parity observable distinct from the quadratic birefringence, so the 'minimal parity sector' hides a potential extra chiral signal the anomaly-closed variant predicts, and the parity sector is a two-parameter (g_R2_parity,g_R3_parity) family bounded by anomaly inflow which the v2.377 forward test would resolve; honest scope -- 0-interior is robust/measure-independent, window edges [-0.034,0.035] toy-basis (anomaly-inflow+cubic-parity O(1) prefactors), the closed value 0.028 toy (v2.371 r + birefringence g_R2_parity), 'second observable' the qualitative reading (magnitude unpinned); an assumption-audit swing, no new datum about the constructed point; 5/5 checks) · [**v2.402 CAPSTONE (refreshed): the candidate theory as a complete QG low-energy EFT -- consolidating v2.383-401 onto the v2.382 profile**](docs/results/2026-07-01-v2.402-qnm-candidate-profile-refreshed.md) (refreshes the v2.382 capstone with ~18 major results since -- ghost-safety, graviton chirality, landscape topology, the matter-dominance arc, the species-scale cutoff, the CMB-S4 decisive test, the c-a basis resolution -- into one coherent LIVE-verified profile; THE CANDIDATE in one sentence: a FIVE-parameter (plus two free moduli g_8/g_C), ~1e-5-predictive, consistency-DRIVEN, matter-DOMINANT candidate for QG's low-energy EFT -- a near-Planckian (cutoff ~0.72 M_Pl), ghost-safe, string-like, WGC-complete theory whose gravitational sector is BOTH forced into existence (anomaly forbids matter without curvature, v2.393) and capped in size (<=40% of matter, hard bound v2.391) by its matter sector; re-verified live -- constructed uniquely feasible (no framework), matter forces gravity, ghost-safe near-Planckian cutoff 0.716 M_Pl, 17.6-sigma CMB-S4 tension; structural spine = two keystones g_4(67%)+g_R2(60%) = matter-gravity locking (v2.401) + two free moduli g_8(dark v2.381)/g_C (Weyl^2 axis v2.399); four channels -> ~2.5 observable directions (v2.380), parity's native face an order-2 graviton chirality (v2.386); landscape connected but non-convex (SDC sole cause v2.383); CMB-S4 (~2030) make-or-break test (v2.395); tiered -- ROBUST (live-verified structure), ANOMALY-DETERMINED/DATA-PINNED (parity + CMB-S4), ASSUMPTIONS-not-predictions (NEW since v2.382: a=c holographic v2.399 + g_6=g_8 v2.392 are constructed-point choices), TOY/OPEN (magnitudes toy-basis, ringdown rank-1 unsourceable v2.209, g_R4 rank-3 blocked); net -- a genuine self-consistent falsifiable matter-dominant candidate complete under the toy basis whose frontier is basis refinement not further constraint-mining; key advance over v2.382 = the honest recognition that a=c and g_6=g_8 are assumptions (results leaning on them survive qualitatively, v2.400); a consolidation inheriting per-cycle caveats, no new datum beyond live re-verification + assumption-recognition; toy basis, two explicit assumptions, one decisive near-future test; 5/5 checks) · [**v2.401 SWING: the basis has a two-keystone structure (g_4 matter + g_R2 curvature) = matter-gravity locking, with no third hidden degeneracy**](docs/results/2026-07-01-v2.401-qnm-two-keystone-structure.md) (generalizes v2.397 (which counted only g_R2's load to diagnose the c-a degeneracy) by mapping EVERY coupling's constraint load -- the # of 42 constraints whose margin it moves: g_4 28/42 (67%), g_R2 25 (60%), g_6 17 (40%), g_R3 10 (24%), g_R2_parity 9 (21%), g_8 5 (12%), g_C 5 (12%); the tightness is carried by a KEYSTONE PAIR (g_4 + g_R2) with a sharp fall-off after -- and the two keystones are EXACTLY the matter-gravity locking (v2.389-393) made structural: the matter coupling and the leading curvature coupling are the two most over-determined couplings, precisely the pair that source/scale/cap each other; g_4 is even MORE load-bearing than g_R2, consistent with matter dominance + CMB-S4 being the decisive test of g_4 (v2.395) -- the single most-constrained coupling is the one a near-future experiment will measure; the fall-off means NO third keystone / NO second c-a-type degeneracy, the c-a resolution correctly landed g_C in the low-load/nearly-free bin (12%, bounded only by the HM wedge, validating v2.399), and the two dark/free couplings (g_8 matter-top-moment v2.381, g_C Weyl^2 axis v2.399) are exactly the two the theory doesn't pin; so the construction rests on a two-coupling spine with everything else derived or free within a bounded modulus -- the structural fingerprint of matter-gravity locking (two locked keystones, a tail of derived couplings, two free moduli), why the feasible region is so small (v2.373) yet has genuine unpinned directions; honest scope -- concrete perturb-and-count measure exact for this stack, 'keystone' basis-dependent (activating g_C dropped g_R2's load 26->25), 'g_4 top' robust ordering but fractions stack-specific, 'no third degeneracy' means none visible WITHOUT a finer basis (splitting matter operators or the g_R4 rank-3 sub-structure blocked-as-unsourceable v2.209 could reveal more) not that the basis is complete; a structural map, no new physical datum; 5/5 checks) · [**v2.400 SWING (marginalize over the c-a modulus): the Weyl^2-sector headlines survive the a=c assumption; only their magnitudes loosen**](docs/results/2026-07-01-v2.400-qnm-weyl-sector-marginalized.md) (the responsible follow-up to v2.399 -- marginalize the Weyl^2-sector predictions over the whole c-a modulus (g_C across the HM wedge [0.112,0.579] at g_R2=0.193) and check whether the a=c assumption threatens the qualitative headlines or only their numbers; only the numbers: (1) extremal-BH entropy shift Delta S_ext=A g_C+B g_4 (v2.378) a=c 0.458 -> marginalized [0.377,0.843] POSITIVE throughout (BHs DECAY robust), (2) Weyl^2 ghost mass 1/sqrt(g_C) (v2.385) a=c 2.28 -> [1.31,2.99] ABOVE cutoff throughout (ghost-safe robust), (3) species cutoff (v2.394) a=c 0.716 -> [0.606,0.747] near-Planckian in (0.5,1) throughout; so the c-a modulus widens the error bars ~2x but never flips a sign or crosses a threshold -- BH decay, ghost-safety, near-Planckian cutoff all robust to the a=c assumption; CLOSES the c!=a arc (v2.397 diagnose the c-a degeneracy -> v2.398 activate the axis -> v2.399 a=c is an assumption -> v2.400 marginalize, headlines survive): resolving the basis's key limitation neither breaks the candidate nor forces it off a=c, it just makes explicit that the Weyl^2-sector magnitudes carry a conformal-collider-bounded uncertainty the degenerate basis hid; the candidate's qualitative profile (string-like/matter-dominant/WGC-complete/ghost-safe/near-Planckian/decaying-extremal-BHs) stands independent of a=c, the resolved basis adds honest error bars not a different theory; honest scope -- marginalization varies only g_C at fixed g_R2/g_4/g_R3 (the exact modulus, fuller marginalization holds a fortiori by monotonicity), toy coefficients so the magnitudes+~2x widening are toy-basis, the ROBUST content is the sign/threshold survival not the magnitude; adds no new datum, value = closing the arc responsibly; 5/5 checks) · [**v2.399 SWING (full c!=a exploitation): the holographic a=c is an assumption, not a prediction -- c-a is a free modulus bounded only by conformal-collider physics**](docs/results/2026-07-01-v2.399-qnm-c-minus-a-modulus.md) (the follow-up to v2.398 -- with the Weyl^2 axis g_C free, does the theory PREFER the holographic a=c value or was a=c the framework default? a=c is an ASSUMPTION: the feasible a/c=g_R2/g_C spans the ENTIRE Hofman-Maldacena wedge [0.333,1.720]=[1/3,31/18] and a/c=1 (a=c) sits INTERIOR (neither edge nor forced center), with c-a bounded ONLY by the conformal-collider (ANEC) wedge (HM the sole c-axis carver, v2.398) -- so no consistency condition selects a=c; the constructed theory's a=c is a choice inherited from the default g_C=g_R2, not a prediction, and the resolved basis carries a genuine new FREE MODULUS (the c-a / Euler-vs-Weyl^2 split); the honest sibling of v2.392 (g_6=g_8 was a Chebyshev-center artifact) -- a=c joins the ASSUMPTION bin of the candidate's honest tiering (v2.382); consequence -- any Weyl^2-sector result (ghost mass 1/sqrt(g_C) v2.385, screening, extremal-BH entropy A g_C+B g_4 v2.378) was computed AT a=c and carries an unpinned c-a modulus a finer analysis must marginalize over (v2.398 showed the ghost swings [1.31,2.97] across the wedge); positive reading -- c-a is exactly the higher-derivative/stringy signature (two-derivative Einstein gravity has c=a; c!=a needs higher-curvature bulk terms this theory HAS) so the theory is CONSISTENT with but does NOT require a non-Einstein anomaly, its size a genuine open prediction bounded by conformal-collider physics; honest scope -- the robust content is measure-INDEPENDENT (a/c spans the wedge, a=c interior, c-a bounded only by HM) but the mean a/c~0.757 and '77% c>a' are MEASURE-DEPENDENT (uniform-in-g_C sampling skews below 1 toward the wedge geometric mean 0.758; uniform-in-a/c would center ~1.03) so the theory does NOT robustly 'prefer c>a' -- explicitly disclaimed; theory-space exploration of the resolved basis, not a new prediction about the constructed point; 4/4 checks) · [**v2.398 SWING (executes v2.397): activating the Weyl^2 axis g_C resolves c != a -- carved solely by the Hofman-Maldacena wedge, and it moves the ghost**](docs/results/2026-07-01-v2.398-qnm-activate-c-axis.md) (EXECUTES v2.397's highest-impact recommendation -- resolve the c-a degeneracy by activating a distinct Weyl^2 coupling g_C != g_R2 -- and finds the machinery was already BUILT but DORMANT: g_C is a first-class coefficient falling back to g_R2 (a=c, a/c=1) when unset, and the Hofman-Maldacena conformal-collider wedge (1/3<=a/c<=31/18, a=g_R2 Euler / c=g_C Weyl^2) is already in the stack, so NO core change was needed -- the c-axis just had to be turned on; turning it on (g_R2=0.193 fixed, g_C free) opens the feasible g_C range to [0.114,0.579] i.e. a/c in [0.333,1.697] == EXACTLY the HM wedge [1/3,31/18]=[0.333,1.722] (matches to ~1%), with BOTH edges bound SOLELY by hofman_maldacena_wedge -- confirming v2.397's prediction that HM is the one constraint that goes live and is the sole carver of the new axis; the constructed theory (g_C=g_R2, a/c=1) sits DEAD-CENTER in the wedge (the holographic/Einstein a=c point); crucially activating g_C physically MOVES the Weyl^2 ghost -- its mass now tracks g_C, m_g/Lambda=1/sqrt(g_C) in [1.31,2.97] across the wedge (vs the fixed 2.28 in the degenerate basis v2.385) -- separating the Weyl^2 sector (ghost/HM/screening) from the Ricci^2/Euler sector (a-theorem/anomaly) the degeneracy had fused into the keystone g_R2 (v2.396); so resolving c!=a opens exactly ONE new conformal-collider-BOUNDED direction (a/c stays in the ANEC wedge, not free), the candidate unchanged (stays at a=c); the highest-impact extension executed + validated at NO core-change cost; honest scope -- ACTIVATES a dormant-but-built axis (authored v1.71) so genuinely the c!=a extension but no new code (safe/reversible in its favour, caveat that it executes not invents the wedge), the a/c wedge is the exact source-cited HM bound (matching to ~1% a real sole-carver check), ghost g_C-dependence uses v2.385's O(1)-schematic relation so [1.31,2.97] schematic (robust point = ghost now VARIES with the split), candidate UNCHANGED so no new prediction about the constructed point -- characterizes the FREEDOM the resolved basis exposes; a full c!=a exploitation is the natural follow-up; 5/5 checks) · [**v2.397 SWING (method / highest-impact next step): the toy basis's key limitation is the c-a degeneracy, and resolving it is the single most valuable extension**](docs/results/2026-07-01-v2.397-qnm-ca-degeneracy.md) (steps back after ~30 swings to ask WHY constraint-mining now yields diminishing returns and what the highest-impact next step is; the answer -- the basis uses a SINGLE curvature-squared coupling g_R2 for BOTH the Euler (a) and Weyl^2 (c) anomaly coefficients (the c-a degeneracy), so g_R2 drives 26 of 42 constraints (62%) -- which is why g_R2 is the keystone (v2.396) and why several genuinely-independent physical constraints are rendered redundant/trivial: the celebrated Hofman-Maldacena conformal-collider bound 1/3<=a/c<=31/18 is TRIVIALLY satisfied (a=c->a/c=1, margin 0.386, zero information), the a-theorem's Delta_a (Euler) can't be distinguished from the Weyl^2 ghost coefficient, GSL/Bekenstein fold onto the same g_R2 as WGC/anomaly; so the concrete next core-engine step (direction-1) is to RESOLVE c!=a -- add a distinct Weyl^2 coupling g_C independent of the Ricci^2/Euler g_R2 -- which would (a) make Hofman-Maldacena a genuine non-trivial a/c wedge, (b) separate the a-theorem from the ghost/screening sector, (c) let the species scale count g_C and g_R2 independently, (d) resolve which of g_R2's 8 keystone roles belong to Ricci^2 vs Weyl^2 (the basis-dependence every recent swing flagged); the honest conclusion of the constraint-exploration phase -- untouched constraints (quantum-focusing/GSL/Bekenstein/HM) all collapse onto g_R2 and add no independent carving, so the frontier moves from 'characterize the region under the current basis' to 'REFINE the basis', c!=a the highest-leverage refinement; honest scope -- 26/42 is a concrete perturb-and-count measure exact for this stack, HM-trivial is exact given the a=c=g_R2 identification, but 'resolve c!=a is highest impact' is a REASONED judgement not a proven optimum (resolving matter operators or g_R4 sub-structure are alternatives); a method/meta result (direction 3 + a direction-1 recommendation) characterizing the BASIS not the theory, value directional; 5/5 checks) · [**v2.396 SWING: g_R2 is the keystone coupling -- the anomaly's forcing of g_R2>0 automatically satisfies the a-theorem, and g_R2 measures the tower's central charge**](docs/results/2026-07-01-v2.396-qnm-gR2-keystone.md) (the a-theorem reduces to g_R2>=0 (docstring flags it redundant) but its integrate-out nugget g_R2~Delta_a/M^4 links three prior results -- (1) the gravitational anomaly FORCES g_R2>0 given matter (v2.393, region-wide min 0.089>0), (2) g_R2>0 IS the a-theorem monotonicity condition Delta_a>=0, so matter sourcing the leading curvature coupling AUTOMATICALLY guarantees RG-flow monotonicity, (3) g_R2 measures the central charge the UV tower carries: Delta_a~g_R2 M^4=0.05 with M the species scale (v2.394); so the a-theorem is IMPLIED by the anomaly through g_R2, satisfied region-wide and never by coincidence; more broadly g_R2 is the KEYSTONE coupling appearing in EIGHT distinct consistency roles all mutually consistent at 0.193 -- anomaly matching (v2.393), a-theorem (this), WGC cap (v2.389), Weyl^2 ghost (v2.385), species counter (v2.394), screening trigger (v2.354), moment tower (v2.375), parity lock (v2.371) -- the single coupling where the amplitude/swampland/thermodynamic/RG sectors all meet, which is why the construction is so tight (pinning g_R2 pins the theory, over-determined by 8 compatible conditions); honest scope -- the Delta_a link uses the docstring heuristic g_R2~Delta_a/M^4 + toy species scale so 0.05 is doubly toy (robust point = the sign/logic g_R2>0 IS the a-theorem condition), and the 8-role centrality is PARTLY a toy-basis artifact (the a_theorem docstring notes the basis collapses Euler-a and Weyl^2-c onto the single g_R2; a finer basis resolving c-a would distribute the roles), so 'keystone' is a statement about THIS basis while the MUTUAL CONSISTENCY of the 8 roles is a real fact; 4/4 checks) · [**v2.395 SWING: CMB-S4 is a decisive make-or-break test -- matter dominance requires a large matter coupling the CMB-S4 forecast excludes at >10 sigma**](docs/results/2026-07-01-v2.395-qnm-cmb-s4-decisive-test.md) (confronts the matter-dominance arc (v2.389-393, matter must be STRONG) with a SOURCED next-gen forecast -- the engine's CMB-S4 constraint (CMB-S4 Science Book) maps g_4 to the inflationary scalar self-interaction CMB-S4 will measure to sigma=0.03 around 0 (single-field slow-roll); the theory REQUIRES g_4 in [0.33,0.63] (matter dominance), so the constructed g_4=0.53 is 17.6 sigma from the forecast and the SMALLEST feasible g_4=0.33 is still 11.1 sigma -- the ENTIRE feasible region (not just the center) is excluded at >10 sigma if CMB-S4 confirms slow-roll; so CMB-S4 (~2030) is MAKE-OR-BREAK -- either it detects the large scalar self-interaction the theory requires (a striking confirmation of matter dominance) or confirms g_4~0 and FALSIFIES the whole construction; the sharpest falsifiability statement the theory supports (an order of magnitude sharper than the parity discrimination v2.377) because it targets the LOAD-BEARING matter sector not the subordinate gravitational one; resolves the v2.381 worry (matter dark to the 4 GRAVITATIONAL channels) -- the matter sector is NOT untestable, it has a decisive NON-gravitational observable in CMB-S4 probing g_4 through inflationary cosmology; the whole matter-dominant edifice rests on g_4 being large and CMB-S4 will confirm or kill it; honest scope -- rests on the engine's TOY identification of g_4 (primarily the matter dim-8 positivity coupling) with the CMB-S4 inflationary self-coupling (a plausible but toy dual role -- if g_4 is a different operator the tension evaporates), sigma=0.03 is the Science Book forecast rescaled to the toy basis, g_4~0.5 toy so the 17.6/11.1 sigma are toy-basis; ROBUST content = the theory requires a large matter coupling (region-wide g_4>=0.33, basis-robust) and a slow-roll confirmation forecasts it near zero, decisive tension IF the identification holds; CMB-S4 is a forecast not data (~2030); 4/4 checks) · [**v2.394 SWING: the candidate theory's actual UV cutoff -- a Dvali species scale of ~0.72 M_Pl, tightly predicted**](docs/results/2026-07-01-v2.394-qnm-species-scale-cutoff.md) (computes a central quantity never done in the prior 27 swings -- the actual UV cutoff where gravity goes strongly coupled and the EFT breaks down, via the engine's Dvali species-scale constraint Lambda_species/M_Pl=1/sqrt(N) with N=1+nu(|g_R2|+|g_C|+|g_R3|), nu=2, N_max=3; the couplings count the same string-like tower (v2.375); constructed N=1.95 -> Lambda_species=0.716 M_Pl, and across the WHOLE feasible family N in [1.37,2.36] so the cutoff is TIGHTLY bounded at Lambda_species/M_Pl in [0.65,0.86] (mean 0.74) -- a NEAR-PLANCKIAN EFT valid to ~0.7 M_Pl before a tower of ~2 effective species makes gravity strong; the species bound (N<=3) is only ~62% saturated (max 79%) so the theory keeps its cutoff above the strong-coupling floor 1/sqrt(3)=0.577 M_Pl; closes the EFT validity ladder self-consistently -- cutoff 0.72 M_Pl < Weyl^2 ghost at 2.28x cutoff ~1.6 M_Pl (v2.385) so the ghost is trans-cutoff as required and the species tower UV-completes the EFT; grounds the tower/species picture (v2.375/388) -- the ~2 species are the low-lying end of the infinite log-convex tower, the species bound the holographic class-B face of the same tower the A/C moment conditions generate (the one place the least-carving B sector v2.374 sets a physical scale); answer to 'where does this QG EFT break down' -- not at M_Pl but ~0.7 M_Pl, lowered by its own light tower; honest scope -- Lambda=M_Pl/sqrt(N) with N a linear |coupling| sum is the engine's TOY Dvali encoding (nu/N_max toy O(1), species-by-coupling-sum a proxy for tower STATES), 0.72 toy-basis scaling with nu, g_C=g_R2; robust structure = sub-Planckian (N>1 always since anomaly forces g_R2>0 v2.393) but near-Planckian (couplings O(0.1) by matter dominance/scale rigidity v2.389/390) + tightly bounded; ghost ladder reuses v2.385's O(1)-schematic mass so ordering robust not the 1.6; 6/6 checks) · [**v2.393 SWING: matter SOURCES the leading gravitational correction -- the anomaly forbids matter without curvature (converse of matter dominance)**](docs/results/2026-07-01-v2.393-qnm-matter-sources-gravity.md) (matter dominance (v2.389/391) bounds gravity from ABOVE; this finds the bound from BELOW -- a direct feasibility test shows pure GR (all couplings zero) is consistent but a theory with a MATTER sector and ZERO higher-curvature gravity is INCONSISTENT (violates anomaly cancellation); the engine's 4D anomaly constraint is a slab |g_4 g_6 - g_R2^2|<=0.2, so given g_4 g_6=0.212 it forces the leading curvature coupling g_R2 into a band [0.108,0.642] around sqrt(g_4 g_6) whose lower edge 0.108>0 means g_R2=0 is FORBIDDEN -- matter cannot exist without a nonzero leading gravitational correction; combined with matter dominance the leading curvature coupling is pinned from BOTH sides (anomaly floors it, WGC/screening cap it -> with only g_R2 on the feasible band is [0.108,0.226] nonzero at both ends), so the higher-curvature gravitational sector is neither absent nor free -- SOURCED into existence by matter from below (anomaly) and bounded in size by matter from above (positivity/WGC); completes the matter-gravity locking picture (matter forces + scales + caps gravity, the tower organizes it internally) and re-reads 'QG is not GR' concretely -- GR WITH matter is inconsistent here, consistency demands the matter-sized higher-curvature completion, pure GR survives only in the empty no-matter limit; honest scope -- the 4D anomaly is the engine's TOY AGW simplification (4D gravity has no perturbative graviton anomaly; the real AGW anomaly is chiral matter in 4k+2 dims) so 'matter sources g_R2' is WITHIN the toy anomaly-matching encoding not a first-principles 4D theorem, band edges toy-basis (scale with tol=0.2/c_anom=1), the wide tolerance means a loose band not a sharp locking so 'forces g_R2 nonzero into a band' is exact but 'determines g_R2' overstates; robust content = anomaly matching forbids the g_R2=0 corner; 4/4 checks) · [**v2.392 SWING (honest negative): g_6 = g_8 is a Chebyshev-center artifact, not a consistency prediction -- the matter Regge edge is free**](docs/results/2026-07-01-v2.392-qnm-g6-g8-artifact.md) (tests a tempting numerical coincidence -- the constructed theory has g_6=g_8=0.4 exactly, i.e. matter moment ratio g_8/g_6=1, which read as a spectrum-edge diagnostic would place a matter state EXACTLY at the Regge edge; is it forced or a max-margin coincidence? ARTIFACT -- across the feasible family g_8/g_6 ranges [0.54,3.2] (mean 1.16, std 0.30), only 35% near-equal and 68% with g_8>g_6, so the top matter moment ratio is a FREE unconstrained direction with 1.0 well interior; so the 'matter state at the Regge edge' reading is NOT robust and g_6=g_8 belongs in the toy-basis-center-coincidence bin not the robust-structure bin (refines the v2.382 tiering -- check coincidental equalities against the family before reading them as structure); dovetails with g_8 being the DARK parameter (v2.381) -- the top matter moment is both observationally invisible AND dynamically free, so nothing (data or consistency) pins where the matter Regge tower ends, consistent with matter dominance setting the SCALE (v2.389) not the internal matter-spectrum SHAPE; a negative result that keeps the prediction list clean; honest scope -- range sampler/toy-basis-dependent but the REFUTATION is robust (factor >5 span, 1.0 interior, not marginal), the spectrum-edge reading is a heuristic the result doesn't depend on, no overclaim risk since it removes a claim; 4/4 checks) · [**v2.391 SWING (adversarial): matter dominance is a HARD bound -- gravity/matter <= ~0.4 everywhere, gravity never reaches matter**](docs/results/2026-07-01-v2.391-qnm-matter-dominance-hard-bound.md) (stress-tests v2.389's matter dominance -- is there ANY consistent theory with strong gravity + weak matter? adversarial search maximizing ||(g_R2,g_R3,g_R2_parity)||/||(g_4,g_6,g_8)|| over multiple 150k-step biased walks; NO counterexample -- constructed ratio 0.29, adversarial MAX 0.40 (per-seed 0.398/0.399/0.403, rock-stable), the search never approaches 1, the gravitational couplings hit their WGC/CEMZ caps before matter can be made small; so 'gravity is the weakest force' is a SHARP CEILING not just a tendency -- the gravitational sector's total Wilson-coefficient strength is at most ~40% of matter's in EVERY consistent theory; upgrades matter dominance (v2.389) from a correlation to a bound and closes the obvious attack (no strong-gravity corner exists); combined with scale-rigidity (v2.390) the gravitational sector is doubly constrained -- fixed in overall scale AND capped at <=40% of matter's strength -- so the higher-curvature corrections are a small matter-subordinate perturbation not free knobs; honest scope -- ~0.40 is from adversarial SEARCH (tight lower estimate of the sup not a proven max, but 3 seeds agree to 0.01 and none nears 1), L2-norm measure (others give different numbers same sub-unity conclusion), 0.40 toy-basis scaling with the O(1) prefactors but the sub-unity STRUCTURE robust from the matter-set ceilings; 5/5 checks) · [**v2.390 SWING: the consistent theory has a definite absolute scale -- consistency alone pins the coupling strength to a factor ~3, no data**](docs/results/2026-07-01-v2.390-qnm-scale-rigidity.md) (follows matter dominance (v2.389) with the sharp question -- is the theory SCALE-INVARIANT (can all couplings be uniformly rescaled g->lambda g and stay consistent, region a cone, only ratios matter) or is the absolute scale fixed? most bounds are scale-homogeneous (positivity, degree-2 dispersion tower, degree-1 CEMZ) and DON'T bound a uniform rescaling, but scaling the constructed point on the THEORY-ONLY stack (no data) stays feasible only for lambda in [0.32, 1.07] -- a bounded window of factor ~3.3, NOT a cone; scale DOWN past 0.32 and cubic_graviton_matter_bound fails (couplings too weak), scale UP past 1.07 and anomaly_cancellation fails (too strong); so a handful of absolute-scale constraints make the theory nearly SCALE-RIGID -- consistency alone, with NO data, pins the overall coupling strength to a factor ~3, the theory has almost no free overall normalization (a predictivity statement complementary to v2.373: not only the RATIOS are carved, the SCALE is too); the constructed max-margin point sits at 91% up the window hard against the strong-coupling (anomaly) ceiling -- the theory lives near the most strongly-coupled scale consistency permits, which is why the anomaly/parity sector is so tightly determined (v2.371, anomaly nearly saturated along the scale direction); sharpens v2.389 -- matter sets the gravitational scale AND consistency nearly fixes the matter scale, so the WHOLE theory is scale-rigid; honest scope -- tests UNIFORM rescaling along the constructed ray (non-uniform not covered), toy O(1) prefactors so exact edges toy-basis but the bounded-window STRUCTURE (broken by absolute-scale constraints not the homogeneous ones) is robust, theory-only so data narrows further; 5/5 checks) · [**v2.389 SWING (unifying cross-sector principle): the gravitational sector has no intrinsic scale -- matter caps every gravitational coupling**](docs/results/2026-07-01-v2.389-qnm-matter-dominance.md) (asks whether the piecemeal cross-sector bounds share ONE organizing principle -- they do: MATTER DOMINANCE; every gravitational coupling is capped by a matter-sector quantity and the caps SCALE with matter -- g_R2<=sqrt(g_4) [WGC], g_R3<=0.8 sqrt(g_4 g_R2) [CEMZ], g_R2_parity<=sqrt(g_4 g_6)-g_R2 [graviton positivity] all hold region-wide, each ceiling correlates 0.70-0.80 with the matter scale sqrt(g_4 g_6), and total gravitational strength sqrt(g_R2^2+g_R3^2+g_R2_parity^2) tracks matter at corr 0.62; raise matter and gravity is ALLOWED to grow, hold matter fixed and the gravitational couplings are boxed -- so 'gravity is the weakest force' (the WGC slogan) is realized as a systematic structural pattern across the WHOLE higher-curvature sector, not just the single WGC inequality: the gravitational EFT is enslaved to the matter EFT with no allowed size of its own; clean division of labour -- the matter sector sets the gravitational SCALE while the intra-gravitational moment tower (g_R3^2<=g_R2 g_R4) sets the internal SHAPE; unifies the anomaly floor (v2.350), birefringence->BH bridge (v2.379), graviton ceiling (v2.387), WGC/ghost (v2.385) as facets of one principle and re-reads the construction as a MATTER theory whose gravitational corrections are the largest matter permits (explaining why matter is load-bearing though observationally dark v2.381); honest scope -- toy O(1) prefactors so ceilings toy-basis but the STRUCTURE (each a matter product/root, none absolute/purely-gravitational) is robust, corr <1 because ceilings also involve g_R2 (matter = leading not sole input), NOT every bound is matter-controlled (the tower is intra-sector, the screening cap is an absolute data bound) so the claim is leading-ceilings + overall-scale are matter-set; 5/5 checks) · [**v2.388 SWING: the SDC hierarchy bound and the string-like tower jointly fix the number of simultaneously-light couplings per sector**](docs/results/2026-07-01-v2.388-qnm-sdc-tower-light-couplings.md) (connects two prior swampland results that look in TENSION -- v2.383's SDC hierarchy bound max/min<=R_max=20 vs v2.375/376's infinite geometrically-decaying tower g~r^n whose deep couplings->0 (max/min->inf would violate the bound); the RESOLUTION -- the deep tower levels are HEAVIER states (the SDC's own light tower at large field distance, so the SDC predicts exactly the tower it appears to forbid), and the hierarchy bound applies within one 'window' whose depth BOTH facts fix: n_max=log(R_max)/log(1/r); curvature tower r=0.466 -> n_max=3.93 (~4), matter tower r=0.756 -> 10.72; the engine's basis (3 curvature g_R2/g_R3/g_R4, 3 matter g_4/g_6/g_8) fits both windows and the CURVATURE basis nearly SATURATES its ~4-slot window (3 of ~4, sat 0.76) while matter uses 3 of ~11 (0.28) -- exactly v2.376's prediction (curvature tower harder/faster-decaying so runs out of hierarchy room sooner); so the two swampland facts jointly EXPLAIN why the gravitational EFT truncates to a few curvature operators (the finite basis is the depth the SDC+tower permit, not arbitrary) and predict the matter sector could support a much deeper (~11) tower of light operators the dark sector (v2.381) hides; honest scope -- n_max is a HEURISTIC counting geometric-floor levels (upper estimate, faster tower gives fewer), R_max=20 toy (n_max~log so weak dependence), ratios toy-basis but the ORDERING (curvature window < matter) robust from v2.376, engine-basis-fits is a consistency check not an independent test; 5/5 checks) · [**v2.387 SWING: graviton positivity caps parity violation ~4x above the CMB value -- a falsifiable ceiling for GW-birefringence searches**](docs/results/2026-07-01-v2.387-qnm-parity-graviton-ceiling.md) (completes the parity-as-graviton arc (v2.386) -- the left-handed polarization-decomposed positivity (Caron-Huot et al. 2024) caps the parity coupling from above at g_R2_parity <= sqrt(kappa g_4 g_6) - g_R2, the graviton-positivity ceiling; constructed ceiling 0.267 vs CMB value 0.06 -> 4.45x headroom, and the ceiling exceeds even the CMB 2-sigma upper edge (0.153); across the whole feasible region the ceiling averages 0.25 and headroom 4.3x (range 2.7-7.1), EVERY theory using <50% of its graviton parity room -- so unlike g_R2 (which sits AT its screening cap v2.354) the parity coupling sits at only ~1/4 of its ceiling, the one sector the theory is NOT pushed to the edge in; the consequence is a concrete FALSIFIABLE target window for GW birefringence -- the theory permits a chiral signal anywhere from the CMB value up to ~4x larger and stay graviton-consistent, but a signal ABOVE the ceiling would violate graviton positivity and falsify the parity-as-graviton picture (a hard upper bound on how chiral gravity can be, turning 'how big is the parity violation' into a bounded interval [CMB, ~4x CMB]); refines the discrimination story (v2.377) -- the anomaly variants 0.06-0.09 sit deep inside the graviton-allowed region, so the ANOMALY determination not graviton positivity pins the value; honest scope -- ceiling scales as sqrt(kappa) so 0.267 is toy but the order-few headroom is robust for any O(1) kappa, CMB numbers toy-basis, the ROBUST content is the ORDERING (parity well below its graviton ceiling region-wide), the target-window is on the amplitude-level coupling (dCS->waveform map absent); 5/5 checks) · [**v2.386 SWING (native parity channel): the theory predicts an order-2 left/right graviton asymmetry, locked to the CMB handedness**](docs/results/2026-07-01-v2.386-qnm-graviton-chirality-asymmetry.md) (the engine maps g_R2_parity to CMB PHOTON birefringence as an order-of-magnitude proxy, but g_R2_parity is the gravitational Chern-Simons/Pontryagin coupling whose NATIVE home is the graviton sector -- the engine's polarization-decomposed positivity (Caron-Huot-de Rham-Tolley-Zhou 2024) gives INDEPENDENT bounds on the two circular graviton polarizations (g_R2 +/- g_R2_parity)^2<=kappa g_4 g_6, so left and right gravitons have different effective quartic self-couplings; constructed L=g_R2+g_R2_parity=0.253 vs R=g_R2-g_R2_parity=0.133 -> 1.9:1 ratio with the left-handed sector closer to its positivity bound (margin 0.148<0.194); across the whole feasible region the asymmetry is ORDER-2 (mean 2.1, range 1.5-3.9) with the left sector tighter in 100% of theories; the asymmetry RATIO is kappa-INDEPENDENT so it is robust (only the margins use kappa=1); the handedness LEFT>RIGHT is locked to the sign of g_R2_parity which the CMB beta>0 selects (v2.364) -- flip the CMB rotation and the graviton asymmetry flips -- so the SAME parity violation seen in the CMB makes left-handed GWs order-2 more strongly self-interacting than right, a large native CROSS-MESSENGER chirality prediction (order-unity, unlike the ~1e-19 GW speed birefringence v2.358); reframes the parity test (its cleanest home is the graviton left/right asymmetry, CMB the toy shadow) and sharpens observability (parity is a chirality STRUCTURE not one channel); honest scope -- source-cited bound + exact L/R decomposition, ratio kappa-independent/robust but it is the AMPLITUDE-level coupling chirality (not the tiny kinematic speed birefringence, dCS->waveform map absent), g_R2_parity magnitude toy + birefringence-contingent though the ~2 ratio follows from g_R2_parity/g_R2~0.3; 5/5 checks) · [**v2.385 SWING (the #1 objection): the constructed theory's Ostrogradsky ghost is kept above the EFT cutoff by the WGC**](docs/results/2026-07-01-v2.385-qnm-ghost-safety.md) (a fresh physics swing at higher-derivative gravity's most-cited objection -- the Weyl^2 term propagates a massive spin-2 Ostrogradsky GHOST; the ghost mass is m_g^2 ~ Lambda^2/g_C so it sits ABOVE the cutoff (a truncation artifact not a low-energy instability) exactly when g_C<1, i.e. m_g/Lambda=1/sqrt(g_C)>1; constructed g_C=g_R2=0.193 -> m_g/Lambda=2.28 (ghost at ~2.3x cutoff), whole feasible region g_C<=0.254<1 -> m_g/Lambda>=1.99 (never within 2x of the cutoff); the GUARANTEE is the WGC -- the engine's g_R2<=sqrt(g_4) with g_4<1 (max 0.63) forces g_C<1, so the SAME weak-gravity condition that decays extremal black holes (v2.378) also keeps the theory's own higher-derivative ghost trans-cutoff, and the deeper resolution is the infinite tower (v2.375) -- a finite-derivative truncation carries the ghost pole but the full string-like tower is what a ghost-free UV completion looks like, so the trans-cutoff ghost is exactly the truncation artifact the tower resolves; the consistent-EFT answer to 'but higher-derivative gravity has ghosts'; honest scope -- m_g^2~Lambda^2/g_C has an O(1) convention factor so the formula is schematic but the parametric g_C<1=>ghost-above-cutoff is robust (satisfied 2x over), g_C=g_R2 is the toy-basis Weyl^2/Ricci^2 identification, 'ghost above cutoff = safe' is the standard EFT stance (not a proof a UV completion exists, though the tower is the candidate), g_4<1 is empirical-robust; 5/5 checks) · [**v2.384 SWING (a tempting hypothesis fails honestly): star-centrality tracks a hub's hierarchy-safety, not its Chebyshev-centrality**](docs/results/2026-07-01-v2.384-qnm-star-convexity-hubs.md) (follows v2.383's non-convexity -- if the region is not convex, is it at least STAR-convex around the constructed Chebyshev-center theory, a 'hub' from which every consistent theory is reachable by a straight consistent path? tempting hypothesis: the max-margin point is the star-center; it FAILS honestly -- the constructed point is a good hub (97% of lines to random consistent theories stay consistent) but NOT specially good: interior hierarchy-safe hubs (min coupling>=0.03) average 94% (same within noise), while hubs with a coupling at/near zero (on the swampland-valley boundary) drop to 74%; star-fraction correlates with the hub's smallest coupling at 0.90 -- so star-centrality is governed by hub HIERARCHY-SAFETY not Chebyshev-centrality, the constructed theory is a good hub only because it happens to be hierarchy-safe (any interior safe theory is equally good), and NO point is a perfect star-center (even safe hubs lose ~4-6% of lines because the swampland valleys pit the region from every vantage -- reaching a near-zero-coupling theory requires curving AROUND the hierarchy-forbidden region); sharpens the topology (connected + approximately-star-convex-from-the-safe-interior but genuinely non-star-convex at the few-percent level, obstruction always the swampland bound never the amplitude sector); honest upshot -- the constructed theory's specialness is its max-margin robustness (v2.322/361) NOT any central hub role, the landscape has no privileged center only a safe interior + swampland-pitted edges; honest scope -- MC percentages carry error but the ordering + 0.90 correlation are robust, inherits v2.383's toy-SDC-proxy scope so exact percentages encoding-specific; 5/5 checks) · [**v2.383 SWING (landscape topology): the space of consistent QG EFTs is NON-convex, and the Swampland Distance Conjecture is the sole cause**](docs/results/2026-07-01-v2.383-qnm-landscape-nonconvexity.md) (a fresh structural conjecture-and-test -- is the consistent-QG-EFT region CONVEX? sample feasible theories, take straight-line convex combinations, check interpolants against the real feasibility oracle; result NON-convex -- ~8% of interpolations between two consistent theories pass through an INCONSISTENT one (oracle-checked, endpoints feasible + midpoint infeasible, not a sampling artifact), and 99.9% of the midpoint violations are the Swampland Distance Conjecture; the CONTROL clinches it -- dropping the SDC restores convexity to 99.99%; so the amplitude-level conditions (positivity/causality/dispersion/anomaly/EFT-hedron) are all CONVEX (intersections of PSD cones + half-spaces) and the ONE genuinely quantum-gravitational condition (the SDC, an aspect-ratio/hierarchy bound max|g|/min|g_nonzero|<=20) is the one that breaks convexity -- interpolating a coupling from a symmetry-protected zero up through tiny-but-nonzero values crosses the hierarchy bound (a large-field-distance/light-tower region); consistent QG EFTs form a CONNECTED but NON-convex family (walk stayed in one component), the non-convexity a swampland signature invisible to the low-energy amplitude bounds -- two consistent theories can be separated by a 'swampland valley'; honest scope -- oracle-checked non-convexity is real, but the SDC is a TOY aspect-ratio proxy for Ooguri-Vafa, a strictly-positive aspect-ratio bound is itself convex so the non-convexity enters via the near-zero-coupling treatment (exact-zero excluded/allowed vs tiny-nonzero included/blows the ratio) = encoding-specific locus, amplitude-convexity is robust (literal PSD/half-space), 'connected' is the sampled component not a global proof; 5/5 checks) · [**v2.382 CAPSTONE: the constructed theory as a complete candidate for quantum gravity's low-energy EFT (swing-arc v2.367-381 consolidation)**](docs/results/2026-07-01-v2.382-qnm-candidate-theory-profile.md) (ties the swing arc into one coherent candidate-theory profile, verifiably re-confirming the robust core LIVE (check()/re-computation), updating the v2.363 ledger; THE CANDIDATE in one sentence -- a FIVE-parameter, ~10^-5-predictive, consistency-DRIVEN candidate for QG's low-energy EFT: an infinite string-like tower in BOTH matter and curvature sectors (v2.375/376), anomaly-DETERMINED parity-odd sector (v2.371), extremal black holes that DECAY with the WGC automatic from matter positivity (v2.378), FOUR falsifiable channels reducing to ~2.5 independent observable directions in the curvature/parity sector (v2.380), and a matter string-like identity theoretically robust but observationally DARK (v2.381); re-verified live -- constructed uniquely satisfies theory+data (no named framework does), all four channel signatures live+nonzero, both sectors multi-state towers (r_matter 0.756/r_curv 0.466 <1), Delta S_ext 0.458>0; tiered -- ROBUST (structure/basis-robust), ANOMALY-DETERMINED (field-theory, toy prefactors), CONJECTURE (tower-unification ringdown value, survived attack only as the rigorous strict floor), CAVEATS (matter string-likeness dark, parity birefringence-contingent v2.329, ringdown magnitude rank-1 unsourceable v2.209, all NUMBERS toy-basis); net -- a genuine self-consistent falsifiable candidate low-energy EFT (string-like, parity-violating, WGC-complete, predictive from consistency alone) whose testable content is the curvature/parity sector and whose one empirical lifeline is cosmic birefringence; a consolidation inheriting the per-cycle caveats, no new claim; toy basis; 5/5 checks) · [**v2.381 SWING: the string-like matter identity is observationally DARK -- g_8 feeds no channel**](docs/results/2026-07-01-v2.381-qnm-dark-parameter.md) (the complement of v2.380 -- correlating each of the 5 genuine inputs with the four channel observables to find the DARK parameters; g_8 is the UNIQUE dark coupling (max|corr| 0.09 with any channel) while every other input is observable (g_R2->screening 1.00/BH 0.88, g_R2_parity->parity 1.00, g_R3->ringdown 0.95, g_4->BH 0.83, g_6->screening/BH ~0.6 indirect); the sting -- g_8 is the top matter dispersion coefficient, entering r_matter = g_6^2/(g_4 g_8) = 0.756, the very quantity establishing the multi-state STRING-LIKE MATTER identity (v2.343); so the coupling that most directly encodes the matter tower's multi-state structure is precisely the one the four channels cannot touch -- the theory's headline ('string-like gravity', v2.342) is on its matter side theoretically established but observationally HIDDEN; the ~2.5 observable directions (v2.380) live entirely in the curvature/parity sector, none probes the matter Regge structure, so a skeptic could grant the four channels and still deny 'string-like matter' (no observation tests it) -- the testable content is the curvature/parity sector, the matter-string-likeness is a consistency inference not an observational prediction; honest scope -- toy channel maps so numbers toy-basis but g_8's darkness is STRUCTURAL (it appears in no channel observable, only the matter dispersion tower no channel reads), g_6's observability is itself indirect (via g_R2), verdict not threshold-sensitive; 5/5 checks) · [**v2.380 SWING (deflationary): the four channels are not four independent tests -- they probe ~2-3 observable directions**](docs/results/2026-06-30-v2.380-qnm-channel-correlation-structure.md) (measures the JOINT structure the bridges hinted at -- the 4x4 correlation matrix of the channel observables across the feasible family + the effective number of independent observables (correlation participation ratio); result -- screening and BH are NEAR-IDENTICAL (corr 0.88, both essentially linear in g_R2, so ONE observable not two), ringdown is ORTHOGONAL to the other three (max 0.12, driven by g_R3 a free direction v2.372), parity PARTIALLY correlated with the g_R2 group (~0.5 via the anomaly link g_R2_parity~sqrt(g_4 g_R2)); effective observable dimension 2.45 of 4, so the theory's genuinely independent observable content is ~2.5 directions -- (i) the leading scale g_R2 (sets screening=BH + half of parity at once) and (ii) the cubic g_R3 (sets ringdown alone); honest DEFLATION of the 'four channels' story -- measuring the g_R2-scale forecasts the others so only RINGDOWN adds a fully orthogonal probe, which also explains WHY the cross-sector bridges exist (they ARE the g_R2-group correlations); to pin the full 5-parameter theory you need both the g_R2-scale channels AND ringdown; honest scope -- toy channel maps + sampled family so the 2.45 is toy-basis, but the STRUCTURE (which couplings each channel depends on) is basis-robust, screening=BH is near-definitional; 5/5 checks) · [**v2.379 SWING (cross-sector bridge): cosmic birefringence sets a lower bound on the extremal-black-hole entropy shift**](docs/results/2026-06-30-v2.379-qnm-birefringence-bh-entropy-bridge.md) (extends the cross-sector-bridge program (v2.350/351) to the new BH channel (v2.378), linking channel 1 (parity/CMB) to channel 4 (BH extremality); three facts chain -- (1) cosmic birefringence via the anomaly floor lower-bounds g_4 g_R2 >= beta_lower^2/rho = 0.037 (v2.350), (2) the Cheung-Liu-Remmen extremal-BH shift is Delta S_ext = g_R2 + 0.5 g_4 (v2.378), (3) AM-GM gives g_R2+0.5 g_4 >= 2 sqrt(0.5 g_4 g_R2) >= 2 sqrt(0.5*0.037) = 0.272 -- so the birefringence detection GUARANTEES a minimum extremal-BH entropy correction Delta S_ext >= 0.27 (every theory fitting the birefringence data has extremal BHs decaying at least that strongly; constructed 0.458 respects it); a genuine bridge from cosmology to horizon physics via two independent pieces of physics (anomaly matching + the WGC/entropy theorem); DATA-SOURCED -- without the detection the parity floor vanishes and the product bound -> 0 (only the weaker matter-only floor B g_4 survives), so it is the birefringence MEASUREMENT not just the operator content that floors the BH entropy; completes the anomaly-sector web -- birefringence lower-bounds g_4 g_R2 (v2.350) which lower-bounds the parity companions (v2.357) AND the BH entropy shift; honest scope -- AM-GM is exact but the 0.27 is toy-basis (scales as 2 sqrt(B beta_lower^2/rho), toy CLR coefficients A=1/B=0.5/g_C=g_R2, higher-derivative-expansion units not absolute entropy), a LOWER bound, birefringence-contingent (v2.329); 5/5 checks) · [**v2.378 SWING (fresh sector): the theory predicts extremal black holes DECAY -- a fourth channel, WGC automatic from matter positivity**](docs/results/2026-06-30-v2.378-qnm-extremal-bh-channel.md) (pivots to a new sector -- black-hole thermodynamics -- and identifies a FOURTH prediction channel (BH extremality) beyond parity/ringdown/screening; by the Cheung-Liu-Remmen/Reall-Santos theorem (engine's wald_entropy_positivity) the leading higher-derivative shift to the near-extremal Reissner-Nordstrom entropy is Delta S_ext = A g_C + B g_4 with Delta S_ext>0 <=> WGC (extremality bound moves so q_ext/m_ext>1, the BH sheds charge); constructed Delta S_ext = 0.193 + 0.5*0.529 = 0.458 > 0 -> extremal RN BHs DECAY; the bold part -- the matter term alone gives a positive floor B g_4 = 0.265 > 0, so for ANY theory with g_4>0 (matter forward positivity, an unavoidable unitarity condition) the shift is positive regardless of the curvature sector, so the WGC is NOT an extra assumption but a CONSEQUENCE of matter positivity -- the theory is 'WGC-complete by construction' (QG's deepest self-consistency, no stable extremal remnants, automatically met); across frameworks all g_4>0 decay (string 0.45/AS 0.35/lqg 0.60/cdt 0.50), only pure GR is marginal (Delta S_ext=0, extremal BHs exactly stable = the GR result); like screening (v2.359) the channel is NON-DISCRIMINATING (a prediction, not a discriminator, shared by all positive-g_4 gravities); honest scope -- A=1,B=0.5 is the engine's SIMPLIFIED source-cited encoding (real coefficients depend on the background), g_C=g_R2 in the engine so that term is toy but the g_4 floor is robust, Delta S_ext in higher-derivative-expansion units not absolute entropy; toy coefficients, rigorous positivity structure; 5/5 checks) · [**v2.377 SWING (forward test): next-generation birefringence discriminates WHICH consistency principle the parity sector obeys**](docs/results/2026-06-30-v2.377-qnm-parity-forward-test.md) (caps the parity-determination arc with a forward-looking claim: the theory has a HIERARCHY of parity determinations predicting distinguishable birefringence -- geometric center 0.204 deg, anomaly-closed (both matchings exact, cubic!=0, v2.371) 0.222, anomaly-saturated (inflow exact, cubic=0, v2.370) 0.266, feasibility-ceiling (no anomaly determination, v2.360) 0.309 -- all BELOW the current central 0.34+/-0.09 (0.34-1.51 sigma), unresolvable at today's 0.09 deg but the SPREADS are the prediction: separating anomaly-saturated from anomaly-closed (testing whether the parity-odd CUBIC is present, the v2.371 fork) needs only ~0.02 deg, and separating any anomaly determination from the ceiling tests whether the parity magnitude is anomaly-DETERMINED or sits at the consistency edge; so a birefringence measurement ~4x sharper (~0.02-0.03 deg) turns the SAME observable that established the parity headline into an INTERNAL test of the theory's anomaly structure (is matching saturated? cubic present? anomaly-determined or ceiling-limited?) -- the sharpest testability statement the parity sector supports; honest scope -- absolute beta values toy-basis (scale with rho/rho_match + the toy 3.4-deg map, shift together under renormalization), the ROBUST content is the ORDERING + relative spreads, 'required precision ~0.02 deg' is a derived statistical spread NOT a sourced instrument forecast, rests on the hint (v2.329); 5/5 checks) · [**v2.376 SWING (attacked, survives): the theory is string-like in BOTH sectors -- two infinite towers, gravity harder than matter**](docs/results/2026-06-30-v2.376-qnm-two-tower-scales.md) (applies v2.375's log-convex tower to the matter sector too -- both (g_4,g_6,g_8) and (g_R2,g_R3,g_R4) are log-convex moment sequences so each generates an infinite tower; within-sector decay ratios at the center: r_matter=g_6/g_4=0.756, r_curv=g_R3/g_R2=0.466, so the curvature tower decays FASTER (higher effective cutoff for gravity's higher-curvature corrections); the ATTACK -- r_matter=0.756 equals the dispersion ratio only because g_6=g_8 at the center, so is 'gravity harder' a g_6=g_8 artifact? survives: across the feasible family the curvature tower decays faster in 89% of samples (means 0.40 vs 0.72), a genuine structural feature not an artifact; sharpens 'string-like in two senses' (v2.342) to 'string-like in both SECTORS as infinite towers at DIFFERENT scales', with a dimensionless scale separation r_curv/r_matter~0.6, gravity the more suppressed (confirming v2.368's distinct-spectra refutation of an identical shared tower); honest scope -- both towers log-convex is rigorous given the dispersive structure, the within-sector ratios are clean, but the cross-sector comparison mixes different normalizations (toy basis) so the robust content is the ORDERING not the 0.6 number, 11% edge exceptions, g_R5+/g_10+ not engine couplings, sampled family; 5/5 checks) · [**v2.375 SWING (all orders): the curvature sector is a log-convex moment tower -- an infinite string-like tower, not a finite-derivative theory**](docs/results/2026-06-30-v2.375-qnm-infinite-curvature-tower.md) (extends the moment tower to ALL orders; the engine mandates one rung g_R3^2<=g_R2 g_R4, but if the curvature couplings are moments of a positive spectral density (the dispersive representation the equivalence principle establishes, v2.369) the WHOLE sequence is LOG-CONVEX g_{R,n}^2<=g_{R,n-1}g_{R,n+1}, and a log-convex sequence lies above the log-linear interpolation through its first two points, so iterating from (g_R2,g_R3)=(0.193,0.09) gives a rigorous geometric LOWER bound g_{R,n}>=g_R2 r^(n-2) with r=g_R3/g_R2=0.466: floor tower 0.193, 0.090, 0.042, 0.020, 0.009, ... all strictly positive so the tower NEVER truncates -- a nonzero Ricci^2/Ricci^3/Riemann^4/R^5/... correction at EVERY order = the signature of a STRING-LIKE UV (infinite Regge tower -> infinitely many higher-derivative operators) and the OPPOSITE of a truncated f(R)/finite-derivative gravity; r=0.466<1 -> the higher-derivative expansion CONVERGES (sensible EFT, finite curvature cutoff), r a proxy for the curvature/Regge scale ratio; the all-orders completion of v2.369 (leading g_R4 strictly above floor -> the WHOLE tower floored away from zero order-by-order); honest scope -- rigorous GIVEN the moment structure (v2.261/v2.369), but g_R5+ are NOT engine couplings (a consequence of the assumed structure not encoded constraints), r=0.466 + values toy-basis (STRUCTURE basis-robust, NUMBERS toy), geometric floor is the minimal log-linear bound not a pinned Regge slope; 5/5 checks) · [**v2.374 SWING: which physics carves the EFT? predictivity by constraint class -- swampland + positivity dominate, holography barely**](docs/results/2026-06-30-v2.374-qnm-predictivity-by-class.md) (decomposes v2.373's consistency-driven predictivity by the engine's own ConstraintClass taxonomy (A amplitude/positivity, B information/holographic, C universality/swampland+anomaly); dropping each class from the theory-only stack and measuring how much the feasible region OPENS: drop C_UNIVERSALITY (11 constraints: WGC/distance-conjecture/anomaly-inflow/'t-Hooft/BNOSSW) opens ~42x, drop A_AMPLITUDE (20 positivity bounds) ~37x, drop B_INFORMATION (7 holographic: Bekenstein/subadditivity/GSL/quantum-focusing) only ~5x; ranking C > A >> B, and C does it with FEWER constraints (11 vs 20) so universality carves most efficiently; PHYSICS statement -- quantum gravity's low-energy EFT is pinned mainly by UNITARITY (amplitude positivity) + SWAMPLAND/ANOMALY consistency, NOT by entropy/holographic bounds (largely implied by positivity+universality for these theories); reproduces at the VOLUME level what v2.325 found for the BINDING; honest scope -- the RANKING is robust (read from dropped-class counts, tens of feasible), the opening-FACTORS order-of-magnitude (rare-event baseline), grouping = the engine's A/B/C taxonomy (CEMZ sits in A), 'B barely carves' is regime-dependent (holography bites for exotic couplings), toy basis, box-dependent; 5/5 checks) · [**v2.373 SWING (method-as-proposal): how predictive is the swampland-complete carving? ~10^-5 of the a-priori volume, almost all from consistency**](docs/results/2026-06-30-v2.373-qnm-program-predictivity.md) (the third mandated direction as a bold quantified claim -- Monte Carlo over an a-priori box of O(1) Wilson coefficients (CP-even in [0,1], parity in [0,0.2]): only ~2e-5 survives the full consistent+observed stack (a ~50000x volume reduction), the theory-only stack already admits ~3.3e-5 (~30000x), so DATA adds only ~1.7x -- the overwhelming majority of the predictive power is THEORETICAL CONSISTENCY not the 4 data constraints; the method-as-proposal made quantitative -- intersecting all consistency conditions determines the higher-derivative gravity EFT to ~10^-5 of its a-priori O(1) volume (~4-5 orders), and it is a CONSISTENCY-driven prediction not a data fit; reconciles with v2.358 (birefringence the only binding data): data is crucial for the PARITY direction (why the parity headline is birefringence-contingent, v2.329) but adds little to the OVERALL volume since the bulk matter+curvature carving is consistency alone; honest scope -- BOX-DEPENDENT magnitude (a convention; ORDER ~10^-5 + the consistency-vs-data SPLIT are the robust content), rare-event Monte Carlo (few feasible samples so large statistical error -- order of magnitude reliable not the precise 50000x), toy basis; 5/5 checks) · [**v2.372 SWING (strong form refuted): does the gravitational sector follow from the matter sector? Mostly no -- 5 genuine inputs**](docs/results/2026-06-30-v2.372-qnm-gravity-from-matter-rigidity.md) (a rigidity swing testing the boldest structural question -- does ALL of gravity's higher-derivative sector follow from the matter content?; fixing the matter couplings (g_4,g_6,g_8) and mapping the feasible gravitational sub-space, the LEADING CURVATURE stays substantially FREE: g_R2 spans [0.108,0.228] (extent 0.12), g_R3 spans [0.000,0.186] (extent 0.19), so gravity is NOT a function of matter -- the strong form is REFUTED, g_R2/g_R3 are genuine independent inputs; but the WEAK form survives (the recent swing arc): given matter+the two leading curvature couplings, the PARITY sector is DETERMINED (closed anomaly system v2.371) and the ringdown quartic is BOUNDED (strict floor v2.369 + cap v2.351); so rigidity is partial+countable -- of the 7 Wilson couplings only 5 are GENUINE inputs (3 matter + 2 leading curvature), the 2 parity couplings determined by them and the ringdown quartic bounded by them; consistency+data+anomaly matching reduce ~8 couplings to a 5-parameter family (not a point, not a function of matter) -- an honest predictive-rigidity measure: strong (parity fixed, ringdown bounded) but not total; honest scope -- sampled lower-bound extents (a better sampler only WIDENS = stronger 'free'), the 5-count treats parity as anomaly-determined (toy prefactors, reverts to 7 if the v2.371 fork unsaturates), data-independent count; toy basis; 5/5 checks) · [**v2.371 SWING (completion): the two exact anomaly conditions CLOSE, uniquely predicting the whole parity-odd sector**](docs/results/2026-06-30-v2.371-qnm-anomaly-closed-parity-sector.md) (completes v2.370 -- that used ONE exact anomaly condition + g_R3_parity=0 input; the engine carries a SECOND exact-in-field-theory condition ('t Hooft matching, v2.353), and taking BOTH as the equalities they physically are gives two equations in two unknowns -- a CLOSED SYSTEM: inflow g_R2_parity^2+2g_R3_parity^2=rho g_4 g_R2 AND 't Hooft g_R3_parity=rho_match(g_4+g_6)g_R2_parity -> unique solution g_R2_parity=0.0654, g_R3_parity=0.0304, fixing the ENTIRE parity-odd sector from the parity-EVEN sector with NO data (verified: both equalities satisfied + globally feasible), including a NONZERO cubic (reversing the bound-form center g_R3_parity=0, v2.352) -- the strongest challenge to v2.364's 'parity is entirely data-driven'; but it lands as a falsifiable FORK not a clean win: the COMPLETE system fits birefringence WORSE than saturation-only (beta=0.222 deg, 1.31 sigma, vs v2.370's 0.266/0.82 sigma) because the nonzero cubic eats anomaly budget -- so the data mildly prefers a SUPPRESSED cubic (better fit, matching incomplete) over the fully-matched cubic (complete, worse fit); a sharper birefringence measurement discriminates; honest scope -- exact-equality structure defensible but the values (0.065,0.030) toy (scale with rho/rho_match), tension mild (both <2 sigma), doesn't overturn v2.364 (sign still data-set), rests on the hint (v2.329); 5/5 checks) · [**v2.370 SWING: anomaly matching (taken as the exact equality it is) predicts the parity magnitude -- and fits the data better**](docs/results/2026-06-30-v2.370-qnm-anomaly-saturation-parity.md) (a fresh swing in the parity sector, challenging v2.364's 'parity magnitude is data-driven'; the engine bounds the parity content by the matter-graviton inflow (g_R2_parity^2+2g_R3_parity^2<=rho g_4 g_R2) conservatively, but anomaly matching ('t Hooft) is an EXACT EQUALITY in field theory (a UV anomaly matches its IR image exactly), so the physical case is SATURATION -- which with the verified g_R3_parity=0 (v2.352) FIXES the parity coupling from the parity-EVEN sector with NO data input: g_R2_parity=sqrt(rho g_4 g_R2)=0.078 (verified to sit exactly on the engine's anomaly boundary + globally feasible); maps to beta=3.4*0.078=0.266 deg, only 0.82 sigma from the measured 0.34+/-0.09 -- and fits BETTER than the Chebyshev center (beta=0.204, 1.51 sigma); so anomaly matching gives a DATA-INDEPENDENT reason to prefer the UPPER edge of the birefringence window (0.078 not the max-margin 0.06), improving the fit -- refining v2.364: the SIGN is still data-set (matching fixes magnitude not handedness) and the magnitude is data-BOUNDED [0.047,0.078], but WITHIN that window the theory has a genuine theoretical PREFERENCE, so parity is NOT purely data-driven; honest scope -- anomaly matching IS exact (defensible structure) but the value 0.078 scales as sqrt(rho) (toy prefactor) and saturation assumes the toy operator content, 'fits better' is mild (0.82 vs 1.51 sigma, both <2), doesn't overturn v2.364, rests on the hint (v2.329); 5/5 checks) · [**v2.369 RIGOROUS UPGRADE: the equivalence principle mandates g_R4 STRICTLY above the moment floor**](docs/results/2026-06-30-v2.369-qnm-ringdown-floor-strict.md) (the tower-unification swing lands its rigorous residue -- and needs NEITHER the shared-tower assumption NOR any form-factor restriction; the chain: (1) matter is multi-state (r_matter=g_6^2/(g_4 g_8)=0.756<1, v2.343) so by strict Cauchy-Schwarz the matter spectral density has >=2 distinct-mass states; (2) the EQUIVALENCE PRINCIPLE (Weinberg soft-graviton theorem) forces the graviton to couple to every energetic state with a nonzero positive coupling, so those >=2 states ARE curvature-sector states -- the shared tower is FORCED not assumed (a massive state can't decouple from gravity); (3) hence the curvature density has >=2 positive-weight points and strict Cauchy-Schwarz gives r_curv<1 STRICTLY for ANY positive form factor; (4) so g_R4 > g_R3^2/g_R2 STRICTLY -- the floor is NEVER saturated; verified two ways (strict CS over 200k random 2-state measures, r<1 every distinct-mass draw; form-factor independence r_curv<1 across s in [-5,5]); so the ringdown quartic is strictly above its moment minimum -- a mandated GR-distinguishable minimum ringdown deviation whenever matter is multi-state; removes v2.367's fragility (no sharp value) and v2.368's moderate-form-factor caveat (only the MAGNITUDE g_R4-floor is form-factor-dependent, its SIGN is not); the arc: v2.367 sharp point (conjecture) -> v2.368 robust band -> v2.369 strict inequality (theorem given equiv principle + multi-state matter, modulo the toy spectral encoding); a STRICT INEQUALITY not a value; toy numbers, rigorous structure; 5/5 checks) · [**v2.368 ATTACK on the v2.367 conjecture: a spin-2-vs-matter form factor breaks the SHARP value but not the BAND**](docs/results/2026-06-30-v2.368-qnm-tower-unification-attack.md) (following the retrospective's rule -- ATTACK the conjecture, don't admire it; the obvious objection to v2.367 is that the spin-2 graviton and matter couple to the shared Regge tower via DIFFERENT form factors, so r_curv need not equal r_matter; tested with a toy two-state tower (masses (1,3), weights (1,0.5)) reproducing r_matter=0.758, then weighting the curvature sector by a spin form factor mu^s (s=0=identical); VERDICT -- the STRONG form dies (the sharp g_R4=0.0555 is the s=0 special case; over a plausible band s in [-1,1] r_curv drifts to [0.75,0.83] and g_R4 to [0.050,0.056], ~10% spread, not a point -- the form-factor objection is real), but the WEAK form SURVIVES (across the whole band and out to s=2 the curvature stays MULTI-STATE and g_R4 stays STRICTLY above the floor 0.042, in a narrow ~1.2-1.3x-floor band well below the causality cap); so the conjecture is downgraded from a POINT to a robust BAND (g_R4~0.050-0.056 from the shared-tower hypothesis, still falsifiable -- g_R4 AT the floor refutes it); a swing that died honestly in its strong form and survived in its weak form; honest scope -- toy two-state tower probing structural sensitivity not computing real g_R4, form factor modeled as mu^s, weak form robust to MODERATE not arbitrary differences; 5/5 checks) · [**v2.367 BOLD SWING: matter-curvature spectral-tower unification pins the ringdown quartic to 1.32x the moment floor**](docs/results/2026-06-30-v2.367-qnm-tower-unification-conjecture.md) (a deliberate swing-for-a-breakthrough, flagged as a CONJECTURE; the matter sector is multi-state (dispersion ratio r_matter=g_6^2/(g_4 g_8)=0.756<1, v2.343); THE CONJECTURE -- if the graviton and matter couple to the SAME string-like Regge tower (the unified UV of v2.338/339/343), the matter and curvature coefficients are moments of the same normalized spectral shape so their scale-invariant dispersion ratios must be EQUAL: g_R3^2/(g_R2 g_R4)=g_6^2/(g_4 g_8)=0.756, which PINS the otherwise-loose ringdown quartic g_R4=floor/r_matter=1.32x floor=0.0555; the swing SURVIVES its first test -- 0.0555 satisfies the moment-tower Cauchy-Schwarz bound, sits inside the v2.337 feasible range [0.05,0.60], and is STRICTLY above the floor (curvature ALSO multi-state, as a shared tower requires), collapsing the loose g_R4 range to a single value; a genuinely new sharp FALSIFIABLE ringdown prediction (falsifier: a ringdown curvature ratio of 1/floor refutes it) that would make ringdown quantitative from a UV-structure argument rather than the unsourceable rank-3 matrix (v2.209); HONEST SCOPE -- a CONJECTURE not an engine result, the identical-spectral-shape assumption is STRONG (spin-2 graviton vs matter couple via different form factors, so ratios need not be equal), the weaker safe version predicts only r_curv<1; a proposal to be attacked, toy basis; 6/6 internal-consistency checks) · [**v2.366 the rank question is ANSWERED (v2.209), correcting v2.365: parity is dark to ringdown, so it rides on birefringence alone**](docs/results/2026-06-30-v2.366-qnm-parity-ringdown-dark-convergence.md) (an honest self-correction + a convergence; v2.365 called the qNM->R4 rank 'the open deep-research question' but the repo ALREADY determined it in v2.209 (fetched 2026-06-29 from the primary literature), which v2.365 failed to reference: a source-backed rank-3 qNM->Bresciani map does NOT exist -- the Bresciani basis (arXiv:2504.12855) is rank-3 but has ZERO QNM apparatus, the only public ringdown analysis (arXiv:2411.17893) is rank-1 per parity-even theory (max parity-even sourceable rank 2) and EXPLICITLY defers the parity sector, so the parity-odd third axis g_R4_c3=Im(c_minus)=Q2.Q2tilde is a DARK axis (observable=False); so the rank is a DETERMINED NEGATIVE not an open question -- a full-rank ringdown discriminator is unsourceable from public data; THE CONVERGENCE (new content): this reinforces the session's parity result independently -- parity is observable ONLY via cosmic birefringence, confirmed TWO structurally independent ways: (a) birefringence is the only BINDING data constraint (v2.358) + (b) the parity-odd curvature operator is DARK to ringdown (v2.209); the engine's own ringdown floor g_R3^2/g_R2 is parity-blind by construction (re-verified live); so the whole parity headline (existence v2.321/magnitude v2.360/sign v2.364) is birefringence-contingent (v2.329) because there is NO second observational handle on parity, in data OR ringdown; rank facts source-cited from the tested v2.209 artifact, non-claiming; 5/5 checks) · [**v2.365 the ringdown channel's quantitative readiness: floor + cap + source coefficients present; the qNM->R4 map is the single blocker**](docs/results/2026-06-30-v2.365-qnm-ringdown-channel-readiness.md) (a cross-arc synthesis connecting this session's ringdown work (v2.349 floor, v2.351 cap) to the repo's pre-existing ParSpec/qEFT source bridge (v2.188-191) -- never previously joined -- to locate exactly why ringdown isn't quantitative; 3 of 4 pieces READY: (1) moment-tower floor g_R4>=g_R3^2/g_R2=0.042 (live), (2) causality cap g_R4_floor<=kappa^2 g_4=0.339 (live), (3) source-backed qEFT ParSpec QNM deformation coefficients from arXiv:2205.05132 (v2.191: p_qEFT=6, a RANK-1 single-ell_qEFT-axis ray); BLOCKED (4) the operator-basis map + axis orientation from the engine's THREE R4 axes (g_R4_c1/c2/c3) onto the ParSpec ell_qEFT axis (ready_for_framework_claim=False, 9 map blockers); piece (4) IS the deep-research question 'is qNM->R4 rank-3 or rank-1?' -- the public ParSpec source gives a rank-1 ray, the engine has 3 axes, and whether they map full-rank or collapse is the missing operator_basis_map; so ringdown's STRUCTURE is complete + a source bridge exists but a numerical delta_omega (and any ringdown framework exclusion) is NOT claimable until the map is supplied; gives the deep-research a precise acceptance target (the v2.190 contract) instead of vague 'ringdown is schematic'; non-claiming synthesis inheriting the v2.190/191 claim boundaries; 5/5 checks) · [**v2.364 is the constructed theory's RIGHT-handedness a prediction or a data-readout? (a pure data-readout)**](docs/results/2026-06-30-v2.364-qnm-handedness-data-readout.md) (the remaining parity question after v2.352 (cubic unpredicted) + v2.360 (quadratic magnitude data-pinned): is the SIGN of g_R2_parity a theoretical prediction? NO -- flipping g_R2_parity -> -g_R2_parity at the constructed point, the handedness-sensitive theoretical constraints (left/right-handed graviton positivity) DO break the reflection symmetry but SWAP as a MIRROR PAIR (left 0.148 <-> right 0.194), leaving the MIN theoretical margin identical for both signs (0.0025 = 0.0025) -- so the theory has NO net handedness preference, both signs are theoretically feasible, and ONLY the cosmic-birefringence data (beta=+0.34>0; margin +0.044 vs -0.364 violated) breaks the tie; so the right-handedness is read off the SIGN of the measurement, not predicted -- had beta<0 been measured the engine would build an equally-consistent LEFT-handed theory; completes the parity-odd-quadratic anatomy: EXISTENCE (v2.321) + MAGNITUDE (v2.360) + SIGN are ALL data-driven, the theory supplies only the CORRELATIONS (anomaly budget + the mirror-pair structure); the sharpest form of the v2.329 dependence -- the parity headline is the DATA structured by the theory, not a parity prediction the theory makes; honest scope -- exact check() output, the mirror-pair is a natural but encoding choice (a chiral-anomaly constraint would break the tie theoretically, none in the engine); 5/5 checks) · [**v2.363 the new-theory program ledger v2: the three-channel state of the theory (v2.343-362 consolidation)**](docs/results/2026-06-30-v2.363-qnm-program-ledger-v2.md) (a capstone consolidation updating the stale v2.323 ledger with this session's arc, VERIFIABLY re-confirming the load-bearing core live (check() calls not assertions): the constructed theory uniquely satisfies theory+data, NO named framework does (v2.322 holds), parity is the sole observational discriminator (beta=0 infeasible), screening is generic, and all three channel falsifiers verify live; findings tiered -- ROBUST (structural/basis-robust: constructed unique, ~3D well-posed region, three channels, cross-sector bridges), DATA-CONTINGENT (the parity prediction + discrimination + screening universality, all resting on the ~3.6-sigma birefringence detection = the only binding data constraint + single point of failure), TOY-BASIS/SCHEMATIC (the numbers; ringdown magnitude rank-1 unsourced pending deep-research), CAVEATS (margins not cross-comparable v2.361; screening generic not distinctive v2.359; center marginal under joint O(1) variation v2.346); single-sentence state -- the engine constructs a parity-deformed curvature-trimmed string-like gravity that uniquely fits current consistency+data, makes three falsifiable predictions, and whose distinctiveness + testability ride almost entirely on cosmic birefringence; 5/5 checks) · [**v2.362 does v2.361's margin-incomparability caveat invalidate v2.341's which-binds headline? (audited: no)**](docs/results/2026-06-30-v2.362-qnm-trilogy-margin-robustness.md) (a QC audit closing the v2.361 -> v2.341 implication loop; v2.361 found the engine's margins aren't cross-comparable (gw_speed ~5e-16 vs theoretical O(0.01-1)), so margin-aggregating headlines need checking; v2.341 (which deep requirement binds) aggregated worst-margins across three groups -- audited robust: (1) NONE of v2.341's groups (unitarity/causality/WGC) contains a data/pathological-scale constraint, all theoretical; (2) their margins are comparably scaled -- all in [0.050, 0.440], max/min ratio 8.9 vs the ~1e13 gw_speed would introduce; (3) the ordering holds at the center -- causality loosest (worst 0.122) > wgc (0.091) > unitarity tightest (0.050) = the 'causality has headroom, unitarity+WGC bind' conclusion, robust to a 2.46x rescale (far above O(1) prefactor noise); so v2.341 is NOT a margin-incomparability artifact, a genuine comparison among comparably-scaled theoretical constraints; the caveat is real but doesn't REACH v2.341 (which never aggregated the data constraints); honest scope -- audits the constructed point (scale-comparability is per-constraint so it suffices), family worst-margins are in v2.341, toy basis; 5/5 checks) · [**v2.361 is the engine's central prediction well-posed? leave-one-out stability of the Chebyshev radius (with a methodological caveat)**](docs/results/2026-06-30-v2.361-qnm-center-leave-one-out.md) (the method-as-proposal validation -- AND an honest methodological discovery made en route; THE TRAP -- a naive Chebyshev radius over ALL constraints is ill-defined because the engine's margins are NOT cross-comparable: gw_speed's margin is ~5e-16 (its bound is itself 5e-16, v2.358) while theoretical margins are O(0.01-1), so the all-constraint min-margin is dominated by gw_speed's tiny absolute scale and a naive leave-one-out is spuriously dominated by dropping gw_speed (the first attempt gave exactly this degenerate result) -- a real caveat for ANY program computation aggregating margins; THE RESULT (data constraints kept for feasibility but excluded from the radius objective): the theoretical Chebyshev radius is 0.037 and dropping each theoretical constraint, the always-slack controls (scalar positivity/HM wedge/cubic pos/EFT box) move it by 0 while only the anomaly/universality family opens it (max +0.006), no single drop blows it open (<<0.5) -- so the central prediction is OVER-DETERMINED by a small theoretical core and INVARIANT to the slack remainder = well-posed, a stable feature of the consistency geometry not balanced on one knife-edge; honest scope -- the radius is a LOCAL annealed-ascent estimate (a better optimizer only sharpens the separation), the separation is robust not the absolute numbers, conditional on the toy basis; 5/5 checks) · [**v2.360 does the theory actually FIT the birefringence data, or merely sit inside the 2-sigma band? (a refuted concern)**](docs/results/2026-06-30-v2.360-qnm-birefringence-goodness-of-fit.md) (a quantitative goodness-of-fit upgrading the parity headline from the weak 'consistent at 2-sigma', and refuting the natural 'it-only-underpredicts' criticism with a number; the constructed center predicts beta=0.204 well below the measured 0.34+/-0.09, so does the theory STRUCTURALLY underpredict? NO -- finding the max feasible parity coupling over the consistent+observed region (greedy ascent) gives g_R2_parity~0.091 -> beta_max~0.31, INSIDE the measured 1-sigma band [0.25,0.43], only 0.34-sigma from central; so the theory reaches the central value at its 2-sigma-high edge and the constructed center's 0.204 is just the conservative Chebyshev choice not the best fit; the parity prediction is a RANGE [0.16,0.31] overlapping the 1-sigma band on its upper end, with a mild LOW-side lean (ceiling 0.31 just below central 0.34 = the theory mildly prefers a smaller birefringence, a soft lean not a tension); combined with v2.359 (parity is the sole discriminator) the channel that matters most FITS; honest scope -- greedy ascent is a search not a global-max proof (true ceiling could be higher = stronger fit), beta map is toy so the residual is soft, rests on the hint (v2.329); 5/5 checks) · [**v2.359 observational fingerprints: each framework's signature across the three channels (and which channel discriminates)**](docs/results/2026-06-30-v2.359-qnm-framework-fingerprints.md) (joins v2.322 (frameworks scored) with v2.356 (three-channel map) by tabulating each named framework's per-channel signature -- new per-channel content + an honest tempering; (1) PARITY is the unique discriminator: birefringence needs g_R2_parity in [0.047,0.153] so beta=0 EXCLUDES the four parity-even frameworks (pure_gr/string/AS/cdt), only constructed (beta 0.204) + lqg (0.272) are parity-consistent and lqg is theory-excluded (v2.322) -> constructed uniquely consistent; (2) SCREENING is GENERIC not distinctive -- EVERY non-GR framework has g_R2 over the cap (string 0.20/AS 0.15/cdt 0.22/lqg 0.30/constructed 0.19) so all 4/4 must screen, honestly TEMPERING v2.354 (screening is a real falsifiable but generic feature of higher-derivative gravity, not a constructed-theory fingerprint; only pure_gr escapes, but it's parity-excluded); (3) RINGDOWN floor ORDERS them, constructed mildest (0.042) consistent w/ v2.336; net -- the constructed theory's observational distinctiveness lives ENTIRELY in the parity channel, so birefringence is not only the only binding data constraint (v2.358) but the only channel that SEPARATES it from the named frameworks; honest scope -- toy framework encodings (not real Veneziano etc.), rests on the hint (v2.329 -- if a systematic, NO channel discriminates); 5/5 checks) · [**v2.358 data-leverage audit: which of the four ingested-data constraints actually bind the constructed theory?**](docs/results/2026-06-30-v2.358-qnm-data-leverage-audit.md) (a completeness audit with a recorded self-correction; of the program's 4 ingested-data constraints, only ONE actively binds the constructed theory in the standard (screened) config -- cosmic birefringence (signed-distance 0.013, the parity channel); submm is VACUOUS screened but LOAD-BEARING unscreened (the screening channel via config switch, v2.354); both GW-propagation constraints DON'T bind -- gw_speed is BLIND (ratio-to-bound 2.3e-5: GW170817 is a frequency-INDEPENDENT speed test, higher-derivative gravity gives a frequency-SUPPRESSED dispersion ~(E_GW/E_cutoff)^2~1e-20) and gw_dispersion is SLACK (signed-distance 0.64); SELF-CORRECTION recorded -- the gw_speed margin prints +0.0000 which LOOKS knife-edge but the bound is itself 5e-16 and the deviation is ~2e-5 of it, so the margin is the FULL bound (maximally slack), a display artifact not a bind; so the region's data leverage is essentially birefringence + submm-when-unscreened, the two GW constraints are honest sector-DOCUMENTATION not discriminators -- which is exactly why birefringence (v2.329) is the single point of empirical failure (it's the only binding data constraint, GW can't rescue/refute the parity headline); thresholds non-sensitive (0.013 vs 0.64 vs 2e-5); toy basis; 5/5 checks) · [**v2.357 the parity and screening channels are correlated: stronger birefringence forces a deeper screening mandate**](docs/results/2026-06-30-v2.357-qnm-parity-screening-correlation.md) (makes the v2.356 channel-correlation EXPLICIT for the parity<->screening pair -- two of the three channels are physically linked, not independent; the anomaly-inflow budget ties the parity coupling to g_R2 (g_R2 >= g_R2_parity^2/(rho g_4)) and g_R2 is exactly what the unscreened Eot-Wash bound caps, so the screening over-cap factor f = g_R2_parity^2/(rho g_4 g_R2_max) scales as g_R2_parity^2 -- a QUADRATIC amplifier; across the data-pinned parity window [0.047,0.078] at constructed g_4, f runs 1.12 -> 1.81 -> 3.06, exceeding 1 EVEN AT THE birefringence floor, so the screening mandate is locked in by the parity data itself; engine confirms -- family corr(g_R2,g_R2_parity)=+0.45 and every member respects the anomaly floor; mechanistically explains v2.355 (screening universality data-linked = the SAME anomaly+birefringence pushing g_R2 past the cap) and refines v2.356 (channels observationally distinct but parity+screening physically coupled), so a future tightening of birefringence would DEEPEN the screening mandate; honest scope -- exact at fixed g_4, the numbers scale 1/g_4 (hence family corr<1 as g_4 varies), rests on the hint (v2.329) + rho (v2.344), toy basis; 5/5 checks) · [**v2.356 the falsifiability map: how to kill the constructed theory, in three independent channels**](docs/results/2026-06-30-v2.356-qnm-falsifiability-map.md) (a synthesis of the session's arc into the single most decision-relevant artifact -- the complete refutation map, every threshold RE-COMPUTED from the engine not asserted; THREE observationally independent channels each with a definite falsifier: (1) PARITY (CMB/GW birefringence) -- predict g_R2_parity in [0.047,0.078]>0, falsify by beta=0 (verified g_R2_parity=0 infeasible) or a GW bound below the CMB floor 0.0471, FULLY data-dependent; (2) RINGDOWN (BH QNMs, LISA/ET) -- predict g_R4 floor 0.042 bracketed [0,kappa^2 g_4=0.339], falsify by a pure-GR ringdown below the floor (g_R3!=0 members), CP-even/data-INDEPENDENT (magnitude rank-1 schematic); (3) SCREENING (Eot-Wash) -- predict the R^2 scalaron screened (g_R2=0.193 is 3.1x over the unscreened cap, verified fails ONLY submm unscreened), falsify by a detected unscreened DE-scale fifth force, data-independent CORE; bottom line -- the most TESTABLE channel (parity) is fully data-contingent + under near-term pressure while the two data-independent-core channels (ringdown, screening) are robust but harder to reach, so 2/3 channels are not fully data-dependent and the theory is not a single-datum construct; complements the v2.323 robustness-tier ledger with the prediction/refutation view; toy basis; 6/6 checks) · [**v2.355 is the screening mandate data-independent? partly: the constructed SECTOR mandates it, the DATA selects the sector**](docs/results/2026-06-30-v2.355-qnm-screening-mandate-robustness.md) (the honest robustness refinement of v2.354's screening mandate -- a clean split; (A) holding the constructed matter+curvature sector (g_4,g_6,g_8,g_R3) with parity=0 AND birefringence OFF, small g_R2 (<=cap) is STILL infeasible at every value, via exactly three CP-EVEN data-independent constraints (graviton forward positivity, cross-sector EFThedron, anomaly cancellation) -- so screening is forced GIVEN the constructed sector with no appeal to the data, unlike the parity headline; (B) but release the sector + drop birefringence and an unscreened feasible theory DOES exist at SMALLER couplings (g_4~0.42, g_R2~0.03), whereas with birefringence ON the unscreened region stays empty -- so the cosmic-birefringence data is what excludes the small-coupling unscreened alternative and selects the screening-mandated constructed sector; net -- the screening mandate's CORE is data-independent (the strongest, first-data-independent component the program has) but its UNIVERSALITY is data-linked; honest scope -- branch-existence is empirical (a witness point for (B), a not-found for emptiness corroborated analytically), CP-even wall location is prefactor-dependent (graviton_fwd_c/efthedron_alpha, v2.345-slack), toy basis; 5/5 checks) · [**v2.354 the theory MANDATES screening: an unscreened dark-energy-scale R^2 fifth force is excluded (a third channel)**](docs/results/2026-06-30-v2.354-qnm-screening-mandate.md) (a genuinely fresh channel -- the sub-mm/Eot-Wash fifth-force sector, kept screened throughout the program; turns out submm_screened=True is LOAD-BEARING not a convenience; the UNSCREENED Eot-Wash bound caps g_R2<=0.063 (a fixed alpha=1/3 dark-energy-scale f(R) scalaron), but the constructed theory has g_R2=0.193, ~3x over -- and under the unscreened stack the constructed point fails ONLY the sub-mm bound (feasible when screened); the high g_R2 is FORCED: anomaly inflow + the birefringence floor give g_R2>=0.037/g_4, needing g_4>=0.59 to meet the cap, and graviton-forward/EFThedron/anomaly-cancellation close even that sliver -- a 30000-point search finds NO feasible unscreened theory (region empty); so the theory makes a definite THIRD-channel prediction (after parity/birefringence + ringdown): the R^2 scalaron must be screened (chameleon/Vainshtein/dark-sector), falsifiable by a detected unscreened DE-scale fifth force; the SAME mechanism as v2.350's g_4 g_R2 floor; honest scope -- emptiness is empirical (corroborated analytically), g_R2_max~0.063 is an order-of-magnitude DATA reading so the 3x factor is soft but the gap is an order of magnitude clear, and it rests on the birefringence hint (v2.329); 5/5 checks) · [**v2.353 what the theory predicts for the parity-odd cubic IF anomaly matching is its physically-motivated equality**](docs/results/2026-06-30-v2.353-qnm-thooft-equality-cubic-prediction.md) (the constructive sequel to v2.352's flagged upgrade point -- a CONDITIONAL second parity-odd prediction, no core-engine change; the engine encodes 't Hooft matching as a conservative bound but its docstring motivates an EQUALITY (ratio fixed by IR matter content); taking the equality, the unpredicted cubic g_R3_parity becomes LOCKED to the data-pinned quadratic by a matter-fixed ratio r=rho_match(g_4+g_6)=0.4645: g_R3_parity = r*g_R2_parity ~ 0.028, data-pinned to a narrow window [0.022, 0.030] instead of v2.352's symmetric [-0.036,0.036] bound; the equality also TIGHTENS the quadratic (cubic eats the anomaly budget -> g_R2_parity upper edge 0.0783->0.0654) and the predicted point is FEASIBLE against the current engine (no violations); so the theory has a latent SECOND parity-odd prediction (both data-pinned) that switches on under one well-motivated upgrade, testable like the first (chiral signal now fixed not just bounded by the same birefringence data); honest scope -- explicitly conditional, magnitude scales with rho_match (toy proxy), fixes the RATIO not the SIGN, rests on the hint (v2.329); 5/5 checks) · [**v2.352 is g_R3_parity = 0 the verified center, or an untested assumption? (the program's one fixed coupling)**](docs/results/2026-06-30-v2.352-qnm-parity-odd-cubic-symmetry.md) (closes a real gap -- the parity-odd CUBIC g_R3_parity has been held at 0 throughout, not even in the 6-coupling family sampler; turns out every constraint that reads it is an EVEN function (g_R3_parity^2 or |g_R3_parity|: anomaly inflow, t Hooft matching, parity-violating cubic bound, complexity cutoff), so the whole feasibility is invariant under g_R3_parity -> -g_R3_parity -- verified directly: all stack margins identical at +/-0.02 (0 asymmetric), feasible window [-0.035, 0.035] symmetric about 0, center 0.0, edge matching the closed-form anomaly bound 0.0355; so g_R3_parity=0 is the VERIFIED Chebyshev center, not an assumption, and the 6-coupling sampler loses nothing; clean honest asymmetry -- the parity-odd QUADRATIC g_R2_parity is data-PINNED away from 0 (g_R2_parity=0 is INfeasible, birefringence needs >=0.047) but the parity-odd CUBIC is UNPINNED (0 feasible+central, no datum breaks its reflection symmetry), so the theory makes exactly ONE parity-odd prediction (the quadratic) and ZERO for the cubic (bounded |g_R3_parity|<~0.036 but unpredicted); the symmetry is exact/structural so the verdict is toy-basis-INVARIANT; flagged -- if t Hooft were an EQUALITY not a bound the cubic would be predicted (the likely future-upgrade point); 6/6 checks) · [**v2.351 a CP-even cross-sector cap: causality bounds the ringdown floor by the leading matter coupling**](docs/results/2026-06-30-v2.351-qnm-ringdown-floor-causality-cap.md) (the CP-even companion to v2.350 -- another exact two-constraint chain; the moment tower forces a ringdown floor g_R4>=g_R3^2/g_R2 and CEMZ causality bounds g_R3^2<=kappa^2 g_4 g_R2, so dividing by g_R2 caps the floor itself: g_R4_floor <= kappa^2*g_4 = 0.64*g_4 -- causality limits how large the guaranteed-minimum ringdown deviation can be, tying its max to the leading matter coupling; combined with v2.349 (floor collapses to 0 where g_R3->0) the floor is fully BRACKETED 0 <= g_R4_floor <= kappa^2 g_4; the engine confirms the analytic cap across the family (every floor below kappa^2 g_4, reaching ~45% of the cap at most where CEMZ is most saturated); the constructed center sits at just 12% of its cap 0.339 -- far from the causality boundary, consistent with v2.339 headroom; so the ringdown floor is not a free magnitude but pinned between zero and a causality-set matter-scaled ceiling; honest scope -- exact algebra but the cap coefficient scales as kappa^2 (cemz_kappa, v2.345-slack), rank-1 schematic qNM->R4 map, CP-even/data-independent, toy basis; 5/5 checks) · [**v2.350 a cross-sector inequality: cosmic birefringence puts a LOWER bound on the parity-EVEN matter x curvature product**](docs/results/2026-06-30-v2.350-qnm-birefringence-parity-even-floor.md) (a genuine swing, the mandated cross-sector-moment direction -- a NEW exact corollary; chaining two exact constraints, the birefringence DATA lower bound g_R2_parity>=0.0471 and the anomaly-inflow upper bound g_R2_parity^2<=rho*g_4*g_R2, eliminates the parity coupling and leaves g_4*g_R2 >= beta_lower^2/rho ~ 0.0369 -- so a PARITY observable, routed through anomaly inflow, places a LOWER bound on the parity-EVEN leading matter x leading curvature product; a theory matching the birefringence detection cannot have an arbitrarily weak matter or curvature sector; the engine confirms it -- the feasible family's min g_4*g_R2=0.0427 stays above the bound and is NEARLY TIGHT at the edge (an active constraint, not loose), constructed sits at 0.1021 (~2.8x); genuinely data-sourced -- without the detection the parity lower edge -> 0 and the bound vanishes (0.0), so it is the DETECTION not the operator content that floors the parity-even product; a new consequence in a DIFFERENT sector than the data obviously constrains; honest scope -- exact algebra but toy-basis inputs so the NUMBER scales as beta_lower^2/rho (v2.344 prefactor) and rests on the hint (v2.329); 5/5 checks) · [**v2.349 is the ringdown floor guaranteed across the whole consistent+observed family, or only at the center?**](docs/results/2026-06-30-v2.349-qnm-ringdown-floor-family.md) (a fresh sector after the parity arc -- the OTHER prediction channel, ringdown, extended from the constructed center to the whole family (the curvature-sector parallel of v2.341); v2.336/337 gave the center's moment-tower floor g_R4 >= g_R3^2/g_R2 = 0.042, but the floor depends on g_R3 and collapses where g_R3->0; sampling the feasible family (2137 pts), g_R3 reaches ~0 within the feasible region so the family floor min is 0.000 (median 0.030, max 0.165) -- the floor is NOT guaranteed family-wide, it is a CENTER property; a pure-GR ringdown would refute the g_R3!=0 members (incl. the constructed theory) but NOT the small-g_R3 sub-family, whose ringdown is indistinguishable from GR at this order; so the ringdown's testability depends on where in the family the true theory sits -- maximal at the center, vanishing on the small-g_R3 edge; sharpens v2.337's floor-firm/magnitude-loose into floor-CONDITIONAL because no data constraint reads g_R3 from below; honest scope -- sampler-dependent extremum (qualitative verdict robust), rank-1 schematic qNM->R4 map, toy basis; CP-even/data-independent; 5/5 checks) · [**v2.348 does the theory survive re-centering under the REAL GW-birefringence bound, against the full stack?**](docs/results/2026-06-30-v2.348-qnm-parity-recenter-real-gw.md) (the global-feasibility completion of v2.347 -- it survives: rebuilding the consistent+observed stack with LIGOBirefringenceBound tightened 0.1->0.05, the original construction (g_R2_parity=0.06) goes infeasible killed by EXACTLY one constraint (ligo_birefringence_bound, tension real + localized to parity), but re-centering the parity coupling to the surviving window's middle (0.0486) is FEASIBLE against the FULL tightened stack with only g_R2_parity changed (matter+curvature untouched, parity is the stiff decoupled direction v2.333); so taking the real GW bound seriously is NOT a falsification but a ~11x SHARPENING -- the parity window narrows [0.0471,0.0783]->[0.0471,0.05] centered ~0.0486 not 0.06; the program self-corrects (v2.347 surfaced the tension, v2.348 resolves+globally verifies it leaves a non-empty region); honest scope -- the 0.05 is the engine's order-of-magnitude docstring estimate so the structure (narrow CMB-floor-to-real-GW window) is robust not the precise numbers; rests on the CMB hint (v2.329); toy basis; 6/6 checks) · [**v2.347 the parity sector is pinched between two birefringence observables (CMB vs GW): a near-term falsification handle**](docs/results/2026-06-30-v2.347-qnm-parity-birefringence-pinch.md) (a fresh sector after the marginality arc -- a concrete falsifiable forecast; the parity coupling g_R2_parity is pushed UP by CMB cosmic birefringence (beta=0.34+/-0.09 deg, best-fit 0.10, 2-sigma floor 0.0471 -- what makes parity nonzero) and bounded from ABOVE by GW birefringence (LIGO/Virgo non-observation); the engine loosens the GW bound to 0.1 in the toy regime but its OWN docstring says the real O3 strength is |g_R2_parity| <~ 0.05; under the toy bound the data-pinned window [0.0471, 0.0783] is open and the constructed 0.06 sits inside, but under the real-O3 estimate the window COLLAPSES to the sliver [0.0471, 0.05] (width ~0.003) and the constructed 0.06 falls OUTSIDE -- the real GW bound already disfavors the central parity value and pins it to ~0.047-0.05 at the CMB floor; sharp falsification threshold: a GW bound below the CMB floor 0.0471 CLOSES the window and falsifies the parity construction (given the CMB hint) -- a genuine 'how to kill it' handle on an O4/O5 timescale; honest scope -- both birefringence maps are toy/order-of-magnitude so the precise exclusion isn't robust, but the STRUCTURE (two observables squeezing from opposite sides, real GW bound at/below the construction) is; rests on the CMB hint (v2.329); 6/6 checks) · [**v2.346 how marginal is the constructed theory under JOINT O(1) prefactor variation?**](docs/results/2026-06-30-v2.346-qnm-prefactor-joint-excursion.md) (closes the v2.345 OAT caveat with a Monte Carlo over the full 11-D prefactor box, each prefactor uniform over its declared factor-of-~2 band; under JOINT variation the constructed theory stays feasible only ~41% of the time -- far below the >=78%-per-axis figure the one-at-a-time sweep reported, and close to the ~38% an independent-breaks model predicts; so the OAT view WAS optimistic, exactly as its caveat warned: the point is genuinely marginal under joint O(1) ignorance (infeasible ~59% of the box), though still a GENUINE feasible point (feasible at canonical, ~41% of the box feasible) -- a thin JOINT cushion, not a fat interior one; the joint fraction sitting just above the independence product means the six load-bearing breaks act through six DISTINCT constraints (bnossw_monogamy 23%, scalar_wgc 22%, matter_s3 14%, complexity 10%, anomaly_inflow 7%, cft 6%) and compound multiplicatively rather than reinforcing; honest conclusion -- the robust content is the result's STRUCTURE (it exists, beats the community frameworks, its qualitative features), not the precise feasibility of the single center point under adversarial joint prefactor choices; sampling-measure + toy-basis caveats; 5/5 checks) · [**v2.345 which of the engine's 11 O(1) prefactors is the constructed theory's feasibility sensitive to?**](docs/results/2026-06-30-v2.345-qnm-prefactor-sensitivity-sweep.md) (a global one-at-a-time sweep generalizing v2.344 from one prefactor to all eleven; the constructed theory is feasible at canonical and across the BULK of every prefactor's declared factor-of-~2 band, but feasible-but-MARGINAL not robustly central: 5 of 11 prefactors are fully SLACK (feasible across the whole band) and 6 are LOAD-BEARING [anomaly_rho, bnossw_pref, cft_alpha, complexity_cmax, matter_s3_cm, scalar_wgc_beta], each breaking only near ONE band edge against a single named constraint; every break is an edge effect, never central -- the point stays feasible across >=78% of every band individually; this is exactly what one expects of the Chebyshev CENTER of a tiny ~3D non-convex region (v2.333/v2.332): the center of a small region is close to many of its walls; confirms v2.344 (anomaly_rho breaks at its 0.03 floor as predicted); honest OAT caveat -- joint multi-prefactor excursions would be more marginal, so the >=78%-per-band headroom is an optimistic upper bound; toy basis; 5/5 checks) · [**v2.344 how robust is the data-pinned parity window to the anomaly-inflow prefactor?**](docs/results/2026-06-30-v2.344-qnm-parity-prefactor-robustness.md) (a robustness audit of the program's most load-bearing + most-caveated result, the parity prediction; the constructed theory's g_R2_parity is pinned from below by the cosmic-birefringence DATA (2-sigma band [0.0471, 0.1529]) and from above by the anomaly-inflow budget g_R2_parity^2 + 2 g_R3_parity^2 <= rho * g_4 g_R2, whose prefactor rho=anomaly_rho is an O(1) choice; stress-testing across the engine's OWN declared band anomaly_rho in [0.03, 0.12]: the joint (data AND anomaly) window stays NON-EMPTY and strictly positive at every rho (it closes only below rho=0.0217, outside the band), so 'parity required, beta=0 excluded' is prefactor-ROBUST; but the specific value 0.06 survives the budget only for rho >= 0.0353, so in the lowest ~17% of the band [0.03, 0.0353) the constructed 0.06 would VIOLATE the budget and the center shifts to ~0.051 -- so the sign/existence of the parity prediction is robust, its precise magnitude is not; the headline to trust is 'g_R2_parity > 0, in [0.047, ~0.08]', not the digit 0.06; closed-form edges match the engine's actual GeneralizedAnomalyInflow constraint exactly (margin 0 at the sqrt(rho g_4 g_R2) edge); honest fragility, rests on the birefringence data being real (v2.329); toy basis; 5/5 checks) · [**v2.343 the matter sector requires a multi-state tower, not a single resonance: a third line for the string-like UV**](docs/results/2026-06-30-v2.343-qnm-spectral-tower.md) (a rigorous, independent third line for the trilogy's string-like-UV conclusion, from the matter sector alone; the matter dispersion bound g_6^2 <= g_4 g_8 is a Cauchy-Schwarz inequality on the moments of the matter spectral density, SATURATED iff a single state dominates and STRICT iff there are >= 2 states; the constructed theory's ratio g_6^2/(g_4 g_8) = 0.756 is strictly below 1, so its UV contains at least two states -- a multi-state TOWER, not a single resonance; this is independent of the unitarity (v2.338) and causality (v2.339) arguments, so THREE separate lines now point to a higher-spin tower UV: positivity/unitarity, causality, and the spectral multi-state requirement; all candidates are multi-state (ratios 0.75-0.84) and the constructed theory at 0.756 is among the most spread, so its tower is broader/lower-lying than string's 0.80 or lqg's 0.84; sharpens the v2.342 string-like-in-two-senses identity; strict-inequality => >=2 states is a theorem given the moment interpretation, but g_4,g_6,g_8 are the TOY encodings so '>=2 states' is in the toy basis and the higher-spin/Regge character is the qualitative pairing with causality; CP-even, data-independent; 4/4 checks) · [**v2.342 the constructed theory is string-like in two independent senses: IR matter and UV completion**](docs/results/2026-06-30-v2.342-qnm-string-like-two-senses.md) (synthesis tying the trilogy's UV conclusion to the matter sector; (2) UV -- the trilogy (v2.338-341) showed unitarity+causality require a higher-spin Regge tower at the cutoff -> string-like UV; (1) IR (shown here) -- the constructed matter couplings (g_4,g_6,g_8)=(0.53,0.40,0.40) are CLOSEST to string_tree_eft of all named frameworks (distance 0.029, vs cdt 0.054, lqg 0.087, AS 0.191, pure_gr 0.774); so the theory is string-like in TWO INDEPENDENT senses, by entirely separate arguments (a coupling-space distance in the IR, a unitarity+causality requirement in the UV); its two DISTINGUISHING features are exactly the non-string pieces -- TRIMMED curvature (forced by consistency, the same trimming giving the mildest ringdown v2.336 + most causality headroom v2.339) and MILD PARITY VIOLATION (absent at string tree level, favored by birefringence data); so the new theory is a PARITY-DEFORMED, CURVATURE-TRIMMED STRING-LIKE GRAVITY -- string-like where consistency + the matter sector demand it, deformed exactly where data (parity) + the consistency boundary (trimmed curvature) push it; a coherent identity, not an arbitrary point; IR distance exact arithmetic but 'string' = the engine's TOY encoding NOT the real Veneziano coefficients (relative claim); UV string-like = the trilogy's qualitative inference; 4/4 checks) · [**v2.341 which deep requirement bounds the theory? causality has headroom; unitarity and the WGC bind**](docs/results/2026-06-30-v2.341-qnm-trilogy-binding.md) (sharpens the trilogy (v2.338-340) from the constructed POINT to the whole consistent+observed FAMILY (v2.332): of the three classic objections to higher-derivative gravity, which BOUNDS the new theory's existence?; taking the worst gradient-normalized margin anywhere in the family (~2100 samples): CAUSALITY (CEMZ+causality bound+Hofman-Maldacena) never drops below +0.052 -> always headroom (a direct consequence of the trimmed cubic curvature giving ~65% causality headroom v2.339, preserved family-wide), UNITARITY (forward positivity+dispersion+EFT-hedron) reaches 0.000 -> BINDING wall, WGC (weak gravity+scalar-WGC+repulsive force) reaches +0.004 -> near-binding; so the new theory's existence is bounded by UNITARITY and the swampland WGC, while causality is comfortably satisfied throughout -- causality, despite being the most famous obstruction to higher-derivative gravity, is NOT a binding wall here because the consistent theories all keep their cubic curvature well below the CEMZ bound; so the defining trimmed-curvature feature buys causality for free, and what the theory must still trade off against is positivity (unitarity) and the WGC; consistent with v2.325 (the active core contains graviton-forward-positivity + repulsive force, NOT the causality bound); all three satisfied everywhere, the question is which is tight; exact minima prefactor-dependent, the ORDERING robust; CP-even, data-independent; 5/5 checks) · [**v2.340 the constructed theory satisfies the Weak Gravity Conjecture: its corrections let extremal black holes decay**](docs/results/2026-06-30-v2.340-qnm-wgc-extremal-decay.md) (the THIRD pillar, completing the deep-consistency trilogy after v2.338 unitarity + v2.339 causality; the WGC is the canonical swampland requirement -- gravity the weakest force, equivalently a large extremal black hole must DECAY -- and for higher-derivative gravity a positivity statement on the curvature corrections (Kats-Motl-Padi, Hamada-Noumi-Shiu): the corrections must shift the extremal mass-charge ratio so the corrected extremal BH is super-extremal and sheds charge, no stable remnant; the constructed theory satisfies the WGC family -- weak_gravity_conjecture +0.534, scalar_wgc +0.136, convex_hull repulsive_force +0.299 -- with comfortable margin (min signed-dist +0.091); TRILOGY: unitarity (v2.338, +0.050 inside, no ghost) + causality (v2.339, 35% of CEMZ bound, no time-advance) + WGC (this, extremal BHs decay) -- the constructed theory passes ALL THREE deep consistency/swampland requirements a higher-derivative gravity must face, each with margin, and all three point the same way (unitarity+causality require a unitary higher-spin string-like UV, WGC says such a completion is in the landscape); so it is not just a feasible point but a candidate that clears the standard objections to its entire class -- ghost-free, causal, WGC-satisfying -- the strongest internal-consistency statement the program can make; CP-even, data-independent; encodings O(1) so exact margins convention-dependent but comfortably-inside robust; 5/5 checks) · [**v2.339 the constructed theory is causal with the most headroom: trimmed cubic curvature keeps it deepest inside the CEMZ bound**](docs/results/2026-06-30-v2.339-qnm-causality-headroom.md) (the companion to v2.338 -- the OTHER deep objection to higher-derivative gravity, CAUSALITY (the CEMZ graviton time-advance: |g_R3|<=0.8 sqrt(g_4 g_R2) else a time advance, unless a higher-spin tower intervenes at the cutoff); every candidate is formally causal but the constructed theory sits at only ~35% of its CEMZ bound -- the DEEPEST inside -- while lqg (g_R3=0.30, outlier cubic) sits at ~88%, right at the causality edge; so the SAME trimmed cubic curvature that made the constructed theory consistent (smallest g_R3, smallest ringdown v2.336) ALSO makes it the most CAUSAL -- it uses barely a third of its causality budget while lqg's large cubic pushes it near a graviton time-advance; CEMZ's deeper content: even satisfying the bound, EFT causality REQUIRES a higher-spin tower at the cutoff (string's Regge tower) -> the constructed theory's UV must contain higher-spin states, pointing to a string-like UV -- the SAME one v2.338's ghost-freedom pointed to; together v2.338+v2.339 show the constructed theory clears BOTH deep requirements (unitarity + causality) with margin, both pointing the UV toward unitary string-like higher-spin physics; CP-even DATA-INDEPENDENT; CEMZ bound O(1)-encoded so the % values convention-dependent but the ORDERING robust; 5/5 checks) · [**v2.338 is the constructed theory ghost-free? positivity IS the EFT unitarity / no-ghost condition**](docs/results/2026-06-30-v2.338-qnm-ghost-freedom.md) (addresses the deepest standard objection to ANY higher-derivative gravity -- the Ostrogradsky/massive-spin-2 GHOST (treated as fundamental, R^2/Riemann^2 operators generically propagate a negative-norm spin-2 mode); two structural facts answer it: (1) EFT FRAMING -- the higher-derivative operators are perturbative corrections, so the would-be ghost sits ABOVE the cutoff, not a propagating dof; (2) POSITIVITY=UNITARITY -- the engine's 20 amplitude-positivity/causality constraints (A_AMPLITUDE class: forward positivity, dispersion towers, CEMZ causality, spin-4 positivity, EFT-hedron/Hofman-Maldacena bounds) ARE the EFT unitarity conditions, holding iff the EFT admits a unitary causal ghost-free UV completion (dispersive representation has no negative-norm states); the constructed theory satisfies ALL 20, and NOT marginally -- min signed distance to any unitarity boundary +0.050, comfortably INSIDE (tightest: dispersion tower +0.050, graviton forward positivity +0.054); so the central object is not 'a point satisfying inequalities' -- those inequalities ARE the no-ghost guarantee and it clears them with room to spare; the engine's core role (intersecting positivity/causality) is exactly what certifies the theory against the ghost; CP-even + DATA-INDEPENDENT (no birefringence dependence); scope: ghost-free in the EFT/positivity sense (NOT as a fundamental Lagrangian -- treated fundamentally it carries the Ostrogradsky ghost like all higher-derivative gravity; positivity = consistent-with a unitary UV completion, not that one is constructed); 4/4 checks) · [**v2.337 the ringdown prediction is a firm floor but a loose magnitude (vs the sharp parity prediction)**](docs/results/2026-06-30-v2.337-qnm-ringdown-prediction-sharpness.md) (completes v2.336 -- the moment-tower FLOOR on the ringdown-active g_R4 vs its full RANGE; the constructed theory's g_R4 spans a WIDE feasible range [0.05, 0.60] (~169% relative spread, a ~10x window) -- floor firm (moment tower g_R4>=g_R3^2/g_R2=0.042) but ceiling=complexity_cutoff (a THEORY constraint, not data) and nothing in between pins it, because NO data constraint reads g_R4 (the opt-in curvature-tower coupling, observationally unconstrained, only theory-bounded); by contrast the parity is data-pinned to ~15% (v2.334); so the testability is ASYMMETRIC -- a SHARP parity prediction (data-pinned, headline) + a FLOORED-BUT-LOOSE ringdown prediction (theory-bounded); the ringdown channel can CONFIRM a minimum quartic-curvature deviation (and a sub-floor pure-GR ringdown would REFUTE the theory) but does not sharply predict its size; a future g_R4-sensitive curvature-sector measurement would sharpen the ringdown the way birefringence sharpened the parity; the v2.336 floor-ordering discriminator stands, the floor (not the magnitude) is the firm part; range width prefactor-dependent, the contrast (ringdown theory-bounded loose vs parity data-pinned sharp) robust; CP-even, INDEPENDENT of the birefringence data; 5/5 checks) · [**v2.336 the constructed theory's ringdown-active quartic curvature: the smallest of all candidates**](docs/results/2026-06-30-v2.336-qnm-ringdown-quartic-floor.md) (a fresh seed RECONNECTING to the project's namesake -- quasi-normal modes / ringdown; the program tested the constructed theory only in the PARITY sector, but the quartic curvature g_R4 is the RINGDOWN-active operator (deforms the (2,2,0) Schwarzschild QNM) and was never analysed for it; with the curvature tower on, the moment-tower mandate g_R4>=g_R3^2/g_R2 (v2.292) FORCES a nonzero quartic whenever g_R3!=0 -- the constructed theory requires g_R4>=0.042, g_R4=0 infeasible; and because it carries the most TRIMMED cubic curvature, its quartic floor 0.042 is the SMALLEST of all candidates: pure GR 0 (no shift), constructed 0.042, AS 0.067, cdt 0.10, string 0.11, lqg 0.30; so it predicts the MILDEST ringdown deviation -- a clean ORDERED CP-even discriminator complementary to the parity-sector tests (birefringence/chiral-GW/leptogenesis) and testing the curvature sector they don't; the constructed theory's trimmed curvature (which made it consistent) also makes it the most GR-like in ringdown, hardest of the higher-derivative candidates to distinguish from Einstein gravity; the g_R4 FLOOR rigorous+exact (moment tower), the g_R4->ringdown-shift SIZE schematic (rank-1 qNM->R^4, deep-research thread), only the floor + ordering claimed; CP-even prediction INDEPENDENT of the birefringence data (no v2.329 caveat); 5/5 checks) · [**v2.335 the parity sector shares an anomaly budget: the data squeezes the cubic parity to zero**](docs/results/2026-06-30-v2.335-qnm-parity-anomaly-budget.md) (a NEW seed -- the second parity coupling g_R3_parity (cubic parity), ignored throughout; is it free, data-favored, or determined?; the two parity couplings are coupled through the generalized gravitational anomaly inflow g_R2_parity^2 + 2 g_R3_parity^2 <= rho_inflow*g_4*g_R2 (a SHARED 'anomaly budget'), plus 't Hooft ratio matching; mapping the feasible cubic-parity range vs the leading parity, it shrinks from ~0.042 at the data-window lower edge (g_R2_parity~0.05) to ~0 at the upper edge (~0.07) -- the boundary combination g_R2_parity^2+2g_R3_parity^2 stays ~constant (~0.0056), the signature of one shared budget; so the cosmic-birefringence data, by requiring a sizable LEADING parity, consumes the budget and SQUEEZES the cubic parity toward zero -- the constructed theory's g_R3_parity=0 is a CONSEQUENCE not a choice; this explains the v2.333/334 single stiff parity direction: the parity sector is effectively ONE-parameter (the leading coupling) because anomaly matching + the data jointly determine the cubic parity from it -- a tighter, anomaly-locked parity structure than the two free couplings the basis nominally allows; engine's literal anomaly_flow constraint + grid scan, rho_inflow/rho_match O(1) prefactors, the shared-budget + squeeze structure robust; rests on the data pinning leading parity (v2.329 caveat); 5/5 checks) · [**v2.334 predictions concentrated in the pinned direction: the matter freedom is observationally hidden**](docs/results/2026-06-30-v2.334-qnm-prediction-invariance.md) (the testability implication of v2.333 -- does the ~3-parameter freedom weaken the theory's predictions?; NO: the discriminating predictions (cosmic-birefringence beta, chiral-GW Pi) are functions of the PARITY coupling alone -- the stiff/data-pinned direction -- so across ~2900 feasible family samples beta=0.20+/-0.03 deg and Pi=0.235+/-0.033, a relative spread of only ~15% set entirely by how well the data pins parity; the 3 FREE directions are matter-sector couplings that vary much more (up to ~60%, g_R3) and do NOT enter the discriminating observables; so the theory is predictively SHARP despite being parametrically LOOSE -- its testable content lives in the single pinned parity direction, its parameter freedom (matter) is observationally HIDDEN in the parity-sector tests; practical consequence: a future birefringence/chiral-GW measurement tests the parity prediction robustly across the WHOLE family, not just at the constructed point (no need to know the unobservable matter couplings); ties the effective-3-parameters (v2.333) to testability -- they are exactly the directions the discriminating data does NOT probe; beta/Pi parity-only by construction, the parity-is-stiff content non-trivial; rests on birefringence pinning parity (v2.329 caveat); 5/5 checks) · [**v2.333 the consistent+observed family is effectively 3-dimensional: matter free, parity pinned**](docs/results/2026-06-30-v2.333-qnm-effective-dimension.md) (the connected feasible family (v2.332) lives in 6 Wilson couplings -- but how many are GENUINELY free?; a PCA over ~2900 random-walk samples gives a participation ratio of 3.0 (effective dim, of 6), top 3 PCs capture 86% of the variance -> the theory+data constraints CORRELATE the couplings and reduce the new theory's effective parameter count from 6 to ~3; the structure is clean+physical: the SOFT (free) directions are MATTER -- PC1~g_8 alone (49%), PC2~g_4+g_6+g_R2 cluster (27%) -- while the STIFFEST (most-determined) direction is the parity coupling g_R2_parity (std 0.009, smallest by far, data-pinned); so the new theory's free parameters are its matter/leading-curvature couplings varying along ~3 directions, and its parity is essentially FIXED by current cosmic-birefringence data; sharpens v2.327 (which individual couplings are tight) into the CORRELATED picture (which COMBINATIONS are free vs pinned), explaining why the tiny non-convex family is a smooth low-D surface not a fat 6D blob -- the engine's new theory is a ~3-parameter family of string-like-matter higher-derivative gravities with a data-fixed parity; PCA sampler/metric convention-dependent, the lower-effective-dim + matter-soft/parity-stiff structure robust; rests on birefringence pinning parity (v2.329 caveat); 6/6 checks) · [**v2.332 the consistent+observed region is connected: one continuous family of theories**](docs/results/2026-06-30-v2.332-qnm-region-connectivity.md) (tests the CONNECTIVITY of the theory+data feasible region -- are the repaired feasible theories (v2.330/331) isolated, or one family?; the constructed framework and the repaired named frameworks differ notably (repaired-lqg g_R3~0.21/parity 0.08 vs constructed g_R3~0.09/parity 0.06), and because the region is NON-CONVEX (v2.304) the straight line constructed->repaired-lqg FAILS almost immediately; yet a feasible-corridor path search CONNECTS the constructed framework to EVERY repaired point through a continuous sequence of feasible theories -> the sampled consistent+observed theories all lie in ONE connected component; so the new theory is a continuous FAMILY of higher-derivative gravities (string-like matter, parity in the data window, curvature in a bounded range) within which the constructed framework is the most-robust point and the repaired named frameworks are continuous deformations -- sharpening the v2.331 attractor picture (the named frameworks are in the SAME connected family, reachable by feasible deformation), and the non-convexity shapes it (cannot interpolate naively, but can deform continuously); connectivity established CONSTRUCTIVELY (a path was found -- proves connection, never disconnection, so doesn't rule out a separate global component); rests on the birefringence data (v2.329 caveat); 5/5 checks) · [**v2.331 the universal repair: every named framework moves toward the constructed theory**](docs/results/2026-06-30-v2.331-qnm-universal-repair.md) (extends v2.330's lqg-repair to ALL named frameworks -- a clean two-mode picture: the four parity-EVEN frameworks (string/AS/cdt, theory-feasible but data-excluded for predicting beta=0) repair by ADDING a parity coupling (g_R2_parity 0->0.048 into the cosmic-birefringence data window, distance ~0.05, rest unchanged), while lqg (data-favored, theory-excluded) repairs by TRIMMING its cubic g_R3 (-0.091, keeping its parity, distance ~0.11); so the named frameworks miss the consistent+observed region in two COMPLEMENTARY directions -- too LITTLE parity or too MUCH cubic -- and every minimal fix moves toward the SAME constructed profile (parity in the data window + trimmed curvature); the constructed theory is an ATTRACTOR of the named frameworks under minimal repair, not an arbitrary engine pick but the common target each is nearest to once consistency + cosmic-birefringence data are imposed; the two modes recover the two diagnoses (parity-even=missing parity v2.322, lqg=outlier cubic v2.311); approximate L2 projection, two-mode structure robust; CRUCIAL CAVEAT (v2.329): the parity-even repairs exist ONLY if the ~3.6-sigma birefringence is real -- if systematic, they need no repair and only lqg's cubic-trim remains; 5/5 checks) · [**v2.330 repairing lqg: the minimal modification that makes the data-favored framework consistent**](docs/results/2026-06-30-v2.330-qnm-repair-lqg.md) (lqg is the program's most striking framework -- DATA-favored (the only named parity-violating one, matches cosmic birefringence) yet THEORY-infeasible (outlier cubic g_R3=0.30, v2.311); what is the MINIMAL repair to consistency?; lqg lies only ~0.10 in coupling space from the consistent+observed region, and the minimal repair primarily TRIMS the outlier cubic g_R3 (-0.091, the single biggest change = exactly the v2.311 problem) and the leading curvature g_R2 (-0.048), while KEEPING the parity violation (g_R2_parity 0.080->0.078, still in the cosmic-birefringence data window); so lqg's PARITY is its RIGHT feature (data-favored, preserved) and its CUBIC CURVATURE is its WRONG one (trimmed); 'repaired lqg' lands in the constructed framework's family -- parity-violating, trimmed curvature -- so lqg and the engine's constructed theory are the SAME KIND of theory differing essentially only in lqg's cubic overshoot; the leading parity-violating QG framework points the right way, a near-miss with a clear minimal fix; approximate projection (L2 convention), the trim-cubic/keep-parity structure robust; 5/5 checks) · [**v2.329 adversarial audit: how robust is the parity conclusion to the null hypothesis?**](docs/results/2026-06-30-v2.329-qnm-adversarial-audit.md) (the most honest capstone -- maximally steelman the case AGAINST the program's headline; the data-favored parity claim rests on the cosmic-birefringence detection (~3.6 sigma, UNCONFIRMED, systematic-prone: dust EB, miscalibration-angle degeneracy); what survives if it's a systematic?; DROP the one cosmic_birefringence_data constraint and ALL FOUR parity-even frameworks (pure_gr, string, AS, cdt) RETURN feasible -- so 'no named framework satisfies both' and 'data requires parity' rest ENTIRELY on that single detection; without it the parity preference collapses to a MILD consistency effect (constructed-parity geom margin 0.0203 vs parity-free 0.0200; even the optimized anomaly gain 0.033-vs-0.020 is ~0.013, prefactor-dependent -- a hint not a requirement); VERDICT: a SINGLE POINT OF FAILURE, the birefringence detection -- if LiteBIRD/CMB-S4 confirm it the headline strengthens (chiral-GW test follows), if it falls the headline collapses to 'consistency mildly prefers parity' and the parity-even frameworks are viable again; what SURVIVES regardless (birefringence-INDEPENDENT): constructed-beats-community 98% (v2.320), lqg-boundary via g_R3 92% (v2.311), one joint curvature carving (v2.309) -- the structural consistency-only core; the durable contribution is that core, the bold data-grounded claim real but contingent on one unconfirmed measurement, stated plainly; 5/5 checks) · [**v2.328 falsifiability roadmap: what would confirm or refute the constructed theory?**](docs/results/2026-06-30-v2.328-qnm-falsifiability-roadmap.md) (a forward-looking, ACTIONABLE synthesis -- how is the constructed both-consistent gravity actually tested?; its single parity coupling (g_R2_parity=0.06) drives three correlated probes, all predicted EXACTLY ZERO by the parity-even frameworks: (1) cosmic birefringence [MEASURED ~3.6 sig] -- constructed predicts beta=0.20 deg [window 0.16-0.27], consistent with measured 0.34+/-0.09 at ~1.5 sig but at the LOW edge [central prefers more parity, lqg-like]; (2) chiral primordial GW / CMB TB-EB [FUTURE LiteBIRD/CMB-S4] -- right-handed Pi=+0.24 vs zero, the cleanest future discriminator; (3) gravitational leptogenesis eta_B -- nonzero sign-consistent, a consistency not a sharp test; plus the matter sector [loose, v2.327, doesn't discriminate on parity]; the theory is genuinely REFUTABLE -- parity-even gravity predicts exactly zero on all three [symmetry statement], the constructed predicts a definite right-handed sign on each, the three correlated [one coupling] -> the suite discriminates and the whole parity story stands or falls on whether cosmic birefringence confirms [a confirmed beta=0 ends it]; magnitudes order-of-magnitude/illustrative, the structural discriminator robust; 5/5 checks) · [**v2.327 how predictive is the constructed theory? the extent of the theory+data region**](docs/results/2026-06-30-v2.327-qnm-theory-data-region-extent.md) (a rigorous completion of the v2.322 capstone -- which showed EXISTENCE of a both-consistent gravity; this measures the EXTENT of the theory+data region; it is TINY (~5e-4 of a +/-0.25 box -> the new theory is tightly pinned), and the pinning is DIFFERENTIAL via deterministic 1D feasible intervals through the constructed point: g_R2_parity pinned to [0.048,0.078] (width 0.030 = exactly the cosmic-birefringence DATA window v2.321), g_R2 ~0.11, g_R3 ~0.13, matter g_4/g_6/g_8 the loosest 0.14-0.23; so the new theory's predictivity is CONCENTRATED in the PARITY sector -- the one pinned by a real measurement not just consistency -- which is precisely the sector carrying its falsifiable signatures (chiral GW v2.319, leptogenesis v2.324); the theory is most sharply predicted exactly where it is most testable (a tighter cosmic-birefringence measurement or chiral-GW detection narrows the parity window directly), while matter/curvature remain a bounded looser consistency-determined region future matter-sector data would tighten; 1D extents exact for the cross-sections, matter/curvature widths prefactor-dependent, parity data-window robust; 5/5 checks) · [**v2.326 the parity coupling is bounded below by both the swampland distance conjecture and the data**](docs/results/2026-06-30-v2.326-qnm-parity-hierarchy-bound.md) (a rigorous follow-up tying the v2.325 active-core finding (distance conjecture = top binder) to the parity finding (v2.321); the distance conjecture caps the coupling HIERARCHY (max/min nonzero coupling <= R_max~20); the data-favored parity-violating theories carry a SMALL parity coupling amid larger matter couplings, STRETCHING the hierarchy: parity-even frameworks ~3-4 (string 3.3, AS 4.0, cdt 3.7) vs parity-violating stretched (constructed 8.8, lqg 15.0 near the R_max limit) -- this is WHY the distance conjecture is the dominant carver, it reacts to the parity feature the data demands; it bites from below (too-small parity -> max/min explodes), forbidding g_R2_parity<0.027; cosmic birefringence independently requires g_R2_parity>=0.048 (tighter); so BOTH a swampland consistency condition AND a real measurement push the parity coupling UP -- it can't be too small -- a convergence joining the program's two dominant threads; lqg's near-limit hierarchy (15 vs ~20) is another face of its boundary status alongside its outlier g_R3 (v2.311); hierarchies EXACT arithmetic, R_max/beta prefactor-dependent, both-bounds-lower direction robust; 5/5 checks) · [**v2.325 the active constraint core: which consistency conditions do the carving work?**](docs/results/2026-06-30-v2.325-qnm-active-constraint-core.md) (a rigorous, DICTIONARY-FREE engine-internal cycle -- a deliberate return from the schematic parity phenomenology; the engine intersects 42 constraints (38 theory + 4 data) -- are they all carving, or does a small core dominate?; sampling realistic higher-derivative theories (the 5 frameworks + constructed candidate, jittered, 480 samples) and tallying each theory's binding + violated constraints: only 29/42 are ever ACTIVE, 13 always SLACK; the carving is dominated by a small CORE -- 7 constraints carry 80% of all binding: swampland_distance_conjecture (top, bounds the max/min coupling spread = the v2.318 forbidden zone), cosmic_birefringence_data, bnossw_monogamy, generalized_anomaly_inflow, t_hooft_anomaly_matching, graviton_forward_positivity, dispersion_tower_g6^2 -- three families (4 universality/anomaly, 2 amplitude, 1 information); the 13 always-slack are trivially satisfied by realistic positive-coupling theories (scalar positivities g>=0, loose data bounds GW speed/dispersion/sub-mm/species/graviton-mass, GSL/EFT-box/Hofman-Maldacena/Wald/cubic-positivity); so the operative conditions are a handful from swampland + universality/anomaly + the one tight data constraint + leading positivity walls -- explaining the binding patterns the whole program kept hitting; 'active' is sample-relative (realistic positive-orthant theories) not non-redundant-in-general; 6/6 checks) · [**v2.324 gravitational leptogenesis: the preferred parity coupling links to the baryon asymmetry**](docs/results/2026-06-30-v2.324-qnm-gravitational-leptogenesis.md) (a bold fresh swing into a new sector -- the matter-antimatter asymmetry; the SAME parity coupling g_R2_parity that drives the chiral GW (v2.319) and matches cosmic birefringence (v2.321) ALSO sources a baryon asymmetry via the mixed gravitational anomaly -- the Alexander-Peskin-Sheikh-Jabbari mechanism, d_mu j^mu_(B-L)~R R-dual: a parity-odd curvature term makes <R R-dual>!=0 in inflation (the same chirality as the chiral GW), producing net lepton number -> sphaleron-converted to baryons ~ g_R2_parity; this UNIFIES THREE cosmological signatures under one coupling -- cosmic birefringence (measured), chiral primordial GW (future CMB TB/EB), baryon asymmetry eta_B~6e-10 (measured) -- all nonzero ONLY for parity-violating gravity; the four parity-EVEN frameworks have <R R-dual>=0 -> source ZERO of all three, CANNOT gravitationally explain the matter-antimatter asymmetry, while the preferred framework (g_R2_parity=0.06) sources all three with aligned signs; so the mild parity violation the program kept arriving at (anomaly-preferred + birefringence-required) is, through one coupling, a candidate gravitational origin for the baryon asymmetry with chiral GW the clean future test; ONLY existence+discriminator+sign+unification claimed NOT the eta_B magnitude (famously scale-dependent, unsourced in toy basis); symmetry-based discriminator robust; 5/5 checks) · [**v2.323 the new-theory program ledger: robust findings, schematic predictions, honest corrections**](docs/results/2026-06-30-v2.323-qnm-program-ledger.md) (a capstone synthesis of the program v2.292-322, classifying every major finding by robustness tier and verifiably re-confirming the ROBUST tier; HEADLINE: the engine constructs the unique higher-derivative gravity consistent with current theory AND current data (string-like matter, trimmed curvature, mild right-handed parity violation) where every named framework is excluded on one axis; TIERS -- ROBUST (encoding+prefactor independent, 6/6 re-confirmed): no named framework satisfies both / parity-even->beta=0 data-excluded (symmetry) / constructed beats community 98% / lqg is the boundary 92% / one joint curvature carving / lqg's g_R3 outlier; MECHANISTIC: amplitude-vs-universality equilibrium, anomaly matching prefers parity, non-convex boundary layer, Hausdorff cutoff ceiling; DATA-FAVORED: cosmic birefringence requires parity; SCHEMATIC PREDICTION (sign robust, magnitude unsourced): chiral GW, n_s handle on cubic; HONEST NEGATIVE/CORRECTION carried forward: the v2.316 deprecated-RFC artifact, the v2.308 cubic/quartic n_s degeneracy, the v2.320 prefactor-fragile community feasibility; the parity finding triply motivated (anomaly mechanism + cosmic-birefringence data + chiral-GW test, all right-handed); durable content structural not numeric (toy basis, 'right streets wrong house numbers'); supersedes v2.299/v2.315; 6/6 checks) · [**v2.322 theory vs data: no named framework satisfies both, but a constructed one does**](docs/results/2026-06-30-v2.322-qnm-theory-vs-data-scorecard.md) (the program's capstone scorecard: score every candidate on BOTH axes -- theoretical consistency (convex_hull) and the four INGESTED-DATA constraints (GW170817 speed, GW dispersion, cosmic birefringence, sub-mm gravity); a sharp DICHOTOMY -- NO named framework satisfies both: the four parity-EVEN frameworks (pure_gr, string, AS, cdt) are theory-feasible but DATA-EXCLUDED (predict beta=0, the measured 0.34+/-0.09 deg ~3.6 sigma rules it out; parity-evenness FORCES beta=0 so this is a symmetry statement robust to prefactors), while lqg is the mirror image -- DATA-FAVORED (parity-violating, matches beta) but THEORY-INFEASIBLE (outlier cubic g_R3, v2.311); each named framework fails exactly one axis; but a CONSTRUCTED framework (string-like matter ~0.5/0.4/0.4, trimmed curvature g_R2~0.19/g_R3~0.09, parity g_R2_parity=0.06 in the v2.321 joint window [0.048,0.078]) satisfies BOTH the full consistency stack AND all four data constraints; so the engine constructs the UNIQUE candidate consistent with current theory AND current data where every community proposal is excluded on one side -- a string-like, trimmed-curvature, mildly right-handed-parity-violating gravity that anomaly matching prefers (v2.318), cosmic birefringence requires (v2.321), and chiral GW would test (v2.319); dichotomy robust (parity-even->beta=0 symmetry), magnitudes order-of-magnitude (3.6-sigma detection); 4/4 checks) · [**v2.321 the measured cosmic birefringence favors parity-violating gravity**](docs/results/2026-06-30-v2.321-qnm-cosmic-birefringence-favors-parity.md) (a real-DATA swing converging with the consistency analysis; the engine's cosmic_birefringence_data constraint encodes the measured isotropic birefringence angle beta=0.34+/-0.09 deg (Minami-Komatsu 2020 / Eskilt-Komatsu 2022) -- nonzero at ~3.6 sigma -- via beta_pred=3.4 deg * g_R2_parity, so a nonzero beta DEMANDS a parity-odd coupling; the four parity-EVEN frameworks (pure_gr, string, AS, cdt) predict beta=0 and are DISFAVORED by the data (excluded ~3.6 sigma), while the data REQUIRES g_R2_parity>=0.048 (2-sigma edge) and centers on 0.10 (1-sigma [0.074,0.126]), with lqg (0.08) inside the band; consistency and data AGREE on sign+existence (both want g_R2_parity>0, right-handed) -- the anomaly-preferred 0.038 (v2.318) is just below the 2-sigma data edge, the data wanting somewhat more; mutually satisfiable -- a constructed framework with parity in the JOINT window [0.048,0.078] passes BOTH all consistency constraints AND the cosmic-birefringence data; so THREE independent lines converge on mild right-handed parity violation: a consistency condition (anomaly matching v2.318), a real measurement (cosmic birefringence, this cycle), and a future observable (chiral primordial GW v2.319, same g_R2_parity) -- the parity finding is data-favored, not just internally motivated; discriminator+sign robust, magnitude order-of-magnitude (3.6-sigma detection, schematic beta map); 5/5 checks) · [**v2.320 does the surviving claim survive prefactor jitter? robust core, fragile community feasibility**](docs/results/2026-06-30-v2.320-qnm-prefactor-robustness-convex-hull.md) (realism program applied to the corrected arc -- jitter all 11 tunable prefactors by an O(1) log-uniform factor [1/2,2] over 60 draws under convex_hull, ask what fraction each conclusion holds; a clean ROBUST/FRAGILE split: constructed framework beats every community 98% + lqg is the boundary 92% = ROBUST [not artifacts of the canonical prefactors], but >=1 community framework feasible only 13% = FRAGILE [in ~87% of draws ALL four community frameworks are infeasible even under convex_hull]; so the v2.317 'three community frameworks feasible' holds only near canonical prefactors -- they sit on the KNIFE'S EDGE, marginally feasible [margins ~+0.005] but pushed out by most O(1) perturbations; the fully honest picture is BETWEEN the extremes -- not the matter_product 'universal exclusion' artifact (v2.315/316), not the v2.317 'comfortably feasible' reading, but MARGINALLY feasible on a thin prefactor sliver; solid across BOTH the RFC-form correction AND prefactor uncertainty: a constructed framework robustly beats the named ones and lqg is robustly the worst; the realism program refining (not overturning) the correction; conservative witness so 98% is a lower bound; 4/4 checks) · [**v2.319 chiral primordial gravitational waves: a CMB parity discriminator**](docs/results/2026-06-30-v2.319-qnm-chiral-primordial-gw.md) (a fresh swing into a new sector -- the falsifiable cosmological signature of the corrected parity finding; the preferred framework's mild gravitational Chern-Simons/Pontryagin coupling g_R2_parity~0.04 (v2.317/318) makes the two circular polarizations of the PRIMORDIAL tensor spectrum unequal during inflation -- net chirality Pi=(P_R-P_L)/(P_R+P_L)=tanh(kappa*g_R2_parity*(k/k_pivot)^n), reproducing standard Chern-Simons-inflation features (one helicity enhanced, |Pi|<1, Pi->0 as coupling->0, chromatic) -- observable as parity-odd CMB TB/EB cross-correlations (LiteBIRD/CMB-S4); a CLEAN DISCRIMINATOR: the four parity-EVEN frameworks (pure_gr, string, AS, cdt) predict Pi=0 EXACTLY (zero TB, zero EB) while only engine_preferred (Pi=+0.15) and lqg (Pi=+0.31) predict nonzero chirality, both RIGHT-handed (sign set by the positive coupling); so a future detection of primordial GW chirality/CMB TB-EB favors the parity-violating frameworks and a null result tightens the coupling toward zero; distinct from v2.252 (photon EM birefringence) and v2.269 (GW propagation birefringence) -- this is inflationary PRODUCTION of the tensor spectrum; discriminator+sign robust, magnitude schematic (CS->chirality amplitude unsourced); 6/6 checks) · [**v2.318 why does consistency reward mild parity violation? anomaly matching is the wall that wants it**](docs/results/2026-06-30-v2.318-qnm-parity-anomaly-mechanism.md) (explains the v2.317 parity reversal mechanistically -- which consistency condition rewards parity?; the binding wall on the parity-free preferred point is t'Hooft ANOMALY MATCHING (sd +0.020), and d(t_hooft signed-distance)/d(g_R2_parity)=+0.366 is POSITIVE -- anomaly matching is RELIEVED by a parity-odd curvature coupling; ranking all constraints by that derivative, the ones relieved are exactly the chirality-sensitive ones (anomaly matching, right_handed_graviton_positivity), the penalized ones (distance conjecture, anomaly-inflow, LIGO birefringence) sit on ample margin -- so trading a little parity raises the worst case; physically sensible since anomalies ARE chirality/parity objects (matching the gravitational chiral anomaly naturally calls for a parity-odd term); two honest caveats that sharpen it: (i) a distance-conjecture FORBIDDEN ZONE at tiny parity (~0.012 makes max/min coupling ratio explode, sd -21.7) so the preferred parity is finite ~0.04 not infinitesimal, (ii) the full +0.033 needs a JOINT shift (parity + larger g_4 + trimmed g_R2), parity-led relaxation not an isolated knob; corrected headline made mechanistic -- the most-robust gravity is mildly parity-violating BECAUSE anomaly matching, the binding universality wall (v2.314/315), is relieved by a parity-odd coupling; engine's literal check() output, sign robust/magnitude prefactor-dependent; 5/5 checks) · [**v2.317 the corrected preferred framework: re-establishing v2.312-315 under convex_hull**](docs/results/2026-06-30-v2.317-qnm-preferred-framework-convex-hull.md) (re-runs the preferred-framework construction + scorecard under the recommended convex_hull RFC form (v2.316) -- which results survive, which change; THREE outcomes: (1) CORE SURVIVES -- the engine still prefers a distinct CONSTRUCTED framework, the Chebyshev center has geom margin +0.033, more robust than every community framework (best: asymptotic_safety +0.019), so 'the engine points to a higher-derivative gravity more robust than the named proposals' holds under the corrected encoding too; (2) FEASIBILITY CORRECTED -- three community frameworks now feasible with a clean ranking AS(+0.019) > string(+0.013) > cdt(+0.011) > pure_GR(marginal) > lqg(-0.047, the only infeasible one, via its outlier cubic v2.311), replacing 'all community infeasible'; (3) PARITY REVERSAL (new finding) -- the corrected preferred framework carries a MILD PARITY VIOLATION (g_R2_parity~0.038) and forcing parity to zero costs ~40% of the robustness (+0.033->+0.020), so under the corrected encoding consistency mildly PREFERS a parity-violating theory, OPPOSITE the parity-free preferred framework under the deprecated form (which suppressed the parity direction via the spurious g_4*g_6 product); corrected headline: the most-robustly-consistent higher-derivative gravity is a string-like, mildly parity-violating theory distinct from every named framework, AS the most-consistent named one, lqg alone excluded; sign-based claims robust, exact parity/ordering prefactor-dependent; 5/5 checks) · [**v2.316 CORRECTION: the 'all frameworks infeasible' verdict was a deprecated-RFC-form artifact**](docs/results/2026-06-30-v2.316-qnm-rfc-form-correction.md) (a scrutiny cycle on the decisive constraint of the recent arc -- and an honest self-correction; v2.315 found every community framework infeasible, all bound by repulsive_force_conjecture, and built 'only pure GR feasible / universality decisive / engine prefers a unique constructed framework' (v2.312-315) on it; build_stack() DEFAULTS the repulsive-force conjecture to its matter_product form g_4*g_6-g_R2-gamma*g_R2^2>=0, which the engine's OWN docstring documents as excluding 'EVERY candidate framework for any gamma>0 -- a 100% universal exclusion that is an artifact of the spurious product (frameworks all have g_4*g_6~g_R2 by construction)', recommending the physically-corrected convex_hull form g_4-g_R2-gamma*g_R2^2>=0; re-scoring under the recommended form changes the picture decisively -- string (+0.006), AS (+0.004), cdt (+0.007) all become FEASIBLE, only lqg stays infeasible (-0.075, now bound by cft_flat_space_bound, a cubic-curvature/CFT condition = the outlier-g_R3 mechanism v2.311 identified); CORRECTED: the universal-exclusion / unique-preferred-framework headlines were specific to the deprecated default form; ROBUST: lqg infeasible under both forms via its outlier g_R3 (v2.311 primary, not the matter_product-specific g_R2 box), and the carving/convexity/moment/cosmology results don't invoke RFC and are unaffected; future cycles use form='convex_hull'; the correction is the engine's literal check() output; 5/5 checks) · [**v2.315 the consistency scorecard: universality is the decisive arbiter**](docs/results/2026-06-30-v2.315-qnm-consistency-scorecard.md) (consolidates the new-theory arc into one ranked scorecard + a coherence audit that the headline results hold together; ranked by worst-case margin: engine_preferred +0.0051 (unique strictly-feasible, bound by t_hooft_anomaly_matching), pure_gr 0.0000 (marginal, positivity wall), then ALL FOUR community frameworks INFEASIBLE -- string -0.040, cdt -0.048, AS -0.053, lqg -0.120 -- and the decisive fact: every community framework fails for the SAME reason, all bound by repulsive_force_conjecture (the swampland member of the UNIVERSALITY family), while the preferred framework is bound by t_hooft anomaly-matching (the OTHER universality member); so the UNIVERSALITY family (anomaly + swampland) is the decisive arbiter throughout -- it kills every community proposal AND sets the preferred framework's robustness ceiling, amplitude positivity always a step behind (v2.314); the preferred framework is precisely the theory that CLEARS the repulsive-force wall the community frameworks crash into [by trimming curvature, g_R3=0.085 below every community value] while balancing the anomaly wall; ties the arc together [carved interior bounded by universality-vs-amplitude v2.314, community overshoot the same wall, preferred threads between v2.312/313, lqg's box v2.311 = same tension past breaking]; engine's literal check() output, audit confirms no contradiction; 5/5 checks) · [**v2.314 what limits the engine-preferred framework? a balance of amplitude positivity and universality**](docs/results/2026-06-30-v2.314-qnm-preferred-framework-equilibrium.md) (explains WHY the v2.312/313 preferred framework sits where it does and why its robustness is only ~0.02: it is a multi-principle EQUILIBRIUM; ranking constraints by signed distance at the preferred point, the tightest SIX cluster within ~0.02-0.04 and split across two of the engine's classes -- C_UNIVERSALITY/anomaly-swampland [t_hooft_anomaly_matching 0.020 (worst case), repulsive_force 0.021, anomaly_cancellation 0.026] and A_AMPLITUDE/positivity [scalar_convexity 0.027, graviton_forward 0.031, cross_sector_efthedron 0.036]; the two families are in TENSION -- finite-difference gradients show raising g_6 IMPROVES the amplitude margin (+0.37) while WORSENING the universality margin (-0.18), so no single coupling move improves the worst case, the preferred point is the SADDLE where positivity and anomaly-matching balance; one mechanism explains the arc -- WHY the consistent interior is small [squeezed between two opposing principle families], WHY pure GR is only marginal [both families saturate together at the origin], WHY the community frameworks fail [they relieve one family at the other's cost, e.g. lqg's repulsive-vs-anomaly box v2.311]; the engine's most-consistent higher-derivative gravity is the unique trade-off point between amplitude positivity and anomaly/universality; engine's own taxonomy + literal check() output; 5/5 checks) · [**v2.313 is the engine-preferred framework metric-robust? three robustness objectives agree**](docs/results/2026-06-30-v2.313-qnm-preferred-framework-robustness.md) (validates v2.312's honest caveat that the raw worst-case margin MIXES heterogeneous scales so the exact optimum isn't canonical -- reconstructs the preferred point under three robustness objectives: raw min-margin [v2.312, scale-mixing], the gradient-normalized geometric CHEBYSHEV center [scale-FREE, largest inscribed ball in the natural metric], and the log-barrier ANALYTIC center [distinct interior-point notion]; all three land in the SAME region -- raw (g_4=0.58,g_R2=0.19,g_R3=0.15), geom (0.57,0.15,0.09), analytic (0.54,0.13,0.05) -- all strictly feasible, all parity-free, all string-like matter [g_4~0.54-0.58, g_6~0.36-0.39, g_8~0.36-0.46] with TRIMMED curvature [g_R2,g_R3 below string's 0.2/0.15], agreeing to within 0.096 in every coupling; so the exact coordinates shift slightly between metrics but the QUALITATIVE preferred framework [string-like matter, softened curvature, no parity] is the same under the principled scale-free metric as the raw one -- the engine's preferred higher-derivative gravity is a robust interior feature, not a normalization artifact; metric-robust = qualitative agreement to ~0.1, not identity; optima approximate not proven-global; 5/5 checks) · [**v2.312 what framework does the engine PREFER? constructing the most-robustly-consistent EFT**](docs/results/2026-06-30-v2.312-qnm-engine-preferred-framework.md) (the new-theory mandate taken literally -- instead of testing community frameworks, CONSTRUCT the one the engine prefers: the Chebyshev center maximizing the worst-case constraint margin over the 7-coupling space; pure GR is feasible but MARGINAL [29 of 38 constraints saturate at the origin -- homogeneous positivity/causality/swampland walls vanishing at zero couplings], yet the feasible set has a real INTERIOR -- a multi-seed coordinate-ascent search finds a strictly-interior point with all 38 margins positive [worst-case +0.0063] at g_4=0.57, g_6=0.40, g_8=0.35, g_R2=0.19, g_R3=0.10, parity=0; this is the ENGINE-PREFERRED framework: a string-tree-EFT-like matter sector [near string 0.5/0.4/0.4] with TRIMMED curvature [g_R2,g_R3 pulled below string's 0.2/0.15 into the interior], MORE robust than pure GR and feasible where ALL FOUR community frameworks are INFEASIBLE [string -0.040, AS -0.053, cdt -0.048, lqg -0.120]; so the engine points to a concrete NEW candidate -- keep string-like matter, soften the curvature until every wall clears -- matching no framework on the menu; sign-based claims scale-independent, exact point convention-dependent; an earlier coarse scan wrongly suggested empty interior, corrected by the thorough search; 7/7 checks) · [**v2.311 what CP-even couplings bind lqg? an outlier cubic, and an unopenable curvature-matter box**](docs/results/2026-06-30-v2.311-qnm-lqg-failure-attribution.md) (completes the v2.310 diagnostic by isolating WHICH couplings bind lqg, failure by failure, against the engine -- two clean mechanisms: (1) an OUTLIER CUBIC CURVATURE g_R3 -- lqg's 0.30 is 2x any peer [string/cdt 0.15, AS 0.10] and is the SOLE cause of graviton_forward_positivity [per-failure attribution]; reducing it to the peer value clears 4 of 6 failures [cft_flat_space_bound, complexity_cutoff, cross_sector_efthedron, graviton_forward_positivity]; (2) an unopenable (g_R2, matter) BOX for the residual two [repulsive_force + bnossw_monogamy] -- scanning g_R2 vs the engine: repulsive/monogamy satisfied only for g_R2<=0.22 but anomaly_cancellation only for g_R2>=0.2675 -> the allowed interval is EMPTY [gap 0.048, lqg actual 0.30]; lqg's leading curvature coupling cannot simultaneously satisfy the repulsive-force bound [wants g_R2 small vs g_4 g_6] and anomaly matching [wants g_R2 large enough to match g_4 g_6 - g_R2^2], no value in between; so lqg's boundary = outlier g_R3 + an unopenable curvature-vs-matter box, squarely CP-even [consistent with v2.310 parity-innocence]; all thresholds the engine's literal check() output; 5/5 checks) · [**v2.310 is lqg the consistency boundary BECAUSE of its parity violation? the engine says no**](docs/results/2026-06-29-v2.310-qnm-lqg-parity-innocence.md) (lqg is repeatedly flagged as the engine's consistency boundary AND is the ONLY parity-violating framework -- natural hypothesis: its parity sector causes its infeasibility; REFUTED by the engine's own verdict via three probes on the full 38-constraint stack: (1) TOGGLE -- lqg fails 6 constraints [bnossw_monogamy, cft_flat_space_bound, complexity_cutoff, cross_sector_efthedron, graviton_forward_positivity, repulsive_force_conjecture] and setting g_R2_parity=g_R3_parity=0 leaves that set IDENTICAL, all 6 CP-even, none reads parity -> parity is INNOCENT; (2) HEADROOM -- lqg's parity is consistent but MARGINAL: at its value zero parity constraints fire but scaling parity up just ~10% (factor 1.1) trips generalized_anomaly_inflow -> lqg sits JUST inside the carved parity window; (3) MAGNITUDE -- uniformly shrinking lqg's CP-even couplings does NOT heal it [6->7 fails at scale 0.8] -> the tension is structural in lqg's coupling RATIOS, not magnitude, not parity; so lqg's boundary status and its parity violation are INDEPENDENT facts -- a clean engine-backed refutation of a plausible hypothesis; every verdict the engine's literal check() output [no schematic map]; 7/7 checks) · [**v2.309 the joint carved curvature region: intersecting all the tower bounds at once**](docs/results/2026-06-29-v2.309-qnm-joint-curvature-region.md) (a consolidating capstone of the curvature-carving arc: the arc bounded each operator SEPARATELY [g_R2 bracket v2.302, CEMZ+cubic on g_R3 v2.303, moment-tower floor on g_R4 v2.292] -- this cycle computes the INTERSECTION, the actual joint (g_R2,g_R3,g_R4) region for a fixed matter sector [synthesis-as-computation, the joint region is a new object not the union of the statements]; intersecting the six engine constraints [GSL, QFC, CEMZ, cubic positivity, Riemann^4 positivity, moment tower] gives ONE nonempty BOUNDED region -- for g_4=0.5: g_R2 in [0,0.99] (QFC cap 2g_4), g_R3 in [-0.5,0.5] (cubic |g_R3|<=g_4), g_R4 floored by moment tower -- only 11.1% of the EFT box, a tightly carved sliver; so the separately-derived bounds are MUTUALLY CONSISTENT and nest into one LADDER (matter->g_R2->g_R3->g_R4); reproduces v2.292 from a new direction -- the moment tower forces g_R4>0 when g_R3!=0, so every framework with default g_R4=0 sits just OUTSIDE and enters exactly at its floor g_R3^2/g_R2 [string 0.1125, AS 0.0667, lqg 0.30, cdt 0.1023]; vectorized region cross-checked point-by-point vs the engine classes [agree to 1e-9]; 6/6 checks) · [**v2.308 CMB spectroscopy of the curvature tower? a clean hypothesis, honestly refuted**](docs/results/2026-06-29-v2.308-qnm-curvature-tower-cmb-spectroscopy.md) (the clean hypothesis: each higher curvature operator imprints on a higher spectral derivative -- R^2->r, R^3->n_s, R^4->running alpha_s; adding the quartic g_R4 + the running [f(R)=R+aR^2+bR^3+cR^4, validated by a strict b=c=0 self-check reproducing the Starobinsky running -2/N^2 to 5e-5]; the data REFUTES it -- the quartic does NOT show up in the running [swings it 0.14sig] but is ANOTHER n_s handle [11.7sig], exactly like the cubic; TWO robust facts replace it: (1) the running is INSENSITIVE to the whole higher-curvature tower [stays at Starobinsky -2/N^2 whether cubic or quartic is on] -> near-Starobinsky running is a robust engine prediction, a large |alpha_s| would mean physics beyond a perturbative plateau; (2) cubic & quartic are DEGENERATE in n_s -> the CMB shape (r,n_s,alpha_s) CANNOT separate g_R3 from g_R4; this SHARPENS v2.307 -- n_s constrains a g_R3/g_R4 COMBINATION not g_R3 alone, and 'n_s pins g_R3' survives only because the moment-tower floor g_R4>=g_R3^2/g_R2 forces the quartic to ~4e-8 [negligible n_s shift 2.8e-4], breaking the degeneracy by fiat though the CMB itself can't; honest negative, numerics validated [real degeneracy not artifact]; 6/6 checks) · [**v2.307 higher-curvature corrections to Starobinsky inflation: n_s is the CMB handle on the cubic**](docs/results/2026-06-29-v2.307-qnm-higher-curvature-inflation.md) (a fresh swing into COSMOLOGY: Starobinsky inflation IS the R^2 (g_R2) corner of the engine's curvature sector; what does the engine's positivity-bounded CUBIC g_R3 do to the CMB observables? Standard f(R)=R+aR^2+bR^3 inflation, validated by a strict b=0 self-check that reproduces Starobinsky exactly [n_s=0.9650 vs analytic 1-2/N=0.9636, r=0.0035 vs 12/N^2=0.0040, both <0.004]; turning on the cubic reveals a sharp ASYMMETRY -- over the window |b|<=0.0015 the tensor ratio r barely moves [0.14 of the BK18 limit, far below 0.036] but the scalar tilt n_s swings ~18 PLANCK SIGMA [0.905-0.980], monotonically with b; so n_s NOT r is the CMB handle on the cubic curvature coupling: the Planck 3-sigma n_s band carves a SUB-WINDOW b in [0, 0.0002] TIGHTER than positivity [parts of the positivity-allowed cubic are CMB-EXCLUDED, even slightly favouring a positive cubic] -- CMB data is MORE constraining than positivity for g_R3, a measurement of n_s pins its sign/magnitude as no ringdown/amplitude bound does, r an independent slack cross-check; numerics exact + Starobinsky-validated, g_R3->b map schematic, amplitude [a~1e9] not addressed [shape only]; 6/6 checks) · [**v2.306 the finite-cutoff moment bound: Hausdorff beats Stieltjes**](docs/results/2026-06-29-v2.306-qnm-finite-cutoff-hausdorff-bound.md) (a fresh swing in a NEW sector: every positivity bound the engine uses is a STIELTJES moment condition -- the Wilson couplings are moments of a spectral density on the whole half-line [0,inf) -- giving only the one-sided tower g_6^2<=g_4 g_8; but a real EFT has a FINITE cutoff, so its UV-completing density has BOUNDED support, and a moment sequence on [0,S] must satisfy the HAUSDORFF conditions, STRICTLY STRONGER: normalizing m_0=g_4, they add the CEILING g_8 <= S*g_6 that positivity NEVER supplies, with minimal support S_min=g_8/g_6; VERIFIED constructively -- the two-atom spectral measure with atoms {0, g_8/g_6} and weights {1-g_6^2/(g_4 g_8), g_6^2/(g_4 g_8)} reproduces (g_4,g_6,g_8) exactly, sits in [0,g_8/g_6], and has non-negative weights precisely when Stieltjes holds; the bound has TEETH positivity lacks: (g_4,g_6,g_8)=(1,0.5,1.0) passes positivity (0.25<=1.0) yet is EXCLUDED by any cutoff S<2.0; so 'the EFT has a cutoff' is a quantitative UPPER bound on the tower, not a slogan; all frameworks pass at S_min 0.875-1.0, sitting at the edge of the finite-support window; EXACT mathematics, physical scale schematic; opt-in not yet wired to the stack; 6/6 checks) · [**v2.305 the exact non-convexity criterion: the dent is a boundary layer**](docs/results/2026-06-29-v2.305-qnm-nonconvexity-exact-criterion.md) (quantifies v2.304: because the repulsive-force bound g_4 g_6 - g_R2 - g_R2^2>=0 is QUADRATIC, the midpoint margin is a CLOSED FORM with no Taylor remainder -- m_mid=(m_A+m_B)/2 - (Dg4*Dg6 - Dg_R2^2)/4 -- matching the engine to 4.4e-16 over 400 random feasible pairs; so the region dents by the EXACT criterion Dg4*Dg6 - Dg_R2^2 > 4*(chord margin), predicting every midpoint; TWO sharp facts: (1) WHERE -- the curvature term -g_R2^2 is convex and only HELPS, so non-convexity lives ENTIRELY in the bilinear matter product g_4 g_6 and dents only where matter couplings CO-VARY (Dg4*Dg6>0, both up or both down together); anti-correlated moves provably never dent; (2) HOW MUCH -- the dent is a thin BOUNDARY LAYER not a generic effect: dent rate 0% at depth 0.3/0.1 from the wall, 6% at 0.03, 12% at 0.01, rising monotonically as the boundary is approached; so the consistent theory space is CONVEX in its interior and dents only in a skin near the repulsive-force wall where co-varying matter sits -- the swampland's non-convexity is precisely the matter sector's bilinearity, confined and quantified to the coefficient; closed form EXACT not a fit; 7/7 checks) · [**v2.304 is the consistent theory space convex? a non-convexity counterexample**](docs/results/2026-06-29-v2.304-qnm-feasible-region-convexity.md) (a deliberate pivot from the 'which principle binds' sub-arc to a STRUCTURAL property of the carved region: is it CONVEX -- i.e. is a mixture of two consistent theories consistent? NO: two theories A (g_4=1.0,g_6=0.3,g_R2=0.22, margin +0.032) and B (g_4=0.2,g_6=0.06,g_R2=0.01, margin +0.002) BOTH satisfy the repulsive-force conjecture g_4 g_6 - g_R2 - g_R2^2>=0, yet their MIDPOINT (g_4=0.6,g_6=0.18,g_R2=0.115) VIOLATES it (margin -0.020) -- a 50/50 mixture of two consistent theories is INCONSISTENT; cause = the BILINEAR matter product g_4 g_6, which dips below the chord between asymmetric points; SHARP CONTRAST with the positivity sector: the curvature moment tower g_R3^2<=g_R2 g_R4 and matter dispersion g_6^2<=g_4 g_8 are rotated second-order cones and stay feasible under averaging -- CONVEX; so convexity is the EXACT dividing line: positivity/moment/dispersion bounds CONVEX [the EFT-hedron is a convex positive geometry], gravitational swampland bounds NON-convex; physically the swampland is NOT the convex complement of a convex landscape and 'interpolating between two consistent quantum gravities stays consistent' FAILS for gravity; counterexample reproducible against the real engine, prefactor-robust; 6/6 checks) · [**v2.303 causality carves the cubic curvature: the hierarchical carving of the curvature tower**](docs/results/2026-06-29-v2.303-qnm-curvature-tower-hierarchical-carving.md) (a fresh swing engaging CEMZ causality -- the 'higher-derivative gravity needs a higher-spin tower' result -- on the cubic curvature: a graviton crossing a shock wave gets a TIME ADVANCE unless |g_R3|<=0.8 sqrt(g_4 g_R2) [geometric mean of matter & leading curvature], complementary to the cubic POSITIVITY bound g_R3<=g_4^2; they cross at g_R2=g_4^3/kappa^2, so CAUSALITY is the active cubic bound for lqg/cdt [more strongly curved] and UNITARITY for string/AS -- causality is the decisive constraint exactly where the 'needs a tower' physics bites; this completes the HIERARCHICAL carving -- the curvature tower is carved order-by-order by DISTINCT principles that CHAIN: g_R2 by four principles [v2.302], g_R3 by causality+unitarity [this cycle], g_R4 floored by the moment tower [v2.292], so matter (g_4,g_6)->g_R2->g_R3->ringdown-active g_R4; the entire higher-curvature sector determined by the matter sector through a LADDER of different QG consistency conditions, one per rung; STRUCTURAL content prefactor-robust, the hierarchy map an organizing statement not a new bound; 6/6 checks) · [**v2.302 the curvature coupling bracketed by four QG principles, three regimes**](docs/results/2026-06-29-v2.302-qnm-curvature-bracket-four-principles.md) (completes the info-theoretic sub-arc: the leading curvature coupling g_R2 is bracketed by FOUR independent QG principles -- a thermodynamic LOWER bound [Generalized Second Law, g_R2>=-0.5] + three UPPER bounds of distinct form: null energy [Quantum Focusing, g_R2<=g_4/alpha=2g_4, LINEAR], entanglement [BNOSSW monogamy, HARMONIC mean], unitarity [positivity, GEOMETRIC mean]; because QFC is linear in g_4 alone it breaks the g_4<->g_6 symmetry of the means, so the binding upper bound PARTITIONS the matter plane into three regimes by x=g_6/g_4: ENTANGLEMENT [x<0.146, g_4>>g_6], UNITARITY [0.146<x<4, balanced], NULL ENERGY [x>4, g_6>>g_4]; the engine's frameworks all sit in the balanced unitarity band [x~0.73-0.80], but a UV completion in either asymmetric regime would have its curvature coupling fixed by entanglement or quantum focusing instead; four complementary windows on one coupling, each decisive in its own corner; QUALITATIVE three-regime partition robust for any O(1) prefactors, only the boundaries prefactor-dependent; 6/6 checks) · [**v2.301 entanglement amplifies the irreducible ringdown: chaining monogamy with the moment tower**](docs/results/2026-06-29-v2.301-qnm-entanglement-amplified-ringdown.md) (combines the two new sectors: v2.300's entanglement cap on g_R2 chained with v2.292's moment-tower floor g_R4>=g_R3^2/g_R2 -> the minimum forced ringdown-active g_R4 = g_R3^2/g_R2_max with g_R2_max=min(entanglement,positivity), so the tighter cap RAISES the ringdown floor; amplification over positivity-only = (2/3)*AM/GM(g_4,g_6); the engine's frameworks are all positivity-floored [amplification 1.000, entanglement slack], but an asymmetric matter sector [g4=5,g6=0.5] has entanglement as the binding cap, AMPLIFYING the irreducible Riemann^4 floor x1.16 [0.117 vs 0.101] -- entanglement forces a consistent higher-curvature gravity to ring MORE than unitarity/causality alone require; the 'it from qubit' structure reaching a GW observable through the consistency chain; amplification prefactor-ROBUST in form [AM/GM>=1 always], coupling-level statement with the QNM map carrying the v2.215/v2.209 caveats; 5/5 checks) · [**v2.300 it from qubit: entanglement monogamy and positivity are complementary curvature bounds**](docs/results/2026-06-29-v2.300-qnm-entanglement-vs-positivity.md) (a fresh swing into the information-theoretic sector the arc hadn't engaged: the engine's BNOSSW entanglement-monogamy bound is g_R2 <= 3 g_4 g_6/(g_4+g_6) = (3/2) HARMONIC-mean of the matter couplings, vs the positivity bound g_R2 <= sqrt(g_4 g_6) = GEOMETRIC mean; harmonic and geometric means CROSS, so neither implies the other -- for nearly-equal matter couplings positivity is tighter, but past a DERIVABLE asymmetry g_4/g_6~6.85 [larger root of 9r=(r+1)^2] entanglement wins; the engine's frameworks all sit in the mild-asymmetry positivity-dominated regime [g4/g6~1.3], so monogamy is currently slack but NOT redundant -- an asymmetric UV point [g4=5,g6=0.5] has its curvature coupling bounded by entanglement [1.36] far below positivity [1.58]; so entanglement structure constrains geometry BEYOND unitarity/causality in the asymmetric regime, a concrete quantitative 'it from qubit' result; QUALITATIVE result prefactor-ROBUST [harmonic<=geometric always], only the crossover location prefactor-dependent; 5/5 checks) · [**v2.299 synthesis capstone: the new-theory arc (v2.292-v2.298), cross-verified**](docs/results/2026-06-29-v2.299-qnm-new-theory-arc-synthesis.md) (consolidates the seven-cycle arc the loop ran after being redirected from reconstructing community physics to INVENTING new QG theory, all load-bearing claims re-verified [6/6]: g_R4 engine-internal [v2.292] -> shared-spectrum cross-sector principle proposed [v2.293], rigorously DEMOTED [v2.294], proved structurally empty + real content rescued [v2.295], third handle W_+ [v2.296] -> falsifiable carving [v2.297] -> helicity-resolved tower deriving the dark parity-odd Riemann^4 [v2.298]; UNIFYING RESULT visible only across the whole arc -- every one of the four independent new structures singles out the SAME framework lqg_induced as the consistency boundary: x=g_R3/g_R2=1 [tower saturated], largest cross-sector W_+=0.80, EXCLUDED by the carving [empty g_R4 band], and the ONLY one forced to carry a parity-odd quartic [ringdown split]; four orthogonal constraints, one convergent verdict, robust not a single-cycle artifact; deliverable = a precise honest characterization [over-claim proposed then demoted, dark coefficient derived] + a method [swampland-complete EFT carving] producing falsifiable region-valued QG predictions; HONEST -- each structure carries its own premise, the lqg verdict is about the TOY encoding not physical LQG, the demoted over-claim is part of the deliverable; session's 8th synthesis capstone, close of the new-theory arc) · [**v2.298 the helicity-resolved curvature moment tower: deriving the dark parity-odd Riemann^4**](docs/results/2026-06-29-v2.298-qnm-helicity-resolved-tower.md) (fresh swing -- a single idea derives a previously-dark coefficient from consistency: the engine splits the graviton into LEFT/RIGHT helicities with separate positivity, so the v2.292 parity-blind curvature tower g_R3^2<=g_R2 g_R4 splits into two helicity towers (g_R3 +/- g_R3_p)^2<=(g_R2 +/- g_R2_p)(g_R4 +/- g_R4_p), whose floors fix the parity-even g_R4 AND the parity-ODD g_R4_p together; for parity-CONSERVING frameworks [string/AS/cdt, g_*_parity=0] the towers coincide -> g_R4_p=0, reducing to v2.292, no ringdown split; but lqg_induced [the lone parity-violating framework, g_R2_parity=0.08, g_R3_parity=0.04] has different L/R floors [0.3042 vs 0.3073] -> the towers FORCE a nonzero parity-odd Riemann^4 g_R4_p=-0.00153, the Bresciani Im(K_minus)/g_R4_c3 component that v2.209 found DARK now ILLUMINATED, derived from the lower parity couplings rather than imported, and predicting a parity-split ringdown [the v2.218 isospectrality-breaking discriminator] forced by the moment structure; 6/6 checks; HONEST -- g_R4_p is the mandate floor not a full determination, does NOT resolve the v2.209 sourcing gap, link to v2.218 structural) · [**v2.297 swampland-complete EFT carving: the method as a falsifiable QG proposal**](docs/results/2026-06-29-v2.297-qnm-method-as-proposal.md) (SIXTH SLICE -- the method-as-proposal made CONCRETE and FALSIFIABLE: the engine's original QG claim is that the route to phenomenology is to INTERSECT every consistency condition [positivity + matter AND curvature moment towers + causality + swampland + holographic + EFT validity] and treat the surviving feasible region as the prediction; applied to the one OBSERVABLE higher-curvature operator -- the ringdown-active Riemann^4 g_R4 -- the carving bounds it to a finite band per framework [floor from the moment-tower mandate g_R4>=g_R3^2/g_R2, ceiling from validity+complexity]: string_tree_eft [0.115,0.345], asymptotic_safety [0.07,0.53], cdt [0.105,0.37] -- a RANGE not a point, the right epistemic status for a carving; and it EXCLUDES -- lqg_induced gets an EMPTY band [its curvature is so over-large even the mandated minimum g_R4 exceeds the complexity cutoff], the recurring anomaly now appearing as non-existence of a consistent g_R4; falsifiable: a Riemann^4 coefficient measured outside the carved band falsifies the conditions/carving/framework; 5/5 checks; HONEST -- not the full 38-constraint carving, O(1)-prefactor-dependent ceiling, g_R4->QNM map carries the v2.209 dark-parity caveat, the thesis is a methodological proposal not a theorem) · [**v2.296 the cutoff bracket: a derived lower bound on the relative curvature coupling**](docs/results/2026-06-29-v2.296-qnm-cross-sector-cutoff-bracket.md) (FIFTH SLICE, completing the cross-sector characterization with the third handle: if the relative weight w(M^2)=(curvature coupling)/(matter coupling) is bounded by W=sup w, the bracket measure (W-w)dmu is positive so {W m_k - c_k} has a PSD Hankel -> C W^2 - B W + A >= 0 -> W >= W_+=(B+sqrt(B^2-4AC))/(2C), a DERIVED lower bound on the max relative curvature coupling; per framework [g_R4 at forced min, A=0, W_+=B/C]: string 0.406, AS 0.389, cdt 0.408, lqg 0.800 [the largest -- its curvature couples most strongly relative to matter, the recurring anomaly], each sharpening the trivial max-r_k bound; physical reading -- some state in the shared tower must couple to curvature at least W_+ times as strongly as to matter; COMPLETES the cross-sector map: w>=0->nothing, w monotone->ratio monotonicity, w<=W->relative-coupling lower bound -- a precise honest characterization of where the shared-spectrum hypothesis has teeth and where it's empty, the genuine new-theory deliverable of the arc; 5/5 checks; W is a relative coupling not the EFT cutoff scale, bounded-weight assumption physical not a theorem) · [**v2.295 when does the shared spectrum bite? the cross-sector constraint as a function of w**](docs/results/2026-06-29-v2.295-qnm-cross-sector-weight-structure.md) (FOURTH SLICE -- settles the cross-sector question structurally and rescues the real content: the tilted measure's Hankel H(t)=H_matter+t H_curv is a nonnegative combination of two PSD matrices, hence PSD for all t>=0 AUTOMATICALLY once both towers hold, at EVERY moment order [verified 2x2 and 3x3] -- so the bare 'w>=0' shared-measure hypothesis can NEVER add a cross-sector constraint [v2.294's negative was structural, not special]; the RESCUE -- new info needs stronger structure on the relative weight w(M^2): if w is MONOTONE [physically, a Regge tower where higher-mass states couple to curvature monotonically] then the ratios r_k=c_k/m_k=<w>_k are monotone, a genuine constraint -- a constructed PEAKED non-monotone weight passes BOTH towers AND the tilted-Hankel yet has non-monotone r_k [so can't come from a monotone shared weight], while all engine frameworks have monotone r_k [string/AS/cdt decreasing, lqg increasing], behaving as if w is monotone; so v2.293's ratio monotonicity was real but its correct premise is 'w monotone' not 'w>=0', v2.294 was right the bare hypothesis is empty; clean map: w>=0->nothing, w monotone->ratio monotonicity, w<=W->cutoff bracket; 5/5 checks; the assumption on w is a physical refinement not a theorem) · [**v2.294 the rigorous cross-sector bound (and an honest correction to v2.293)**](docs/results/2026-06-29-v2.294-qnm-cross-sector-tilted-hankel.md) (THIRD SLICE -- the swing-then-honestly-check discipline in action: sharpening the v2.293 calibrated ratio-band to a rigorous bound, the single-shared-measure hypothesis implies the tilted measure (1+t w)dmu is positive for all t>=0, so {m_k+t c_k} has a PSD Hankel ∀t>=0 -> the 2x2 bound A t^2+B t+C>=0 with A=g_R2 g_R4-g_R3^2, C=g_4 g_8-g_6^2, cross term B=g_4 g_R4+g_R2 g_8-2 g_6 g_R3, giving B>=-2sqrt(AC) [NO calibration]; HONEST FINDING -- by AM-GM and the two towers g_4 g_R4+g_R2 g_8>=2sqrt(g_4 g_8 . g_R2 g_R4)>=2 g_6 g_R3, so B>=0 ALWAYS, the 2x2 tilted-Hankel adds NOTHING beyond the two towers, and the v2.293 decoupled point [g_R4=2.0] that the calibrated band rejected has B=0.96>=0 and PASSES the rigorous bound -- so v2.293's 'strictly stronger' claim was OVER-STATED, demoted here; genuine new cross-sector info needs HIGHER moments [3x3 Hankel: g_10, g_R5], the next operator extension; 5/5 checks; reports the prior cycle's heuristic teeth as calibration not rigor) · [**v2.293 the cross-sector moment principle: matter and curvature share one spectral density**](docs/results/2026-06-29-v2.293-qnm-cross-sector-moment-principle.md) (SECOND SLICE of the new-theory arc, the breakthrough swing -- a PROPOSED new constraint with a verified separating example: if matter [g_4/g_6/g_8] and curvature [g_R2/g_R3/g_R4] operators are forward-limit moments of ONE shared spectral density [true for any UV completion where all operators descend from one tower, e.g. string's Regge tower], the curvature moments are the matter moments reweighted by a single positive w, so the ratios r_k=g_curv,k/g_matter,k must lie in ONE band; the engine's real frameworks obey it strikingly [string r=[0.40,0.375,0.281] tight monotone band, cdt similar, lqg monotone-increasing consistent with its over-coupled curvature], but a 'decoupled' point with string's matter + an over-large g_R4=2.0 PASSES both separate moment towers yet has r=[0.40,0.37,5.0] spread 2.4 -> REJECTED by the cross-sector band [no single positive weight averages to 0.4 twice and 5.0 once]; so the cross-sector condition is STRICTLY STRONGER than both towers combined -- a genuine new inter-sector constraint not derivable from either alone; HONEST STATUS: novel/unproven, hypothesis-dependent [single-shared-spectrum assumption], band threshold calibrated not derived, the separating counterexample exact; 5/5 checks) · [**v2.292 the g_R4 core-engine extension: the Riemann^4 operator becomes engine-internal**](docs/results/2026-06-29-v2.292-qnm-gr4-engine-extension.md) (FIRST SLICE of the user-authorized new-theory arc -- the loop redirected from reconstructing community physics toward INVENTING new QG theory: the engine now carries the Riemann^4 operator g_R4 [the first ringdown-active curvature operator, v2.233] + the curvature dispersion-tower constraints g_R4>=0 and g_R3^2<=g_R2 g_R4 [the v2.234 mandate, the curvature analog of the matter tower g_6^2<=g_4 g_8], new first-class engine code in src/itb/constraints/curvature_dispersion_tower.py, opt-in via build_stack(include_curvature_tower=True); the mandate BITES -- every curvature framework is INFEASIBLE at g_R4=0 and heals exactly at the forced minimum g_R3^2/g_R2 [string 0.1125, AS 0.0667, lqg 0.30, cdt 0.1023, reproducing v2.234], pure_gr needs none; so the engine's OWN positivity now MANDATES a nonzero Riemann^4 -- the ringdown sector is no longer imported but forced by the constraint geometry; full suite green [1995 passed], zero cascade; NEXT: promote g_R4 into frameworks/default stack + the cross-sector moment principle) · [**v2.291 the tidal deformability of a neutron star: the GW170817 observable and the EOS tension**](docs/results/2026-06-29-v2.291-qnm-ns-tidal-deformability.md) (closes the loop between v2.289/290 stellar structure and v2.270 GW170817: integrating the Hinderer l=2 tidal perturbation alongside TOV for the v2.290 stiff polytrope, the 1.4 Msun star has Love number k_2=0.090 and tidal deformability Lambda=1295 -- ABOVE the GW170817 bound Lambda_1.4<800 [measured ~190], because the stiff EOS that v2.290 needed for M_max~2 makes the 1.4 Msun star too LARGE [R=15.4 km vs the implied <~13.5 km]; this is the genuine well-known EOS tension reproduced from first principles -- heavy pulsars [PSR J0740 M>=2.08] demand a STIFF EOS while GW170817 demands a SOFTer one at 1.4 Msun, and a single Gamma=2 polytrope can't do both [real nuclear matter is soft at intermediate / stiff at high density]; a NS Love number is NONZERO vs the v2.235 BH=0, so Lambda is an 'is it a BH?' discriminator; 5/5 checks incl. the disfavored-by-GW170817 verdict) · [**v2.290 the neutron-star maximum mass: the TOV limit and the stability turnover**](docs/results/2026-06-29-v2.290-qnm-tov-maximum-mass.md) (builds on v2.289: why can't a neutron star be arbitrarily heavy? Integrating the TOV equation for a Gamma=2 polytrope P=K eps^2 and sweeping the central density, the mass-radius curve TURNS OVER -- M rises to a MAXIMUM of 2.00 Msun at R=11.2 km then falls, the TOV limit; the turnover is the stability boundary [below it dM/d rho_c>0 stable, above it <0 -> compressing LOSES mass -> collapse to a BH, both verified], the relativistic effect with no Newtonian analog [pressure that should support the star instead gravitates]; the EOS stays causal c_s^2<=0.85<1 on the stable branch; the toy ~2 Msun limit brackets the observed heaviest pulsar PSR J0740+6620 [2.08 Msun] and the GW170817 remnant [~2.2-2.3], with the causal Rhoades-Ruffini argument capping ANY NS at ~3.2 Msun; with v2.289 the relativistic-astrophysics sector now bounds both how compact [beta<8/9] and how heavy [M<M_TOV] cold matter can be; 6/6 checks, toy stiff polytrope) · [**v2.289 relativistic stellar structure: the TOV equation, the Buchdahl bound, and compactness**](docs/results/2026-06-29-v2.289-qnm-tov-compactness.md) (a fresh relativistic-astrophysics sector bridging the BH thread and the GW program: the Tolman-Oppenheimer-Volkoff equation [pressure both responds to AND sources gravity] makes the central pressure of a uniform-density star diverge at compactness beta=2GM/Rc^2=8/9 -- the BUCHDAHL BOUND, no static star can be more compact without infinite central pressure; a first-principles RK4 TOV integration reproduces the analytic Schwarzschild interior to 1e-4 [P_mid num=ana, m(R)/M=1], validating the solver; the compactness hierarchy: Sun 4e-6, white dwarf 2.5e-4, canonical 1.4 Msun neutron star 0.345, ultracompact photon-sphere onset 0.667 [R<3GM/c^2 -> rings with light like a BH, the v2.247 ECO echo regime], Buchdahl 0.889, black hole 1.0; ties the same compactness the v2.270 GW170817 tidal measurement constrains to a first-principles bound; 6/6 checks, uniform-density exact case [realistic EOS gives the M-R curve/max mass, not modelled here]) · [**v2.288 synthesis capstone: the engine feasible-region investigation (v2.281-v2.287), cross-verified**](docs/results/2026-06-29-v2.288-qnm-feasible-region-synthesis.md) (consolidates the seven-cycle engine-reconnection arc -- after a long from-scratch phenomenology run the loop turned back to the engine and mapped its feasible region -- with all 7/7 load-bearing claims re-verified live through the real check()/Theory API; the honest split stated up front: ROBUST = the feasible region is NOT just GR [a higher-derivative witness is feasible], it's a bounded curvature wedge whose g_R2 ceiling ~0.2 holds for every prefactor [anomaly+repulsive, not the tunable knobs], and the engine independently encodes the reconstructed GW/swampland phenomenology; CANONICAL-ONLY = the positivity ratio ceiling x~0.83 [moves to [0.6,1.2] across prefactors] and the lqg-vs-others positivity verdict [flips across the prefactor range]; so the whole investigation = one robust geometric fact + a clearly-flagged prefactor-dependent detail, the realism program drawing exactly the line it exists to draw; the session's 7th synthesis capstone) · [**v2.287 is the feasible curvature wedge robust? the realism program applied to v2.286**](docs/results/2026-06-29-v2.287-qnm-wedge-robustness.md) (stress-tests the v2.286 wedge against the documented O(1)-prefactor ranges -- and honestly DEMOTES one of its conclusions: sweeping the positivity prefactors graviton_fwd_c [0.8,1.6] x efthedron_alpha [0.8,1.5], the g_R2 ceiling is ROCK-SOLID robust [stays 0.15 for EVERY sample, since it's set by anomaly/repulsive not the tunable knobs -- so 'g_R2 only up to ~0.2' is a genuinely robust conclusion], but the positivity ratio ceiling x=g_R3/g_R2 is strongly prefactor-SENSITIVE [0.60 tight -> 1.20 loose, canonical 0.80] and that range STRADDLES the framework ratios; since string/AS/cdt sit at x~0.7 and lqg at x=1.0, WHICH frameworks fail the positivity wall is NOT robust [at loose prefactors lqg passes; at tight prefactors string fails too] -- so the v2.282/286 lqg-vs-others separation is the CANONICAL verdict, not a prefactor-robust one; the realism program working as intended, surfacing not burying the caveat; 5/5 checks incl. the check that the verdict is non-robust) · [**v2.286 mapping the feasible curvature region: the (g_R2, g_R3) wedge the engine allows**](docs/results/2026-06-29-v2.286-qnm-feasible-curvature-region.md) (maps the full 2D curvature region: for each (g_R2, g_R3) build an optimal matter completion and check feasibility -> the allowed sector is a bounded WEDGE [g_R2 up to 0.16 (<=0.2 hard), g_R3 capped at each g_R2 by graviton_forward_positivity = the v2.261/262 moment tower, verified by pushing past the ceiling]; the ratio ceiling x=g_R3/g_R2~0.83 is the key number -- it sits BELOW lqg's x=1.0 [so lqg is outside the positivity wedge, the v2.282 failure] and ABOVE string/AS/cdt's 0.67-0.75 [so those would pass positivity, missing on the matter-product floor instead, v2.284]; unifies the whole sub-arc -- every framework's standing is its position vs the wedge's two walls [anomaly/repulsive g_R2 ceiling + positivity x-ceiling], and the v2.285 witness sits inside both; 5/5 checks) · [**v2.285 the feasible region is not just GR: a higher-derivative theory the engine accepts**](docs/results/2026-06-29-v2.285-qnm-feasible-higher-derivative.md) (answers the v2.281-284 question CONSTRUCTIVELY: is the engine's feasible region only general relativity? NO -- the point g_4=0.5,g_6=0.3,g_8=0.3,g_R2=0.1,g_R3=0.05 passes all 38 constraints [verified feasible] with nonzero curvature; but the region is tightly constrained -- anomaly cancellation [|g_4 g_6-g_R2^2|<=0.2] and the repulsive-force conjecture [g_4 g_6>=g_R2+g_R2^2] overlap only for g_R2<=0.2, a HARD curvature ceiling for any matter completion; a hand-built completion demonstrably reaches g_R2~0.15 [trips another constraint there], provably no further than 0.2; the toy frameworks miss because their matter product sits on the bare WGC line g_4 g_6=g_R2 just BELOW the repulsive floor g_R2+g_R2^2 [string 0.20<0.24, etc.] -- a prefactor-step under the window, not stranded in an empty region; closes the sub-arc: the engine admits higher-derivative physics up to g_R2<=0.2 and the toy encodings sit at its edge; 5/5 checks) · [**v2.284 anatomy of the binding bound: why the repulsive-force conjecture defines the feasible edge**](docs/results/2026-06-29-v2.284-qnm-repulsive-force-edge.md) (dissects the single constraint all three valid-UV frameworks fail (v2.283): the Heidenreich-Reece-Rudelius repulsive-force conjecture margin = g_4 g_6 - g_R2 - gamma g_R2^2 [matter product vs curvature cost = linear-WGC term + quadratic-RFC enhancement, verified to match the engine to 1e-9]; the four higher-derivative frameworks split cleanly: string_tree_eft and cdt sit EXACTLY on the original-WGC boundary [g_4 g_6 = g_R2, linear margin 0] and fail ONLY by the quadratic gamma g_R2^2 RFC enhancement [margins -0.04, -0.048] -- feasible under the weaker WGC, tipped over by the stronger conjecture; asymptotic_safety and lqg fail even the linear WGC [matter product < g_R2, genuinely below the boundary]; pure_gr trivially saturates; so the feasible edge is one interpretable inequality, the toy frameworks calibrated at the WGC line so the RFC quadratic term is the discriminator -- the engine's tightest gravitational-universality bound, one prefactor-step from the encodings by design; 5/5 checks) · [**v2.283 the edge of the feasible region: how each framework sits against the full engine**](docs/results/2026-06-29-v2.283-qnm-curvature-headroom.md) (running the canonical 38-constraint roster reveals a sharp picture: ONLY pure_gr is strictly feasible; string_tree_eft, asymptotic_safety and cdt each miss by EXACTLY ONE constraint -- repulsive_force_conjecture, a magnitude-driven gravitational-universality bound, a single marginal near-miss within the engine's representative-O(1)-prefactor band; lqg_induced is the deep outlier failing SIX [the 5 extra = the curvature moment-tower positivity family graviton_forward_positivity/cross_sector_efthedron/cft_flat_space + complexity + monogamy, exactly the v2.282 x=g_R3/g_R2=1 pathology], and is uniquely UNREPAIRABLE by curvature scaling [turning curvature down to relieve positivity makes it fail anomaly_cancellation which NEEDS the curvature -- an anomaly-vs-positivity tension]; ties v2.262/281/282 into one feasibility-geometry map; honestly corrects this cycle's own initial 'headroom' premise with the engine's literal verdict; NOT a claim string theory is in the swampland but that the toy encoding sits one prefactor-step over a single bound; 5/5 checks) · [**v2.282 decomposing the lqg framework anomaly: which couplings source each constraint failure**](docs/results/2026-06-29-v2.282-qnm-lqg-anomaly-decomposition.md) (deepens v2.281: running the full engine, lqg_induced violates SIX constraints across three classes [amplitude_bootstrap, gravitational_universality, information_theoretic]; scaling lqg's curvature sector (g_R2,g_R3,parities) vs matter sector (g_4,g_6,g_8) toward zero and reading the engine margins shows ALL SIX failures heal when the curvature sector is turned off -- so lqg's anomaly is fundamentally a CURVATURE-sector problem [its couplings sit on the moment-tower positivity boundary x=g_R3/g_R2=1, v2.262]; graviton_forward_positivity [the forward-limit positivity that IS the v2.261/262 moment tower] is PURELY curvature-driven [matter off leaves it at -0.060, no help]; so lqg isn't failing for 6 unrelated reasons -- its one over-large boundary-saturating curvature sector trips positivity + universality + monogamy together; the v2.262 moment-tower diagnosis and the engine's full suite now agree on diagnosis AND mechanism; 5/5 checks, real check()/Theory API) · [**v2.281 cross-validating the engine's GW + swampland constraints against the reconstructed bounds**](docs/results/2026-06-29-v2.281-qnm-engine-gw-swampland-crossvalidation.md) (a deliberate RECONNECTION to the engine after the long GW-phenomenology arc: those cycles reconstructed GW/swampland bounds from scratch, and the engine ALREADY encodes the same physics as live constraints -- species_scale_bound IS the v2.264 Lambda=M_Pl/sqrt(N), gw_speed_bound IS the v2.251/270 5e-16 GW170817 value, etc. [graviton-mass a documented source-choice difference: engine's tighter Will'18 1.27e-23 eV vs reconstruction's GW150914 1.2e-22 eV]; running the engine on all 5 frameworks, FOUR satisfy the entire 11-constraint GW+swampland+graviton-positivity sector and the lone failure is lqg_induced, which violates graviton_forward_positivity [margin -0.06] -- the forward-limit positivity that is EXACTLY the v2.261/262 moment-tower physics, so the engine INDEPENDENTLY reproduces the v2.262 finding that lqg is the marginal/anomalous framework [x=g_R3/g_R2=1 on the positivity boundary]; two strands agree on the bounds AND on which framework fails them; 5/5 checks, uses the real check()/frameworks() API) · [**v2.280 the four classic tests of GR and the PPN parameters gamma, beta**](docs/results/2026-06-29-v2.280-qnm-ppn-solar-system.md) (the weak-field complement to the v2.278/279 binary pulsars: the PPN framework reduces every solar-system test to two Eddington parameters gamma/beta, GR = gamma=beta=1; light grazing the Sun deflects 1.7515" [the famous 1.75"] and since it scales (1+gamma)/2 the Newtonian gamma=0 prediction is exactly HALF [0.876", Eddington's 1919 discriminant], the Shapiro delay shares that factor [Cassini gamma-1=(2.1+/-2.3)e-5, the tightest], Mercury's perihelion precesses 42.99"/century mixing gamma+beta as (2+2gamma-beta)/3, and the gravitational redshift g h/c^2=2.46e-15 [Pound-Rebka, GPS 38us/day] tests the equivalence principle; with v2.278/279 [strong field] the PPN tests [weak field] bracket GR across all gravitational potentials -- the classical foundation any QG completion must reproduce; 8/8 checks) · [**v2.279 the double pulsar PSR J0737-3039: general relativity's most stringent test**](docs/results/2026-06-29-v2.279-qnm-double-pulsar.md) (the sequel to v2.278: in the double pulsar BOTH neutron stars are visible so the system OVER-determines its two masses -- periastron advance omega_dot fixes the total mass M=2.587 Msun, the directly-measured ratio R=1.0714 splits it into m_A=1.338/m_B=1.249 Msun, and then every other post-Keplerian parameter is PREDICTED and matches to ~1%: Einstein delay gamma 0.41%, GW orbital decay Pdot_b 0.33%, Shapiro range r 0.94%; the five PK curves intersect at one point in the mass plane [they need not], and the Shapiro shape s=sin i sharpens it to ~0.05% [observed/GR=0.99987, Kramer 2006]; with v2.278 the binary pulsars confirm both that GR's quadrupole GW emission is real AND the full strong-field two-body dynamics is Einsteinian -- the classical bedrock under the v2.266-272 GW program; 6/6 checks, source-backed Damour-Deruelle PK formulas) · [**v2.278 the Hulse-Taylor binary pulsar: the quadrupole formula and the first evidence for GWs**](docs/results/2026-06-29-v2.278-qnm-hulse-taylor-quadrupole.md) (a fresh sector off the black-hole cycles -- the cleanest classical confirmation GWs are real: the Peters quadrupole orbital-decay formula dP/dt=-(192pi/5)(G^5/3/c^5)(m1 m2/M^1/3)(2pi/P)^5/3 f(e) reproduces PSR B1913+16's measured period decay to 0.19% [predicted -2.4026e-12 vs observed -2.398e-12, ratio 1.0019] -- the 1993 Nobel-winning indirect GW detection two decades before LIGO; the eccentricity factor f(e)=(1+73/24 e^2+37/96 e^4)/(1-e^2)^7/2 boosts emission ~12x at e=0.617 [periastron whips], orbit merges in ~300 Myr; anchors the GW program v2.266-272 -- the same quadrupole emission those cycles probed, measured classically in one binary; 6/6 checks, source-backed Weisberg-Huang params) · [**v2.277 synthesis capstone: the black-hole thermodynamics sub-arc (v2.257-v2.276), cross-verified**](docs/results/2026-06-29-v2.277-qnm-bh-thermodynamics-synthesis.md) (consolidates the six-cycle BH-thermodynamics / quantum-geometry sub-arc into one structure, 6/6 cross-program checks: the unifying object is the horizon AREA -- it is the ENTROPY S=A/4 [v2.257], the BOUND it saturates [v2.258, Bekenstein=holographic=S_BH verified equal], QUANTIZED in units dA=4ln3 carrying entropy ln3 [v2.274], a RADIATOR through the barrier at its photon sphere [v2.273], the ISLAND that returns the information [v2.275], and a PHASE via its free energy [v2.276]; the Hawking temperature 1/(8piM) is literally the same number in SI [v2.257] and natural units [v2.273/274/276, verified T_H(M_Pl)/T_Pl=1/8pi], the area law scales S~M^2/T~1/M/evap~M^3 consistently; so BH thermodynamics is not six topics but one -- the horizon area read six ways; the session's 6th synthesis capstone after v2.242/250/256/265/271) · [**v2.276 the Hawking-Page transition: black holes have a phase diagram (and it is holographic)**](docs/results/2026-06-29-v2.276-qnm-hawking-page-transition.md) (completes the BH-thermodynamics sub-arc: unlike a flat-space hole, a Schwarzschild-AdS_4 black hole has a genuine phase diagram -- T(r_+)=(1/4pi)(1/r_+ + 3r_+/L^2) has a MINIMUM at r_+=L/sqrt3 [T_min=sqrt3/2piL, below which no BH exists], the specific heat flips sign there [small branch unstable C<0, large branch stable C>0], and the free energy F=M-TS=(r_+/4)(1-r_+^2/L^2) changes sign at the HAWKING-PAGE point r_+=L, T_HP=1/piL [below it thermal AdS dominates F>0/BH metastable, above it the large BH dominates F<0] -- a first-order transition between empty thermal AdS and a big black hole; Witten showed it's HOLOGRAPHIC, the gravity dual of the confinement-deconfinement transition of the boundary gauge theory; 5/5 checks, exact textbook AdS_4 thermodynamics) · [**v2.275 the Page curve: the entanglement signature of black-hole unitarity**](docs/results/2026-06-29-v2.275-qnm-page-curve.md) (caps the BH-entropy thread by confronting the information paradox: the fine-grained Hawking-radiation entropy is the MINIMUM of two saddles [the quantum-extremal-surface / island formula] S_rad=min(S_rad^thermal, S_BH^remaining) -- the coarse-grained Hawking answer early, the remaining-horizon answer late; with S_BH=(M/M0)^2 S0 the two cross at the Page point M=M0/sqrt2 [exactly half the entropy radiated, S=S0/2], which via the M^3 evaporation law [v2.257] is the Page time t/tau=1-2^(-3/2)~0.646; the Page curve rises to that peak then comes back DOWN to 0 [pure final state, information RETURNED] -- vs Hawking's monotonic rise to S0 [information LOST]; the island saddle is the modern resolution [Penington; Almheiri et al.], so the same horizon that sets the v2.257 entropy + v2.273 greybody + v2.274 area quantum is the late-time island bringing information out; 6/6 checks, phenomenological island-formula model as flagged) · [**v2.274 QNM asymptotics, black-hole area quantization, and the Barbero-Immirzi tension**](docs/results/2026-06-29-v2.274-qnm-area-quantization.md) (ties the validated QNM ringdown v2.210 to BH entropy v2.257/258 and loop quantum gravity: the highly-damped n->inf Schwarzschild QNM real part Re(omega_inf)=ln3/(8pi M)=0.04371 [the famous Hod/Motl-Neitzke value], read via Bohr correspondence as a mass-transition quantum, makes the horizon area change by dA=32pi M dM=4 ln3 -- the mass CANCELS so the area quantum is UNIVERSAL [verified M=0.5..10]; the evenly-spaced spectrum gives entropy quantum dS=ln3 = exactly 3 microstates per quantum [e^dS=3], reproducing S=A/4 discretely; matched to the LQG area spectrum with lowest spin j=1 [Dreyer] it fixes Barbero-Immirzi gamma=ln3/(2pi sqrt2)=0.124 -- but standard LQG entropy-counting gives ~0.2375 [j=1/2], a factor 1.92 TENSION reported honestly as a genuine open QG problem not a solved check; 6/6 checks; ln3 used as source-backed input since the WKB solver can't reach n->inf) · [**v2.273 black-hole greybody factors: how the potential barrier shapes the Hawking spectrum**](docs/results/2026-06-29-v2.273-qnm-greybody-hawking.md) (a fresh BH-physics thread REUSING the validated v2.210 WKB/Regge-Wheeler machinery: Hawking radiation is a greybody not a blackbody -- a quantum must tunnel out through the SAME barrier that sets the ringdown QNMs, so the flux is the Planck spectrum weighted by the transmission Gamma_l(omega); the Schutz-Will WKB Gamma=1/(1+exp(2pi(V_max-omega^2)/sqrt(-2V''))) from the reused barrier peak rises 0->1 through exactly 1/2 at omega^2=V_max, the eikonal high-L peak becomes the photon-sphere orbit [omega_c b_c/(L+1/2)->1 to <0.1% by L=40 -> capture sigma=pi b_c^2=27pi, the v2.230 shadow], the low-freq limit is the horizon-AREA theorem sigma->16pi; the greybody SUPPRESSES the low-frequency Hawking flux below blackbody -- so one barrier governs both how a BH RINGS [QNMs v2.210] and GLOWS [Hawking v2.257], tying ringdown+shadow+temperature into one object; 5/5 checks, WKB-limited at low omega as flagged) · [**v2.272 the stochastic gravitational-wave background: a spectral zoo and the inflation gap**](docs/results/2026-06-29-v2.272-qnm-stochastic-gw-background.md) (a fresh GW-cosmology probe: the background is diagnosed by its SPECTRAL INDEX, and the arithmetic linking the strain slope alpha, the energy-density slope 2alpha+2 and the pulsar-timing index gamma=3-2alpha is exact [invariant gamma+Omega_slope=5, verified]; SMBH binaries give h_c~f^-2/3 -> Omega_GW~f^2/3, gamma=13/3, inflation/cosmic-strings are FLAT [gamma=5], phase transitions peaked -- so the slope separates the candidates; the NANOGrav 15yr nHz signal [A~2.4e-15] converts to Omega_GW~8e-9 at the SMBH-binary prediction [measured gamma~3.2 mildly shallower than 13/3, reported honestly]; the decisive QG point is the GAP -- the PRIMORDIAL inflationary background Omega_GW~(1/24)Omega_r r A_s <= 2.8e-16 at r<0.036 sits orders below PTA/LISA/LIGO [1e-9..1e-13], so a stochastic detection is astrophysical/exotic NOT a direct image of inflation; 6/6 checks) · [**v2.271 synthesis capstone: the graviton-observables arc (v2.266-v2.270), cross-verified**](docs/results/2026-06-29-v2.271-qnm-graviton-observables-synthesis.md) (consolidates the five-cycle GW/graviton-observable arc -- everything a gravitational wave measures about the graviton -- into one structure, 5/5 cross-program checks: the five observables map to five DISTINCT properties [MASS v2.266 / INFRARED-soft-charge v2.267 / SPIN-helicity v2.268 / PARITY v2.269 / EXTRA-DIMS-running-M_Pl v2.270]; the modules share the helicity-2 backbone [v2.269's circular polarizations built from EXACTLY v2.268's two GR tensor modes, 1e-12]; the propagation sector factorizes by two binary labels -- chromatic-vs-achromatic x parity-even-vs-odd -- into mass-dispersion [chromatic/even ~1/E^2], birefringence [chromatic/odd ~f], distance-ratio [achromatic/even], one empty cell, so a propagation anomaly is diagnosable from frequency-dependence + handedness alone; every test reduces to GR in its null limit; the session's 5th synthesis capstone after v2.242/v2.250/v2.256/v2.265) · [**v2.270 the GW vs EM luminosity-distance ratio: a standard-siren test of modified GW propagation**](docs/results/2026-06-29-v2.270-qnm-gw-em-distance-ratio.md) (the achromatic-AMPLITUDE partner of v2.269's chromatic-phase birefringence: in modified gravity the GW amplitude doesn't redshift as GR demands, so the GW-inferred distance d_GW differs from the true EM distance d_EM -- a standard siren [GW170817, a BNS with an EM counterpart in NGC 4993] measures the ratio directly; both standard parametrizations [Belgacem-Maggiore running-Planck Xi(z)=Xi_0+(1-Xi_0)/(1+z)^n, Pardo extra-dim [1+(d_EM/Rc)^n]^((D-4)/2n)] reduce EXACTLY to 1 in GR [Xi_0=1, D=4], verified to 1e-15; D>4 leaks gravitons off the brane -> d_GW>d_EM monotonically [D=7 a 13% excess at 40 Mpc], so GW170817's ~1 agreement pins D=4.02+/-0.07 [Pardo et al.]; ACHROMATIC [frequency-independent] -- complementary to the v2.269 chromatic channel, splitting modified GW propagation into parity-odd-chromatic + parity-even-achromatic; 6/6 checks) · [**v2.269 GW amplitude birefringence: parity-violating gravity splits the graviton helicities**](docs/results/2026-06-29-v2.269-qnm-gw-birefringence.md) (the graviton analog of the v2.252 cosmic birefringence, on the engine's g_R2_parity sector: gravitational Chern-Simons gravity amplifies one graviton helicity and damps the other as they propagate, h_{R,L}=h^GR exp(+/-zeta), zeta~kappa k D; the right/left circular polarizations e_{R,L}=(e_plus +/- i e_cross)/sqrt2 are exact helicity +/-2 eigenstates [verified: a line-of-sight rotation by psi multiplies them by exp(-/+2 i psi), matched 1e-12]; the falsifiable signature is a propagation-induced net circular polarization on an intrinsically LINEAR source [Stokes V=0 in GR] V=tanh(2 zeta), nonzero ONLY under parity violation and CHROMATIC [zeta~f, verified] -- distinguishing it from achromatic source/inclination effects; GR limit V=0 exactly, 5/5 checks; extends the GW thread mass->IR->polarization->propagation) · [**v2.268 GW polarization content: GR's two modes vs the six of a general theory (and the interferometer rank-5 wall)**](docs/results/2026-06-29-v2.268-qnm-gw-polarizations.md) (a falsifiable-gravity probe: a massless spin-2 theory carries up to SIX GW polarizations [Eardley-Lee-Will E(2)] forming a complete basis for the symmetric 3x3 strain [rank 6, verified], but GR excites only the TWO transverse-traceless tensor modes -- so detecting any non-tensor mode falsifies GR; the sharp result is that the Michelson response tensor D=1/2(a(x)a-b(x)b) is TRACELESS, so the two scalar modes [breathing diag(1,1,0), longitudinal diag(0,0,1)] have proportional traceless parts and are DEGENERATE for any interferometer network -- the (detectors x 6) response matrix saturates at rank FIVE not six [Chatziioannou-Yunes-Cornish], a single overhead detector responds only to plus, GR needs just 2 detectors; 8/8 checks, why GW170814's 3-detector test compares tensor/vector/scalar hypotheses rather than measuring all amplitudes) · [**v2.267 the gravitational-wave memory effect and the infrared triangle**](docs/results/2026-06-29-v2.267-qnm-gw-memory-infrared-triangle.md) (a fresh QG-structure/GW-observable thread: a GW burst leaves a PERMANENT strain offset Delta h=h(+inf)-h(-inf)=integral hdot dt -- exactly the zero-frequency Fourier mode hdot(omega=0), one vertex of Strominger's infrared triangle [memory <-> Weinberg soft-graviton theorem <-> BMS supertranslations]; the linking identity is exact and numerically verified on a toy burst [memory=hdot(0) to 1e-6, the oscillatory chirp carries NO memory, the radiated-energy spectrum |hdot(omega)|^2 plateaus at |Delta h|^2 as omega->0 = the soft theorem, 4/4 checks]; astrophysically the Christodoulou memory Delta h~(G/c^4 R)Delta E_rad is ~5-20% of peak strain [Favata] -- a DC offset LIGO chases by stacking ~sqrt(N) and LISA detects per-event for supermassive mergers; extends v2.266 from the graviton's mass to its infrared structure) · [**v2.266 graviton mass bounds from gravitational-wave dispersion (the low-energy mirror of LIV)**](docs/results/2026-06-29-v2.266-qnm-graviton-mass-dispersion.md) (fresh QG-phenomenology sector after the swampland arc: a graviton rest mass makes GWs dispersive v_g/c=sqrt(1-(m c^2/E)^2), so an inspiral's high/low frequencies arrive split by Delta t=(D/2c)(m c^2)^2(1/E1^2-1/E2^2); at the LVC bound m_g<1.2e-22 eV the Compton wavelength is lambda_g=1.03e13 km [REPRODUCES the published >1e13 km] and GW150914's 35-250 Hz band would smear ~14 ms over its ~0.2 s signal -- the detectability scale that sets the bound; this is the LOW-energy mirror of the v2.251 LIV probe [mass deficit ~1/E^2 worst at low energy vs Planck LIV ~E^n worst at high energy -- opposite energy dependence is the discriminant], and the kHz speed deficit ~4.5e-22 is far inside the GW170817 |Dv/c|<1e-15 bound so the in-band dispersive test is the sensitive one; float-cancellation guard on 1-v/c via the series, like the v2.247 tortoise) · [**v2.265 synthesis capstone: the swampland web (WGC + SDC + TCC + species scale), cross-verified**](docs/results/2026-06-29-v2.265-qnm-swampland-web-synthesis.md) (consolidates the four-cycle swampland sub-arc into one interlocking structure and PROVES the modules mutually consistent: 6/6 cross-program checks pass -- the WGC's full M_Pl and the inflation/TCC/species reduced M_Pl differ by EXACTLY sqrt(8 pi) [5.0103 vs 5.0133, verified], the LIGHT TOWER is the unifying object [SDC says it descends with field distance -> that growing tower IS the species count N lowering the Dvali cutoff Lambda_sp=M_Pl/sqrt(N) -> SDC+TCC both push r down, TCC strictly stronger r_TCC~7e-45 << r_SDC~2e-3 << current 0.036], and WGC+species both cut the EFT off below Planck; so 'is this EFT in the swampland?' is one coherent structure -- cutoff [WGC+species] + field range [SDC] + observable [TCC: no detectable primordial GWs] -- not four slogans; the session's 4th synthesis capstone after v2.242/v2.250/v2.256) · [**v2.264 the Dvali species scale: how many light fields lower the QG cutoff**](docs/results/2026-06-29-v2.264-qnm-species-scale.md) (completes the swampland sub-arc: in a theory with N light species the quantum-gravity cutoff is the Dvali species scale Lambda_sp=M_Pl/sqrt(N), NOT the Planck mass -- fixed two ways that agree on the -1/2 exponent [BH entropy: the smallest semiclassical BH at the species scale has entropy EXACTLY N, verified; + running Newton constant M_Pl,eff^2~N Lambda^2]; the SM's ~118 light dof already lower gravity's true cutoff to ~0.09 M_Pl ~ 2e17 GeV, and reaching a TeV cutoff needs N~1e32 species -- REPRODUCING the famous ADD large-extra-dimensions count since the species scale IS the ADD fundamental scale; closes the swampland web [a v2.255 SDC light tower makes N grow with field distance -> Lambda_sp drops below M_Pl, tying WGC v2.254 + SDC v2.255 + TCC v2.263 + species scale into one structure]; -1/2 exponent prefactor-free robust, absolute cutoff order-of-magnitude) · [**v2.263 the Trans-Planckian Censorship Conjecture: why it predicts unobservable primordial GWs**](docs/results/2026-06-29-v2.263-qnm-trans-planckian-censorship.md) (fresh QG-consistency/swampland probe extending v2.253/v2.255: the TCC e^N<M_Pl/H_f caps the inflationary Hubble rate at H<M_Pl e^{-N}, which via the measured A_s caps the tensor-to-scalar ratio at r_max(N)=2 e^{-2N}/(pi^2 A_s); even for the most generous e-fold count N~44 [the CMB-pivot value] r_max~6e-31 and V^{1/4}_max~9e8 GeV -- REPRODUCING the Bedroya-Vafa headline [V^{1/4}<~6e8 GeV, r<~1e-30] to order of magnitude, ~27 orders below the CMB-S4/LiteBIRD reach r~1e-3; so the TCC predicts primordial GWs are FOREVER unobservable and a single r>~1e-3 B-mode detection would FALSIFY it -- a sharp decidable test, exponentially stronger than the v2.255 SDC's mild small-r preference; round-trip H(r)<->H_max(N) self-consistent to 1e-9) · [**v2.262 forced per-framework higher-curvature towers from Hankel positivity (lqg is marginal)**](docs/results/2026-06-29-v2.262-qnm-framework-curvature-tower.md) (applies the v2.261 moment tower to the engine's ACTUAL framework couplings: each consistent framework's MINIMAL Hankel-saturating higher-curvature tower is a geometric sequence g_R(k+2)=g_R2*x^k with ratio x=g_R3/g_R2 [the effective mass^2 of the lightest curvature state, single-state saturating every Hankel minor, g_R4_min reproduces v2.234 exactly]; the ratio x is a new per-framework diagnostic -- string [x=0.75], asymptotic_safety [0.67], cdt [0.68] have CONVERGING towers [x<1 -> operators decouple, a healthy EFT] but lqg_induced sits exactly at x=1.0 [g_R2=g_R3=0.3] -- a MARGINAL tower [g_R4=g_R5=g_R6=0.3, operators do NOT decouple], flagging lqg a second independent way [it already fails 4/38 engine constraints + needs the largest forced g_R4]) · [**v2.261 the curvature dispersion tower as a Stieltjes moment sequence (Hankel positivity)**](docs/results/2026-06-29-v2.261-qnm-curvature-moment-tower.md) (novel theory advance extending v2.234: the higher-curvature couplings of any consistent UV completion are MOMENTS of a positive spectral density g_R(k+2)=sum w_i x_i^k, so all Hankel matrices must be PSD -- v2.234's g_R4>=g_R3^2/g_R2 is exactly the 2x2 Hankel minor, and the full tower [verified PSD to 4x4 + shifted-Stieltjes] is the complete EFT-hedron hierarchy; a sequence violating ANY Hankel minor has no positive spectral representation -> swampland) · [**v2.260 Leaver continued-fraction machinery (validated) + an honest blocker diagnosis**](docs/results/2026-06-29-v2.260-qnm-leaver-continued-fraction.md) (focused 2nd attempt at the high-accuracy Leaver QNM solver -- honest NEGATIVE result: the 3-term recurrence coefficients can't be derived symbolically [the symbolic-exponent shortcut drops the e^{iwr} essential-singularity terms -> spurious 5-term; keeping it explicit makes sympy.simplify time out], so they need analytic hand-derivation or the published Leaver-1985 values; NO guessed solver shipped [claim-gating], but the reusable continued-fraction evaluator + root-finder ARE delivered and validated on analytic cases to 1e-9, ready for the coefficients) · [**v2.259 the cosmological constant problem: 'the worst prediction in physics'**](docs/results/2026-06-29-v2.259-qnm-cosmological-constant-problem.md) (observed dark energy ~(2.3 meV)^4 vs QFT vacuum ~M_cutoff^4: overshoot 1e122 [Planck], 1e58 [electroweak], still 1e33 even at the electron mass -- NO cutoff gets it right, so QG must explain the ~120-order suppression; the de Sitter swampland conjecture sharpens it [a true positive Lambda may be forbidden -> quintessence not Lambda, a falsifiable w != -1], tying it to the SDC v2.255 and the engine's cc_naturalness) · [**v2.258 the holographic bound, the Bekenstein bound, and the cosmic entropy budget**](docs/results/2026-06-29-v2.258-qnm-holographic-bound.md) (the Bekenstein [S<=2piRE] and holographic [S<=A/4] bounds are BOTH saturated by a black hole [S_BH=4piM^2=2pi(2M)(M)=(16piM^2)/4, verified equal] -- so a BH is the max-entropy object and entropy is capped by boundary AREA not volume; the observable universe's holographic capacity ~2e122 nats vs its actual entropy ~3e104 [SMBH-dominated] means it has used only ~1e-18 of capacity -- the quantitative arrow-of-time / low-entropy-past puzzle; reconnects to the engine's entropy constraints) · [**v2.257 black-hole thermodynamics: entropy, Hawking temperature, evaporation, Page time**](docs/results/2026-06-29-v2.257-qnm-black-hole-thermodynamics.md) (fresh fundamental-QG thread: a solar-mass hole stores S~1.5e77 bits [= horizon AREA/4 -- the holographic principle, S~area not volume] at T_H~6e-8 K [colder than the 2.7 K CMB, so it absorbs not radiates] and lives ~2e67 yr; the M^3 evaporation law makes a M~1.7e11 kg primordial BH evaporate exactly NOW [gamma-ray signal], and the Page time ~t_evap/2 is the information-recovery turnover -- the quantitative core of the paradox whose horizon-structure resolutions the echoes v2.247 probed) · [**v2.256 synthesis: the QG-phenomenology + swampland arc, cross-verified**](docs/results/2026-06-29-v2.256-qnm-qg-phenomenology-synthesis.md) (consolidates v2.251-v2.255 into two axes -- QG PHENOMENOLOGY [dispersion / parity birefringence / primordial GWs] and QG CONSISTENCY [Weak Gravity + Distance conjectures] -- which MEET in the tensor sector [the SDC predicts small r, the primordial-GW channel measures it]; 6/6 checks pass [reduced vs full M_Pl via sqrt(8pi), E_Planck consistency, the r/Lyth link, engine reconnections exist] -- the session's third cross-verified synthesis capstone) · [**v2.255 the Swampland Distance Conjecture: the light tower, and why it predicts small r**](docs/results/2026-06-29-v2.255-qnm-swampland-distance-conjecture.md) (companion swampland pillar: a large field excursion brings down an exponentially light tower m~M_Pl e^{-alpha Delta phi/M_Pl}, breaking the EFT; via the Lyth bound [Delta phi/M_Pl ~ sqrt(r/0.01)] this PREDICTS small r -- a sizeable r needs trans-Planckian Delta phi the SDC disfavours, so r<0.036 [Delta phi~1.9 M_Pl] is mildly tense and a future r<0.002 would be sub-Planckian/swampland-safe, a falsifiable swampland statement about the v2.253 tensor sector) · [**v2.254 the Weak Gravity Conjecture: 'gravity is the weakest force' as a QG-consistency test**](docs/results/2026-06-29-v2.254-qnm-weak-gravity-conjecture.md) (fresh swampland probe reconnecting to the engine's distance_conjecture / eft_validity sector: an extremal RN black hole [Q=M] needs a super-extremal state [z = q g M_Pl/m >= 1] to discharge, or the EFT is in the swampland; every SM charged particle satisfies it by huge margins [electron z~7e21, EM beats gravity by z^2~5e43], far from the boundary z=1; magnetic WGC sets the cutoff Lambda <~ g M_Pl ~ Planck) · [**v2.253 primordial GWs: the direct observational window onto quantum gravity**](docs/results/2026-06-29-v2.253-qnm-inflation-tensor-qg.md) (QG-cosmology capstone: the tensor-to-scalar ratio fixes the inflation energy scale V^{1/4}=(3/2 pi^2 A_s r)^{1/4} M_Pl, so the current r<0.036 puts inflation at V^{1/4}~1.4e16 GeV [0.7x GUT], H_inf~5e13 GeV, ~1e12x above the LHC; the deep point [Krauss-Wilczek] is that primordial tensor modes ARE the quantized graviton's vacuum fluctuations stretched across the sky, so a r>0 B-mode detection is direct evidence gravity is quantized -- the one place QG is observable) · [**v2.252 vacuum birefringence: the energy-dependent parity-violation probe vs the CMB hint**](docs/results/2026-06-29-v2.252-qnm-vacuum-birefringence.md) (reconnects the spacetime-structure thread to the engine's parity sector: the dim-5 Myers-Pospelov coupling rotates polarization by Delta chi ~ xi E^2 D, so high-energy GRB polarimetry EXCLUDES it at xi < ~5e-17 [tighter for higher-energy bands]; this contrasts with the engine's energy-INDEPENDENT cosmic-birefringence beta=0.34 deg HINT [dim-4 Chern-Simons, g_R2_parity] -- consistent because they are different operators, so the energy dependence is the discriminant) · [**v2.251 Lorentz-violation / modified dispersion: testing Planck-scale spacetime**](docs/results/2026-06-29-v2.251-qnm-lorentz-violation-dispersion.md) (fresh QG-phenomenology pivot off black holes: an energy-dependent speed v(E)=c(1-(E/E_QG)^n) makes quanta arrive separated by Delta t~(D/c)Delta E/E_QG, so a single 30 GeV GRB photon reaches E_QG~0.5 E_Planck for linear LIV -- but LIGO gravitons [~1e-12 eV] give a bound 22 orders weaker since it scales with messenger ENERGY [verified exact at fixed D,dt]; GW170817's GW-EM speed comparison is instead the strong constant-offset bound, so photon + GW channels are complementary) · [**v2.250 synthesis: the 'is it really a black hole?' test suite, cross-verified**](docs/results/2026-06-29-v2.250-qnm-bh-hypothesis-tests-synthesis.md) (consolidates v2.235-v2.249 into two probes / six channels -- the HORIZON tested three ways [static Love number / echo reflection / tidal-heating absorption] and the SPIN via superradiance [boson exclusion / continuous-GW beacon / spin gaps] -- and GUARANTEES consistency: 5/5 checks pass, the same Omega_H drives superradiance and tidal heating, the echo barrier is the photon sphere, the BH Love number is zero) · [**v2.249 horizon tidal heating: the absorption channel of the 'is it a black hole?' test**](docs/results/2026-06-29-v2.249-qnm-horizon-tidal-heating.md) (third horizon-test channel: a BH exchanges energy through its horizon with flux sign = sign(Omega_orbit - Omega_H), the SAME superradiance condition -- for fast-spinning holes [a*>~0.4] r_crit drops below the ISCO so the ENTIRE prograde inspiral is superradiant [hole feeds energy to the orbit, e.g. a*=0.9: r_crit=1.74<ISCO=2.32]; a horizonless ECO has no such flux so the inspiral phase differs, tying the horizon-test and superradiance suites together via the shared Omega_H) · [**v2.248 the frequency-domain face of echoes: the ECO trapped-mode comb**](docs/results/2026-06-29-v2.248-qnm-echo-trapped-modes.md) (the reflective surface replaces the black hole's single broad QNM with a COMB of narrow trapped modes spaced Delta f = 1/Delta t_echo [18 Hz for a 30 Msun hole], with N ~ f_QNM*Delta t_echo teeth under the envelope; the count N~22 is SCALE-INVARIANT [f_QNM~1/M, Delta t~M] so every Planck-scale-surface ECO shows the same ~22 trapped lines whether stellar or supermassive -- the spectral signature complementing the v2.247 time-domain delay) · [**v2.247 black-hole echoes: the ringdown delay probing horizon-scale new physics**](docs/results/2026-06-29-v2.247-qnm-black-hole-echoes.md) (fresh QG-at-the-horizon probe: if a reflective quantum-corrected surface replaces the horizon at r=2M(1+eps), waves bounce to the photon-sphere barrier producing echoes delayed by Delta t = 2[r*(3M)-r*(surface)] ~ -4M ln(eps); the LOGARITHMIC sensitivity means a Planck-scale surface [eps~1e-40] still gives only ~368 M = 54 ms for a 30 Msun hole [the LIGO echo-search regime], 36 orders of magnitude in eps changing the delay only ~10x -- the information-paradox question made observational) · [**v2.246 superradiance spin-down endpoint: the Regge trajectory observed spins test**](docs/results/2026-06-29-v2.246-qnm-superradiance-regge-trajectory.md) (completes the superradiance thread with its data-comparison side: a hole spins down until Omega_H(a*_f)=alpha, so a*_f(alpha) is the MAX spin it can retain [alpha=0.1 spins a near-extremal hole to a*=0.385, extracting delta a*=0.61] -- a measured high spin excludes every boson with alpha<Omega_H(a*_obs), so Cygnus X-1 [a*~0.95] and GRS 1915+105 [a*~0.98] exclude bosons up to alpha~0.36-0.41; the spin gaps that turn superradiance into a real ultralight-boson constraint) · [**v2.245 the boson cloud as a continuous-GW source (direct-detection side of superradiance)**](docs/results/2026-06-29-v2.245-qnm-boson-cloud-gw.md) (completes the superradiance thread: the cloud annihilates to gravitons at f_GW = mu c^2/(pi hbar), mapping the boson-mass windows directly onto detector bands -- stellar-BH clouds radiate at 52-2020 Hz [LIGO/Virgo/KAGRA], a 1e6 SMBH at mHz [LISA], M87* at uHz [PTA] -- so superradiance is two-sided: a measured spin both EXCLUDES a boson [indirect] and PREDICTS a monochromatic GW line a continuous-wave search can target [direct]) · [**v2.244 superradiant Regge-plane exclusion: the actual ultralight-boson bound**](docs/results/2026-06-29-v2.244-qnm-superradiance-regge-exclusion.md) (completes v2.243 by folding in the Detweiler l=m=1 growth rate M*Gamma=(1/48)(a*-2(r+/M)alpha)alpha^8: each observed fast-spinning hole carves a DECADE-WIDE exclusion window -- bounded above by the superradiance threshold alpha<Omega_H and below by growth-time-vs-age -- ruling out ultralight bosons ~1e-13 eV [stellar] to ~1e-21 eV [M87*] across the axion / fuzzy-DM / dark-photon ranges; honest that the (1/48) coeff and N~180 e-folds are representative) · [**v2.243 black-hole superradiance: observed spins as an ultralight-boson detector**](docs/results/2026-06-29-v2.243-qnm-superradiance-boson-bound.md) (fresh BSM probe reconnecting to the new-physics-constraint purpose: the superradiance condition mu < m*Omega_H/M with the Kerr horizon angular velocity Omega_H [0 -> 1/2 extremal, verified] means a fast-spinning hole grows a boson cloud and spins down, so observed spins EXCLUDE ultralight bosons -- alpha_max~0.31 at a*=0.9 gives mu windows from ~4e-12 eV [stellar] to ~6e-21 eV [M87*], spanning the QCD-axion / fuzzy-dark-matter / dark-photon ranges; honest that the actual exclusion needs the alpha^9 growth-time vs BH age) · [**v2.242 synthesis capstone: the black-hole strong-field program, cross-verified**](docs/results/2026-06-29-v2.242-qnm-strong-field-synthesis.md) (consolidates ~30 cycles into five themes -- R4/qEFT ringdown sensitivity, the operator-sector bridge mandating Riemann^4, geodesic strong-field null tests, the zero tidal Love number, and the Kerr generalization -- and GUARANTEES internal consistency: 8/8 cross-program checks pass [the photon-sphere Omega_c=1/(3sqrt3) is identical across the shadow/eikonal-frequency/Lyapunov-damping modules, Schwarzschild ISCO matches the Kerr a=0 limit, WKB omega_220 matches Berti, BH Love number=0]) · [**v2.241 eikonal Kerr ringdown damping (photon-orbit Lyapunov exponent)**](docs/results/2026-06-29-v2.241-qnm-kerr-eikonal-damping.md) (completes the Kerr eikonal QNM: the damping = the photon-orbit Lyapunov rate lambda=sqrt(-V''/2)/tdot, reproducing the Schwarzschild lambda=Omega_c identity and FALLING toward extremal [0.192->0.029 at a=0.998, the zero-damping limit] so the ringdown quality factor RISES 0.5->8.1 -- near-extremal black holes ring for many more cycles, the prime targets for ringdown spectroscopy; full eikonal omega_lmn(a) now from the photon orbit alone) · [**v2.240 eikonal Kerr ringdown from the photon orbit (spin sets the QNM frequency)**](docs/results/2026-06-29-v2.240-qnm-kerr-eikonal-ringdown.md) (connects the Kerr thread back to ringdown: omega_R ~ (l+1/2) Omega_ph(a) with Omega_ph=1/(r_ph^{3/2}+/-a), reproducing Schwarzschild and SPLITTING under spin -- prograde frequency rises to the extremal 0.5 [~2.6x Schwarzschild], retrograde falls to ~0.143; a higher-spin remnant rings HIGHER, the photon-orbit origin of how LIGO measures the final BH spin from the ringdown, without needing the deferred Teukolsky/Leaver solver) · [**v2.239 Kerr generalization: spin splits and amplifies the strong-field observables**](docs/results/2026-06-29-v2.239-qnm-kerr-strong-field.md) (real rotating black hole via the exact Bardeen-Press-Teukolsky formulas: spin splits the v2.238-locked observables into prograde/retrograde branches -- as a:0->1 the prograde ISCO shrinks 6->1 [frame-dragging] and photon orbit 3->1 while retrograde grows to 9/4 -- and amplifies the prograde accretion efficiency from 5.72% to the extremal 1-1/sqrt3~42.3%, the rotational energy extraction that powers AGN/quasars; Schwarzschild a=0 reproduced exactly) · [**v2.238 unified deformation fingerprint (one parameter, all strong-field observables)**](docs/results/2026-06-29-v2.238-qnm-unified-deformation-fingerprint.md) (capstone of the v2.229-v2.237 sub-program: a single non-Kerr deformation eps/r^3 shifts ALL strong-field observables coherently -- shadow shrinks [-0.056] while ringdown frequency rises [+0.056, EXACTLY locked, residual 1e-11], with ringdown damping, ISCO radius, accretion efficiency [+0.076] and merger frequency [+0.111] responding independently; the shadow [EM], ringdown [GW], disk efficiency [EM] and merger frequency [GW] are not independent knobs but one deformation seen six ways -- a falsifiable EM+GW signature) · [**v2.237 the ISCO and accretion efficiency (matter-orbit companion to the photon sphere)**](docs/results/2026-06-29-v2.237-qnm-isco-accretion.md) (reproduces the Schwarzschild ISCO observables exactly from the geodesic effective potential -- r=6M, E=sqrt(8/9), 5.72% radiative efficiency, Omega=1/(6sqrt6) -- the inner accretion-disk edge [EM] AND inspiral->merger transition [GW], a multi-messenger probe like the shadow; a near-horizon deformation shrinks the ISCO, raising the disk efficiency and merger frequency coherently) · [**v2.236 how a deformation lifts the GR-zero tidal Love number (the signal)**](docs/results/2026-06-29-v2.236-qnm-love-number-deformation.md) (the tidal-sector null test: via stable first-order perturbation theory [the outward integration is ill-conditioned], any localized near-horizon deformation lifts the v2.235 GR-zero Love number LINEARLY, with the induced response = the convergent overlap integral int P_l^2(x)^2 V dx [verified vs closed form: n=6->4.8, n=8->0.686, n=10->0.229] -- the transfer function from a deformation to the observable, decreasing with localization; the tidal counterpart of the v2.231 photon-sphere null test) · [**v2.235 the black-hole tidal Love number vanishes ('is it a BH?' test)**](docs/results/2026-06-29-v2.235-qnm-tidal-love-number.md) (fresh static-response observable: the horizon-regular static even-parity Schwarzschild perturbation is the associated Legendre POLYNOMIAL P_l^2 [3(x^2-1), 15x(x^2-1)], solving the perturbation ODE to machine precision with NO x^{-l-1} induced-response tail [integrated tail fraction <1e-9], so the tidal Love numbers k_2=k_3=0 EXACTLY -- the sharp GR prediction a measured nonzero k_l would falsify, probed by LIGO inspiral; complement to the ringdown tests) · [**v2.234 scoping a Riemann^4 (g_R4) axis: the positivity-mandated bridge**](docs/results/2026-06-29-v2.234-qnm-gr4-axis-scoping.md) (read-only: a g_R4 axis would carry 5 constraint types from the engine's existing machinery [positivity, curvature Cauchy-Schwarz tower g_R3^2<=g_R2*g_R4, forward dominance, spin-6, CEMZ causality]; the verifiable bridge -- the curvature dispersion tower FORCES g_R4 >= g_R3^2/g_R2, so 4/5 known frameworks MUST carry a nonzero Riemann^4 [string 0.11, AS 0.067, cdt 0.10, lqg 0.30; pure GR none] -- so the engine's own positivity logic MANDATES the ringdown-active operator, not merely allows it) · [**v2.233 why ringdown and the core Wilson engine probe complementary sectors**](docs/results/2026-06-29-v2.233-qnm-engine-operator-sector-bridge.md) (structural bridge: Schwarzschild is Ricci-flat [R=0, R_ab=0, verified on Vulcan + numerically], with Kretschmann K=48/r^6 != 0, so the engine's Ricci-scalar operators g_R2/g_R3 VANISH on-shell and are ringdown-blind, the Gauss-Bonnet quadratic is 4D-topological, and the FIRST ringdown-active curvature operator is the quartic Riemann^4 [dim-8 'R4'] -- one order BEYOND the engine's basis; g_8 is the matter s^4 moment not curvature, so the bridge needs a new g_R4 axis, not a g_8 constraint) · [**v2.232 inverting a two-parameter photon-sphere deviation from shadow + ringdown**](docs/results/2026-06-29-v2.232-qnm-photon-sphere-inversion.md) (completes the geodesic sub-arc: the two independent geodesic observables -- shadow radius b_c [=ringdown frequency, locked] and ringdown damping lambda -- invert a two-parameter non-Kerr deformation [Jacobian det 1.4e-3, cond 67.6, recovery exact to 1e-5], with the ringdown frequency a redundant third measurement for an over-determined GW/EM consistency check; also scopes the core 7-D Wilson engine and records the high-value bridge -- a source-backed ringdown constraint on the quartic g_8 operator) · [**v2.231 photon-sphere deviation: a ringdown<->shadow null test**](docs/results/2026-06-29-v2.231-qnm-photon-sphere-deviation.md) (quantifies v2.230: a one-parameter non-Kerr metric bump locks the eikonal ringdown frequency and the shadow radius to EQUAL-AND-OPPOSITE fractional shifts [d ln Omega_c = -d ln b_c, residual <1e-10, parametrization-independent since Omega_c*b_c=1] -- a GW/EM null test; the ringdown DAMPING [Lyapunov rate, probing f''(r_ph)] is independent, so shadow + ringdown freq + ringdown damping over-determine the deformation) · [**v2.230 photon sphere = common origin of ringdown + the BH shadow**](docs/results/2026-06-29-v2.230-qnm-shadow-multimessenger.md) (multi-messenger: the shadow radius b_c=3sqrt3 and Omega_c*b_c=1 exactly, so a LIGO/Virgo ringdown [omega_R*b_c/(l+1/2)->1, to 0.8% at l=12] and an EHT shadow probe the SAME geometric quantity 1/b_c -- a GW/EM cross-channel test of the Kerr hypothesis where a non-Kerr/quantum-corrected photon sphere shifts BOTH coherently; the overtone imaginary parts form an equally-spaced ladder at -1/b_c) · [**v2.229 eikonal QNM <-> photon-sphere correspondence**](docs/results/2026-06-29-v2.229-qnm-photon-sphere-correspondence.md) (fresh self-contained thread: black-hole ringdown has a geodesic origin -- the photon-sphere orbital frequency and Lyapunov exponent both equal 1/(3sqrt3)=0.19245 for Schwarzschild from first principles, and the in-house WKB QNMs converge to them as l grows [omega_R/(l+1/2)->Omega_c to 0.8%, -omega_I/(n+1/2)->lambda to 0.3% at l=12, monotonically]; ringdown is the light-crossing ring of the photon sphere damped at the null-ray peel-off rate, the geometric-optics foundation the EFT corrections perturb) · [**v2.228 end-to-end R4 detectability forecast (+ Leaver solver foundation)**](docs/results/2026-06-29-v2.228-qnm-r4-end-to-end-forecast.md) (integrates the 12-cycle ringdown program into one reach number: gamma_reach(5sigma) = 0.665/(rho*sqrt(N)), so a current single event reaches gamma~0.067 [edge of perturbative validity] and a 3G network gamma~0.0007, overtone-damping dominated and steep-p=6 limited so loud events beat quiet populations; also lays the Leaver high-accuracy-solver foundation -- the exact Regge-Wheeler recurrence derived symbolically on Vulcan, found to be 5-term not 3-term, validated solver honestly deferred not shipped) · [**v2.227 covariance-corrected no-hair detectability forecast**](docs/results/2026-06-29-v2.227-qnm-nohair-covariance-forecast.md) (closes the v2.226 gap with the realistic two-mode joint covariance: the isolated-resolvability rho_crit was optimistic by ~7-9x, but the strong mode correlation [~0.86] partially cancels in the RATIO observable [full rho*sigma 38% below diagonal in the damping channel]; realistic rho_crit ~26/gamma freq vs ~0.13/gamma damp, the damping no-hair channel still dominating ~197x -- the whole-program overtone-damping conclusion survives the realistic covariance treatment) · [**v2.226 black-hole-spectroscopy no-hair consistency test**](docs/results/2026-06-29-v2.226-qnm-nohair-consistency.md) (fresh thread: the GR no-hair frequency fingerprint omega_lmn/omega_220 is reproduced to <0.2% vs Berti [330/220=1.606, 440/220=2.168], and R4 VIOLATES the 220/221 no-hair consistency from source-backed qEFT coefficients -- freq ratio shifts -1.31/gamma, damping ratio -172/gamma, with the damping channel ~131x more sensitive [inheriting dtq_1=171.35], so the no-hair test is overtone-damping dominated like the rest of the R4 program; cross-multipole violation left uncomputed since only 220/221 qEFT coeffs are published) · [**v2.225 multipole robustness: the isospectrality-breaking response is physical**](docs/results/2026-06-29-v2.225-qnm-parity-breaking-multipole.md) (rules out a low-l WKB artifact in v2.224: the monotonic-in-j breaking persists at the WKB-accurate l=3,4 [residual ~1e-6], and it FALLS sharply with l [j=10: 95%/12%/4% for l=2/3/4] as the RW and Zerilli potentials converge to the common centrifugal term -- exactly the predicted physics; bonus, the breaking concentrates at the LOUDEST l=2 mode, so the R4 sweet spot l=2,j=10 is doubly favorable for observation) · [**v2.224 isospectrality-breaking response (axial vs polar sensitivity)**](docs/results/2026-06-29-v2.224-qnm-parity-sensitivity.md) (supplies the polar side of the v2.218 discriminator from in-house machinery: using the validated Zerilli solver, the axial-vs-polar QNM response to a common (r_H/r)^j deformation differs MONOTONICALLY more as the deformation localizes near the horizon -- 12% at j=2 to 95% at j=10, exactly where the R4 quartic operator lives, so R4 is near-optimally placed to be probed by isospectrality-resolved ringdown; the parity-ratio is method-consistent/robust while absolute per-j values carry the v2.212 WKB caveat) · [**v2.223 population scaling of R4 ringdown bounds**](docs/results/2026-06-29-v2.223-qnm-r4-population-scaling.md) (pivots off the single-event arc to the population: because the QNM deformation scales as the 6th power of the length scale, multi-event stacking tightens the ell bound only as N^-1/12 -- halving it needs ~4096 events -- while a single event k-times louder in SNR is worth k^2 events; the source-backed scaling law is claim-grade, and the naive 2-event inverse-variance combination [50.0 km] is compared honestly to the published combined bound [51.3 km]; verdict: chase SNR, not event count) · [**v2.222 the overtone covariance wall**](docs/results/2026-06-29-v2.222-qnm-overtone-covariance-wall.md) (caps the ringdown arc: adding the n=2 overtone to the joint Fisher inflates the first-overtone resolvability R_tau1 a FURTHER ~35x [2.53->13.97->494 isolated->2->3-mode] and explodes the Fisher condition number 1e6->3e9 -- the first-principles mechanism behind the published overtone-overfitting concerns; the overtone R4 advantage erodes 140x->10x at 3 modes, so the OPTIMAL R4 strategy is the low-order 2-mode model: enough to exploit the overtone sensitivity, few enough to stay off the wall) · [**v2.221 two-mode (220+221) joint Fisher: overtone advantage survives mode covariance**](docs/results/2026-06-29-v2.221-qnm-r4-twomode-fisher.md) (honest refinement of v2.220: the overlapping fundamental+overtone are strongly correlated [~0.86], so a joint fit inflates BOTH modes' resolvability by ~4-5.5x -- the single-mode numbers were optimistic by that factor [amplitude-independent, a Schur-complement geometry property]; but because both degrade similarly, the overtone's R4-reach advantage only falls 177x->140x, still ~2 orders of magnitude in the damping channel: the v2.220 conclusion is ROBUST) · [**v2.220 net R4 reach per overtone (sensitivity x resolvability)**](docs/results/2026-06-29-v2.220-qnm-r4-overtone-reach.md) (closes the ringdown arc: multiplying the v2.217 overtone sensitivity by the v2.219 per-mode resolvability, the n=1 overtone's DAMPING channel reaches ~177x deeper than the fundamental at equal SNR -- its dtq_1=171.35 sensitivity decisively overwhelms its low-Q resolvability penalty [R_f balloons 0.55->3.77]; the overtone keeps the deeper reach as long as its SNR exceeds just ~0.56% of the fundamental's, so overtone ringdown is the dominant R4 lever in actual detection reach, not just raw sensitivity) · [**v2.219 first-principles ringdown resolvability**](docs/results/2026-06-29-v2.219-qnm-ringdown-resolvability.md) (self-contained Fisher-matrix calc turning a QNM deviation into a detector SNR: the universal coefficients rho*sigma_f/f=0.554, rho*sigma_tau/tau=2.00 for the l=2 n=0 mode [validated scale-invariant / function-of-Q-only, matching Berti-Cardoso-Will]; a fractional deviation delta needs rho>2.8/delta at 5sigma, so the source-backed axial R4 reach sharpens from eta_2~0.04 at current SNR~8 [edge of perturbative] to ~0.003 at 3G/LISA SNR~100) · [**v2.218 axial<->polar QNM isospectrality + R4 parity-splitting discriminator**](docs/results/2026-06-29-v2.218-qnm-isospectrality.md) (self-contained, needs no blocked appendix: the axial Regge-Wheeler and polar Zerilli potentials yield the SAME QNM spectrum through the in-house solver to ~1e-6 [l=3], confirming GR isospectrality; the ~9e-4 residual is the parity-splitting noise floor, and the source-backed axial R4 shift sits ~3.5e4x above it -- so isospectrality breaking is the parity-resolved R4 discriminator the v2.209 'g_R4_c3 is dark' finding flagged; full splitting magnitude stays gated on the un-sourceable even-parity correction) · [**v2.217 R4 ringdown overtone-sensitivity hierarchy**](docs/results/2026-06-29-v2.217-qnm-r4-overtone-sensitivity.md) (source-backed: the n=1 first overtone is ~491x more sensitive to the R4 quartic operator than the n=0 fundamental, with an OPPOSITE-sign damping response -- overtone ringdown is the dominant lever for higher-curvature gravity; the full odd-parity delta_V stays un-sourceable so that McManus cross-check negative is preserved) · [**v2.216 R4 ringdown cross-validation**](docs/results/2026-06-29-v2.216-qnm-r4-cross-validation.md) (two independent source-backed routes to the R4 QNM shift agree on SIGNS -- frequency down, damping up -- but the convention-independent damping/frequency ratio disagrees ~2.6x: the single-(r_g/r)^10 term is qualitatively right, quantitatively incomplete; honest negative preserved) · [**v2.215 source-backed R4 ringdown sensitivity**](docs/results/2026-06-29-v2.215-qnm-r4-sensitivity.md) (the engine computes the R4 quartic odd-parity sensitivity -1728 e_10 by fusing the SGB potential with the McManus e_10) · [**v2.214 operator->QNM sensitivity via the published parametrized basis**](docs/results/2026-06-29-v2.214-qnm-parametrized-basis.md) (the contraction route: decompose delta_V in the McManus (r_H/r)^j basis, contract with peer-reviewed e_j) · [**v2.213 Riccati QNM solver + sensitivity precision requirement**](docs/results/2026-06-29-v2.213-qnm-riccati-precision-requirement.md) (independent QNM cross-check; scoped that recomputing the sensitivity in-house needs ~1e-3 precision) · [**v2.212 operator->QNM sensitivity vs published parametrized-ringdown**](docs/results/2026-06-29-v2.212-qnm-parametrized-validation.md) (honest negative: the WKB-at-peak sensitivity is stable but NOT accurate vs the McManus et al. e_j) · [**v2.211 QNM solver validation suite**](docs/results/2026-06-29-v2.211-qnm-validation-suite.md) (the in-house QNM solver reproduces 8 tabulated modes across the scalar/EM/gravitational Regge-Wheeler family) · [**v2.210 first-principles WKB QNM solver**](docs/results/2026-06-29-v2.210-qnm-wkb-solver.md) (validated in-house black-hole-spectroscopy: reproduces omega_220/omega_221 to ~0.2% and computes operator->QNM sensitivities) · [**v2.209 R4 ParSpec qNM sourceable-rank determination**](docs/results/2026-06-29-v2.209-r4-parspec-qnm-sourceable-rank-determination.md) (source-cited finding that a full-rank R4 ringdown discriminator is not source-backed and the parity-odd axis g_R4_c3 is dark) · [**v2.208 R4 ParSpec pyRing likelihood-rerun packet**](docs/results/2026-06-21-v2.208-r4-parspec-pyring-likelihood-rerun-packet.md) (specifies the reproducible pyRing runtime-coordinate rerun packet) · [**v2.207 R4 ParSpec qNM-to-Bresciani source-route graph**](docs/results/2026-06-21-v2.207-r4-parspec-qnm-bresciani-source-route-graph.md) (records the public-source route graph and the remaining missing operator edge) · [**v2.206 R4 ParSpec public-likelihood packet gate**](docs/results/2026-06-21-v2.206-r4-parspec-public-likelihood-packet.md) (documents the public-likelihood acceptance gate and current no-public-packet finding) · [**FINDINGS.md**](docs/FINDINGS.md) (curated findings) · [**ROADMAP.md**](docs/ROADMAP.md) (where the research should go next) · [**Results index**](docs/results/INDEX.md) (chronological)

---

## What this is

The engine represents a higher-derivative gravitational EFT by **8 dimensionless Wilson coefficients**:

| sector | coefficients | meaning |
|---|---|---|
| matter | `g_4, g_6, g_8` | forward-limit scattering positivity moments (s², s³, s⁴) |
| graviton | `g_R2` (R²/Euler), `g_C` (Weyl²), `g_R3` (R³) | curvature couplings |
| parity | `g_R2_parity, g_R3_parity` | gravitational Chern–Simons / Pontryagin |

It then asks three questions, in order:

1. **Which theories are even consistent?** Intersect 38 theoretical constraints (positivity, causality, holography, swampland, anomaly flow, BH-entropy/WGC). The survivors form a thin "island" in coefficient space.
2. **What do the consistent theories predict?** Each is a point with a falsifiable observable fingerprint (sub-mm gravity, η/s, cosmic birefringence, GW signals, BH entropy, inflation…).
3. **What does the data say?** Ingest real measurements as additional constraints (sub-mm gravity, cosmic birefringence, GW speed & dispersion) and watch the island shrink — sometimes to a tension.

---

## The big picture — what the engine found

The program has generated 300+ indexed research notes. The throughline:

### A consistent quantum-gravity EFT is rare and low-dimensional
The consistent "island" is **~0.6% of a comparable-magnitude coefficient box** and **~3.4 effective dimensions** — one stiff collective-scale mode with several sloppy directions (the swampland's "thin landscape," made quantitative). The most-robustly-consistent point (the island's Chebyshev center) predicts a near-holographic `a/c ≈ 0.92`, a KSS-violating `η/s ≈ 0.81`, and a **sub-mm fifth force at ~93 µm** that independently reproduces a dark-energy-scale scenario. *(v1.73–75)*

### One operator, many epochs
The single R² coupling `g_R2` plays three roles depending on its scale: the **Starobinsky inflaton** at a high cutoff (n_s ≈ 0.964, r ≈ 0.004 — dead-center in the Planck+BICEP sweet spot), the **dark-energy scalaron / sub-mm fifth force** at the meV cutoff, and the **holographic η/s and a/c** of a putative dual. The engine connects amplitude positivity, holography, black-hole thermodynamics, inflation, and dark energy as facets of the *same* coefficients. *(v1.67, 71, 72, 82, 86)*

### Data ingested — and a sharp tension
The engine is one of few QG-phenomenology tools that **folds in real experiments**:
- **Sub-mm gravity (Eöt-Wash)** *excludes* the naive unscreened dark-energy scalaron by ~7×, collapsing the island to ~2% of its theoretical volume. *(v1.76–77)*
- **Cosmic birefringence (Minami–Komatsu, β = 0.34°±0.09°)** makes the engine *prefer a nonzero, positive-handed parity coupling* — the first data-driven preference for a parity-violating universe. *(v1.78)*
- These two, plus the engine's own anomaly-inflow axiom, are in **~2.8σ tension** (robust across the toy-mapping uncertainty): a consistent EFT can match the birefringence only if the scalaron is **screened**. *(v1.79–80)*
- **GW170817 speed** is *blind* to higher-derivative gravity (frequency-suppressed), but **LIGO's GW dispersion test** reaches the dark-energy cutoff — *which observable* matters as much as which sector. *(v1.84–85)*

### Current live-data frontier: R4 through GWOSC and LALSuite
The latest loop, v2.170-v2.208, turns the most promising gravitational-wave route into an executable nonclaiming pipeline:
- **v2.170-v2.171:** the symbolic R4 scale route now has a strict packet contract for a future source-backed `Lambda_R4` policy; no numeric policy is present yet.
- **v2.172-v2.173:** the parallel Weyl/G8 route now has a joint-likelihood packet contract; it remains blocked on a real `g_C+g_8` likelihood.
- **v2.174:** the engine selected the Bresciani-axis plus public-GWOSC reanalysis path after checking source/public candidates.
- **v2.175-v2.181:** the route gained a Bresciani R4 axis dictionary, public GW170608 H1/L1 HDF5 ingestion, source-backed 5PN/7PN R4 PN kernels, and a source-backed GWOSC projection seed.
- **v2.182-v2.183:** remote Linux validation runs LALSuite 7.7.1/IMRPhenomD, and the H1/L1 detector response now has a LALSuite-calibrated target.
- **v2.185:** the real GWOSC projection now uses that calibrated H1/L1 response instead of the v2.181 deterministic detector proxy.
- **v2.186:** the projection now exports a deterministic 81-point nuisance-grid covariance contribution over event mass, symmetric mass ratio, coalescence time, and phase.
- **v2.187:** remote Linux validation builds a coarse network likelihood/posterior over the three R4 axes using real GWOSC strain, LALSuite IMRPhenomD baselines, calibrated H1/L1 channel response, and the established nuisance grid; the next blocker is replacing the linearized PN/IMR bridge with a source-owned full R4 IMR sampler.
- **v2.188:** the source-owned sampler blocker is split by importing the ParSpec higher-curvature ringdown route as a primary-source bridge for quartic EFT, including the published `ell_qEFT <= 51.3 km` bound; the remaining blockers are the engine-axis map, public posterior samples, event-set alignment, and systematics export.
- **v2.189:** the continuity ledger preserves every run from v2.170-v2.188 with note paths, artifact paths, retained details, numeric digests, frontier changes, and remaining blockers so top-level summaries do not drop intermediate results.
- **v2.190:** the missing ParSpec `ell_qEFT -> (g_R4_c1,g_R4_c2,g_R4_c3)` map is now an executable packet contract with explicit subrequirements for source-axis power, Bresciani-basis orientation, normalization, covariance pushforward, likelihood export, event alignment, and systematics.
- **v2.191:** the public arXiv source package is audited and hashed; it resolves the source-axis power subpiece with `p_qEFT = 6`, preserves the qEFT QNM deformation coefficients and event bounds, and confirms that no machine-readable qEFT likelihood object is present in the package.
- **v2.192:** the ParSpec source events are mapped to exact GWOSC event versions (`GW150914-v3`, `GW200129_065458-v1`) and public 4 kHz HDF5 strain URLs; the remaining event blocker is the actual same-event R4 likelihood rerun plus the GW200129 V1 detector-policy choice.
- **v2.193:** remote Linux validation reruns the coarse R4 likelihood on H1/L1 public strain for `GW150914-v3` and `GW200129_065458-v1`; the H1/L1 same-event subpiece is ready, while the GW200129 V1 response, source-event nuisance covariance, ParSpec likelihood, and qEFT axis map remain claim blockers.
- **v2.194:** remote Linux validation reruns the source-event likelihood with event-specific detector topology: `GW150914` uses H1/L1 and `GW200129` uses H1/L1/V1 with event-time LALSuite antenna responses; the V1 detector-response blocker is removed, while source-event nuisance covariance, ParSpec likelihood, and the qEFT axis map remain claim blockers.
- **v2.195:** the v2.194 source-event likelihood now exports event-specific and combined nuisance-marginalized R4 covariance for GW150914 and GW200129; the source-event covariance blocker is removed, while the public ParSpec/qEFT likelihood, source-backed operator-basis/axis map, posterior sampler, systematics, and external review remain claim blockers.
- **v2.196:** the published ParSpec qEFT 90% bounds are converted into an event-aligned, nonclaiming `ell_qEFT_km` surrogate attached to the v2.195 source-event packet; this removes the stale event-set mismatch and source-axis mismatch subpieces, while the machine-readable public likelihood and engine-axis map remain claim blockers.
- **v2.197:** the qEFT source-axis route now has a checked `ell_qEFT -> qNM deformation` Jacobian using the source coefficients preserved in v2.191; the missing `qNM deformation -> Bresciani R4 engine axes` operator-basis map remains the decisive claim blocker.
- **v2.198:** the ParSpec continuity ledger preserves the v2.190-v2.197 run chain with note paths, artifact paths, retained details, numeric digests, blocker changes, validation counts, and claim gates so README/FINDINGS/reports do not lose intermediate details.
- **v2.199:** the source-event absolute gamma metadata is now attached from GWOSC preferred PE rows: `M_f,z = 63.1,0.09` for GW150914 and `60.2,0.18` for GW200129; absolute gamma is computable for the source events while the qNM-to-Bresciani operator map and public likelihood remain blockers.
- **v2.200:** the qNM-to-Bresciani gate now defines the exact source-backed packet shape needed next: a finite 3x4 matrix from qNM deformation axes into `g_R4_c1/c2/c3`, with row rank 3. The current qEFT source object is only a rank-1 gamma-to-qNM ray, so the engine blocks any map or claim promotion.
- **v2.201:** the public pyRing `EFT_QNMs` branch is now a hash-pinned source probe. It confirms six quartic plus/minus branch tables and a rank-4 local branch-column QNM matrix, while preserving the decisive blockers because plus/minus are mode-splitting branches, not independent Bresciani operator axes.
- **v2.202:** pyRing's imaginary-frequency convention now has a source-backed linearized conversion into fractional damping-time axes. The exported spin-zero tau matrix is rank 2, while the route remains blocked on pyRing-to-Bresciani orientation, independent operator-axis interpretation, public likelihood, systematics, and adversarial review.
- **v2.203:** the pyRing quartic EFT frequency and damping-time rows are now evaluated at source-event remnant spins for GW150914 and GW200129. This exposes a few-percent pyRing-runtime Berti-GR versus ParSpec-high-spin normalization gap and keeps the claim gate closed.
- **v2.204:** the pyRing-runtime-to-ParSpec-high-spin normalizer gap now has an explicit nonclaiming policy. Runtime Berti-GR normalization is allowed for internal pyRing reruns, while ParSpec high-spin rows are comparison-only until a source-backed operator-axis map exists.
- **v2.205:** the pyRing-to-Bresciani orientation source audit is now complete as a no-map ledger. pyRing branch directions remain local QNM branch-splitting columns, and synthetic branch-to-operator maps are explicitly forbidden.
- **v2.206:** the public ParSpec/qEFT likelihood route now has a tested packet gate. The current public recheck finds no machine-readable posterior samples, covariance matrix, or log-likelihood grid; the published-bound surrogate remains nonclaiming continuity evidence only.
- **v2.207:** the source-route graph for the qNM-to-Bresciani hard blocker is now explicit. Public sources provide ParSpec qNM axes, qEFT and pyRing/Cano QNM pieces, and Bresciani K-to-engine projection, but no source-backed qNM-to-K operator edge; the best next build is a pyRing EFT likelihood-rerun packet in runtime coordinates.
- **v2.208:** the pyRing EFT likelihood-rerun packet is now concrete. It pins public pyRing and GWOSC inputs, the paper-named pyRing execution commit, source-event remnant spins, a 12-row event/direction config grid, runtime-coordinate policy, and the required posterior/log-likelihood export contract; execution, systematics, external review, and the qNM-to-Bresciani map remain blockers.

The latest artifact is [v2.208 R4 ParSpec pyRing likelihood-rerun packet](docs/results/2026-06-21-v2.208-r4-parspec-pyring-likelihood-rerun-packet.md). The latest route graph is [v2.207](docs/results/2026-06-21-v2.207-r4-parspec-qnm-bresciani-source-route-graph.md), and the latest public-likelihood gate is [v2.206](docs/results/2026-06-21-v2.206-r4-parspec-public-likelihood-packet.md). The current roadmap is [docs/ROADMAP.md](docs/ROADMAP.md). This is progress toward a live gravitational-wave discriminator, not a framework exclusion or discovery claim.

### The "data-driven EFT"
Folding consistency + the two ingested experiments points to a specific, registered theory — `discovered_data_driven` — a **screened-scalaron, positive-handed-parity EFT** that matches cosmic birefringence and predicts GW/PTA parity signals just below current reach. It sits where *none* of the 13 textbook frameworks do. `itb predict discovered_data_driven`. *(v1.79)*

### Meta-experiments: auditing the engine itself
- **The Gödel test:** the ~37 theoretical axioms are **internally consistent** (no contradiction among the principles); every minimal inconsistent core contains a *data* constraint — the tensions are empirical, not logical. *(v1.87)*
- **The minimum decisive experiment set:** **6 measurements** pin all 8 coefficients (matter scattering ×2, GW/CMB birefringence, BH entropy, η/s, a cubic-graviton amplitude). The inflation detection contributes *nothing* to pinning; g_8 and g_R3 are genuine blind spots with no current probe. *(v1.88)*
- **The phylogenetic tree:** treated as RG-running couplings under a toy asymptotic-safety flow, the frameworks form a **rooted tree** — with a connected clade of discrete-spacetime approaches and **LQG flowing to Group Field Theory** (its actual proposed UV completion). *(v1.89)*

A full, navigable account of every cycle is in [docs/FINDINGS.md](docs/FINDINGS.md) and the [results index](docs/results/INDEX.md).

---

## Quickstart

```bash
git clone https://github.com/hassard0/itb-engine && cd itb-engine
python -m venv .venv && . .venv/bin/activate     # (Windows: .venv\Scripts\activate)
pip install -e ".[dev]"
pytest -q                                         # current full suite is recorded in the latest run note

# the affirmative answer: what a consistent QG EFT looks like + its full fingerprint
itb predict discovered_data_driven
itb predict string_tree_eft --json

# write the current multi-persona research-swarm agenda
itb swarm-plan

# run a flagship experiment (each writes a figure + JSON to /tmp and prints a summary)
python experiments/island_census.py 300000        # how rare/low-dimensional is the island
python experiments/min_experiment_set.py           # what is the minimum experiment program
python experiments/godel_test.py 1500000           # is the engine internally consistent
```

Most experiments parallelize across cores; heavy Monte-Carlo runs are intended for a multi-core workstation or a separately configured remote Linux worker.

Remote validation is intentionally configured outside committed documentation.
Use your own SSH configuration or environment-specific wrapper when reproducing
remote checks; do not commit private access details.


---

## How it works

### The constraint stack (`experiments/stack.py`)
`build_stack(...)` assembles the **38 theoretical constraints** (3 classes: A amplitude, B information, C universality). Real data is **opt-in** so the theoretical-only stack is preserved:

```python
from stack import build_stack
theory = build_stack(bnossw_mean="geometric", rfc_form="convex_hull")     # 38 theoretical constraints
with_data = build_stack(..., include_data=True,          # + Eöt-Wash sub-mm
                             include_birefringence=True,  # + Minami–Komatsu
                             include_gw_speed=True,        # + GW170817 speed
                             include_gw_dispersion=True)   # + LVK dispersion
```

Each constraint exposes `.evaluate(theory) → (satisfied, margin, signed_distance_margin)`, `.gradient(...)`, and a `.constraint_class`. Full list in [docs/CONSTRAINTS.md](docs/CONSTRAINTS.md).

### The realism program
Six "knife-edge" prefactors are tunable knobs with stated factor-of-~2 plausibility windows (`CANONICAL`, `PLAUSIBLE_RANGES` in `stack.py`). Every headline result is re-checked across that uncertainty — e.g. the birefringence-vs-gravity tension survives 99% of the mapping box (v1.80). This is the repo's core discipline: **which conclusions survive when we admit we only know the house numbers to within a factor of two.**

### Observables & predictions (`src/itb/predict.py`, `src/itb/gravitational_observables.py`)
Each framework has a falsifiable fingerprint — sub-mm Yukawa range, η/s (KSS units), GW/cosmic birefringence, chiral Hellings–Downs (PTA), BH extremal-entropy shift (WGC), Starobinsky (n_s, r). `itb predict <framework>` renders it; the Jacobians power the Fisher / experimental-design analyses.

### Frameworks (`src/itb/frameworks/`)
14 encoders: catalogued (string tree-EFT, asymptotic safety, LQG-induced, CDT, causal sets, group field theory, Hořava–Lifshitz, emergent gravity, Penrose–Diósi, pure GR), engine-discovered (`discovered_novel`, `discovered_parity_violating`, `discovered_high_g8`), and the data-driven (`discovered_data_driven`).

---

## Honest limitations

- **Toy prefactors.** Most constraint coefficients are O(1) placeholders; the realism program tells you which conclusions are robust to that, but exact coordinates move with the numbers.
- **Order-of-magnitude observable mappings.** Cross-sector maps (κ_β for birefringence, lam_map for η/s, the BH-entropy and GW-dispersion normalizations) are schematic; the robust content is signs, orderings, and scalings — not precise values.
- **Hints, not discoveries.** Cosmic birefringence is a ~3.6σ hint; screening is modeled as a binary flag (really density-dependent); CMB β is the axion–*photon* coupling while GW/PTA are axion–*graviton* (linked only under a single-axion assumption).
- **Schematic RG / ghost.** The phylogeny uses toy beta functions; higher-derivative gravity is scheme-dependent and has a massive spin-2 ghost — the flow is *structure*, not trajectories.

The program is built to surface these honestly rather than hide them; several cycles are *negative* or *self-correcting* results (e.g. the a-theorem is redundant in this basis, v1.70; GW170817 is blind, v1.84).

---

## Architecture

```
itb-engine/
├── src/itb/
│   ├── theory.py              Wilson-coefficient dataclass
│   ├── constraints/           38 theoretical constraints + 4 opt-in data constraints
│   ├── frameworks/            14 framework encoders
│   ├── gravitational_observables.py, observables.py, holographic_ac.py
│   ├── predict.py             `itb predict` fingerprint
│   ├── fisher.py              Fisher metric on theory space
│   ├── scope.py               engine-validity (local / Lorentz / fundamental flags)
│   ├── cli.py                 `itb` command
│   └── api/server.py          FastAPI web app
├── experiments/              277 cycle scripts (island census, center, data ingestion,
│                              meta-experiments) + stack.py (the constraint assembler)
├── tools/                    reproduction helpers, validation helpers, build_index.py
├── tests/                    319 test files (latest full-suite count is in the v2.208 report)
├── docs/
│   ├── FINDINGS.md           ← curated findings (read this)
│   ├── CONSTRAINTS.md        ← the constraint stack, by class
│   └── results/              319 indexed result notes
└── legacy/                   early-program report generators (v1.0–v1.19)
```

---

## License

See [LICENSE](LICENSE).
