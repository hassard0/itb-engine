# v2.403 - SWING: g_R3_parity=0 is a third constructed-point assumption -- the minimal parity sector is a choice, and it hides a second parity observable

## Question

The constructed theory sets the parity-odd cubic `g_R3_parity = 0` (parity only in the quadratic). Is that
forced -- or, like a=c (v2.399) and g_6=g_8 (v2.392), a constructed-point assumption?

## Result (a third assumption)

```text
  activating g_R3_parity as an independent axis (constructed + g_R3_parity free):
    feasible range [-0.034, 0.035], g_R3_parity = 0 INTERIOR (not an edge)
    edges bound by generalized_anomaly_inflow
    anomaly-closed value r*g_R2_parity ~ 0.028 (v2.371): feasible (well inside)
```

All 5/5 checks pass. `g_R3_parity = 0` is **interior** to the feasible window, so it is a **choice, not a
prediction** -- specifically the anomaly-*saturated* choice (v2.370, cubic suppressed).

## What this establishes

The parity-odd cubic is a free direction bounded by the anomaly inflow, and the two anomaly variants of the
v2.377 fork are literally two points on this axis: saturated (0) vs closed (0.028), both consistent,
distinguished only by whether the second anomaly-matching condition is imposed. This adds a **third entry to
the candidate's ASSUMPTION tier** (v2.402): a=c (v2.399), g_6=g_8 (v2.392), and now g_R3_parity=0. The
systematic lesson: the max-margin (Chebyshev) point's apparent simplicity -- holographic a=c, equal matter
moments, minimal parity -- is largely **assumption**, and the theory's genuine predictions are the ROBUST-tier
structural ones (matter dominance, ghost-safety, string-like towers, near-Planckian cutoff), not these tidy
coincidences. Physically the consequence is real: `g_R3_parity != 0` sources a **second, cubic-order parity
observable** distinct from the quadratic birefringence, so the "minimal parity sector" assumption hides a
potential extra chiral signal -- exactly the one the anomaly-closed variant predicts. So the parity sector is
richer than the constructed point shows (a two-parameter `(g_R2_parity, g_R3_parity)` family bounded by anomaly
inflow), and which point is realized is a genuine open question the v2.377 forward test would resolve.

## Honest scope

`g_R3_parity=0` being interior is the robust, measure-independent finding -- a choice, not forced. The window
edges `[-0.034, 0.035]` are toy-basis (set by the generalized-anomaly-inflow and cubic-parity constraints with
O(1) prefactors); the robust content is that 0 is interior and the anomaly-closed value is feasible. The
anomaly-closed value 0.028 = `r g_R2_parity` uses v2.371's toy `r` and the birefringence-derived `g_R2_parity`
(v2.329), so it too is toy-basis; the robust point is that a nonzero `g_R3_parity` is allowed and even
preferred by one anomaly variant. This is a basis-structure / assumption audit (like v2.392/399), adding a
third assumption and no new physical datum about the constructed point -- it characterizes the freedom
`g_R3_parity=0` hides. "Second parity observable" is the qualitative physical reading (a parity-odd cubic
curvature operator sources cubic-order parity violation); its magnitude is unpinned. Robust content:
`g_R3_parity=0` is an unforced constructed-point assumption, the parity-odd cubic is a free
anomaly-inflow-bounded direction, and the anomaly-closed variant prefers it nonzero -- a third assumption
completing the audit.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_third_assumption_parity_cubic.py --n 401 --out experiments/results/v2.403/qnm_third_assumption_parity_cubic.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_third_assumption_parity_cubic.py -q
```

Result: feasible g_R3_parity [-0.034, 0.035], 0 interior; anomaly-closed 0.028 feasible; edge = anomaly inflow;
`5 passed`.

## Next (v2.404)

Three assumptions audited (a=c, g_6=g_8, g_R3_parity=0). Next: a fresh swing, or honestly flag if the frontier
is reached. Bold swings, honest failures.

## References

```text
this repo: v2.371 (anomaly-closed -> nonzero g_R3_parity), v2.370 (anomaly-saturated -> cubic suppressed), v2.377 (saturated-vs-closed fork), v2.399 (a=c assumption), v2.392 (g_6=g_8 assumption), v2.402 (assumption tier), src/itb/constraints/cubic_parity.py + anomaly_flow.py
physics: gravitational anomaly inflow; parity-odd cubic curvature operator
```
