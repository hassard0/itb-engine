# v2.396 - SWING: g_R2 is the keystone coupling -- the anomaly's forcing of g_R2>0 automatically satisfies the a-theorem, and g_R2 measures the tower's central charge

## Question

The a-theorem reduces in the engine to `g_R2 >= 0` (docstring flags it as redundant). Does its integrate-out
nugget `g_R2 ~ Delta_a/M^4` connect to prior results?

## Result (a-theorem from the anomaly; g_R2 as keystone)

```text
  (1) the gravitational anomaly FORCES g_R2 > 0 given matter (v2.393; region-wide min g_R2 = 0.089 > 0)
  (2) g_R2 > 0 is exactly the a-theorem monotonicity condition Delta_a >= 0
  (3) g_R2 measures the tower's central charge: Delta_a ~ g_R2 M^4 = 0.05  (M = species scale 0.72 M_Pl, v2.394)

  g_R2 appears in EIGHT distinct consistency roles, all consistent at g_R2 = 0.193:
    anomaly matching (v2.393) | a-theorem (v2.396) | WGC cap (v2.389) | Weyl^2 ghost (v2.385)
    species counter (v2.394)  | screening (v2.354) | moment tower (v2.375) | parity lock (v2.371)
```

All 5/5 checks pass. Matter sourcing `g_R2` (v2.393) **automatically** guarantees RG-flow monotonicity -- the
same coupling anomaly matching forces nonzero is what makes the a-theorem hold.

## What this establishes

The a-theorem is **implied by the gravitational anomaly** through `g_R2`: the anomaly forbids `g_R2 = 0`
region-wide, which is exactly `Delta_a >= 0`, so the a-theorem is satisfied everywhere in the family and never
by coincidence. And `g_R2` measures the central charge the UV tower carries above the IR (`Delta_a ~ 0.05`).
More broadly, `g_R2` is the theory's **keystone coupling** -- the single quantity where the amplitude
(positivity), swampland (WGC, species, anomaly), thermodynamic (ghost, screening), and RG (a-theorem) sectors
all intersect, in eight mutually-consistent roles. That is why the whole construction is so tightly determined:
pinning `g_R2` pins the theory, and `g_R2` is over-determined by eight compatible conditions.

## Honest scope

The `Delta_a` link uses the docstring heuristic `g_R2 ~ Delta_a/M^4` (a representative integrate-out relation,
not a computed coefficient) and the toy species scale, so the number `Delta_a ~ 0.05` is doubly toy; the robust
point is the sign/logic -- `g_R2 > 0` (anomaly-forced) *is* the a-theorem condition, so the two are linked and
`Delta_a > 0` is guaranteed. Crucially, the EIGHT-role centrality is **partly a toy-basis artifact**: the
a_theorem docstring itself notes the basis collapses the Euler (a) and Weyl^2 (c) anomalies onto the single
coefficient `g_R2`, so a finer basis resolving `c - a` would distribute the roles among distinct
Ricci^2/Weyl^2/Euler couplings and `g_R2` would not carry all eight. So "keystone" is a statement about *this*
toy basis -- useful for understanding why the construction is tight, not a claim that one physical operator does
all this work. The eight roles are each individually verified at the constructed point (and `g_R2 > 0`
region-wide), so the MUTUAL CONSISTENCY is a real fact; the single-keystone interpretation is basis-dependent.
This synthesizes prior results plus the one new (heuristic) a-theorem/anomaly link. Robust content: `g_R2 > 0`
is anomaly-forced, which implies the a-theorem, and `g_R2` is (in this basis) the coupling where all sectors
meet -- over-determined but consistent.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_gR2_keystone.py --n 20000 --out experiments/results/v2.396/qnm_gR2_keystone.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_gR2_keystone.py -q
```

Result: `Delta_a ~ 0.05`; `g_R2 > 0` region-wide (min 0.089); 8 roles all consistent; `4 passed`.

## Next (v2.397)

g_R2 is the keystone. Next: a fresh swing or a new consistency conjecture. Bold swings, honest failures.

## References

```text
this repo: src/itb/constraints/a_theorem.py (a-theorem as g_R2>=0, integrate-out Delta_a), v2.393 (anomaly forces g_R2>0), v2.394 (species scale M), v2.389 (WGC), v2.385 (ghost), v2.354 (screening), v2.375 (moment tower), v2.371 (parity lock)
physics: Komargodski-Schwimmer 2011 (a-theorem); Alvarez-Gaume-Witten (anomaly)
```
