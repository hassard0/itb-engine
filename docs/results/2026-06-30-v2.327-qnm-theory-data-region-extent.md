# v2.327 - How predictive is the constructed theory? The extent of the theory+data region

## Question

v2.322 showed the EXISTENCE of a higher-derivative gravity satisfying both theoretical consistency and the
four ingested-data constraints. How tightly do the combined constraints pin it -- which sectors are
determined, which are free?

## Result (tiny region; the parity sector is the most sharply pinned, by data)

The region is tiny and the pinning is differential:

```text
  box feasible fraction (+/-0.25 box):  ~5e-4   (the new theory is tightly pinned)

  coupling       feasible interval        width    pinned by
  g_R2_parity    [0.048, 0.078]           0.030    DATA (cosmic birefringence)
  g_R2           [0.115, 0.227]           0.112    consistency
  g_R3           [0.028, 0.160]           0.132    consistency
  g_6            [0.304, 0.448]           0.144    consistency
  g_4            [0.401, 0.593]           0.192    consistency
  g_8            [0.304, 0.530]           0.226    consistency
```

The parity coupling is the most sharply determined -- pinned to `[0.048, 0.078]` (width 0.030), exactly the
cosmic-birefringence DATA window of v2.321 -- while the leading curvature `g_R2` is moderately constrained
(~0.11) and the matter sector `g_4/g_6/g_8` is the loosest (0.14-0.23), bounded only by consistency. All
5/5 checks pass.

## What this establishes

The new theory's predictivity is concentrated in the PARITY sector -- the one piece pinned by a real
measurement rather than only by consistency -- and that is precisely the sector that carries its
falsifiable cosmological signatures (chiral primordial GW v2.319, gravitational leptogenesis v2.324). So
the theory is most sharply predicted exactly where it is most testable: a tighter future cosmic-
birefringence measurement, or a chiral-GW detection, narrows the parity window directly, while the
matter/curvature sectors remain a bounded but looser consistency-determined region that future
matter-sector data (collider positivity, GW dispersion) would tighten. The combined theory+data
constraints do not merely permit the constructed gravity -- they pin its observationally-relevant sector to
a narrow, falsifiable window.

## Honest scope

All values are the engine's literal `check()` output under convex_hull + all four data constraints. The 1D
extents are deterministic line searches through the constructed point (step 0.002) -- exact for that
point's cross-sections; the full region is 6-dimensional and non-box, so the 1D widths characterize the
extent through the chosen point, not the global diameter. The box-fraction (~5e-4) is from uniform sampling
of a +/-0.25 box and is a rough size proxy (only ~10 hits land), so it indicates "tiny" robustly but its
exact value is noisy. The parity window's width and its match to the cosmic-birefringence data window are
the robust, data-pinned content; the matter/curvature widths depend on the O(1) constraint prefactors and
the constructed center (convention-dependent). The cosmic-birefringence pinning carries its ~3.6-sigma and
order-of-magnitude-map caveats (v2.321). The qualitative result -- parity sharply pinned by data,
matter/curvature looser by consistency -- is the durable content. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_theory_data_region_extent.py --out experiments/results/v2.327/qnm_theory_data_region_extent.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_theory_data_region_extent.py -q
```

Result: the theory+data region is tiny (~5e-4 of the box); the parity coupling is the tightest-pinned
(width 0.030, matching the cosmic-birefringence window); the matter sector is looser; `6 passed`.

## Next (v2.328)

The new theory's predictivity is characterized. A fresh sector or continued audits, at a deliberate pace.
Honest negatives reported as part of the search.

## References

```text
this repo: v2.322 (theory+data existence), v2.321 (cosmic-birefringence parity window), v2.319/v2.324 (parity-sector signatures)
```
