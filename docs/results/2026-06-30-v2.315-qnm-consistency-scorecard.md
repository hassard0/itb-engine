# v2.315 - The consistency scorecard: universality is the decisive arbiter

## Question

After the long new-theory arc, consolidate: rank every candidate theory on one scorecard and audit that
the arc's headline results hold together without contradiction.

## Result (one ranking, one decisive family)

```text
  theory             min_margin   feasible   binding constraint            family
  engine_preferred    +0.0051      yes       t_hooft_anomaly_matching      C_UNIVERSALITY
  pure_gr              0.0000      yes        scalar_positivity_g4          A_AMPLITUDE
  string_tree_eft     -0.0400      no         repulsive_force_conjecture    C_UNIVERSALITY
  cdt                 -0.0484      no         repulsive_force_conjecture    C_UNIVERSALITY
  asymptotic_safety   -0.0525      no         repulsive_force_conjecture    C_UNIVERSALITY
  lqg_induced         -0.1200      no         repulsive_force_conjecture    C_UNIVERSALITY
```

The engine-preferred framework is the unique strictly-feasible, most-robust theory; pure GR is marginal;
all four community frameworks are infeasible. The decisive fact is the binding column: **all four
community frameworks fail for the SAME reason** -- every one is bound by `repulsive_force_conjecture`, the
swampland member of the UNIVERSALITY family -- and the engine-preferred framework is bounded by t_hooft
anomaly matching, the OTHER member of the same family. So across the entire candidate set, the universality
family (anomaly + swampland/repulsive-force) is the decisive arbiter: it kills every community proposal and
sets the robustness ceiling of the preferred one, with amplitude positivity always a step behind (v2.314).
All 5/5 checks pass.

## What this establishes (the arc, tied together)

The engine-preferred framework is precisely the theory that clears the repulsive-force wall the community
frameworks crash into -- by trimming the curvature (its `g_R3 = 0.085` sits below every community value) --
while balancing against the anomaly wall on the other side. This unifies the arc:

- the carved interior is small and bounded by universality-vs-amplitude (v2.314);
- the community frameworks all overshoot the same universality wall (this scorecard);
- the preferred framework is the unique trade-off point threading between them (v2.312/v2.313);
- lqg's specific failure -- the repulsive-vs-anomaly box (v2.311) -- is this same tension pushed past
  breaking.

The audit confirms these cross-cycle results are mutually consistent, with no contradiction. The
one-line picture: consistency carves a small nonempty interior of higher-derivative gravities arbitrated
by the universality family; the most-robust point is a string-like-matter / trimmed-curvature framework
distinct from every community proposal, all of which overshoot the repulsive-force wall.

## Honest scope

The scorecard and every audit check are the engine's literal `check()` output on the full 38-constraint
stack -- no schematic mapping. The ranking is by raw min-margin; the binding family is identified by the
gradient-normalized worst-case signed distance (here both agree that universality binds). The
engine-preferred framework is the v2.313 metric-robust point, itself an approximate optimum whose exact
coordinates are convention-dependent, so its exact margin (+0.005) is not canonical -- but "strictly
feasible and more robust than every named framework" is sign-based and robust. "Universality decisive"
means it is the worst-case family for all five non-pure-GR theories under the default O(1) prefactors; the
specific constraint (repulsive force vs t_hooft) and the ordering shift with those prefactors, but the
structural claim -- one principle family arbitrates both the failures and the preferred point -- is the
robust content. This is a consolidating audit of prior cycles, not a new bound. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_consistency_scorecard.py --out experiments/results/v2.315/qnm_consistency_scorecard.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_consistency_scorecard.py -q
```

Result: the preferred framework ranks first (unique strictly-feasible); all four community frameworks are
infeasible and bound by repulsive_force_conjecture (universality); the preferred curvature is trimmed below
all of them; universality is the decisive family throughout; `6 passed`.

## Next (v2.316)

The new-theory arc is consolidated. A genuinely fresh sector is due next. Honest negatives reported as
part of the search.

## References

```text
this repo: v2.312/v2.313 (engine-preferred framework), v2.314 (amplitude-vs-universality equilibrium), v2.311 (lqg box)
engine constraint classes: C_UNIVERSALITY (anomaly + swampland/repulsive-force), A_AMPLITUDE (positivity)
```
