# v2.341 - Which deep requirement bounds the theory? Causality has headroom; unitarity and the WGC bind

## Question

The trilogy (v2.338 unitarity, v2.339 causality, v2.340 WGC) certified the constructed POINT. Across the
whole consistent+observed FAMILY (v2.332), which of the three classic objections to higher-derivative
gravity actually BOUNDS the new theory's existence, and which has headroom to spare?

## Result (causality has headroom; unitarity and the WGC bind)

Taking the worst (smallest) gradient-normalized margin anywhere in the family (~2100 samples) for each
requirement family:

```text
  requirement                                          worst margin in family
  CAUSALITY  (CEMZ + causality bound + Hofman-Maldacena)  +0.0519   -> always headroom (least constraining)
  UNITARITY  (forward positivity + dispersion + EFT-hedron)  0.0000   -> BINDING (a family wall)
  WGC        (weak gravity + scalar-WGC + repulsive force)  +0.0045   -> near-binding
```

The family is robustly causal everywhere (causality never drops below +0.05) -- a direct consequence of the
trimmed cubic curvature that gives the constructed theory ~65% causality headroom (v2.339), preserved
across the family. Unitarity by contrast reaches 0 (a binding wall), and the WGC reaches +0.004
(near-binding). All 5/5 checks pass (all three are satisfied everywhere; the question is which is tight).

## What this establishes

Of the three classic objections, the new theory's existence is bounded by UNITARITY and the swampland WGC,
while causality is comfortably satisfied throughout. This sharpens the trilogy: all three hold at the
constructed point with margin, but only unitarity and the WGC are operative limits on the family -- the
graviton-forward-positivity (unitarity) and repulsive-force/WGC walls are exactly the active constraints
v2.325 identified as carving the region, while causality, despite being the most famous obstruction to
higher-derivative gravity, is NOT a binding wall here, because the consistent theories all keep their cubic
curvature well below the CEMZ bound. So the new theory's defining trimmed-curvature feature buys it
causality for free; what it must still trade off against is positivity (unitarity) and the weak gravity
conjecture.

## Honest scope

The "worst margin anywhere in the family" is taken over a random-walk sample (~2000 feasible points, seeded,
step 0.03), so the minima are reached only insofar as the sampler reaches the family boundary; that
unitarity hits ~0 (binding) and causality stays ~+0.05 is the engine's literal signed-distance output. The
exact minima depend on the O(1) constraint prefactors (the CEMZ kappa, the positivity normalizations, the
WGC encodings), so the values (0.05, 0.00, 0.004) are convention-dependent -- the robust, structural
content is the ORDERING: causality has clear headroom while unitarity binds and the WGC nearly binds,
consistent with v2.339 (causality headroom from trimmed cubic) and v2.325 (the active core contains
graviton-forward-positivity and repulsive force, not the causality bound). The grouping into "unitarity /
causality / WGC" families is by physical role. The family is the cosmic-birefringence-constrained one, but
the three pillars here are CP-even and their binding/headroom pattern is data-independent. Toy basis, O(1)
prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_trilogy_binding.py --out experiments/results/v2.341/qnm_trilogy_binding.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_trilogy_binding.py -q
```

Result: across the family, causality's worst margin is +0.052 (headroom), unitarity's is 0.000 (binding),
the WGC's is +0.004 (near-binding); causality is the least constraining pillar; `6 passed`.

## Next (v2.342)

The operative deep requirements are identified (unitarity + WGC bind; causality has headroom). A fresh
sector or continued audits, at the <=10-minute cadence. Honest negatives reported as part of the search.

## References

```text
this repo: v2.338 (unitarity), v2.339 (causality headroom), v2.340 (WGC), v2.325 (active constraint core), v2.332 (connected family)
```
