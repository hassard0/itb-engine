# v2.381 - SWING: the string-like matter identity is observationally DARK -- g_8 feeds no channel

## Question

v2.380 found the four channels have effective dimension ~2.5. The complement: which *couplings* are
observable, and which are DARK (consistency-constrained but feeding no observation)?

## Result (g_8 is the unique dark coupling -- and it carries the multi-state matter signature)

Correlating each of the five genuine inputs with the four channel observables across the feasible family:

```text
  coupling      max|corr|   status       (per-channel)
  g_R2           1.00       observable    screening 1.00, BH 0.88
  g_R2_parity    1.00       observable    parity 1.00
  g_R3           0.95       observable    ringdown 0.95
  g_4            0.83       observable    BH 0.83
  g_6            0.60       observable    screening/BH ~0.6 (indirect)
  g_8            0.09       DARK          nothing
```

All 5/5 checks pass. **g_8 is the unique dark coupling** -- max correlation ~0.1 with any channel -- while
every other input is observable. The sting is *what g_8 is*: the top matter dispersion coefficient, entering
`r_matter = g_6^2/(g_4 g_8) = 0.756` -- the very quantity that establishes the multi-state, **string-like
matter** sector (v2.343).

## What this establishes

An honest limitation on the theory's headline. "Parity-deformed, curvature-trimmed, **string-like** gravity"
(v2.342) is, on its string-like *matter* side, theoretically established but **observationally hidden**:
parity, screening, ringdown, and BH extremality all probe the curvature+parity sector (`g_R2`, `g_R3`,
`g_R2_parity`, with `g_4` partly), and **none probes the matter Regge structure**. The one coupling that most
directly encodes the matter tower's multi-state structure (`g_8`) is precisely the one the four channels
cannot touch. This sharpens v2.380: the ~2.5 observable directions live entirely in the curvature/parity
sector; the matter multi-state structure is inferred only through consistency (the dispersion tower, the
anomaly links), never measured. A skeptic could grant the theory's four channels and still deny the
"string-like matter" claim -- there is no observation that tests it. The falsifiable, testable content of the
theory is its curvature/parity sector; its matter-string-like identity is a consistency inference, not an
observational prediction.

## Honest scope

The correlations use the toy channel maps over a sampled family (v2.380 scope), so exact numbers are
toy-basis. But the DARKNESS of `g_8` is structural: `g_8` appears in NO channel observable and its only role is
the matter dispersion tower `g_6^2 <= g_4 g_8`, which no channel reads -- so its ~0.1 residual is purely the
indirect family coupling and stays small in any basis. `g_6`'s "observable" (~0.6) is itself *indirect* (its
family correlation with `g_R2`, not a direct channel dependence), which is the same point -- the matter
sector's observability is entirely indirect. The 0.25 threshold is conventional; `g_8` at 0.09 is far below and
the others far above, so the verdict is not threshold-sensitive. This concerns the FOUR catalogued channels; a
future observable reading the matter dispersion directly (a graviton-matter amplitude) would illuminate `g_8`,
but none is in the engine's channel set. Robust content: `g_8` (the matter-multi-state coefficient) is dark to
all four channels, so the string-like matter identity is observationally hidden. Toy channel maps, structural
coupling dependence.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_dark_parameter.py --n 30000 --out experiments/results/v2.381/qnm_dark_parameter.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_dark_parameter.py -q
```

Result: `g_8` uniquely dark (max|corr| 0.09); all others observable; `g_R2`->screening/BH, `g_R3`->ringdown,
`g_R2_parity`->parity; `5 passed`.

## Next (v2.382)

The testable content is the curvature/parity sector; matter string-likeness is hidden. Next: a fresh swing or
a consolidation of the swing arc (v2.367-381). Bold swings, honest failures.

## References

```text
this repo: v2.380 (channels ~2.5 independent), v2.372 (5 genuine inputs), v2.343 (multi-state matter r=0.756), v2.342 (string-like identity), v2.356/v2.378 (the four channels)
structural: coupling-to-channel correlation; g_8 enters only the matter dispersion tower
```
