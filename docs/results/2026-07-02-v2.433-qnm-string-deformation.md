# v2.433 - a concrete UV-embedding lead (option #4): the candidate is string tree-EFT + the data-required parity deformation

Option **#4 (embed the candidate in a UV-complete theory)** is the deepest "solve" path. Rather than only scope
it, this cycle finds a concrete lead by locating the candidate relative to the engine's **string tree-level EFT**
framework — which, unlike a bare carved point, has a known UV completion (string theory).

## Result

```text
  string tree-EFT couplings:  g_4=0.50  g_6=0.40  g_8=0.40  g_R2=0.20  g_R3=0.15  g_R2_parity=0.00
  candidate:                  g_4=0.53  g_6=0.40  g_8=0.40  g_R2=0.19  g_R3=0.09  g_R2_parity=0.06

  string tree-EFT feasible under the rigorous core AND the effective rigorous cage:  YES
  full theory+data stack rejects string tree-EFT by exactly ONE constraint:  cosmic_birefringence_data
  parity-even distance candidate <-> string:  0.067   (essentially on top of it)
  material difference:  the parity coupling only (0.00 vs 0.06)
```

All 5/5 checks pass.

## What this establishes

String tree-EFT **is** precisely the parity-conserving rival (v2.420) — feasible in the rigorous cage, rejected
only by cosmic birefringence — and its parity-even couplings sit within 0.067 of the candidate. So **the candidate
= string tree-EFT + the data-required parity deformation.** This turns the deepest solve-path from open-ended into
specific: the candidate is a *small parity deformation of a UV-completable theory*, so it can inherit string
theory's UV completion up to the parity turn-on. The concrete #4 task becomes **"find a string compactification
whose low-energy EFT is string-tree-like *and* carries the axionic / gravitational-Chern-Simons parity term that
generates g_R2_parity"** — rather than "match to some UV theory."

And it dovetails with the whole program: the candidate's *one* data-selected, rigor-uncageable degree of freedom
— the parity (v2.431/v2.420) — is exactly the deformation that carries a known UV-completable theory (string
tree-EFT) to the candidate. **The rigorous cage, the empirical verdict, and the UV embedding all point at the same
single object: the parity coupling.**

## Honest scope

"String tree-EFT" here is the *engine's framework encoder* — a schematic model of string tree-level dim-8
coefficients with O(1) values, **not** a specific compactification's computed spectrum. So "the candidate is
string tree-EFT + parity" is a statement about the engine's string-EFT *proxy* (the candidate is a small parity
deformation of the engine's best string-like point), a genuine lead — not a proof that a real string vacuum
reproduces the candidate. Actually establishing #4 still requires the real string-theory computation (a
compactification's α′ corrections + the axionic parity sector), which the engine cannot do — this cycle *scopes*
that to a specific target rather than executing it. The distance 0.067 uses the dimensionless O(1) couplings
(magnitudes toy). "Rejected only by birefringence" is contingent on the ~3.6σ hint.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_string_deformation.py --out experiments/results/v2.433/qnm_string_deformation.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_string_deformation.py -q
```

Result: string in the cage, rejected only by birefringence, parity-even distance 0.067, difference = parity;
`4 passed`.

## Next: the solve-paths are mapped and #4 given a concrete target — holding for the user's steer

#1 empirical (decision-ready), #2 rigor (at its ceiling), #4 UV-embedding (now a concrete string-compactification
target), #3 real-normalization (blocked). Executing #3/#4 needs external inputs. Flagging for the user.

## References

```text
this repo: v1.74 (nearest framework = string tree-EFT), v2.420 (parity-conserving rival), v2.431 (rigorous cage; parity = the rigor-uncageable dof), v2.386 (parity chirality), v2.329 (birefringence hint)
physics: string tree-level EFT has a UV completion; the parity deformation would come from an axionic / gravitational-Chern-Simons term in a specific compactification
```
