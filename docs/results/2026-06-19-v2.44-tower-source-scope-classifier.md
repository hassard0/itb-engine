# v2.44 - Tower source-scope classifier: guard-ready is not source-ready

**Date:** 2026-06-19
**Compute:** local run plus fresh Vulcan clone `~/codex-runs/itb-engine-main-v244a`
**Input:** v2.34, v2.37, v2.38, and v2.41 candidate evidence rows
**Artifact:** `experiments/results/v2.44/tower_source_scope_classifier_audit.json`

## Question

v2.41 added a promotion guard for known positive controls. v2.44 asks:

> Can the engine classify source scope separately from the narrower positive-control
> guard?

Yes. `src/itb/tower.py` now exposes `classify_tower_source_scope(evidence)`.

## Result

```json
{
  "candidate_count": 9,
  "positive_control_candidates": 7,
  "finite_range_candidates": ["ashmore_ruehle_quintic_kk"],
  "promotion_guard_ready_candidates": ["non_positive_control_excluding_fixture"],
  "generic_framework_claim_ready_candidates": [],
  "claimable_framework_exclusions_now": []
}
```

The classifier distinguishes:

- known string-compatible positive controls
- finite-range single-compactification evidence
- scalar Laplacian subtower evidence
- missing framework-owned endpoint metadata
- missing framework-owned displacement metadata

The synthetic non-positive-control fixture still passes the narrow promotion guard, but it
is not generic-source ready because it lacks framework-owned endpoint and displacement
metadata.

## Command

```bash
python experiments/tower_source_scope_classifier_audit.py \
  --out experiments/results/v2.44/tower_source_scope_classifier_audit.json
```

Remote verification:

```bash
python tools/vulcan.py run \
  'set -e; cd ~/codex-runs/itb-engine-main-v244a; \
   .venv/bin/python -m pytest \
     tests/test_tower_promotion_guard.py \
     tests/test_tower_source_scope_classifier_audit.py -q; \
   .venv/bin/python -m ruff check \
     src/itb/tower.py \
     experiments/tower_source_scope_classifier_audit.py \
     tests/test_tower_source_scope_classifier_audit.py; \
   .venv/bin/python experiments/tower_source_scope_classifier_audit.py \
     --out experiments/results/v2.44/tower_source_scope_classifier_audit.json'
```

## Bottom line

The repository now separates three questions that were previously tangled:

- Is the evidence schema-ready?
- Is it blocked by a known positive-control family?
- Is it source-scoped enough for a generic framework claim?

No current evidence row clears all three.
