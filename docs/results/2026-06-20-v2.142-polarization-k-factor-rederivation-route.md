# v2.142 - Polarization K-factor rederivation route specified

## Question

v2.141 corrected the Gross-Witten source trail but still could not ingest a
machine-checkable primary `K` formula.

Can the fallback rederivation path be made executable, so a future
polarization-to-spinor derivation has concrete acceptance gates before it feeds
the v2.139 Bresciani matching workbench?

## Result

Artifact:
`experiments/results/v2.142/polarization_k_factor_rederivation_route.json`

Current route status:

```text
polarization_k_factor_rederivation_route_specified_no_source_packet
```

Claimable framework exclusions now:

```text
[]
```

## Required Packet Fields

```text
source_urls
source_equation_refs
four_dimensional_policy
polarization_dictionary
source_k_formula
helicity_components
normalization
source_backed_derivation
```

Accepted four-dimensional policies:

```text
four_dimensional_on_shell_riemann_with_eom_terms_removed
four_dimensional_on_shell_weyl_ricci_flat
```

## Rederivation Stages

```text
fix_four_dimensional_policy
define_polarization_dictionary
state_source_k_formula
project_helicity_components
normalize_to_engine_lambda_r4
invert_and_check_positivity
```

## Validator Behavior

The empty packet is rejected with missing-source and missing-component blockers.

The synthetic control supplies numeric components:

```text
K_plus = 0.3
K_minus = 0.1 + 0.05 i
overall_R4_factor = 8.0
```

But it is still rejected:

```text
ready_for_k_factor_projection = false
blockers = [source_backed_derivation_missing]
```

No packet is projection-ready:

```text
ready_k_factor_projection_packets = []
```

## Interpretation

The fallback route now has executable acceptance tests. The engine can evaluate
a future K-factor rederivation packet, reject fixtures, and only feed v2.139
when `K_plus`, `K_minus`, normalization, primary-source URLs, a four-dimensional
policy, and source-backed derivation metadata are all present.

## Next Build Action

```text
derive_or_ingest_source_backed_k_rederivation_packet
```

## Local Validation

```bash
.venv/Scripts/python.exe experiments/polarization_k_factor_rederivation_route.py --out experiments/results/v2.142/polarization_k_factor_rederivation_route.json
.venv/Scripts/python.exe -m pytest tests/test_polarization_k_factor_rederivation_route.py -q
.venv/Scripts/python.exe -m ruff check experiments/polarization_k_factor_rederivation_route.py tests/test_polarization_k_factor_rederivation_route.py
.venv/Scripts/python.exe tools/build_index.py
.venv/Scripts/python.exe -m pytest -q
```

Result:

```text
wrote experiments\results\v2.142\polarization_k_factor_rederivation_route.json
5 passed
All checks passed!
wrote docs/results/INDEX.md (253 notes)
1145 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone base:

```text
59066a3
```

Generated file hashes on Vulcan:

```text
d044fa9a77d2e30bbb29c84b8a6b15f776f5bf7be6a78745dadfe1e7379577af  docs/results/INDEX.md
0a2990bb3c6d56cde6d7af9376f1439105dd011b7b4b3257b46fee112b282caa  experiments/results/v2.142/polarization_k_factor_rederivation_route.json
```

Commands:

```bash
.venv/bin/python experiments/polarization_k_factor_rederivation_route.py --out experiments/results/v2.142/polarization_k_factor_rederivation_route.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.142/polarization_k_factor_rederivation_route.json
.venv/bin/python -m pytest tests/test_polarization_k_factor_rederivation_route.py -q
.venv/bin/python -m ruff check experiments/polarization_k_factor_rederivation_route.py tests/test_polarization_k_factor_rederivation_route.py
.venv/bin/python -m pytest -q
```

Result:

```text
generated files were byte-stable
5 passed
All checks passed!
1152 passed, 1 warning
```
