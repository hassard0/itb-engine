# v2.434 - THE BOLD DECISION: the candidate's UV completion is heterotic string theory (not type II), and the parity coupling IS the heterotic axion

The deepest solve-path is #4 — identify the UV completion. v2.433 established *candidate = string tree-EFT +
parity deformation*. This cycle makes the specific, falsifiable call of **which string**, using real
string-theory facts checked against the engine. **Two independent arguments converge on heterotic.**

## Argument 1 — the candidate rigorously requires a nonzero R² term

- **Type II** superstring, tree level: **R⁴-only** leading curvature correction (no R², no R³).
- **Heterotic** string: **R² (Gauss–Bonnet) at order α′** (plus R⁴).
- **Engine fact:** given the candidate's cubic-curvature (g_R3) and parity couplings, setting the leading
  curvature coupling **g_R2 = 0 is excluded** by source-exact bounds (`graviton_forward_positivity` +
  `cemz_causality` + `cross_sector_efthedron`).

So an R⁴-only (type-II) curvature sector is **rigorously incompatible** with the candidate's R³ and parity — the
candidate is **R²-bearing = heterotic-pattern**.

## Argument 2 — the parity coupling is the heterotic axion

The candidate's single data-selected, rigor-uncageable degree of freedom — the parity coupling g_R2_parity
(v2.431/v2.420) — is a **gravitational-Chern–Simons / parity-odd curvature term** (`R∧R̃`). The heterotic string
**uniquely** carries the **model-independent axion** (from the B-field), whose Green–Schwarz coupling to `R∧R̃` is
*exactly* such a term. So the deformation that turns the parity-conserving string point (v2.433) into the
candidate is **natively present in heterotic string theory** — it is the heterotic axion.

## The decision

**The candidate = a heterotic compactification's tree-level EFT + its model-independent axion, with the parity
coupling = the axion.** (5/5 checks pass. Type II R⁴-only is a *distinct*, consistent theory — rejected by the
full stack via anomaly + birefringence — but disfavored as the candidate's UV completion because it lacks both
the required R² and the axionic parity.)

This is the boldest genuine solve-toward result the program can produce: it **names a specific UV completion**
(heterotic, not type II) and identifies the **physical origin of the candidate's one free coupling** (the
heterotic axion), turning option #4 from "match to some string vacuum" into a **specific, falsifiable target** — a
heterotic compactification whose R²:R³:R⁴ coefficients and axionic `R∧R̃` coupling land in the candidate's
rigorous cage. It also ties back to the empirical verdict: in this reading, the candidate's cosmic-birefringence
parity **is the model-independent axion's cosmological coupling** — so a birefringence detection is a detection of
the heterotic axion.

## Honest scope

A **structural identification** from real string-theory facts (type II R⁴-only vs heterotic R² + model-independent
axion — both textbook) checked against the engine, **not** a heterotic compactification computation: I have not
computed a specific heterotic vacuum's R²:R³:R⁴ coefficients or its axion coupling and shown they equal the
candidate's — that is the remaining real work, now given a specific target. The engine fact (R2=0 excluded given
R3+parity) is source-exact (v2.411 caveat). The mapping of g_R2→R²/Gauss–Bonnet, g_R3→R³, g_R2_parity→axionic
`R∧R̃` is a defensible basis identification, not a derived matching. **Type II is disfavored, not excluded** as a
theory (a loop/flux-induced R² could mimic the pattern; the clean statement is tree-level). The parity=axion
identification is well-motivated but the coefficient is toy. All prior candidate caveats carry (O(1) magnitudes,
birefringence-hint-contingent).

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_heterotic_identification.py --out experiments/results/v2.434/qnm_heterotic_identification.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_heterotic_identification.py -q
```

Result: R2=0 excluded given R3+parity; type-II distinct; decision = heterotic + axion; `5 passed`.

## Next: the UV completion is named — the concrete #4 target is a heterotic vacuum computation

The remaining real work (a heterotic compactification's coefficients vs the cage) needs a string-theory
computation the engine can't do — but it now has a specific, falsifiable target. Flagging for the user.

## References

```text
this repo: v2.433 (candidate = string tree-EFT + parity), v2.417 (matter x cubic-curvature forces R^2), v2.431 (parity = rigor-uncageable dof), v2.420 (parity-conserving rival = the string point), v2.386 (parity = gravitational chirality)
physics: type II tree-level R^4 (ζ(3)α'^3); heterotic R^2 Gauss-Bonnet at α'; heterotic model-independent axion + Green-Schwarz R∧R-tilde (parity-odd gravitational term)
```
