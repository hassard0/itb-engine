# v2.167 - Post-external-G8-spec frontier: contract ready, no real G8 packet

## Question

After v2.166 defined the exact external G8 packet needed to join the calibrated
GW alpha likelihood, how should the global discriminator frontier update?

## Result

Artifact:
`experiments/results/v2.167/post_external_g8_spec_frontier.json`

Current route status:

```text
post_external_g8_spec_frontier_no_claim_route
```

Claim-ready routes:

```text
[]
```

Current in-repo promotion-ready routes:

```text
[]
```

Alpha packet ready for G8 join:

```text
true
```

Ready current external G8 packets:

```text
[]
```

## Frontier Ranking

Priority order:

```text
1. future_public_r4_shape_likelihood_ingestion
2. external_spin4_or_detector_g8_measurement_packet_spec
3. registered_native_tower_adapter_authoring
4. weyl_g8_joint_frontier
5. gw_parity_operator_normalization_search
6. r4_symbolic_scale_resolution
```

Diagnostic-ready infrastructure routes:

```text
future_public_r4_shape_likelihood_ingestion
external_spin4_or_detector_g8_measurement_packet_spec
registered_native_tower_adapter_authoring
r4_symbolic_scale_resolution
```

The G8 route is no longer a vague measurement ask. It is a concrete packet
contract, but still blocked by:

```text
ready_current_external_g8_packet_missing
base_g8_adapter_gate_failed
alpha_join_extension_gate_failed
```

## Claim Boundary

This refresh updates route ordering and blocker wording. It does not create a
framework exclusion. The current frontier still requires real external evidence:
R4 needs a public shape-likelihood packet, G8 needs a real spin-4/detector packet
that fills v2.166, native towers need a source-owned registered-framework
adapter, Weyl/G8 needs dual likelihoods, parity needs a source-backed operator
bridge, and R4 scale needs a numeric compactification/frame policy.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/post_external_g8_spec_frontier.py --out experiments/results/v2.167/post_external_g8_spec_frontier.json
.venv/Scripts/python.exe -m pytest tests/test_post_external_g8_spec_frontier.py tests/test_external_g8_measurement_packet_spec.py tests/test_post_adapter_frontier_refresh.py -q
.venv/Scripts/python.exe -m ruff check experiments/post_external_g8_spec_frontier.py tests/test_post_external_g8_spec_frontier.py
```

Result:

```text
wrote experiments\results\v2.167\post_external_g8_spec_frontier.json
16 passed
All checks passed!
```

Full local suite:

```text
1285 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2167
base = 7689739
```

Determinism:

```text
ea6081d441d26399f4ad7fe40467688bf9920d7bf7cac34da6d353546a7cd9d4  docs/results/INDEX.md
f3c603912240a89860b477f05e3c38daf7b76903ca8b4e565f2520218382529e  experiments/results/v2.167/post_external_g8_spec_frontier.json
```

Commands:

```bash
.venv/bin/python experiments/post_external_g8_spec_frontier.py --out experiments/results/v2.167/post_external_g8_spec_frontier.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.167/post_external_g8_spec_frontier.json
.venv/bin/python -m pytest tests/test_post_external_g8_spec_frontier.py tests/test_external_g8_measurement_packet_spec.py tests/test_post_adapter_frontier_refresh.py -q
.venv/bin/python -m ruff check experiments/post_external_g8_spec_frontier.py tests/test_post_external_g8_spec_frontier.py
.venv/bin/python -m pytest -q
```

Result:

```text
16 passed
All checks passed!
1292 passed, 1 warning
```
