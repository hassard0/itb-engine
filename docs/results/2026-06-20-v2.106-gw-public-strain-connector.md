# v2.106 - GW public strain connector: URLs connected, bytes not ingested

## Question

v2.105 selected the next implementation step:

```text
connect_public_strain_to_alpha_waveform_likelihood
```

Can we connect the public GW170608 strain source at the event-version and strain
file level, while keeping byte ingestion and waveform residual work explicit?

## Result

Artifact:
`experiments/results/v2.106/gw_public_strain_connector.json`

Current route status:

```text
public_strain_urls_connected_hdf5_loader_missing
```

Claimable discriminator now:

```text
false
```

## Sources Used

| source | role |
|---|---|
| <https://gwosc.org/api/v2/event-versions/GW170608-v3> | Official GWOSC event-version record used for GWTC-1 confident `GW170608-v3`. |
| <https://gwosc.org/api/v2/event-versions/GW170608-v3/strain-files> | Official GWOSC strain-file endpoint used for H1/L1 HDF5 records. |
| <https://gwosc.org/GWTC-1/> | GWTC-1 release context and strain-data notes. |

## Connector

The selected event version is:

```text
GW170608-v3
catalog = GWTC-1-confident
detectors = H1, L1
gps = 1180922494.5
```

The connector records four 4 kHz HDF5 products:

| detector | duration | gps start | event offset | expected samples |
|---|---:|---:|---:|---:|
| H1 | 32 | 1180922479 | 15.5 | 131072 |
| L1 | 32 | 1180922479 | 15.5 | 131072 |
| H1 | 4096 | 1180920447 | 2047.5 | 16777216 |
| L1 | 4096 | 1180920447 | 2047.5 | 16777216 |

The current public connector has URL/segment readiness:

```text
public_strain_urls_ready = true
```

It is not connector-ready because:

```text
hdf5_loader_not_run
strain_bytes_not_ingested
alpha_waveform_residual_not_connected
```

## Next Build Action

```text
implement_hdf5_strain_loader_smoke_test
```

## Interpretation

This connects the route to real public GWOSC strain products without pretending
we have performed the reanalysis. The next artifact should download or read the
32-second H1/L1 HDF5 files, verify `/strain/Strain` sample counts and GPS
metadata, and expose arrays to the alpha likelihood harness.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_public_strain_connector.py --out experiments/results/v2.106/gw_public_strain_connector.json
.venv/Scripts/python.exe -m pytest tests/test_gw_public_strain_connector.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_public_strain_connector.py tests/test_gw_public_strain_connector.py
```

Focused validation passed with 6 tests. Ruff passed.

## Vulcan Reproduction

Validated on `vulcan` in a clean checkout at
`/home/admin/codex-runs/itb-engine-main-v2106`.

```bash
.venv/bin/python experiments/gw_public_strain_connector.py --out experiments/results/v2.106/gw_public_strain_connector.json
.venv/bin/python -m pytest tests/test_gw_public_strain_connector.py -q
.venv/bin/python -m ruff check experiments/gw_public_strain_connector.py tests/test_gw_public_strain_connector.py
.venv/bin/python -m pytest -q
```

Focused validation passed with 6 tests. Ruff passed. Full-suite validation
passed with 956 tests and one existing Starlette/httpx deprecation warning.
