# v2.77 - Post-GW retirement frontier: g8 is next

## Question

v2.76 retired direct Ng PPV promotion for the current engine. This iteration asks
which discriminator route should be prioritized next instead of circling the
retired GW parity path.

## Result

Artifact: `experiments/results/v2.77/post_gw_retirement_frontier.json`

Current route status:

```text
post_gw_frontier_no_claim_ready_route_g8_next
```

Claim-ready routes:

```text
none
```

Top priority:

```text
g8_high_moment_measurement
```

## Priority Order

| rank | route | status |
|---:|---|---|
| 1 | `g8_high_moment_measurement` | Measurement contract defined; external packet missing. |
| 2 | `weyl_g8_joint_frontier` | Theory frontier confirmed; joint external cuts missing. |
| 3 | `native_tower_evidence` | Adapter harness ready; native spectra missing. |
| 4 | `gw_parity_source_native_archive` | Retained as non-promoting source material. |
| 5 | `cmb_beta_em_axion` | Retired as direct gravity discriminator. |

## Interpretation

Retiring direct GW parity promotion leaves no solved discriminator. The next
best route is `g_8` high-moment measurement search because it does not require
inventing a new parity operator-normalization bridge. It asks for a cleaner
external object: a public, engine-normalized high-moment or energy-correlator
likelihood that can constrain the existing `g_8` axis.

## Reproduction

```powershell
.\.venv\Scripts\python experiments\post_gw_retirement_frontier.py --out experiments\results\v2.77\post_gw_retirement_frontier.json
.\.venv\Scripts\python -m pytest tests\test_post_gw_retirement_frontier.py tests\test_gw_parity_route_decision.py -q
.\.venv\Scripts\python -m ruff check experiments\post_gw_retirement_frontier.py tests\test_post_gw_retirement_frontier.py
.\.venv\Scripts\python -m pytest -q
```

Focused validation passed with 8 tests. Full-suite validation passed with 804
tests and one existing Starlette/httpx deprecation warning.
