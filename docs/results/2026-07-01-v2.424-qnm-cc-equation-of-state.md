# v2.424 - CORE EXTENSION CC3: the dark-energy equation of state — the candidate predicts w ≳ −1, testable by DESI/Euclid

Third slice of the cosmological-constant sector (CC1 admits dark energy bounded by g_R2; CC2 selects dS/Minkowski
over AdS). This slice reads out the **observable** consequence — the dark-energy equation of state `w`, which
distinguishes a true cosmological constant (`w = −1`) from quintessence (`w > −1`).

The refined de Sitter conjecture admits a positive vacuum energy two ways:
- **tachyonic route** (V″<0 at an extremum): a static vacuum → `w = −1` (true CC — the route CC1 encoded);
- **slope route** (M_Pl|V′|/V ≥ c): a rolling scalar → quintessence → `w = −1 + (2/3)·½(V′/V)²`.

## Result

```text
  static / true-CC route (tachyonic):     w = -1.00
  full dS-conjecture slope (c~0.8):        w = -0.79   (DISFAVORED: ~8-sigma from observed w = -1.03 +/- 0.03)
  mild scalaron-plateau quintessence:      w = -0.98
  candidate predicted w band: [-1.00, -0.98]   (static to mildly quintessent)
```

All 5/5 checks pass.

## What this establishes

The candidate's dark energy rides the **flat Starobinsky R² scalaron plateau** (g_R2, v1.86), whose near-zero
slope gives `w ≈ −1` — so the candidate predicts **w ≳ −1** (a true cosmological constant or *mild* quintessence),
consistent with current data and sitting on the same side as the **2024 DESI hint of evolving dark energy**
(w₀ slightly > −1). A *full* dS-conjecture O(1) slope would give w ~ −0.8, ~8σ from observations — **disfavored**.
This places the candidate at a genuine **tension — the same plateau-vs-swampland tension v2.410 flagged for
inflation**: the flat plateau wants `w = −1` (static, and the refined dS conjecture then needs its tachyonic
route), while the conjecture's slope condition wants `w > −1` (rolling). The observable resolution is a
**DESI/Euclid measurement of w(z)**: clean `w = −1` favors the static/tachyonic reading; a small `w > −1` (as
DESI 2024 hints) favors mild quintessence; a large `w > −0.9` would disfavor the candidate's flat-plateau dark
energy. So CC3 adds a **third observable front to the falsification portfolio** (after CMB birefringence and
CMB-S4 inflation): the dark-energy equation of state, testable *now* by DESI and soon by Euclid.

## Honest scope

An **observable-readout analysis** of the CC sector, not a new hard constraint. The `w` values depend on the
scalaron slope V′/V, which the dimensionless couplings do not fix (needs the potential + an absolute scale), so
the *specific* w numbers are illustrative order-of-magnitude — the robust content is qualitative: static → w=−1,
slope → w>−1, a full dS-conjecture O(1) slope is data-disfavored, and the candidate's flat plateau lands near
w=−1. The refined dS conjecture is conjectural (the CC sector is `sourced_proxy` throughout). The DESI-2024
evolving-DE hint is itself preliminary/contested. This is the dynamics readout, not the CC magnitude problem.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_cc_equation_of_state.py --out experiments/results/v2.424/qnm_cc_equation_of_state.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_cc_equation_of_state.py -q
```

Result: static w=−1, full-slope w=−0.79 (disfavored), candidate band [−1.00, −0.98]; `5 passed`.

## Next (v2.425): CC arc capstone or a fresh direction

The CC sector now has three slices (admits DE / selects dS / predicts w≳−1). Next: a CC capstone tying the sector
together, or a fresh direction. Bold swings, honest failures.

## References

```text
this repo: v2.422 (CC1 admits DE), v2.423 (CC2 selects dS), v2.410 (Starobinsky-vs-swampland tension), v1.86 (g_R2 scalaron), v2.421 (falsification portfolio -- w is a new front)
physics: Ooguri-Palti-Shiu-Vafa 2018 (refined dS, quintessence preference); quintessence w = -1 + (2/3)eps_V; Planck w=-1.03+/-0.03; DESI 2024 (evolving-DE hint); Euclid
```
