# v2.301 - Entanglement amplifies the irreducible ringdown: chaining monogamy with the moment tower

## Question

A fresh swing combining the two new sectors. v2.300 found entanglement monogamy and positivity cap the
leading curvature coupling `g_R2` (harmonic vs geometric mean). v2.292 found the curvature moment tower
mandates the ringdown-active `g_R4 >= g_R3^2/g_R2`. Chained together, do entanglement and the moment
tower say something new about the OBSERVABLE ringdown operator?

## Result (entanglement amplifies the irreducible ringdown floor)

The smallest `g_R4` a consistent theory can carry (for given `g_R3`) is `g_R3^2` divided by the LARGEST
allowed `g_R2`:

```text
  g_R4_floor = g_R3^2 / g_R2_max ,   g_R2_max = min( entanglement_bound , positivity_bound )
```

So the tighter cap on `g_R2` raises the ringdown floor. The amplification over positivity alone is

```text
  g_R4_floor(combined) / g_R4_floor(positivity) = positivity_bound / entanglement_bound
      = (g_4 + g_6) / (3 sqrt(g_4 g_6)) = (2/3) * AM/GM(g_4, g_6)
```

```text
  framework          binding sector   g_R4 floor   positivity-only   amplification
  string_tree_eft    positivity       0.0503       0.0503            1.000
  asymptotic_safety  positivity       0.0289       0.0289            1.000
  cdt                positivity       0.0480       0.0480            1.000
  lqg_induced        positivity       0.1732       0.1732            1.000

  asymmetric UV point (g_4=5, g_6=0.5, g_R3=0.4):
     floor 0.1173  vs  positivity-only 0.1012   ->   entanglement AMPLIFIES x1.160
```

For the engine's frameworks (all mildly asymmetric) positivity is the binding cap, so their ringdown
floor is the positivity value and entanglement adds no amplification (factor 1). But for an asymmetric
matter sector entanglement is the tighter cap and RAISES the ringdown floor by `(2/3) AM/GM = 1.16` --
entanglement forces a larger minimum Riemann^4 deformation than unitarity and causality alone require.
All 5/5 checks pass.

## What this establishes

The information-theoretic and amplitude sectors do not just bound the leading curvature complementarily
(v2.300); chained through the moment tower they make a sharper statement about the OBSERVABLE ringdown
operator: in the asymmetric regime where entanglement bites, a consistent higher-curvature gravity must
ring MORE than positivity predicts. This is the "it from qubit" structure reaching a gravitational-wave
observable through the consistency chain -- entanglement amplifying the irreducible ringdown signature.

## Honest scope

An exact chaining of two encoded bounds (BNOSSW monogamy prefactor `1/3`, positivity `kappa = 1`) with
the v2.292/v2.234 moment-tower floor -- algebra, verified. The amplification factor `(2/3) AM/GM` is
prefactor-ROBUST in form (`AM/GM >= 1` always, so entanglement amplifies whenever it is the binding cap),
though the threshold `AM/GM = 1.5` and the crossover shift with the O(1) prefactors (as in v2.300). The
"ringdown floor" is on the dimensionless coupling `g_R4`; mapping it to an actual QNM deformation carries
the v2.215 sensitivity, the v2.209 dark-parity caveat, and the EFT length-scale, so "rings MORE" is the
COUPLING-level statement, with the observable amplitude honestly uncertain. The engine's frameworks are
positivity-floored (amplification 1); the amplification is realized only for asymmetric matter sectors.
Toy basis, O(1) prefactors. A fresh-sector new-theory result chaining entanglement to the ringdown
observable.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_entanglement_amplified_ringdown.py --out experiments/results/v2.301/qnm_entanglement_amplified_ringdown.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_entanglement_amplified_ringdown.py -q
```

Result: frameworks are positivity-floored (amplification 1); the asymmetric point shows entanglement
raising the ringdown floor by `(2/3) AM/GM = 1.16`; `6 passed`.

## Next (v2.302)

Fresh-sector swings continue -- the holographic / quantum-focusing constraints vs positivity, the
generalized second law as a curvature bound, or another new direction. Honest negatives reported as part
of the search.

## References

```text
this repo: v2.300 (entanglement vs positivity), v2.292 (moment-tower g_R4 floor), v2.234 (g_R4 mandate)
Bao, Nezami, Ooguri, Stoica, Sully, Walter, 'The Holographic Entropy Cone' (2015); Caron-Huot et al 2021
```
