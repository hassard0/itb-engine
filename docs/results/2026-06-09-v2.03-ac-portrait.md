# v2.03 — The holographic (a,c) RG portrait: the conformal-collider plane that unifies the program

**Date:** 2026-06-09
**Compute:** Vulcan.

---

## One plane, the whole program

The Euler/Weyl² basis split (v1.71) turned out to be the spine of the engine. v2.03 draws
the **(a, c) = (g_R2, g_C)** plane once, with everything the program learned about it
overlaid — a single figure (`experiments/ac_portrait.png`) that ties six cycles together.

## What's on the plane

- **The Hofman–Maldacena wedge** `1/3 ≤ a/c ≤ 31/18` (shaded). The endpoints are *free
  fields*: the floor a/c=1/3 is the **free scalar**, the ceiling a/c=31/18 is the **free
  vector** — the HM bounds are saturated by free-field content. [v1.71]
- **The a/c=1 diagonal** — the **symmetric double copy** (N=8-SUGRA-like, a=c). [v1.94]
- **η/s contours** — `1 − 4π(η/s) = (c−a)/c` ⟹ **η/s (KSS units) = a/c**, so constant-a/c
  rays are constant-η/s lines; a/c < 1 is a KSS-violating dual. [v1.72]
- **The RG flow** (v1.89 β-functions) as streamlines: β_a = −0.4(a−0.15), β_c = −0.8c²
  (Weyl² asymptotically free) flowing to the UV fixed point (a*=0.15, c*=0) — with the
  **a-theorem** requiring instead a* ≥ 0.45 (the toy fixed point is too low). [v1.99]
- **The 14 frameworks** (a=g_R2, c=g_C), the **data-driven EFT** (★), and the **v1.74
  island center** (✚).

## What the portrait shows

| structural fact | reading off the plane |
|---|---|
| consistency | **12/12 curvature frameworks sit inside the HM wedge** |
| double copy | all sit **below the a/c=1 diagonal** → asymmetric double copies (v1.94) |
| viscosity | a/c < 1 ⟹ **η/s < 1/4π**, KSS-violating duals (v1.72) |
| complexity | c > 0 ⟹ **dC/dt > 1**, Lloyd super-saturation (v1.98) |
| RG flow | streamlines push toward a low UV fixed point the a-theorem **forbids** (needs a*≥0.45, v1.99) |
| what to bound | the **vertical c-axis (Weyl²) is the fattest island direction** (v2.02) |

The frameworks form a tight cluster **inside the wedge, below the diagonal, in the
KSS-violating, complexity-super-saturating, sub-a*=0.45 region** — a single coherent
picture of "what a consistent higher-derivative gravity looks like" in the conformal-
collider plane. The most a=c-balanced (most double-copy-like) framework and the island
center both sit just below the diagonal at a/c ≈ 0.85–0.92.

## Why this matters

The (a,c) plane is where the program's threads converge: amplitude positivity (the wedge),
the double copy (the diagonal), transport (η/s rays), thermodynamics/complexity (the c-axis),
the RG/a-theorem (the flow), and the experimental priority (the fattest direction). The
Euler-vs-Weyl² split — the engine's single most productive modeling choice — is *visibly*
the organizing axis. A reader who internalizes this one figure understands the spine of the
whole program.

## Honest scope

Toy a=g_R2, c=g_C identification + the schematic v1.89 flow; the free-field wedge endpoints
are exact textbook ratios, but the framework coordinates and the flow are toy. The robust
content is the **unifying structure** — wedge, diagonal, η/s rays, a-theorem direction, the
framework cluster — in one plane.

## What's committed

- `experiments/ac_portrait.py` — the master (a,c) portrait (wedge, diagonal, η/s contours,
  RG streamlines, frameworks, island center, UV fixed point) + the framework table.
- `tests/test_ac_portrait.py` — 3 tests (wedge endpoints = free-field ratios; a survivor in
  the wedge; the η/s = a/c relation). Suite **537 green**.

## Headline

A single conformal-collider **(a,c) portrait** unifies the program: the 12 consistent
frameworks cluster **inside the Hofman–Maldacena wedge, below the a/c=1 double-copy
diagonal, as KSS-violating, complexity-super-saturating duals**, while the RG flow drives
toward a UV fixed point the a-theorem forbids (a* ≥ 0.45) and the **Weyl² c-axis** is the
direction most worth bounding next. The Euler-vs-Weyl² split is the visible spine of the
whole engine.

## Reproducibility

```bash
python experiments/ac_portrait.py
pytest tests/test_ac_portrait.py -q
```
