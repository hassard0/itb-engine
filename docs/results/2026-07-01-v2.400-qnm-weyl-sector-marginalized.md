# v2.400 - SWING (marginalize over the c-a modulus): the Weyl^2-sector headlines survive the a=c assumption; only their magnitudes loosen

## Question

v2.399 downgraded a=c to a framework assumption and exposed the c-a split as a free modulus (bounded only by
the Hofman-Maldacena wedge), so every Weyl^2-sector result computed *at* a=c carries that modulus. Does the a=c
assumption threaten the qualitative headlines, or only the numbers?

## Result (headlines survive; magnitudes loosen ~2x)

Marginalizing over the whole modulus (`g_C in [0.112, 0.579]` at `g_R2 = 0.193`):

```text
  (1) extremal-BH entropy Delta S_ext = A g_C + B g_4 (v2.378):
        a=c 0.458  ->  marginalized [0.377, 0.843]   POSITIVE throughout  -> extremal BHs DECAY (robust)
  (2) Weyl^2 ghost mass m_g/Lambda = 1/sqrt(g_C) (v2.385):
        a=c 2.28   ->  marginalized [1.31, 2.99]      ABOVE cutoff throughout -> ghost-safe (robust)
  (3) species cutoff Lambda/M_Pl (v2.394):
        a=c 0.716  ->  marginalized [0.606, 0.747]    near-Planckian (0.5,1) throughout (robust)
```

All 5/5 checks pass. The c-a modulus widens the error bars by ~2x but never flips a sign or crosses a
threshold.

## What this establishes

The honest resolution of v2.399's flag: downgrading a=c to an assumption does **not** threaten the earlier
qualitative results -- BH decay, ghost-safety, and the near-Planckian cutoff are all robust to it; the modulus
only widens their magnitude uncertainty. This **closes the c!=a arc**:

- v2.397 diagnosed the c-a degeneracy (g_R2 drives 62% of constraints; HM trivial) and recommended resolving it;
- v2.398 activated the Weyl^2 axis `g_C` (found the machinery built-but-dormant; HM the sole c-axis carver);
- v2.399 showed a=c is an assumption, not a prediction (c-a a free modulus in the HM wedge);
- v2.400 marginalizes the Weyl^2-sector predictions over that modulus and finds the headlines survive.

So resolving the basis's key limitation neither breaks the candidate nor forces it off a=c -- it makes explicit
that the Weyl^2-sector magnitudes carry a conformal-collider-bounded uncertainty the degenerate basis had
hidden. The candidate's qualitative profile -- string-like, matter-dominant, WGC-complete, ghost-safe,
near-Planckian, decaying extremal black holes -- stands independent of the a=c assumption; the resolved basis
adds honest error bars, not a different theory.

## Honest scope

The marginalization varies only the newly-freed `g_C` across its HM wedge at FIXED `g_R2, g_4, g_R3` -- exactly
the modulus v2.399 exposed; a fuller marginalization over all couplings holds a fortiori because each
prediction is monotone in `g_C` and stays on-side at both wedge edges. The predictions use the engine's toy
coefficients (A=1, B=0.5; `1/sqrt(g_C)` O(1)-schematic; nu=2), so the magnitudes and their ~2x widening are
toy-basis; the ROBUST content is that every headline stays on the correct side of its threshold across the
entire conformal-collider-allowed range -- a sign/ordering statement, not a magnitude one. This marginalizes
prior toy-encoded results over a prior (source-cited) wedge, adding no new datum; its value is closing the
c!=a arc responsibly. Robust content: marginalizing the Weyl^2-sector predictions over the c-a modulus loosens
their magnitudes ~2x but preserves every qualitative headline.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_weyl_sector_marginalized.py --out experiments/results/v2.400/qnm_weyl_sector_marginalized.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_weyl_sector_marginalized.py -q
```

Result: Delta S_ext [0.38, 0.84] (>0), ghost [1.31, 2.99] (>1), cutoff [0.61, 0.75] (near-Planckian); all
headlines survive; `5 passed`.

## Next (v2.401)

The c!=a arc is closed. Next: a fresh swing in a new direction. Bold swings, honest failures.

## References

```text
this repo: v2.399 (a=c assumption / c-a modulus), v2.398 (activated c-axis / HM wedge), v2.378 (BH entropy), v2.385 (ghost), v2.394 (species cutoff), v2.397 (c-a degeneracy diagnosis)
physics: Hofman-Maldacena 2008 (a/c wedge); Cheung-Liu-Remmen (BH entropy); Dvali (species scale)
```
