# v2.418 - even the "one residual toy" is mostly rigorous+data: the parity magnitude is rigorously capped and data-pinned

## The move

The de-toying arc (v2.411–417) concluded the candidate's single residual toy is the parity-magnitude coefficient
(the anomaly-inflow ρ). This cycle dissects that magnitude by scanning g_R2_parity (other couplings at candidate
values) under nested stacks.

## Result

```text
  rigorous+implied (no data, no toy)             :  g_R2_parity in [0.0, 0.267]   <- RIGOROUS CEILING (left_handed_graviton_positivity)
  rigorous+implied + birefringence DATA (no toy) :  g_R2_parity in [0.048, 0.152] <- DATA pins both sides
  full (+ toy anomaly-inflow)                    :  g_R2_parity in [0.048, 0.078]  (toy cap sqrt(rho*g_4*g_R2)=0.078)
  candidate value                                =  0.06   (feasible WITHOUT the toy)
```

All 5/5 checks pass.

## What this establishes

The parity magnitude is far more rigorous than "one toy coefficient" suggested: **(1)** it has a **rigorous
ceiling** g_R2_parity ≤ 0.267 from parity-decomposed (left-handed graviton) positivity — amplitude positivity
caps how parity-violating the theory can be, with the candidate ~4× below it (consistent with v2.387); **(2)** the
**cosmic-birefringence data** pins it to [0.048, 0.152], setting the lower edge and most of the upper; **(3)** the
**toy** anomaly-inflow coefficient's *only* effect is tightening the upper edge from 0.152 to 0.078. Crucially the
candidate value **0.06 is feasible on the rigorous ceiling + data alone** — it lies in [0.048, 0.152] without the
toy — so the toy does *not* determine the candidate's parity value. Combined with the sign (chirality) being
rigorous+data (v2.386/364), the candidate's parity coupling rests on rigorous positivity (ceiling + sign) and
real data (birefringence); the lone toy plays a minor, non-value-determining role. So the whole de-toying arc's
bottom line strengthens: **the candidate QG EFT is essentially rigorous + data throughout** — matter-gravity
structure source-exact (v2.411–417), and even the parity magnitude rigorously bounded and data-pinned. The one
constraint whose *form* is toy turns out not to set any candidate value, only to sharpen a bound the data already
imposes.

## Honest scope

Windows are 1D scans of g_R2_parity with the other five couplings at candidate values, so edges are at that
slice; the qualitative decomposition (rigorous ceiling / data window / toy upper-edge tightening) is the robust
content. The rigorous ceiling carries the v2.411 "source-exact in form" caveat. The birefringence "data" window is
itself contingent on the ~3.6σ birefringence hint (v2.408/329) — "data-pinned" means pinned by that measurement,
which could still evaporate; the point is that the pin is *data*, not the toy. The toy cap 0.078 matches its
analytic value √(ρ·g_4·g_R2), confirming its role is exactly the parity² ≤ ρ·g_4·g_R2 upper bound. This doesn't
make ρ "real"; it shows the candidate value doesn't depend on it.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_parity_window.py --out experiments/results/v2.418/qnm_parity_window.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_parity_window.py -q
```

Result: rigorous ceiling 0.267; data window [0.048, 0.152]; toy tightens upper to 0.078; candidate 0.06 needs no
toy; `5 passed`.

## Next (v2.419): continue swings

The parity magnitude is now decomposed (rigorous ceiling + data pin + minor toy edge). Next: a fresh swing. Bold
swings, honest failures.

## References

```text
this repo: v2.414-417 (de-toying arc -> parity magnitude as residual toy), v2.387 (parity ~4x below its cap), v2.386/364 (parity sign/chirality), v2.408/329 (birefringence datum/hint)
physics: parity-decomposed amplitude positivity (Caron-Huot-de Rham-Tolley-Zhou 2024); cosmic birefringence beta=0.34+/-0.09 deg (Minami-Komatsu / Eskilt-Komatsu)
```
