# v2.103 - GW alpha interval surrogate: covariance fixture, not evidence

## Question

v2.102 selected alpha-bar likelihood reproduction as the next build target. Can
we at least build a reproducible covariance fixture from the published
`alpha_bar_1` / `alpha_bar_2` intervals without pretending it is a real
source-native likelihood?

## Result

Artifact:
`experiments/results/v2.103/gw_alpha_interval_surrogate.json`

Current route status:

```text
alpha_interval_surrogate_built_nonclaiming_reanalysis_required
```

Claimable discriminator now:

```text
false
```

## Sources Used

| source | role |
|---|---|
| <https://arxiv.org/abs/2407.08929> | Published marginal `alpha_bar_1` and `alpha_bar_2` intervals used to build the development-only surrogate. |

## Surrogate

The artifact converts central 90% intervals into one-dimensional Gaussian sigma
estimates using:

```text
z = 1.6448536269514722
```

| parameter | sigma minus | sigma plus | sigma avg | asymmetry |
|---|---:|---:|---:|---:|
| `alpha_bar_1` | 0.6261955369 | 1.1855158222 | 0.9058556795 | 1.8932038835 |
| `alpha_bar_2` | 1.7752339492 | 2.5047821475 | 2.1400080483 | 1.4109589041 |

The resulting diagonal covariance fixture is:

```text
[[0.8205745121703398, 0.0],
 [0.0, 4.579634446914725]]
```

## Gate Result

The v2.102 source-native adapter correctly rejects this surrogate as a claim
source:

```text
source_native_likelihood_export_missing
systematics_not_closed
shared_eft_domain_not_bounded
```

The covariance is useful for development fixtures, plotting, and downstream
schema tests. It is not a posterior, has no correlation information, and does
not close waveform/systematics requirements.

## Next Build Action

```text
build_public_gw170608_reanalysis_run_manifest
```

## Interpretation

This loop made forward progress without lying about the evidence. We now have a
machine-readable alpha covariance fixture and a machine-readable proof that it
cannot be promoted. The next run should specify the actual public reanalysis
manifest: strain inputs, validation posteriors, waveform implementation,
sampler settings, and expected alpha likelihood outputs.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_alpha_interval_surrogate.py --out experiments/results/v2.103/gw_alpha_interval_surrogate.json
.venv/Scripts/python.exe -m pytest tests/test_gw_alpha_interval_surrogate.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_alpha_interval_surrogate.py tests/test_gw_alpha_interval_surrogate.py
```

Focused validation passed with 6 tests. Ruff passed.

## Vulcan Reproduction

Validated on `vulcan` in a clean checkout at
`/home/admin/codex-runs/itb-engine-main-v2103`.

```bash
.venv/bin/python experiments/gw_alpha_interval_surrogate.py --out experiments/results/v2.103/gw_alpha_interval_surrogate.json
.venv/bin/python -m pytest tests/test_gw_alpha_interval_surrogate.py -q
.venv/bin/python -m ruff check experiments/gw_alpha_interval_surrogate.py tests/test_gw_alpha_interval_surrogate.py
.venv/bin/python -m pytest -q
```

Focused validation passed with 6 tests. Ruff passed. Full-suite validation
passed with 938 tests and one existing Starlette/httpx deprecation warning.
