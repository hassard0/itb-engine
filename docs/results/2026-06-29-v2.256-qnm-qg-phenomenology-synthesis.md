# v2.256 - Synthesis: the QG-phenomenology + swampland arc, cross-verified

## Question

The session's third sub-program (v2.251-v2.255) pivoted off black holes to ask, sector by sector:
what does quantum gravity PREDICT observably (phenomenology), and which low-energy theories admit a
QG completion (consistency / swampland)? Map the arc and GUARANTEE its internal consistency.

## Cross-arc consistency (the verifiable content)

```text
  [OK] M_Pl(reduced, v2.253) == M_Pl(full, v2.254) / sqrt(8 pi)
  [OK] E_Planck(v2.251) == M_Pl(full, v2.254)
  [OK] SDC: r=0.036 (v2.253 bound) is trans-Planckian (Delta phi > M_Pl)
  [OK] engine reconnection: itb.constraints.cosmic_birefringence exists
  [OK] engine reconnection: itb.constraints.distance_conjecture exists
  [OK] engine reconnection: itb.constraints.eft_validity exists

  all_consistent = True
```

The reduced Planck mass (inflation, v2.253) and the full Planck mass (WGC, v2.254) agree via the
`sqrt(8 pi)` factor; the dispersion `E_Planck` (v2.251) equals the WGC Planck mass; the Distance
Conjecture (v2.255) correctly flags the current `r` bound (v2.253) as trans-Planckian; and the
engine's swampland/parity constraints exist.

## The arc map (two axes)

```text
  QG PHENOMENOLOGY (what QG predicts observably)
    v2.251   LIV dispersion (propagation)    energy-dependent speed -> GRB bounds E_QG ~ E_Planck
    v2.252   vacuum birefringence (parity)   dim-5 parity violation -> xi < ~1e-17 (GRB polarimetry)
    v2.253   primordial GWs (early universe) tensor r -> inflation ~GUT; r>0 = graviton quantization

  QG CONSISTENCY (which theories admit a QG completion / swampland)
    v2.254   Weak Gravity Conjecture         gravity is the weakest force; the SM satisfies it ~1e43
    v2.255   Swampland Distance Conjecture   light tower -> predicts small r (via the Lyth bound)
```

**The two axes MEET in the tensor sector:** the Swampland Distance Conjecture (consistency) predicts
small `r`, which the primordial-GW channel (phenomenology) measures -- a rare case where a swampland
criterion and an observation constrain the same number. With v2.242 (strong-field GR) and v2.250
(black-hole hypothesis tests), the session now has THREE cross-verified synthesis capstones.

## What this establishes

A verified-consistent, navigable synthesis of the QG-phenomenology + swampland arc, confirming the
modules share the Planck scale consistently (reduced and full), linking the inflation and swampland
cycles through `r`, and confirming the engine reconnections. It organizes the session's
beyond-black-hole QG work into the phenomenology-vs-consistency structure.

## Honest scope

A SYNTHESIS / consistency capstone, not a new measurement. The per-cycle caveats carry
(order-of-magnitude time-of-flight and birefringence bounds; slow-roll inflation relations; the WGC
and SDC are conjectures with O(1) / mild-vs-strong ambiguities; the Lyth coefficient convention).
Self-contained reconstructions of real QG-phenomenology and swampland criteria, not detection claims
or published bounds. Parity-odd `g_R4_c3` stays dark (v2.209).

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_qg_phenomenology_synthesis.py --out experiments/results/v2.256/qnm_qg_phenomenology_synthesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_qg_phenomenology_synthesis.py -q
```

Result: 6/6 cross-arc consistency checks pass (`all_consistent = True`); two-axis / five-cycle map;
`4 passed`.

## Next (v2.257)

With three synthesis capstones, the open high-value directions remain the two deferred efforts --
(a) the user-authorized `g_R4` core-engine extension (scoped v2.234), or (b) the Teukolsky/Leaver
precise QNM solver -- or (c) a fresh thread (another QG observable, or a return to the repo's core
multi-constraint engine).

## References

```text
this repo: v2.251-v2.255 result notes (docs/results/), docs/results/INDEX.md
```
