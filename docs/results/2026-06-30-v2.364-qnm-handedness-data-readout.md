# v2.364 - Is the constructed theory's RIGHT-handedness a prediction or a data-readout? (a pure data-readout)

## Question

v2.352 showed the parity-odd cubic is symmetric (unpredicted); v2.360 showed the parity-odd quadratic's
magnitude is data-pinned. The remaining parity question: the constructed theory violates parity RIGHT-handedly
(`g_R2_parity = +0.06 > 0`) -- is that SIGN a theoretical prediction, or set entirely by the data? Unlike the
cubic, the engine has explicitly handedness-sensitive theoretical constraints (left/right-handed graviton
positivity), so the answer is not obvious.

## Result (a pure data-readout)

Flipping the sign at the constructed point (`g_R2_parity -> -g_R2_parity`):

```text
  handedness-sensitive theoretical constraints:  left/right-handed graviton positivity (they SWAP: 0.148 <-> 0.194)
  min theoretical margin:   +g_R2_parity 0.0025  ==  -g_R2_parity 0.0025   (no net preference)
  theoretically feasible:   +Yes,  -Yes           (both signs OK)
  birefringence margin:     +0.044,  -(-0.364)     (only the DATA breaks the tie)
```

All 5/5 checks pass. The left/right graviton positivities DO break the reflection symmetry individually, but
they are **mirror-paired** -- the swap leaves the *min* theoretical margin identical for both signs, so the
theory has **no net preference** for either handedness. Both signs are theoretically feasible; only cosmic
birefringence (`beta = +0.34 > 0`) breaks the tie, forcing right-handed.

## What this establishes

The constructed theory's right-handedness is read off the sign of the measurement, not predicted: had
birefringence measured `beta < 0`, the engine would build an equally-consistent **left-handed** theory. This
completes the honest anatomy of the parity-odd quadratic -- its **existence** (v2.321: beta=0 excluded), its
**magnitude** (v2.360: within ~0.3-sigma of the data), and now its **sign** are ALL data-driven. The theory
contributes only the **correlations**: the anomaly budget linking parity to the matter+curvature sector
(v2.335/v2.350/v2.357) and the left/right-graviton-positivity structure that makes both handedness equally
consistent -- not the parity sector's values. It is the sharpest statement of the v2.329 dependence: the
parity headline is *the data, structured by the theory*, not a parity prediction the theory makes on its own.

## Honest scope

Exact: the margins are the engine's literal check() output at the constructed point with the sign flipped;
the left/right swap and the identical min-theoretical-margin are numerical facts (not a fit); "both signs
theoretically feasible" is a direct satisfaction check. The result is a statement about the ENGINE's
encoding: the left/right graviton positivity constraints are constructed as a mirror pair (kappa symmetric) --
the physically natural choice (theoretical consistency treats both handedness equally), but an encoding
choice. A genuinely parity-VIOLATING theoretical constraint (e.g. a chiral anomaly preferring one handedness)
would break the tie theoretically, and none is in the engine. The birefringence sign and its map are the
toy/data inputs (v2.347/v2.329). Robust content: the theory's consistency is handedness-symmetric as a pair,
so the sign is data-set; the specific margins are toy-basis numbers. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_handedness_data_readout.py --out experiments/results/v2.364/qnm_handedness_data_readout.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_handedness_data_readout.py -q
```

Result: left/right graviton positivities swap under the flip but the min theoretical margin is identical for
both signs; both signs theoretically feasible; only birefringence breaks the tie (+0.044 vs violated);
`5 passed`.

## Next (v2.365)

A fresh sector or continued audits, at the <=10-minute cadence. Honest negatives reported as part of the
search.

## References

```text
this repo: v2.352 (parity-odd cubic symmetric/unpredicted), v2.360 (quadratic magnitude data-pinned), v2.321 (beta=0 excluded), v2.329 (the single point of failure)
this repo: src/itb/constraints/parity_violation.py (left/right handed graviton positivity, the mirror pair); v2.335/v2.350/v2.357 (the anomaly correlations the theory DOES supply)
```
