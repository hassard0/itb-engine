# v2.131 - Gravity R4 axis candidate: source preserved, non-claiming

## Question

v2.130 selected the next implementation step:

```text
register_bresciani_v2_as_gravity_r4_axis_extension_candidate
```

Can the useful Bresciani v2 formalism be preserved as a new gravity-sector axis
candidate without falsely promoting it as engine `g_8`?

## Result

Artifact:
`experiments/results/v2.131/gravity_r4_axis_extension_candidate.json`

Current route status:

```text
gravity_r4_axis_candidate_registered_nonclaiming
```

Claimable discriminator now:

```text
false
```

## Candidate Contract

The candidate registers the source-backed S=2 coefficients:

```text
g_R4_c1 <- c_1^(2)  for (Mpl^2 * Riemann^2)^2
g_R4_c2 <- c_2^(2)  for (Mpl^2 * Riemann * Riemann_dual)^2
g_R4_c3 <- c_3^(2)  for the mixed Riemann^2 / Riemann-dual term
```

It also preserves the source combinations:

```text
g_R4_plus      <- c_1^(2) + c_2^(2)
g_R4_minus_abs <- abs(c_1^(2) - c_2^(2) + i*c_3^(2))
```

Source equations retained:

```text
s^4/(2*pi) * (abs(c_plus) + 1.4*abs(c_minus)) <= 1
c_1^(2) >= 0
c_2^(2) >= 0
(c_3^(2))^2 <= 4*c_1^(2)*c_2^(2)
```

## Result Status

```text
candidate_registered = true
axis_contract_ready = false
claimable_discriminator_now = false
```

Promotion blockers:

```text
r4_dimensionless_engine_normalization_missing
registered_framework_r4_projection_missing
r4_measurement_likelihood_missing
r4_engine_constraint_integration_missing
r4_claim_guard_missing
```

## Next Build Action

```text
derive_framework_r4_projection_requirements
```

## Interpretation

The source is now preserved in the research program without abusing the `g_8`
contract. This opens a clean gravity-sector extension route, but the candidate
cannot enter discriminator math until the engine defines normalization,
framework projections, measurements or likelihoods, and a promotion guard.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gravity_r4_axis_extension_candidate.py --out experiments/results/v2.131/gravity_r4_axis_extension_candidate.json
.venv/Scripts/python.exe -m pytest tests/test_gravity_r4_axis_extension_candidate.py -q
.venv/Scripts/python.exe -m ruff check experiments/gravity_r4_axis_extension_candidate.py tests/test_gravity_r4_axis_extension_candidate.py
.venv/Scripts/python.exe -m pytest
```

Result:

```text
5 passed
All checks passed!
1083 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Validated on Vulcan (`admin@192.168.4.178`) from a fresh clone of
`origin/main` at `c09f8df`, with the v2.131 patch applied using
`git apply --index`.

Setup:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -e ".[dev]" lalsuite
```

Commands:

```bash
.venv/bin/python experiments/gravity_r4_axis_extension_candidate.py --out experiments/results/v2.131/gravity_r4_axis_extension_candidate.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.131/gravity_r4_axis_extension_candidate.json
sha256sum docs/results/INDEX.md experiments/results/v2.131/gravity_r4_axis_extension_candidate.json
.venv/bin/python -m pytest tests/test_gravity_r4_axis_extension_candidate.py -q
.venv/bin/python -m ruff check experiments/gravity_r4_axis_extension_candidate.py tests/test_gravity_r4_axis_extension_candidate.py
.venv/bin/python -m pytest -q
```

Hashes after deterministic regeneration:

```text
edbb176c144c1243b1aa01c08f527d94ac05438581504fe7fed71c24df6a22af  docs/results/INDEX.md
da58d1b511480b05c7148e15549209f765189ba29523ec8047dfd5fb7e5b626d  experiments/results/v2.131/gravity_r4_axis_extension_candidate.json
```

Result:

```text
5 passed
All checks passed!
1090 passed, 1 warning
```
