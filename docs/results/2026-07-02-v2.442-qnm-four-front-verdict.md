# v2.442 - the four-experiment / three-keystone unified verdict: LiteBIRD r and DESI w both test the g_R2 keystone, so the candidate is over-determined

Dreaming, consolidating. v2.441 gave the candidate a fourth near-term front (LiteBIRD r ≈ 0.004 / n_s ≈ 0.964 from
the g_R2 Starobinsky plateau). Unifying all four reveals a sharp structure — **four experiments, only three
keystone couplings** — because g_R2 drives *two* of the fronts.

## The four fronts → three keystones

```text
  experiment                    keystone        candidate prediction
  1. CMB birefringence          g_R2_parity     beta > 0 (positive handedness)
  2. CMB-S4 matter              g_4             matter-dominant signal present
  3. DESI/Euclid dark energy    g_R2            w > -1 (mild quintessence plateau)
  4. LiteBIRD tensors           g_R2            r ~ 0.004, n_s ~ 0.964 (Starobinsky plateau)

  independent keystones: {parity, matter, g_R2} = 3  ->  candidate = 1 of 2^3 = 8 sign-patterns
  g_R2 OVER-DETERMINED by DESI (w) AND LiteBIRD (r)
```

All 5/5 checks pass.

## The sharp new falsifier

Fronts 3 and 4 are **locked together** by the single g_R2 > 0 keystone: `w > -1` and the Starobinsky plateau
(`r ≈ 0.004`) must **co-occur**, because the same positive R² scalaron gives the late-time *and* early-time
plateaus. So **g_R2 is over-determined** — measured by two independent experiments:

> If LiteBIRD sees r ≈ 0.004 (plateau) but DESI sees w < −1 (phantom), or vice versa, **the single-scalaron story
> is falsified** — even though each front alone looks fine. A joint (r ≈ 0.004) AND (w > −1) confirms g_R2 twice
> over.

This is the payoff of the inflation front: it doesn't just add a fourth independent test, it turns the **g_R2
keystone** — the same coupling that is rigorously forced (v2.417), caps gravity (v2.412), and is the leading
UV-completion discriminant (v2.434–440) — into a **doubly-measured, internally-checkable** prediction. The
candidate's cosmological history (R² inflation → R² dark energy) becomes a **single falsifiable arc**, not two
separate claims. Across the three independent keystones the candidate is still 1 of 8 sign-patterns (maximally
falsifiable, per v2.430) — the fourth experiment adds the internal g_R2 cross-check on top.

## Honest scope

A **structural / falsifiability-accounting** cycle, not a new numerical prediction: it re-partitions the four
fronts by keystone and identifies the over-determination. "Present/absent" uses the sign structure (each front
keyed to a coupling being positive) — the robust content; magnitudes (β, r, w) remain O(1)-toy / plateau-class
(v2.441's caveats carry: r ≈ 0.004 is the Starobinsky value *any* positive-plateau model shares; w > −1 is the
refined-dS-conjecture proxy, sourced_proxy-tier). "Over-determined" correctly means two experiments probe the same
coupling — a genuine consistency cross-check — but it does **not** make r and w numerically predict each other
(they are the same sign/plateau-class signature at two epochs, not a computed r(w) relation; a unified g_R2
potential across both epochs is not computed). "1 of 8" counts the three independent keystones, consistent with
v2.430; the fourth experiment *sharpens* rather than adding a fourth independent axis.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_four_front_verdict.py --out experiments/results/v2.442/qnm_four_front_verdict.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_four_front_verdict.py -q
```

Result: four experiments, three keystones; g_R2 over-determined by DESI + LiteBIRD; 1 of 8 patterns; `5 passed`.

## Next: the candidate is a single falsifiable cosmological arc — keep dreaming

Four experiments, three keystones, a doubly-checked g_R2. Next swings: the primordial imprint of the
near-inflation-scale tower, or another bold principle. Bold swings, honest failures.

## References

```text
this repo: v2.441 (inflation front), v2.430 (three-front verdict), v2.417 (g_R2 forced), v2.412 (g_R2 caps gravity), v2.422-425 (g_R2 dark energy), v2.434-440 (g_R2 / UV completion)
physics: Starobinsky r=12/N^2 ~ 0.004; refined dS conjecture w >~ -1; cosmic birefringence beta; LiteBIRD / DESI-Euclid / CMB-S4 / CMB-EB (~2030s)
```
