# v2.437 - the asymptotic-safety discriminator fails at low energy: the species scale can't tell a fixed point from a string — the UV-completion degeneracy is robust, and the genuine discriminator is the tower spectrum

Bold swing on a genuinely different solve-path (**asymptotic safety** = a UV fixed point, no tower; vs string/CDT
= a fundamental scale + a tower). The natural discriminator is the **species scale**: a bare fixed-point theory
should be UV-complete with *no* sub-Planckian cutoff (N → 1), while a string/CDT theory has a tower (N > 1). Does
it separate them?

## Result — no (an honest negative)

```text
  framework          N      Lambda_sp/M_Pl   tower?   R2?
  candidate          1.57   0.80             yes      yes
  string_tree_eft    1.70   0.77             yes      yes
  asymptotic_safety  1.50   0.82             yes      yes   <- also has a tower/cutoff!
  cdt                1.74   0.76             yes      yes
  pure_gr            1.00   1.00             no       no    <- the only N=1, but lacks R^2 and is far/excluded
  (viable R^2-bearing spread in N: 0.24 -- clustered, does NOT discriminate)
```

The species scale N = 1 + ν(|g_R2|+|g_C|+|g_R3|) is built from the **low-energy** curvature couplings, which the
convergent completions *share* — so string, asymptotic safety, and CDT all get N ≈ 1.5–1.7 and the species scale
**cannot distinguish them.** Even this semi-UV quantity is, in the engine, a low-energy quantity. (5/5 checks.)

## What this establishes

This honest negative **sharpens v2.436**: the UV-completion degeneracy (string ≈ CDT ≈ asymptotic safety at low
energy) is **robust** — not even the species scale / cutoff breaks it. **The genuine discriminator is the tower
*spectrum* itself** — what the light states at the cutoff *are*: a **Regge trajectory** (m² linear in spin ⇒
string), a **Kaluza–Klein / decompactification tower** (⇒ large extra dimensions), or effectively **no tower with
the metric running to a fixed point** (⇒ asymptotic safety). That is a genuinely UV / high-energy observable (the
pattern of resonances near the cutoff), **not accessible to low-energy amplitude-carving.**

So the "other paths" *are* discriminable — but only by reaching the UV, which tells us precisely what a
QG-*solving* experiment must measure: **the spectrum of states at the species / cutoff scale, not more low-energy
Wilson coefficients.** This closes the honest loop on the UV question: the low-energy program has extracted
everything it can (a rigor-caged candidate, a named leading completion, a maximally-falsifiable low-energy
verdict), and the remaining discrimination is a distinct, harder, UV measurement — the true frontier.

## Honest scope

The species scale is the engine's toy Dvali proxy (ν, N_max O(1)) and the frameworks are schematic O(1) encoders,
so the specific N values are illustrative; the robust content is that they're all > 1 and clustered (spread ~0.24
among viable completions), not the precise numbers. "Asymptotic safety should have N → 1" is the idealized
bare-fixed-point expectation; the engine's AS encoder carries nonzero R²/R³ (hence N=1.5) — which is itself the
point: its *low-energy* couplings look like the others. The "tower spectrum discriminates" claim is standard QG
phenomenology, not an engine computation (the engine carves Wilson coefficients, not the state content). A
negative result plus its constructive reading.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_uv_discriminator.py --out experiments/results/v2.437/qnm_uv_discriminator.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_uv_discriminator.py -q
```

Result: all R²-bearing completions have towers N≈1.5–1.7 (spread 0.24); species scale doesn't discriminate; `5 passed`.

## Next: the low-energy UV question is closed — the frontier is a UV spectral measurement

The discrimination frontier is now precise (the tower spectrum). Next: a different bold path (S-matrix bootstrap
two-sided bounds; is-gravity-quantum table-top), or the user's steer. Bold swings, honest failures.

## References

```text
this repo: v2.436 (UV tournament -- degeneracy), v2.394 (species scale), v2.375 (string-like tower), src/itb/constraints/species_scale.py
physics: Dvali species scale; asymptotic safety (UV fixed point, no tower); string (Regge tower); KK/decompactification (extra dimensions); the tower spectrum discriminates the UV completion
```
