# v2.145 - String R4 normalization bridge: equation ready, bridge missing

## Question

v2.144 fixed the source-backed supersymmetric R4 helicity shape:

```text
K_plus = 1
K_minus = 0
```

Can the string tree-level R4 contact coefficient now be promoted into the
engine `Lambda_R4` convention?

## Result

Artifact:
`experiments/results/v2.145/string_r4_normalization_bridge.json`

Current route status:

```text
string_r4_normalization_equation_ready_bridge_missing
```

Claimable framework exclusions now:

```text
[]
```

## Source-Fixed Terms

Russo's type-IIB four-graviton tree expansion fixes the scalar R4 contact
coefficient multiplying the kinematic factor:

```text
2*zeta(3) = 2.404113806319
```

v2.144 fixes the helicity shape:

```text
K_plus = 1
K_minus = 0
```

## Bridge Equation

The executable bridge is:

```text
overall_R4_factor =
  2*zeta(3)
  * k_convention_bridge
  * engine_lambda_r4_unit_conversion
```

where:

```text
k_convention_bridge = K_Russo / Kallosh_R4_shape
engine_lambda_r4_unit_conversion = alpha-prime/string-scale units to Lambda_R4 units
```

The current repo does not have source-backed values for either bridge.

## Control

The unit bridge control is deliberately marked as a control, not a real ready
packet. If both bridges are set to 1, the algebra would derive:

```text
c_plus = 0.30051422579
c_minus = 0
g_R4_c1 = 0.150257112895
g_R4_c2 = 0.150257112895
g_R4_c3 = 0
positivity passed = true
```

That proves the equation path, but it is not promoted because the two bridge
values are not source-backed.

## Current Blockers

```text
k_convention_bridge_missing_or_not_source_backed
engine_lambda_r4_unit_conversion_missing_or_not_source_backed
source_backed_normalization_missing
```

## Next Build Action

```text
source_k_convention_bridge_or_define_engine_lambda_r4_unit
```

## Local Validation

```bash
.venv/Scripts/python.exe experiments/string_r4_normalization_bridge.py --out experiments/results/v2.145/string_r4_normalization_bridge.json
.venv/Scripts/python.exe -m pytest tests/test_string_r4_normalization_bridge.py -q
.venv/Scripts/python.exe -m ruff check experiments/string_r4_normalization_bridge.py tests/test_string_r4_normalization_bridge.py
```

Result:

```text
wrote experiments\results\v2.145\string_r4_normalization_bridge.json
6 passed
All checks passed!
1161 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2145
base = f52c75f
```

Determinism:

```text
4f7d875d986bd269b2eb33f8ee49d9e202134363019566662f7d9d953927182e  docs/results/INDEX.md
3f13f5e9efd1cdf31e01c2434fe592e0fa946077c67e8a7531937010cd117413  experiments/results/v2.145/string_r4_normalization_bridge.json
```

Commands:

```bash
.venv/bin/python experiments/string_r4_normalization_bridge.py --out experiments/results/v2.145/string_r4_normalization_bridge.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.145/string_r4_normalization_bridge.json
.venv/bin/python -m pytest tests/test_string_r4_normalization_bridge.py -q
.venv/bin/python -m ruff check experiments/string_r4_normalization_bridge.py tests/test_string_r4_normalization_bridge.py
.venv/bin/python -m pytest -q
```

Result:

```text
6 passed
All checks passed!
1168 passed, 1 warning
```
