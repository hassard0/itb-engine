# v2.365 - The ringdown channel's quantitative readiness: floor + cap + source coefficients present; the qNM->R4 map is the single blocker

## Question

This session's ringdown work (v2.349 floor, v2.351 cap) was never connected to the repo's pre-existing
ParSpec/qEFT ringdown source bridge (v2.188-191). Joining them: exactly what is ready, and exactly what blocks
a *quantitative* ringdown prediction? (And how does that relate to the open deep-research question, "is the
qNM->R4 sensitivity rank-3 or rank-1?")

## Result (3 of 4 pieces ready; the map is the single blocker -- and it IS the rank question)

```text
  READY    (1) floor present   -- moment tower g_R4 >= g_R3^2/g_R2 = 0.042 (v2.349, re-verified live)
  READY    (2) cap present     -- causality g_R4_floor <= kappa^2 g_4 = 0.339 (v2.351, live)
  READY    (3) source coeffs   -- qEFT ParSpec QNM deformations, arXiv:2205.05132 (v2.191): p_qEFT = 6
  BLOCKED  (4) engine->ParSpec map -- operator-basis map + axis orientation from the engine's 3 R4 axes
           (g_R4_c1, g_R4_c2, g_R4_c3) onto the ParSpec ell_qEFT axis; ready_for_framework_claim = False
```

All 5/5 checks pass. Piece (4) **is** the deep-research question: the public ParSpec source supplies a
**rank-1 ray** (a single `ell_qEFT` / ParSpec-gamma length scale -> the QNM shifts), the engine carries
**three** R4 operator directions, and whether those three map *full-rank* (rank-3) to the QNM observables or
*collapse* to the single ParSpec ray is precisely the missing `operator_basis_map` + `engine_axis_orientation`
(9 map blockers remain).

## What this establishes

The honest state of the ringdown channel: its **structure is complete** (floor bracketed `0 <= g_R4_floor <=
kappa^2 g_4`, re-verified), a **source bridge and source coefficients exist** (v2.191), but a numerical
`delta_omega` prediction -- and any ringdown-based framework exclusion -- is **not claimable** until the
rank-3-vs-rank-1 axis map is supplied. This connects the session's floor/cap to the June ParSpec infrastructure
(previously disconnected) and gives the deep-research a **precise acceptance target**: the v2.190
`operator_basis_map` contract (source-backed operator-basis + axis-orientation from `ell_qEFT` into
`g_R4_c1/c2/c3`). It replaces the vague "ringdown is schematic" with an exact one-subpiece blocker.

## Honest scope

Pieces (1)-(2) are re-verified live (exact arithmetic on the constructed couplings); pieces (3)-(4) are READ
from the tested v2.191/v2.190 artifacts, not re-derived -- this is a synthesis/status, inheriting their claim
boundaries verbatim: the source coefficients are genuinely source-backed (arXiv:2205.05132), but the
engine->ParSpec map is explicitly NOT a claim (v2.190 is a non-claiming contract; v2.191
`ready_for_framework_claim = False`). "Rank-1 ray" describes the PUBLIC ParSpec source's single `ell_qEFT`
axis as preserved in the repo -- whether a full rank-3 map is achievable from other sources is the open
question, not answered here. The floor/cap remain toy-basis + rank-1-schematic-magnitude (v2.336): they bound
the g_R4 COUPLING, not a sourced frequency shift. No framework exclusion is claimable from ringdown
(unchanged). Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_ringdown_channel_readiness.py --out experiments/results/v2.365/qnm_ringdown_channel_readiness.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_ringdown_channel_readiness.py -q
```

Result: floor 0.042 <= cap 0.339 (live); source p_qEFT=6 present; the engine->ParSpec map is the single
blocked piece (3/4 ready, framework-claim-ready False); the rank-3-vs-rank-1 question is the operator_basis_map
blocker; `6 passed`.

## Next (v2.366)

The highest-value fresh input is the deep-research deliverable satisfying the v2.190 `operator_basis_map`
contract (a source-backed `ell_qEFT -> g_R4_c1/c2/c3` operator-basis + axis-orientation map, with its rank).
Absent that, continued audits at the <=10-minute cadence. Honest negatives reported.

## References

```text
this repo: v2.349 (ringdown floor), v2.351 (causality cap), v2.336 (rank-1 schematic magnitude) -- structural pieces re-verified
this repo: v2.188 (source bridge), v2.190 (engine-axis map contract, non-claiming), v2.191 (source-asset audit: p_qEFT=6, QNM coefficients) -- ParSpec pieces read
source: arXiv:2205.05132 / PhysRevD.107.044030 (qEFT ParSpec ringdown bounds); Bresciani R4 axes g_R4_c1/c2/c3
```
