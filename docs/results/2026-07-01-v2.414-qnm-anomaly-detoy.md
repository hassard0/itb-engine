# v2.414 - ENGINE IMPROVEMENT (de-toying step 3, anomaly capstone): matter-sources-gravity is RIGOROUS, not anomaly-dependent -- the only genuine toy left is one parity coefficient

## The move

The anomaly sector was the last load-bearing toy (v2.413). It has two parts:

- **`anomaly_cancellation`** `g_4·g_6 = c_anom·g_R2²` (matter ↔ curvature): self-described "representative
  simplification / toy 4D form". The physics is decisive — Alvarez-Gaumé–Witten gravitational anomalies live in
  4k+2 dimensions; **in 4D there is no pure gravitational anomaly**, so this `matter² = curvature²` equality is
  a **toy artifact** with no literature-exact backing in a pure gravity+matter basis.
- **`generalized_anomaly_inflow`** `g_R2_parity² + 2 g_R3_parity² ≤ ρ·g_4·g_R2` (parity ≤ matter×curvature): the
  *concept* — gravitational anomaly inflow bounding parity-violating couplings — is real physics (Harlow et al
  TASI 2022, cited), with a toy coefficient ρ=0.06.

## Result (the headline is rigorous, the artifact is redundant)

Does matter-sources-gravity (v2.393, "matter forbids matter without curvature") — the result the toy artifact
was credited with — actually depend on it? **No.** Fixing matter at the constructed values and lowering the
leading curvature coupling g_R2 toward zero:

```text
  g_R2 floor given matter:
    rigorous+implied core (NO anomaly):  0.108   <- forced by graviton_forward_positivity + cross_sector_efthedron
    full stack (with the toy anomaly):   0.114   <- the toy anomaly only nudges it +6% (1.056x)
```

All 5/5 checks pass. A theory with matter couplings but a vanishing leading curvature coupling violates
**cross-sector amplitude positivity**, with or without the anomaly — so **matter-sources-gravity is rigorous**.

## What this establishes

The anomaly sector de-toys cleanly. The matter↔curvature *structure* is rigorous (not anomaly-dependent); the
toy `anomaly_cancellation` is a **removable 4D artifact** whose headline result stands rigorously without it
(the rigorous floor 0.108 vs full-stack 0.114). The second piece, `generalized_anomaly_inflow`, is
conceptually-real anomaly-inflow physics with a toy coefficient — the **residual genuine toy** — and it governs
the parity *magnitude*, exactly the sector that was always birefringence-contingent (v2.408/329). So this
**closes the de-toying arc (v2.411–414)**: the candidate's *entire matter-gravity content is rigorous* — LQG
excluded (v2.411), matter dominance's ceiling and BH decay implied (v2.412), the two most speculative proxies
harmless (v2.413), and now matter-sources-gravity rigorous with the anomaly artifact removable — and the single
residual toy is one parity-magnitude coefficient, which needs the cosmic-birefringence datum to fix anyway. The
engine is not a toy: its matter-gravity physics is source-exact, and the toy has been chased from the whole
stack down to one coefficient in the parity sector.

## Honest scope

The g_R2 floors are computed along the single matter-fixed ray (constructed matter values, g_R2 varied), so
0.108 is the rigorous floor on *that ray*, not a global statement — but the mechanism (cross-sector positivity
excludes matter-without-leading-curvature) is general, and the point is that the *rigorous* bounds do the
excluding. "`anomaly_cancellation` is a toy artifact" is a physics judgement (4D pure-gravitational anomaly
vanishing is standard; the `matter²=curvature²` form has no literature-exact backing here) plus the constraint's
own self-description — it does **not** mean the matter→curvature *conclusion* is wrong (it's rigorously true, and
has an independent real basis in matter loops generating R²), only that the engine's anomaly *derivation* was
toy and redundant. I did **not** remove `anomaly_cancellation` from the stack (that would shift the region ~1.36×
and rebake many goldens); the result is that it's removable-*in-principle* without costing the headline,
established by the floor comparison. "One residual toy" is scoped to the matter-gravity + parity Wilson sector
this engine carves.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_anomaly_detoy.py --out experiments/results/v2.414/qnm_anomaly_detoy.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_anomaly_detoy.py -q
```

Result: rigorous g_R2 floor 0.108 vs full 0.114 (anomaly 1.056×); low-g_R2 excluded by graviton forward
positivity + cross-sector EFThedron; `4 passed`.

## Next (v2.415): the de-toying arc is closed -- consolidate or open a new front

The candidate's matter-gravity content is now established rigorous; the residual toy is the parity-magnitude
coefficient (real-concept, toy ρ). Next: either a consolidation note refreshing FINDINGS with the "matter-gravity
rigorous" status, or a fresh swing. Bold swings, honest failures.

## References

```text
this repo: v2.393 (matter-sources-gravity, now rigorous), v2.413 (anomaly = last load-bearing toy), v2.411/412 (rigor core + implied), v2.408/329 (parity is birefringence-contingent), src/itb/constraints/anomaly.py (self-described toy form)
physics: Alvarez-Gaume-Witten 1984 (gravitational anomalies in 4k+2 dims; none pure-gravitational in 4D); Harlow et al TASI 2022 (anomaly inflow); Arkani-Hamed-Huang-Huang EFThedron + Caron-Huot-Van Duong (cross-sector positivity)
```
