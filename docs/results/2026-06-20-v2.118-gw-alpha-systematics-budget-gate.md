# v2.118 - GW alpha systematics budget gate: two components bounded

## Question

v2.117 selected the next implementation step:

```text
close_alpha_packet_systematics_budget
```

Can we close any systematics components with current executable evidence without
overstating the alpha packet as claim-ready?

## Result

Artifact:
`experiments/results/v2.118/gw_alpha_systematics_budget_gate.json`

Current route status:

```text
alpha_systematics_budget_partially_bounded_nonclaiming
```

Claimable discriminator now:

```text
false
```

## Method

The harness loads the v2.117 projected alpha packet and the v2.115 marginal
likelihood, then attaches per-component evidence for the v2.102 systematics
budget.

Bounded components:

```text
sampler_convergence
public_data_reproducibility
```

Open components:

```text
waveform_systematics
detector_calibration
prior_sensitivity
eft_truncation
```

## Result Status

Adapter state:

```text
engine_projection_ready = true
systematics_budget_status = open
bounded_components = [sampler_convergence, public_data_reproducibility]
open_components = [waveform_systematics, detector_calibration, prior_sensitivity, eft_truncation]
adapter_blockers = [systematics_not_closed]
claim_ready = false
```

The result remains nonclaiming because:

```text
g8_joint_component_missing
gw_cubic_alpha_axis_not_framework_discriminator_by_itself
likelihood_scale_not_calibrated_to_noise_evidence
systematics_not_closed
```

## Next Build Action

```text
bound_waveform_calibration_prior_and_eft_systematics
```

## Interpretation

The systematics blocker is now narrower: deterministic grid convergence and
public-data reproducibility are bounded by current artifacts. The packet still
cannot promote because waveform, calibration, prior-sensitivity, and EFT
truncation systematics remain open.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_alpha_systematics_budget_gate.py --out experiments/results/v2.118/gw_alpha_systematics_budget_gate.json
.venv/Scripts/python.exe -m pytest tests/test_gw_alpha_systematics_budget_gate.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_alpha_systematics_budget_gate.py tests/test_gw_alpha_systematics_budget_gate.py
```

Focused validation passed with 6 tests. Ruff passed.

## Vulcan Reproduction

Validated on `vulcan` in a clean checkout at
`/home/admin/codex-runs/itb-engine-main-v2118`.

```bash
.venv/bin/python experiments/gw_alpha_systematics_budget_gate.py --out experiments/results/v2.118/gw_alpha_systematics_budget_gate.json
.venv/bin/python -m pytest tests/test_gw_alpha_systematics_budget_gate.py -q
.venv/bin/python -m ruff check experiments/gw_alpha_systematics_budget_gate.py tests/test_gw_alpha_systematics_budget_gate.py
.venv/bin/python -m pytest -q
```

Focused validation passed with 6 tests. Ruff passed. Full-suite validation
passed with 1020 tests, 7 optional LALSuite skips under the standard dev
environment, and one existing Starlette/httpx deprecation warning.
