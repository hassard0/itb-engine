# v2.439 - the two layers of solving quantum gravity: the program lives entirely in Layer 2 (which EFT) and presupposes Layer 1 (is gravity quantum)

Following the most foundational other path: underneath everything the program carves is an assumption — that
gravity *is* quantum. Is that assumption itself part of the solve, and where does it sit relative to the candidate?

## Engine fact (verified)

Every coupling the engine carves is **higher-derivative**: g_R2 (R²), g_C (Weyl²), g_R3 (R³), g_R4 (R⁴), the
parity terms (`R∧R̃`), the matter dim-8+ (g_4…g_10), and g_Lambda (vacuum energy). The **Einstein-Hilbert term R
— the graviton kinetic term / Newton's constant — is *not* a tunable coupling**; the lowest gravitational operator
carved is R². So the whole program carves higher-derivative *corrections* on top of a quantum graviton it takes
as **given**. The program *presupposes* gravity is quantum.

## The two layers

```text
  LAYER 1 -- IS gravity quantum? (does it carry a graviton?)
    status in program:  PRESUPPOSED (the engine carves only higher-derivative corrections; Einstein R not carved)
    empirical status:   classical collapse (parameter-free Diosi-Penrose) EXCLUDED ~14 orders (v1.90) -> weakly favours quantum
    decisive test:      BMV -- gravitationally-induced entanglement of two mesoscopic masses; a classical (LOCC)
                        field cannot entangle -> positive result = gravity carries quantum d.o.f. (~2030s)
    probes:             the LEADING / Newtonian graviton sector

  LAYER 2 -- WHICH quantum-gravity EFT? (given a quantum graviton)
    status in program:  the ENTIRE program -- near-unique rigor-caged candidate + correlated 2030 verdict +
                        heterotic-string leading completion
    decisive test:      the correlated make-or-break signature (v2.430): birefringence + CMB-S4 matter + dark-energy w
    probes:             the HIGHER-DERIVATIVE couplings the engine carves
```

All 5/5 checks pass.

## What this establishes

"Solving quantum gravity" has **two layers**, and the program occupies exactly one. The two are
**observationally separate**: Layer 1 (BMV) probes the *leading / Newtonian* graviton sector — the part the
engine presupposes and does *not* carve — while Layer 2 (the candidate's correlated signature) probes the
*higher-derivative* couplings the engine *does* carve. So the candidate is **agnostic to BMV** (its dim-8
structure doesn't affect the Newtonian entanglement rate) and BMV is agnostic to the candidate. The complete
solve needs **both**: BMV to establish gravity is quantum (Layer 1), and the correlated cosmological signature to
pin which EFT (Layer 2).

This is the honest, complete map of "how do we solve quantum gravity": the program has finished all of Layer 2's
low-energy work (carved the candidate, tiered its rigor, made it maximally falsifiable, named its leading UV
completion), and the remaining pieces are **(i)** Layer 1 — the table-top confirmation that gravity is quantum
(BMV) — and **(ii)** the UV frontier *within* Layer 2 — the cutoff-scale tower spectrum that discriminates the UV
completion (v2.437–438). **Two experiments, two layers: is it quantum (BMV, table-top), and which EFT (the
correlated cosmological signature, CMB-S4/DESI).**

## Honest scope

A **foundational framing** grounded in one verified engine fact (the coupling space is higher-derivative only;
Einstein R is not carved) plus standard results — no new computation. The BMV argument (a classical/LOCC field
can't entangle two masses, so induced entanglement ⇒ quantum gravitational d.o.f.) is the standard
Bose–Marletto–Vedral reasoning, itself *debated* (whether the mediator must be quantum is contested) — "decisive"
means *leading near-term probe*, not settled implication. The Diósi–Penrose exclusion rules out *one* classical
model, not all classical-gravity possibilities. "Observationally separate" holds at leading order (BMV ~ order G;
the candidate ~ dim-8). "Presupposing Layer 1" is a correct reading of the engine's operator content, not a claim
the presupposition is proven.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_two_layers.py --out experiments/results/v2.439/qnm_two_layers.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_two_layers.py -q
```

Result: engine carves only higher-derivative operators (Einstein R not carved); two layers, observationally
separate; `4 passed`.

## Next: the "how do we solve QG" map is complete — two layers, two experiments

The complete picture: Layer 1 (is gravity quantum — BMV, table-top) + Layer 2 (which EFT — the program, tested by
the correlated cosmological signature; UV completion via the cutoff spectrum). Next: the user's steer, or a fresh
swing. Bold swings, honest failures.

## References

```text
this repo: v1.90 (Diosi-Penrose classical collapse excluded), v2.430 (correlated 2030 verdict = Layer 2 test), v2.434-438 (heterotic completion + UV frontier), the engine's higher-derivative coupling content
physics: Bose-Marletto-Vedral / Marletto-Vedral (gravitationally-induced entanglement => quantum gravitational d.o.f.); Diosi-Penrose gravitational collapse; the Einstein-Hilbert term is the presupposed graviton, not a carved coupling
```
