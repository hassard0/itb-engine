# v2.369 - RIGOROUS UPGRADE: the equivalence principle mandates g_R4 strictly above the moment floor

## Question (landing the rigorous residue of the swing)

The tower-unification arc: v2.367 conjectured a sharp ringdown value (fragile); v2.368's attack downgraded it
to a band (robust to moderate form factors). What is the *rigorous* core -- the part that survives with no
assumption and no form-factor restriction?

## Result (a strict inequality, form-factor-independent)

The equivalence principle removes both weaknesses. The argument:

1. **Matter is multi-state:** `r_matter = g_6^2/(g_4 g_8) = 0.756 < 1` (v2.343) means, by strict
   Cauchy-Schwarz on the spectral moments, the matter spectral density has **>= 2 distinct-mass states**.
2. **Equivalence principle** (Weinberg soft-graviton theorem): the graviton couples to *every* energetic
   state with a nonzero, positive coupling. So those >= 2 matter states **are** curvature-sector states -- the
   "shared tower" is not assumed, it is *forced* (a massive state cannot decouple from gravity).
3. Hence the curvature spectral density has >= 2 positive-weight support points, and strict Cauchy-Schwarz
   gives `r_curv = g_R3^2/(g_R2 g_R4) < 1` **strictly, for any positive form factor**.
4. Therefore `g_R4 > g_R3^2/g_R2` **strictly** -- the moment-tower floor is **never saturated**.

Verified two ways:

```text
  strict Cauchy-Schwarz:   200k random 2-state measures, every distinct-mass r < 1 (gap to 1 ~ 1e-11;
                           the bound 1 is approached only as the two masses coincide)
  form-factor independence: r_curv < 1 across the extreme range s in [-5, 5]  (min/max 0.75 / 0.996)
  single state:            r = 1 exactly (the excluded degenerate case)
```

All 5/5 checks pass.

## What this establishes

The defensible arc endpoint: **the ringdown quartic is strictly above its moment minimum (`g_R4 > 0.042`,
saturation excluded) -- a mandated, GR-distinguishable minimum ringdown deviation -- whenever the matter
sector is multi-state**, which the engine's dispersion relation already shows. This is weaker than a number
but stronger than a conjecture: it is a *theorem* given the equivalence principle and multi-state matter
(modulo the engine's spectral encoding). It removes v2.367's fragility (no sharp value claimed) and v2.368's
"moderate form factor" caveat (the strict inequality is form-factor-**independent** -- only the *magnitude*
`g_R4 - floor` depends on the form factor, its *sign* does not).

The arc, honestly: **v2.367 sharp point (conjecture, fragile) -> v2.368 robust band (moderate form factors)
-> v2.369 strict inequality (rigorous, form-factor-independent).** The swing converged from speculation to a
defensible theorem.

## Honest scope

The STRUCTURE is rigorous and standard: strict Cauchy-Schwarz on a >= 2-atom positive measure (verified over
200k draws), and the equivalence principle / soft-graviton theorem forcing universal positive graviton
coupling. The CAVEAT is the identification: `g_4/g_6/g_8` as matter spectral moments and `g_R2/g_R3/g_R4` as
moments of the graviton's coupling to the same states are the engine's TOY dispersive encodings -- the
argument is basis-robust (an inequality direction + a coupling positivity), but the specific numbers
(`r_matter = 0.756`, floor `0.042`) are toy. The result is a STRICT INEQUALITY (`g_R4 > floor`), **not a
value** -- it says the minimum ringdown deviation is strictly nonzero beyond the naive floor, not how large
(the v2.368 band `[0.050, 0.056]` is the form-factor-dependent magnitude). It rests on the matter-multistate
result (`r_matter < 1`, itself the toy-basis v2.343 finding); a single-state matter encoding would not fire
the argument. Toy basis for the numbers; rigorous for the structure.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_ringdown_floor_strict.py --n 200000 --out experiments/results/v2.369/qnm_ringdown_floor_strict.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_ringdown_floor_strict.py -q
```

Result: matter multi-state; strict Cauchy-Schwarz (every distinct-mass r < 1); r_curv < 1 across s in [-5,5]
(form-factor-independent); so `g_R4 > floor` strictly, saturation excluded; `5 passed`.

## Next (v2.370)

The ringdown floor is now a strict inequality (rigorous) with a form-factor-dependent magnitude band (v2.368).
Next swing: a fresh conjecture in a different sector (e.g. a swampland/no-global-symmetry argument for why the
parity coupling must be nonzero on theoretical, not data, grounds), or push the strict floor toward an
observable. Bold swings, honest failures.

## References

```text
this repo: v2.367 (the conjecture), v2.368 (the form-factor attack -> band), v2.343 (matter multi-state r=0.756), v2.349 (the moment floor this strictly un-saturates)
physics: Weinberg soft-graviton theorem / equivalence principle (universal graviton coupling); strict Cauchy-Schwarz / Hausdorff-Stieltjes moment positivity
```
