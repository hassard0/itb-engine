# v2.95 - External dependency blocker synthesis: current run cannot promote

## Question

After v2.91-v2.94, does the current run still have a defensible in-repo path to
a quantum-gravity discriminator claim, or is it blocked on external evidence?

## Result

Artifact: `experiments/results/v2.95/external_dependency_blocker_synthesis.json`

Current route status:

```text
research_frontier_blocked_pending_external_evidence
```

Repeated blocker:

```text
real_engine_normalized_external_packet_missing
```

Claimable discriminator now:

```text
false
```

## Evidence Chain

| version | result |
|---|---|
| v2.91 | No current in-repo promotion-ready route remains. |
| v2.92 | Retained routes require explicit external evidence contracts. |
| v2.93 | The intake gate rejects synthetic, incomplete, unknown, or route-rejected packets. |
| v2.94 | Current public source probe found no schema-ready or claim-ready packet. |

## Unblock Conditions

- A real engine-normalized `g_8` packet passes the v2.93 intake gate.
- A registered framework supplies native tower spectra and excluding tower evidence.
- A source-backed PPV-to-engine parity operator bridge appears.
- Engine-normalized `g_C` and `g_8` packets become available with a joint likelihood.

## Interpretation

The current run cannot honestly promote a defensible quantum-gravity
discriminator. The in-repo frontier is externally gated: contracts and intake
validation are ready, but no current public source provides a complete packet
that passes the gate.

## Vulcan Reproduction

Validated on `vulcan` in a clean checkout at
`/home/admin/codex-runs/itb-engine-main-v295`.

```bash
.venv/bin/python experiments/external_dependency_blocker_synthesis.py --out experiments/results/v2.95/external_dependency_blocker_synthesis.json
.venv/bin/python -m pytest tests/test_external_dependency_blocker_synthesis.py -q
.venv/bin/python -m ruff check experiments/external_dependency_blocker_synthesis.py tests/test_external_dependency_blocker_synthesis.py
.venv/bin/python -m pytest -q
```

Focused validation passed with 5 tests. Full-suite validation passed with 890
tests and one existing Starlette/httpx deprecation warning.
