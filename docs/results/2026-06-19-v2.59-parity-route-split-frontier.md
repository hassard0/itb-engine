# v2.59 - Parity route split: CMB beta retired as a direct gravity-parity discriminator

## Question

v2.58 blocked the direct CMB-beta to gravitational-parity map. This audit turns
that blocker into a route split: CMB beta remains an EM/axion channel, while the
engine gravitational parity axes should be tested by GW birefringence, PTA
chirality, or a source-backed multimessenger common-axion model.

## Result

Artifact: `experiments/results/v2.59/parity_route_split_frontier.json`

Current route status:

```text
parity_routes_split_no_claim_ready_path
```

Claim-ready routes:

```text
none
```

Direct CMB beta to engine gravity parity:

```text
retired
```

## Split Routes

| route | channel | engine gravity parity route? | status |
|---|---|---:|---|
| `cmb_em_axion_beta` | electromagnetic axion-photon | no | external EM hint, not engine gravity discriminator |
| `gw_gravitational_birefringence` | gravitational-wave parity | yes | right engine axis, external measurement missing |
| `pta_chiral_sgwb` | gravitational-wave chirality | yes | future design probe, not measurement ready |
| `multimessenger_common_axion` | EM plus gravity common field | no | common-origin hypothesis, not adapter |

The correct engine-parity priority is now:

1. `gw_gravitational_birefringence`
2. `multimessenger_common_axion`
3. `cmb_em_axion_beta`
4. `pta_chiral_sgwb`

## Key Point

The implemented `GravitationalBirefringence` observable touches the right axes:

```text
g_R2_parity
g_R3_parity
```

But it still lacks:

- `no_external_gw_parity_measurement_packet`
- `toy_frequency_normalization`
- `no_framework_excluding_gw_likelihood`

## Interpretation

The overloaded birefringence route is now split. CMB beta remains a source-backed
EM/axion hint, but it is retired as a direct engine-gravity-parity discriminator.
The right engine axis is GW gravitational birefringence, currently blocked on an
external likelihood and source-backed frequency normalization.

## Reproduction

```powershell
.\.venv\Scripts\python experiments\parity_route_split_frontier.py --out experiments\results\v2.59\parity_route_split_frontier.json
.\.venv\Scripts\python -m pytest tests\test_parity_route_split_frontier.py tests\test_birefringence_adapter_literature_sourceability.py -q
.\.venv\Scripts\python -m ruff check experiments\parity_route_split_frontier.py tests\test_parity_route_split_frontier.py
```

Focused validation passed with 10 tests.
