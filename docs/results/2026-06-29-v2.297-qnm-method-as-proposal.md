# v2.297 - Swampland-complete EFT carving: the method as a falsifiable QG proposal

## Question

SIXTH SLICE of the new-theory arc -- the method-as-proposal (the third user-chosen direction), made
concrete. The arc built the ringdown-active `g_R4` operator into the engine (v2.292) and characterized
the cross-sector moment structure (v2.293-v2.296). What is the engine's actual original QG claim, and
can it be made falsifiable rather than a manifesto?

## Result (the method, made into a falsifiable prediction)

**Thesis.** The route to quantum-gravity phenomenology is not to guess one Lagrangian but to INTERSECT
every known consistency condition -- forward positivity, the matter AND curvature dispersion/moment
towers, causality, swampland/WGC, holographic and information-theoretic bounds, EFT validity -- and study
the survivor. The carved feasible region IS the prediction.

**The falsifiable output.** Apply the carving to the one OBSERVABLE higher-curvature operator, the
ringdown-active Riemann^4 coefficient `g_R4`. The intersection bounds it to a finite band per framework
(floor from the curvature moment-tower mandate `g_R4 >= g_R3^2/g_R2`, ceiling from EFT validity +
complexity):

```text
  framework          floor (mandate)   carved g_R4 band [min, max]   width
  string_tree_eft    0.1125            [0.115, 0.345]                0.230
  asymptotic_safety  0.0667            [0.070, 0.530]                0.460
  cdt                0.1023            [0.105, 0.370]                0.265
  lqg_induced        0.3000            EMPTY -- carving REJECTS
```

So the method predicts not a point but a RANGE for the admitted frameworks -- the right epistemic status
for a carving (the consistent theory space is a region). And it does more than bound: it **excludes**.
`lqg_induced` gets an EMPTY band -- its curvature sector is so over-large that even the mandated minimum
`g_R4` pushes its weighted complexity over the cutoff, so the carving rejects its Riemann^4 sector
outright. The same recurring anomaly the whole arc has flagged now appears as the non-existence of any
consistent `g_R4`. All 5/5 checks pass.

The falsifiable content is sharp: a Riemann^4 coefficient inferred from a future ringdown measurement
that lands OUTSIDE the carved band would falsify the consistency conditions, the carving, or the
framework -- exactly as a measured Wilson coefficient outside a positivity bound would.

## What this establishes

This reframes the whole arc and states what it is FOR. The `g_R4` extension (v2.292) gave the engine the
observable operator; the cross-sector characterization (v2.293-v2.296) mapped what the moment structure
can and cannot force; and this cycle states the engine's genuine, original contribution: a falsifiable,
region-valued QG prediction carved from consistency alone -- bounding the ringdown-active operator for
consistent frameworks and excluding the inconsistent one.

## Honest scope

The carved `g_R4` band is the engine's literal feasibility verdict on the `g_R4`-bounding sub-stack
(moment-tower mandate + Riemann^4 positivity + EFT validity + complexity), exact for those constraints.
It is NOT the full 38-constraint carving (the frameworks fail other constraints, e.g. repulsive_force,
independently of `g_R4` -- v2.283); this isolates the operator's own band. The CEILING depends on choices
the toy basis fixes to O(1): complexity `c_max = 1.5`, validity box `= 2.0`, and the `g_R4` complexity
weight (set to 3.0 as a dim-8 operator) -- v2.287 showed such boundaries move with the prefactors, so the
band is a representative range, not a calibrated number. The map from `g_R4` to an actual ringdown QNM
shift carries the v2.215 sensitivity AND the v2.209 dark-parity caveat (the parity-odd Riemann^4
component is not source-backed), so "falsifiable by ringdown" is the channel with the observable
deformation honestly uncertain. The thesis (carving = prediction) is a methodological proposal, not a
theorem. A new-engine-theory result: the method made concrete and falsifiable on the observable operator,
with all the O(1) / dark-parity uncertainty stated.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_method_as_proposal.py --out experiments/results/v2.297/qnm_method_as_proposal.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_method_as_proposal.py -q
```

Result: the admitted frameworks (string, AS, cdt) get finite `g_R4` bands floored at the mandate and
ceilinged below validity; the carving rejects lqg (empty band); `6 passed`.

## Next (v2.298)

The new-theory arc (g_R4 extension -> cross-sector characterization -> method-as-proposal) is complete
through all three user-chosen directions. Options: (a) promote `g_R4` into the framework encoders +
default stack so the carving is the canonical engine state; (b) a synthesis capstone of the arc; (c) a
fresh new-theory swing in a different sector. Honest negatives reported as part of the search.

## References

```text
this repo: v2.292 (g_R4 extension), v2.293-v2.296 (cross-sector characterization), v2.234 (mandate)
this repo: v2.215 (R4->QNM sensitivity), v2.209 (dark parity-odd), v2.285-v2.287 (feasible region / realism)
Caron-Huot et al 2021 (EFT-hedron); Adams, Arkani-Hamed, Dubovsky, Nicolis, Rattazzi 2006 (positivity = causality)
```
