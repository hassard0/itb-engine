# v2.267 - The gravitational-wave memory effect and the infrared triangle

## Question

A fresh QG-structure / GW-observable thread (continuing v2.266). A passing gravitational-wave burst
leaves a PERMANENT relative displacement between freely-falling test masses -- the memory effect.
Strominger's "infrared triangle" claims this is one of three faces of the same physics
(memory <-> soft-graviton theorem <-> BMS supertranslations). Can the linking identity be made
concrete and verified, and how big is the astrophysical signal?

## Result

The memory is the permanent strain offset `Delta h = h(+inf) - h(-inf) = integral hdot dt`, which is
exactly the **zero-frequency Fourier mode** of the strain rate, `hdot(omega=0)`. The soft-graviton
theorem says the radiated-energy spectrum `dE/domega ~ |hdot(omega)|^2` approaches a NONZERO constant
as `omega -> 0`, set by the memory. On a toy strain-rate burst (a net-DC Gaussian "memory" bump plus a
zero-mean oscillatory chirp), all four identity checks pass:

```text
  memory (integral hdot dt)   = 1.000000
  zero-frequency mode hdot(0) = 1.000000     (matched to 1e-6)
  low-freq spectrum plateau   = 0.997        (|memory|^2 = 1.000, flat to ~0.3% over omega<0.05)

  [PASS] oscillation_carries_no_memory        (the chirp's time-integral is ~0)
  [PASS] memory_equals_zero_frequency_mode    (Delta h = hdot(omega=0))
  [PASS] memory_equals_dc_bump_area
  [PASS] soft_theorem_low_freq_plateau        (spectrum(omega->0) -> |Delta h|^2)
```

So the oscillatory radiation carries **no** memory; only the DC part does, and that DC part *is* the
soft-theorem zero-mode. The third face is the BMS supertranslation: the permanent test-mass
displacement is a transition between two inequivalent BMS vacua, so the memory measures a
supertranslation-charge difference.

Astrophysically the Christodoulou (nonlinear) memory is `Delta h ~ (G/c^4 R) Delta E_rad`:

```text
  source                        raw Delta h     raw / peak strain
  GW150914 (3 Msun @ 410 Mpc)   3.5e-22         0.35
  GW170817 (BNS @ 40 Mpc)       4.8e-23         0.48
  SMBH 1e6 Msun (LISA @ 6.7 Gpc) 2.1e-19        0.02
```

The raw `(G/c^4 R) Delta E` overestimates; the proper TT/angular projection brings the memory to the
`~5-20%` of peak strain quoted by Favata -- a permanent DC offset, undetectable per-event by LIGO but
accumulable by stacking (`~sqrt(N)`) and per-event detectable for LISA supermassive mergers.

## What this establishes

An asymptotic-symmetry / soft-theorem structure of quantum gravity (the IR triangle) has a concrete,
falsifiable gravitational-wave observable, and the identity connecting its three faces
(`Delta h = hdot(omega=0)`, `spectrum(omega->0) = |Delta h|^2`) is exact and numerically verified. This
extends the v2.266 graviton thread from the graviton's mass to its infrared structure.

## Honest scope

The memory<->zero-mode<->soft-theorem identity is EXACT, and the numeric checks verify it on a TOY
analytic burst (Gaussian DC bump + windowed sinusoid), not a real numerical-relativity waveform -- an
illustration of the identity, not a waveform prediction. The astrophysical `Delta h ~ (G/c^4 R) Delta E`
is an ORDER-OF-MAGNITUDE scaling carrying an `O(1)` angular/TT-projection factor (the proper
Christodoulou memory is `~5-20%` of peak, Favata 2010); the per-event `memory/peak` ratios are raw
(pre-projection) and the `h_peak` values are representative. The BMS / supertranslation face is a
structural statement (Strominger et al.), cited not derived here. A QG-structure / GW-phenomenology
result, not an engine constraint refit.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_gw_memory_infrared_triangle.py --out experiments/results/v2.267/qnm_gw_memory_infrared_triangle.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_gw_memory_infrared_triangle.py -q
```

Result: memory = zero-frequency mode (1e-6); spectrum at `omega=0` equals `|memory|^2`; the
low-frequency band plateaus there to `~0.3%`; oscillation carries no memory; memory scales linearly in
`Delta E` and inversely in `R`; `7 passed`.

## Next (v2.268)

The fresh GW/QG-observable thread continues -- e.g. extra GW polarization content (GR's 2 vs up to 6
in alternative gravity) as a falsifiable test, GW birefringence/parity in the propagation sector, or
the user-authorized `g_R4` core-engine extension.

## References

```text
Strominger, 'Lectures on the Infrared Structure of Gravity and Gauge Theory', arXiv:1703.05448
Christodoulou, 'Nonlinear nature of gravitation and gravitational-wave experiments', PRL 67 (1991) 1486
Favata, 'The gravitational-wave memory effect', Class. Quantum Grav. 27 (2010) 084036
Weinberg, 'Infrared photons and gravitons', Phys. Rev. 140 (1965) B516
this repo: v2.266 (graviton mass), v2.253 (graviton quantization)
```
