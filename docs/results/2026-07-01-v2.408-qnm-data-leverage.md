# v2.408 - SWING: the candidate's data-pinning hangs on one measurement -- cosmic birefringence is the only load-bearing datum

## Question

v2.373 gave the *aggregate* data leverage (all four ingested measurements add ~1.7×). Which of the four
actually pins the candidate -- is its data support broad, or concentrated in one experiment?

## Result (only birefringence carves)

Dropping each data constraint and measuring how much the local feasible region grows:

```text
  full-stack local feasible fraction: 0.0047
  drop cosmic birefringence  ->  opens region 7.4x   <- the only load-bearing datum
  drop sub-mm (screening)    ->  opens region 1.0x
  drop GW speed              ->  opens region 1.0x
  drop GW dispersion         ->  opens region 1.0x
```

All 5/5 checks pass. Only cosmic birefringence carves; the other three don't shrink the local region.

## What this establishes

The candidate's **data support is concentrated in one ~3.6σ hint** (cosmic birefringence, v2.329). Sub-mm is
satisfied throughout the local region -- the theory screens (v2.354), so the bound mandates screening as a
qualitative *feature* but never bites inside the feasible region -- and GW speed/dispersion are
frequency-suppressed and non-binding (v2.358). So the complete honest picture is **two-sided**: the candidate's
*structure* is robust on four independent axes (family, basis, adversarial, prefactor -- v2.405-407), but its
*data-pinning* is **birefringence-contingent**, resting on a single unconfirmed measurement. These are not in
tension -- the swampland-complete carving (theory constraints) does most of the work (data adds only ~1.7×
aggregate, v2.373), and the one datum that binds is birefringence, which both pins the parity magnitude *and*
breaks the Z2 handedness mirror to select a unique candidate (v2.406). So "the one experiment to watch" is
unambiguous: **cosmic birefringence (LiteBIRD / CMB-S4)** is the candidate's empirical lifeline -- and CMB-S4's
*independent* test of the matter sector (v2.395, the large-g_4 requirement) is the second, orthogonal probe. The
two make-or-break measurements are birefringence (parity / data-pinning) and the CMB-S4 inflationary
self-coupling (matter dominance).

## Honest scope

Leverage is the growth of the LOCAL feasible fraction (constructed +/- 0.15) when a constraint is dropped, so
the 7.4× is a local number; global leverage would differ, but birefringence's *dominance* (the only data
constraint that binds locally) is robust. "Sub-mm opens 1.0×" means it does not shrink the local region -- NOT
that it is irrelevant: it mandates screening (a real qualitative constraint), satisfied throughout the box, so
its region-shrinking leverage is ~0 while its role is real. GW speed/dispersion are genuinely non-binding
(v2.358). The "drop birefringence -> Z2 returns" link is the v2.406 mechanism. A per-constraint leverage audit
of the four data inputs -- no new physical datum, it *quantifies* the pervasive "birefringence-contingent"
caveat. The birefringence detection itself is a ~3.6σ *hint*, not a discovery (v2.329). Robust content: of the
four ingested measurements only cosmic birefringence carves the candidate region (7.4× local leverage vs 1.0×),
so the candidate is structurally robust but data-pinned by a single unconfirmed measurement.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_data_leverage.py --n 6000 --out experiments/results/v2.408/qnm_data_leverage.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_data_leverage.py -q
```

Result: birefringence opens 7.4×, sub-mm/GW-speed/GW-dispersion each 1.0×; `4 passed`.

## Next (v2.409)

Data-pinning localized to one measurement. Next: a fresh swing, keeping FINDINGS.md current. Bold swings,
honest failures.

## References

```text
this repo: v2.373 (aggregate data leverage), v2.406 (birefringence breaks the Z2 mirror), v2.404 (dark channels), v2.358 (GW non-binding), v2.354 (sub-mm mandates screening), v2.329 (birefringence hint caveat), v2.395 (CMB-S4 matter test)
physics: cosmic birefringence beta=0.34+/-0.09 deg (Minami-Komatsu / Eskilt-Komatsu); Eot-Wash sub-mm; GW speed/dispersion
```
