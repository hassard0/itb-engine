# v1.86 — R² is the inflaton: the engine's g_R2 sector is the observationally-favored Starobinsky inflation — one operator across two epochs

**Date:** 2026-06-08
**Compute:** Vulcan. Formulas + numbers + citations confirmed with Dr. M.

---

## A major new sector

Every prior cycle treated the R² coupling g_R2 as a *late-universe* object — a
dark-energy-scale scalaron giving the sub-mm fifth force (v1.76–77). But the **same
R² operator**, at a *high* cutoff, is the **Starobinsky inflaton** — the
single-field inflation model the data prefers. v1.86 connects the engine's g_R2
sector to the early universe.

## The prediction (Dr. M.-confirmed)

Starobinsky R² inflation, to leading order in the e-fold number N:

        n_s = 1 − 2/N ,    r = 12/N² .

| N | n_s | r |
|---|---|---|
| 50 | 0.960 | 0.0048 |
| **55** | **0.964** | **0.0040** |
| 60 | 0.967 | 0.0033 |

The scalaron mass is fixed by the Planck scalar amplitude A_s ≈ 2.1×10⁻⁹ to
M ≈ 3×10¹³ GeV (the dimensionless R² coefficient ~10⁹ in Planck units).

## Confrontation — dead-center in the Planck sweet spot

Planck 2018 + BICEP/Keck 2021: **n_s = 0.9649 ± 0.0042, r < 0.036 (95%)**.

- Starobinsky N=55 (n_s=0.964, r=0.004) sits **within 1σ on n_s and an order of
  magnitude below the r limit** — `consistent_with_data = True`. R² inflation is
  *the/among the best-fit single-field models* (`inflation.png`).
- For contrast, the figure shows the **excluded** classics: m²φ² (r≈0.13, far above
  the BK limit) and natural inflation (disfavored). α-attractors and Higgs inflation
  sit *on top of* Starobinsky.
- **12 of 13 frameworks are inflation-viable** (g_R2 > 0) — every consistent
  framework except pure GR (g_R2 = 0, no R² term, hence no plateau).

## The multi-scale story — one operator, two epochs

The engine's dimensionless g_R2 is the coefficient common to both framings:

- **High cutoff** (scalaron mass ~3×10¹³ GeV, fixed by A_s) → **Starobinsky
  inflation**, the Planck sweet spot.
- **Low cutoff** (~meV, the dark-energy scale) → the **light scalaron / sub-mm fifth
  force** (v1.77, excluded unless screened).

One R² operator, two cosmological epochs. A *single real theory commits to one
scale* — these are alternative framings, not simultaneous predictions — but the
engine makes vivid that the *same* higher-derivative term the bootstrap/positivity
machinery constrains is, at the inflationary scale, the favored model of the early
universe, and at the dark-energy scale, the (screening-requiring) late-universe
scalaron.

## Honest scope (Dr. M.)

- **n_s and r are set by N** (the e-fold number / plateau geometry), **not** by the
  value of the dimensionless g_R2 (which only fixes the amplitude / energy scale).
  So the engine does *not* predict (n_s, r) from g_R2; the `StarobinskyInflation`
  observable's Jacobian w.r.t. every Wilson coefficient is **zero** by construction.
- The robust content is the **sign/viability**: a *positive* R² term (g_R2 > 0,
  which every consistent framework has) gives the plateau potential and hence the
  Planck sweet spot. R² inflation *works*, and is favored — that is the statement.

## What's committed

- `src/itb/gravitational_observables.py` — `StarobinskyInflation` (n_s, r, viability;
  zero Jacobian by design). Added to the `itb predict` fingerprint.
- `experiments/inflation.py` — the n_s–r plane vs Planck+BK with the Starobinsky
  line and excluded models (`inflation.png`).
- `experiments/master_scorecard.py` — an "inflation: best-fit / DETECTED" row.
- `tests/test_inflation.py` — 5 tests (formulas, Planck consistency, viability needs
  g_R2>0, observables coupling-independent, in fingerprint). Suite **484 green**.

## Headline

The engine's R² sector is the observationally-favored inflation model: g_R2 > 0
gives Starobinsky inflation at n_s ≈ 0.964, r ≈ 0.004 — dead-center in the Planck +
BICEP/Keck region — and the *same* operator is the dark-energy scalaron at a low
cutoff. One higher-derivative term, constrained by the bootstrap, spanning the
inflationary and dark-energy epochs.

## Reproducibility

```bash
python experiments/inflation.py
itb predict string_tree_eft      # includes starobinsky_inflation_ns_r_N55
pytest tests/test_inflation.py -q
```

*Citations: Starobinsky, PLB 98 (1980) 171; Planck 2018 inflation, Akrami et al.,
A&A 641 (2020) A6; BICEP/Keck, Ade et al., PRL 127 (2021) 261802.*
