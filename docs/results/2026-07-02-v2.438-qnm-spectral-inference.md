# v2.438 - the low-energy amplitude does infer part of the UV: the candidate's Hankel saturation shows a multi-state tower (not a single resonance), refining v2.437

Bold swing on the S-matrix / spectral-density angle, done *safely* with the bound the engine already has. The
matter dispersion bound `g_6² ≤ g_4·g_8` is Cauchy–Schwarz on the positive spectral density ρ(m²) (the Wilson
coefficients are its moments): it is **saturated** (s = g_6²/(g_4·g_8) = 1) iff ρ is a single δ — *one* massive
resonance — and **interior** (s < 1) iff ρ has support on two or more masses — a **multi-state tower**. So s reads
off part of the UV spectrum from the low-energy amplitude.

## Result

```text
  candidate saturation  s = 0.756   (INTERIOR < 1  =>  multi-state tower, NOT a single resonance)
  string_tree_eft       s = 0.80        asymptotic_safety  s = 0.75        cdt  s = 0.83
  (lqg 0.84, excluded;  pure_gr undefined -- no dim-8 matter)
  closest by saturation: asymptotic safety (0.75)   |   closest by distance: string (0.067)   -> rankings DIFFER
```

All 5/5 checks pass.

## What this establishes

**Two consequences.** *(1) A genuine partial UV inference:* amplitude positivity, beyond carving the couplings,
**establishes that the candidate's UV is a tower** (a spread spectral density) — ruling out a single-resonance /
weakly-coupled-single-state completion. The candidate is "stringy" in the concrete sense of *having many states*,
and the tower whose *existence* v2.375 asserted from log-convexity is here confirmed from the interior saturation.
*(2) It still does not discriminate the completions:* all are interior with s ≈ 0.75–0.84 within the O(1) encoder
spread, and — tellingly — the saturation **ranks them differently** from the parity-even distance (on saturation
the candidate is closest to asymptotic safety, on distance closest to string), so no single low-energy measure
picks a winner — reinforcing the robust UV-completion degeneracy (v2.436).

**Net, this sharpens exactly what the low-energy program can and cannot say about the UV:** it *can* confirm a
tower **exists** (the candidate is a multi-state theory, s < 1), but it *cannot* see the tower's **type** (Regge
vs Kaluza–Klein vs fixed-point) — the tower's existence is a low-energy fact, its character is the UV frontier.
The amplitude-carving program's honest UV output is: *"the candidate is a multi-state (stringy) EFT whose specific
UV completion the low-energy data cannot resolve — that needs the cutoff-scale spectrum."*

## Honest scope

The "saturation = single state, interior = tower" reading is the exact moment-problem interpretation
(Cauchy–Schwarz equality iff a single-δ measure) — rigorous *in structure* (the coefficients-as-moments
convention). **But** the candidate's g_6 = g_8 = 0.4 is a Chebyshev-center *artifact* (v2.392), so the precise
s = 0.756 is at an artifact point, not a prediction; the robust content is only that **s < 1** (interior ⇒
multi-state), which holds across the feasible region and all frameworks — not the exact value. The frameworks are
schematic O(1) encoders, so the per-framework s and the differing rankings are encoder-level (the point is that
they cluster and rank inconsistently, i.e. degeneracy, not the numbers). "Multi-state tower" is an inference about
the spectral density's *support* (≥2 masses), not a computed spectrum.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_spectral_inference.py --out experiments/results/v2.438/qnm_spectral_inference.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_spectral_inference.py -q
```

Result: candidate interior (s=0.756), all viable completions interior, rankings differ; `5 passed`.

## Next: the low-energy UV story is complete (a tower exists; its type is the frontier)

Between v2.436/437/438 the low-energy program's UV reach is now mapped: it confirms a multi-state tower and a
leading completion (string) but cannot resolve the completion or the tower type. Next: a genuinely different path
(the is-gravity-quantum foundational layer / BMV), or the user's steer. Bold swings, honest failures.

## References

```text
this repo: v2.437 (UV discriminator = tower type), v2.436 (UV degeneracy), v2.375 (log-convex tower asserted), v2.392 (g_6=g_8 Chebyshev artifact), dispersion_tower (the Hankel bound)
physics: Cauchy-Schwarz / Hankel positivity on the spectral density; saturation = single state, interior = multi-state; string/KK/fixed-point differ in tower TYPE not existence
```
