# v2.305 - The exact non-convexity criterion: the dent is a boundary layer

## Question

v2.304 showed the consistent theory space is non-convex via one counterexample. Two quantitative
questions remain: is there an EXACT criterion for which mixtures dent the region, and HOW MUCH of the
region is affected -- is the non-convexity generic, or confined?

## Result (an exact closed form; a boundary-layer dent)

Because the repulsive-force conjecture `g_4 g_6 - g_R2 - g_R2^2 >= 0` is a QUADRATIC form, the margin
along any mixing segment is exactly quadratic in the mix parameter `t`, so the midpoint margin is a closed
form with no Taylor remainder -- an identity, not a fit:

```text
  m_mid = (m_A + m_B)/2  -  (Dg4*Dg6 - Dg_R2^2)/4        (Dx = x_B - x_A)
```

It reproduces the engine to **4.4e-16 (machine precision)** over 400 random feasible pairs. The region
therefore dents -- two consistent theories mix to an inconsistent one -- by the EXACT criterion

```text
  Dg4*Dg6  -  Dg_R2^2  >  4 * (m_A + m_B)/2              (predicts every midpoint, 200/200)
```

Two things are now sharp:

**(1) WHERE.** The curvature term `-g_R2^2` is convex and only ever *lowers* the dent (`-Dg_R2^2` helps
feasibility), so the non-convexity lives ENTIRELY in the bilinear matter product `g_4 g_6`, and dents the
region only along directions where the two matter couplings CO-VARY (`Dg4*Dg6 > 0`, both larger or both
smaller together). Every denting pair found has co-varying matter; anti-correlated matter moves (g_4 up,
g_6 down) provably never dent.

**(2) HOW MUCH.** The dent is a thin BOUNDARY LAYER, not a generic interior effect:

```text
  depth eps from the repulsive-force wall      dent rate (random pairs at that depth)
  0.30                                          0%   (0/300)
  0.10                                          0%   (0/300)
  0.03                                          6%   (18/300)
  0.01                                         12%   (36/300)
```

Random interior mixes essentially never fail; the dent rate rises monotonically only as theories
approach the wall. Worst dent found: midpoint margin -0.047. All 5/5 checks pass.

## What this establishes

The consistent theory space is convex in its interior and dents only in a skin near the repulsive-force
wall, exactly where co-varying matter couplings sit. The swampland's non-convexity is therefore not a
vague "gravity is different" -- it is precisely the matter sector's bilinearity, confined to a measurable
boundary layer, with an exact direction (`Dg4*Dg6 > 0`) and an exact criterion, quantified to the
coefficient. This turns the v2.304 existence result into a complete local characterization.

## Honest scope

The closed form is EXACT (machine precision), not approximate -- a quadratic constraint has no Taylor
remainder, so this is an identity. It is derived for the engine's encoded repulsive-force conjecture
specifically (`gamma=1`, the bilinear `g_4 g_6` form); the criterion and the co-variation reading are
exact for THAT constraint, and since it is one genuine member of the 38-constraint stack, the full
feasible region inherits the dent along these directions. The boundary-layer dent RATES are
sampler-dependent (uniform g_4, g_6 in a box; g_R2 fixed to sit at depth eps); the specific percentages
are not physical, but the QUALITATIVE structure -- ~0% in the interior, rising monotonically as eps -> 0
-- is robust and follows from the exact criterion (denting needs the chord margin small, i.e. near the
boundary). Other constraints add their own convex walls that only REDUCE the achievable dent within the
true feasible region; the criterion remains the exact local non-convexity condition. Toy basis, O(1)
prefactors. A quantified, exact follow-up to v2.304.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_nonconvexity_exact_criterion.py --out experiments/results/v2.305/qnm_nonconvexity_exact_criterion.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_nonconvexity_exact_criterion.py -q
```

Result: closed form matches the engine to 4.4e-16; the criterion predicts every midpoint; the dent rate
is ~0% in the interior and rises to ~12% at depth 0.01 (a boundary layer); anti-correlated matter never
dents; `7 passed`.

## Next (v2.306)

The non-convexity is now exactly characterized (criterion + boundary layer). Options: a fresh swing in a
different sector, or a synthesis of the structural arc (v2.304 non-convex, v2.305 exact criterion).
Honest negatives reported as part of the search.

## References

```text
this repo: v2.304 (non-convexity counterexample), v2.284 (repulsive-force anatomy), v2.292 (moment tower)
Caron-Huot, Van Duong 2021 (the EFT-hedron is a convex positive geometry); convex-WGC discussions
src/itb/constraints/swampland_variants.py (RepulsiveForceConjecture)
```
