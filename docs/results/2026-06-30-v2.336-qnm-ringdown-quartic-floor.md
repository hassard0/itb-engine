# v2.336 - The constructed theory's ringdown-active quartic curvature: the smallest of all candidates

## Question

A fresh seed reconnecting to the project's namesake -- quasi-normal modes / ringdown. The program has
tested the constructed theory only in the PARITY sector. The quartic curvature `g_R4` is the
RINGDOWN-active operator (it deforms the (2,2,0) Schwarzschild quasi-normal mode) and has never been
analysed for the constructed theory. What does the constructed theory predict for it?

## Result (a nonzero but minimal ringdown-active quartic)

With the curvature tower on, the moment-tower mandate `g_R4 >= g_R3^2/g_R2` forces a nonzero quartic
whenever the cubic `g_R3` is nonzero. The constructed theory requires `g_R4 >= 0.042`, and `g_R4 = 0` is
infeasible. The floors, ordered:

```text
  theory              g_R4 floor = g_R3^2/g_R2
  pure_gr             0.0000   (g_R3 = 0 -> no ringdown shift)
  engine_constructed  0.0420   <- smallest nonzero
  asymptotic_safety   0.0667
  cdt                 0.1023
  string_tree_eft     0.1125
  lqg_induced         0.3000
```

Because the constructed theory carries the most TRIMMED cubic curvature, its quartic floor is the SMALLEST
of all candidates -- so it predicts the MILDEST ringdown deviation among the higher-derivative frameworks,
while pure GR has `g_R4 = 0` and no shift. All 5/5 checks pass.

## What this establishes

The ringdown channel is a clean, ordered CP-even discriminator complementary to the parity-sector tests
(birefringence, chiral GW, leptogenesis): a sufficiently precise (2,2,0) ringdown measurement would place
each candidate by the size of its quartic-curvature deviation -- pure GR at zero, the constructed theory
just above it, and the community frameworks higher up -- testing the curvature sector the parity probes do
NOT. Notably, the constructed theory's trimmed curvature, which made it consistent in the first place, also
makes it the most GR-like in ringdown: the hardest of the higher-derivative candidates to distinguish from
Einstein gravity by a ringdown measurement. This reconnects the program to its original ringdown thread.

## Honest scope

The g_R4 FLOOR is rigorous and exact: the moment-tower mandate `g_R4 >= g_R3^2/g_R2` is the engine's literal
constraint (v2.292), the floor values are exact arithmetic, and the feasibility verdicts (`g_R4 = 0`
infeasible, floor feasible) are the engine's literal output with the curvature tower on. What is SCHEMATIC
is the map from `g_R4` to an actual ringdown frequency/damping shift: the deep-research thread established
that the published qNM->R^4 sensitivity is only a rank-1 (single-combination) ray with order-of-magnitude
coefficients, so the SIZE of the (2,2,0) deviation per unit `g_R4` is not sourced here -- only the floor on
`g_R4` and the ORDERING of the candidates (constructed smallest, pure GR zero) are claimed. The floors
depend on the encoded `g_R2`, `g_R3` (toy O(1) values); the ordering follows from the constructed theory's
trimmed cubic curvature and is robust to that. This is a curvature-sector (CP-even) prediction, independent
of the cosmic-birefringence data (it does not use `g_R2_parity`), so unlike the parity headline it does NOT
rest on the v2.329 birefringence caveat. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_ringdown_quartic_floor.py --out experiments/results/v2.336/qnm_ringdown_quartic_floor.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_ringdown_quartic_floor.py -q
```

Result: the constructed theory requires `g_R4 >= 0.042` (g_R4=0 infeasible), the smallest nonzero floor of
all candidates; pure GR sits at zero; the ordering places the constructed theory just above GR; `6 passed`.

## Next (v2.337)

The constructed theory now has a CP-even ringdown prediction (the mildest deviation). A fresh sector or
continued audits, at a deliberate pace. Honest negatives reported as part of the search.

## References

```text
this repo: v2.292 (moment tower g_R4 >= g_R3^2/g_R2), v2.309 (joint region g_R4 floors), v2.317 (constructed framework)
the (2,2,0) quasi-normal mode / quartic-curvature ringdown deformation (rank-1 qNM->R^4 sensitivity, deep-research thread)
```
