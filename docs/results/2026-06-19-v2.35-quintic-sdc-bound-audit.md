# v2.35 - Quintic SDC bound audit: finite-range evidence is not an asymptotic test

**Date:** 2026-06-19
**Compute:** local run plus fresh Vulcan clone `~/codex-runs/itb-engine-main-v235a`
**Input:** v2.34 quintic tower evidence candidate, Ashmore/Ruehle `2103.07472`, and Etheredge et al. `2206.04063`
**Artifact:** `experiments/results/v2.35/quintic_sdc_bound_audit.json`

## Question

v2.34 added the first non-synthetic, primary-source tower candidate. v2.35 asks:

> Does the sourced quintic KK slope adjudicate any SDC bound or framework claim?

No. It gives a real finite-range slope, but the asymptotic SDC-bound comparison is
scope-mismatched.

## Source Slope

Ashmore/Ruehle fit a quintic scalar Laplacian eigenvalue as:

```text
lambda_3 ~= 56.4 * exp(-(0.906 +/- 0.034) d(2,rho))
```

Since `m_KK ~ sqrt(lambda)`, v2.34 converted this to:

```json
{
  "alpha_mean": 0.453,
  "alpha_95ci": [0.436, 0.470]
}
```

## Bound Contexts

| bound context | value | result |
|---|---:|---|
| Ashmore/Ruehle finite-range `1/sqrt(6)` reference | `0.4082482904638631` | candidate 95% interval above bound |
| Etheredge et al. d=4 asymptotic lightest-tower bound `1/sqrt(2)` | `0.7071067811865475` | candidate 95% interval below bound, but not adjudicable |
| Etheredge et al. d=10 reference `1/sqrt(8)` | `0.35355339059327373` | context only, not the 4D engine discriminator |

The d=4 comparison is not a falsification because the scopes are different:

- Etheredge et al. discuss an asymptotic lightest-tower bound in d dimensions.
- Ashmore/Ruehle compute a finite-range scalar Laplacian subtower on one quintic
  compactification.
- The current engine needs a framework-owned tower spectrum, not a single source
  comparison.

## Command

```bash
python experiments/quintic_sdc_bound_audit.py \
  --out experiments/results/v2.35/quintic_sdc_bound_audit.json
```

Remote verification:

```bash
python tools/vulcan.py run \
  'set -e; cd ~/codex-runs/itb-engine-main-v235a; \
   .venv/bin/python -m pytest tests/test_quintic_sdc_bound_audit.py -q; \
   .venv/bin/python -m ruff check \
     experiments/quintic_sdc_bound_audit.py \
     tests/test_quintic_sdc_bound_audit.py; \
   .venv/bin/python experiments/quintic_sdc_bound_audit.py \
     --out experiments/results/v2.35/quintic_sdc_bound_audit.json'
```

## Result

```json
{
  "decisive_sdc_tests_now": [],
  "claimable_framework_exclusions_now": []
}
```

## Bottom line

The sourced quintic candidate is useful evidence, not a solved discriminator. The next
scientific step is either an asymptotic lightest-tower extraction for this compactification
or a scoped framework variant that explicitly represents the one-parameter quintic.
