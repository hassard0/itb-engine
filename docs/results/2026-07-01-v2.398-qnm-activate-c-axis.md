# v2.398 - SWING (executes v2.397): activating the Weyl^2 axis g_C resolves c != a -- carved solely by the Hofman-Maldacena wedge, and it moves the ghost

## Question

v2.397 identified resolving the c-a degeneracy (a distinct Weyl^2 coupling `g_C != g_R2`) as the highest-impact
next extension. Execute it -- what does the resolved c-axis reveal?

## Result (the extension was already built, just dormant)

`g_C` is a first-class coefficient that falls back to `g_R2` (a = c, a/c = 1) when unset, and the
Hofman-Maldacena wedge (`1/3 <= a/c <= 31/18`, a = `g_R2` Euler, c = `g_C` Weyl^2) is already in the stack -- so
**no core change was needed**; the c-axis just had to be turned on.

```text
  g_R2 = 0.193 fixed, g_C free:
    feasible g_C in [0.114, 0.579]  ->  a/c = g_R2/g_C in [0.333, 1.697]
    == Hofman-Maldacena wedge [1/3, 31/18] = [0.333, 1.722]   (matches to ~1%)
    BOTH edges bound SOLELY by hofman_maldacena_wedge
  constructed (g_C = g_R2, a/c = 1) sits DEAD-CENTER in the wedge (holographic a = c)
  Weyl^2 ghost mass now tracks g_C: m_g/Lambda = 1/sqrt(g_C) in [1.31, 2.97]  (was fixed 2.28, v2.385)
```

All 5/5 checks pass. Resolving c != a opens **exactly one** new direction, carved **solely** by the
conformal-collider (ANEC) wedge, and it is **bounded** (a/c stays in `[1/3, 31/18]`), not free.

## What this establishes

v2.397 executed and validated: the Hofman-Maldacena wedge is the one constraint that goes live when c != a and
is the sole carver of the new axis, exactly as predicted. Activating `g_C` physically **moves the Weyl^2
ghost** -- its mass now tracks `g_C` (not `g_R2`), so the c-a split separates the Weyl^2 sector (ghost, HM,
screening) from the Ricci^2/Euler sector (a-theorem, anomaly) that the degeneracy had fused into the single
keystone `g_R2` (v2.396). The candidate theory is **unchanged** -- it stays at `a = c` (the natural holographic /
Einstein value, dead-center in the wedge); what changes is that the basis now *resolves* the Euler-vs-Weyl^2
freedom the toy basis had hidden, and that freedom is a bounded one-parameter c-a split. This is the concrete
next step the program needed, and it cost no core surgery because the engine was already built for it.

## Honest scope

This ACTIVATES a dormant-but-built axis: `g_C` and the HM wedge already exist (authored v1.71); the swing sets
`g_C` independently (which every `g_C`-aware constraint already reads) and maps the result. So it is genuinely
the c != a extension, but it required no new code -- a point in its favour (safe, reversible) and a caveat (the
wedge physics was authored earlier; this executes and validates it, it does not invent it). The a/c wedge is
the exact source-cited Hofman-Maldacena bound, and its matching the feasible `g_C` range to ~1% is a real check
that HM is the sole carver. The ghost-mass `g_C`-dependence uses v2.385's O(1)-schematic relation, so the
[1.31, 2.97] range is O(1)-schematic; the robust point is that the ghost mass now *varies* with the c-a split.
The candidate theory is UNCHANGED (stays at a = c), so this adds no new prediction about the constructed point;
it characterizes the FREEDOM the resolved basis exposes and confirms v2.397. A full c != a exploitation
(re-running the swing arc with `g_C` free to see whether the candidate should move off a = c) is the natural
follow-up. Robust content: activating `g_C` opens exactly one new axis, carved solely by the HM ANEC wedge to
a/c in `[1/3, 31/18]`, constructed dead-center, ghost mass now tracking `g_C` -- v2.397 executed and validated
at no core-change cost.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_activate_c_axis.py --n 400 --out experiments/results/v2.398/qnm_activate_c_axis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_activate_c_axis.py -q
```

Result: feasible `g_C` in [0.114, 0.579] == a/c [0.333, 1.697] (HM wedge); sole carver HM; ghost [1.31, 2.97];
`5 passed`.

## Next (v2.399)

The c-axis is resolved and bounded. Natural follow-up: re-examine whether the candidate should move off a = c
(a full c != a exploitation), or a fresh swing. Bold swings, honest failures.

## References

```text
this repo: v2.397 (resolve c-a = highest-impact next step), src/itb/constraints/hofman_maldacena.py (a/c wedge, a=g_R2/c=g_C), v2.396 (g_R2 keystone / c-a collapse), v2.385 (Weyl^2 ghost), v1.71 (HM wedge results note)
physics: Hofman-Maldacena 2008 (conformal collider, 1/3<=a/c<=31/18); Euler (a) vs Weyl^2 (c) central charges; two-derivative holographic duals force a=c
```
