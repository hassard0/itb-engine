# v2.233 - Why the ringdown thread and the core Wilson engine probe complementary sectors

## Question

After 16 cycles of black-hole ringdown / QNM physics, what is the precise structural relationship
to the repo's core 7-D Wilson engine (`g_4, g_6, g_8, g_R2, g_R3, g_R2_parity, g_R3_parity`) -- and
why have the two not directly connected? (The v2.232 reconnaissance proposed a "ringdown constraint
on `g_8`" bridge; is that right?)

## Result

Schwarzschild curvature invariants (M=1), verified symbolically on Vulcan (sympy 1.14) and
re-verified numerically here from the orthonormal tidal tensor:

```text
Ricci scalar R = 0,   Ricci tensor R_ab = 0   (Schwarzschild is Ricci-flat / vacuum)
Kretschmann   K = R_abcd R^abcd = 48 M^2 / r^6  != 0   (checked at r = 3, 5, 10: exact match)
Gauss-Bonnet  G = K   (since R = R_ab = 0)
```

Operator-sector classification for **tensor-ringdown activity** on Schwarzschild:

```text
operator                          on-shell value     ringdown-active   in engine basis
g_R2  (R^2, Ricci scalar)         0   (R = 0)            NO                YES
g_R3  (R^3, Ricci scalar)         0   (R = 0)            NO                YES
Gauss-Bonnet (quad. Riemann)      K (4D topological)     NO                no
Riemann^4 (quartic, dim-8 "R4")   ~ K^2 != 0             YES               no
g_8   (matter s^4 moment)         --  (not curvature)    NO                YES
```

**The two threads probe complementary operator sectors.** Because Schwarzschild is Ricci-flat, the
engine's Ricci-scalar curvature operators (`g_R2`, `g_R3`) vanish on-shell and are ringdown-blind
for the tensor modes; the quadratic-Riemann Gauss-Bonnet term is 4D-topological (inert); and the
FIRST curvature operator that modifies the Schwarzschild tensor ringdown is the QUARTIC Riemann
invariant (`~K^2`, dimension-8) -- exactly the "R4" the QNM thread studied (Bresciani; SGB), because
`K != 0` on-shell. That ringdown-active operator is **NOT in the engine's basis** (which stops at the
cubic Ricci scalar `g_R3`), and the engine's `g_8` is the **matter spin-4 moment, not a curvature
operator** -- so the v2.232 "ringdown constraint on `g_8`" idea was a misidentification.

**Bridging the threads requires EXTENDING the engine with a Riemann^4 (`g_R4`) curvature axis**, not
adding a constraint on an existing coefficient.

## What this establishes

A rigorous, verifiable structural explanation of the 16-cycle ringdown thread's relationship to the
repo's core engine: they are complementary, separated by the Ricci-flatness of Schwarzschild, which
makes the leading ringdown-active curvature operator the dimension-8 Riemann^4 that lies one order
beyond the engine's Ricci-scalar basis. It corrects the earlier bridge misidentification and
defines the concrete (if larger) work a real bridge needs: a `g_R4` basis extension.

## Honest scope

Leading-order, Schwarzschild (non-rotating), tensor-sector statement: `f(R)` gravity DOES add a
separate massive SCALAR mode, and the Ricci-scalar operators can matter on rotating or
matter-sourced backgrounds -- "ringdown-blind" is specifically the leading tensor QNM on the
Ricci-flat Schwarzschild background. The `Riemann^4 -> ringdown` link is the established literature
result the QNM thread used; the absolute `g_R4` normalization stays un-sourceable (the standing
v2.215 / Bresciani-axis-map blocker). This is a STRUCTURAL clarification, not a new bound. Parity-odd
`g_R4_c3` stays dark (v2.209).

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_engine_operator_sector_bridge.py --out experiments/results/v2.233/qnm_engine_operator_sector_bridge.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_engine_operator_sector_bridge.py -q
```

Result: `K = 48/r^6` matched at `r = 3, 5, 10`; Ricci-flat confirmed; operator-sector
classification; `4 passed`. Curvature invariants cross-verified on Vulcan (sympy): `R = 0`,
`K = 48 M^2/r^6`, `G = K`.

## Next (v2.234)

Either (a) the dedicated `g_R4` basis-extension scoping for the engine (what positivity / causality
constraints a Riemann^4 axis would carry, from the existing efthedron/causality machinery -- a real
multi-tick effort), (b) the deferred Leaver high-accuracy solver, or (c) a fresh QG observable.

## References

```text
curvature invariants verified on Vulcan (sympy 1.14): R = 0, K = 48 M^2/r^6, G = K
Bresciani et al. (arXiv:2504.12855); Silva, Ghosh, Buonanno (arXiv:2205.05132) -- R4 ringdown
this repo: core 7-D Wilson engine (g_4..g_R3_parity), v2.215 (R4 normalization blocker)
```
