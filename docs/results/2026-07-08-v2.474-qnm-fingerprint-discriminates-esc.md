# v2.474 - the scale-clean fingerprint discriminates the ESC dichotomy: the candidate favors the heterotic-string branch

v2.440 left the Emergent String Conjecture dichotomy **open** — the candidate's tower is a heterotic-string tower
XOR a Planckian-KK decompactification — and v2.438 concluded the low-energy data can confirm a tower *exists* but not
its *type*. That predates the scale-clean fingerprint (v2.464–466). Connecting them updates the verdict.

The two branches have **different mass spectra**, hence different scale-clean double-ratios:

```text
  Regge (string):  m^2 ~ n  =>  a_p ~ x^p zeta(p)   =>  (a_2 a_4)/a_3^2 = zeta(2)zeta(4)/zeta(3)^2 = 1.23
  KK (decompact.): m   ~ n  =>  a_p ~ y^p zeta(2p)  =>  (a_2 a_4)/a_3^2 = zeta(4)zeta(8)/zeta(6)^2 = 1.05
```

Both are **scale-independent** (the string scale / KK radius cancels, as in v2.464), but they **differ** (1.23 vs
1.05) because m²~n and m~n put different ζ-arguments in the moments. So the scale-clean double-ratio — a purely
**low-energy** quantity — discriminates the tower *type*, which v2.438 (before the fingerprint existed) said the
low-energy data could not do.

The candidate's low double-ratio (g_4·g_8)/g_6² = **1.32** is close to Regge (~7% off) and far from KK (~26% off) —
so it **favors the heterotic-string branch**, giving the heterotic identification (v2.434) independent low-energy
support and sharpening the v2.440 dichotomy toward the string branch. 5/5 checks pass.

## Honest scope

A **proof-of-concept at the flat-residue toy level**, not a proof. The 1.23/1.05 values assume flat residues on each
branch; real heterotic and real KK residues differ, shifting both — so the **separation** (1.23 vs 1.05) is the
robust content (m²~n vs m~n is a genuine spectral distinction), but the precise numbers and the 7%-vs-26% margin are
toy-level. Only the **low rung** discriminates: the candidate's high double-ratio is moment-tower-floor-saturated
(g_10 at the floor, a Chebyshev artifact) and unusable. The candidate's 1.32 itself partly reflects the g_6 = g_8
Chebyshev artifact. And this discriminates only *within* the ESC dichotomy (Regge vs KK) — it doesn't exclude the
non-string completions (CDT/asymptotic-safety) the ESC presupposes away. So the robust claim: a scale-clean
**preference** for the string branch at the toy level, updating v2.438's "low-energy can't get the tower type" to
"the fingerprint gives a partial, toy-level handle, favoring Regge."

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_fingerprint_discriminates_esc.py --out experiments/results/v2.474/qnm_fingerprint_discriminates_esc.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_fingerprint_discriminates_esc.py -q
```

Result: Regge 1.23 vs KK 1.05 (differ, both scale-independent); candidate 1.32 favors Regge (7% vs 26%); `3 passed`.

## References

```text
this repo: v2.440 (ESC dichotomy, open), v2.438 (low-energy confirms tower exists NOT type), v2.464-466 (scale-clean fingerprint), v2.434 (heterotic ID), v2.392 (g_6=g_8 Chebyshev artifact)
physics: Regge m^2 ~ n (string) vs KK m ~ n (decompactification); moment zeta-values; Emergent String Conjecture (Lee-Lerche-Weigand 2019)
```
