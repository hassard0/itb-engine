# v2.149 - R4 adapter registry hook: internal projection, no promotion

## Question

v2.148 built a policy-scoped `string_tree_eft` R4 projection packet. Can that
packet be exposed through a registry-style adapter hook for internal projection
queries without mutating the live framework registry or promoting a framework
claim?

## Result

Artifact:
`experiments/results/v2.149/r4_adapter_registry_hook.json`

Current route status:

```text
r4_adapter_registry_hook_ready_nonpromoting
```

Claimable framework exclusions now:

```text
[]
```

## Registry Hook

The registry exposes one adapter:

```text
adapter_id = string_tree_eft_r4_shape_policy_unit_v1
framework = string_tree_eft
axis_family = gravity_R4_Riemann4
registration_scope = internal_projection_algebra
claim_path_enabled = false
framework_registry_mutation = false
```

Internal exposure status:

```text
adapter_exposed_for_internal_use = true
policy_scope_ready = true
base_projection_ready = true
strict_source_projection_ready = true
exposure_blockers = []
```

Claim promotion remains disabled:

```text
claim_promotion_allowed = false
claimable_framework_exclusion_now = false
guard_ready_for_framework_claim = false
```

Current claim blockers:

```text
absolute_type_II_string_alpha_prime_R4_coefficient
discriminator_math_not_excluding
engine_Lambda_R4_unit_conversion
measurement_likelihood_missing_or_incomplete
registry_claim_path_disabled
source_backed_K_Russo_to_Kallosh_shape_bridge
```

## Interpretation

The R4 adapter is now queryable by future internal tooling. This is deliberately
not a mutation of `itb.predict.FRAMEWORKS`, not a live claim path, and not a
framework exclusion. It is the wiring needed to let future R4 diagnostics consume
the v2.148 packet without bypassing the v2.133/v2.137 guards.

## Next Build Action

```text
wire_r4_registry_hook_into_projection_query_surface
```

The next artifact should add a query-surface or frontier-side reader that
consumes the registered R4 projection packet for diagnostics while preserving
the measurement-likelihood and absolute-normalization claim blockers.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/r4_adapter_registry_hook.py --out experiments/results/v2.149/r4_adapter_registry_hook.json
.venv/Scripts/python.exe -m pytest tests/test_r4_adapter_registry_hook.py -q
.venv/Scripts/python.exe -m ruff check experiments/r4_adapter_registry_hook.py tests/test_r4_adapter_registry_hook.py
```

Result:

```text
wrote experiments\results\v2.149\r4_adapter_registry_hook.json
6 passed
All checks passed!
1184 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2149
base = b4fd509
```

Determinism:

```text
5cdd7f8d8f81559e1f60bc4bd19f8dc08605e22dddbd1d6d76eaaa8f1e2ba0c8  docs/results/INDEX.md
dd7369bc80c5a64fc54142ae1adee9122ac849b3b34885a247735493ea9d3c87  experiments/results/v2.149/r4_adapter_registry_hook.json
```

Commands:

```bash
.venv/bin/python experiments/r4_adapter_registry_hook.py --out experiments/results/v2.149/r4_adapter_registry_hook.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.149/r4_adapter_registry_hook.json
.venv/bin/python -m pytest tests/test_r4_adapter_registry_hook.py -q
.venv/bin/python -m ruff check experiments/r4_adapter_registry_hook.py tests/test_r4_adapter_registry_hook.py
.venv/bin/python -m pytest -q
```

Result:

```text
6 passed
All checks passed!
1191 passed, 1 warning
```
