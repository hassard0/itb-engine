# v2.105 - GW alpha likelihood stub: source-native packet harness

## Question

v2.104 selected the next implementation step:

```text
implement_minimal_alpha_waveform_likelihood_stub
```

Can we build an executable source-native alpha likelihood surface with the right
packet contract, while keeping it nonclaiming until public strain is connected?

## Result

Artifact:
`experiments/results/v2.105/gw_alpha_likelihood_stub.json`

Current route status:

```text
minimal_alpha_likelihood_stub_ready_synthetic_only
```

Claimable discriminator now:

```text
false
```

## Stub

The stub uses the v2.103 covariance fixture and defines a quadratic likelihood
over:

```text
alpha_bar_1
alpha_bar_2
```

It exports a v2.102-compatible source-native packet with:

```text
posterior_or_likelihood_export.status = reproduced_source_native_likelihood
```

The v2.102 adapter accepts the packet shape:

```text
native_adapter_ready = true
adapter_blockers = []
```

It remains nonclaiming because:

```text
synthetic_fixture_not_real_source
engine_projection_not_ready
g8_joint_component_missing
```

## Next Build Action

```text
connect_public_strain_to_alpha_waveform_likelihood
```

## Interpretation

The route now has an executable likelihood harness. This is not evidence, but it
does remove ambiguity from the next implementation step: replace the quadratic
stub residual with a public-GW170608 strain-driven waveform likelihood while
preserving the v2.102 packet contract.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_alpha_likelihood_stub.py --out experiments/results/v2.105/gw_alpha_likelihood_stub.json
.venv/Scripts/python.exe -m pytest tests/test_gw_alpha_likelihood_stub.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_alpha_likelihood_stub.py tests/test_gw_alpha_likelihood_stub.py
```

Focused validation passed with 6 tests. Ruff passed.

## Vulcan Reproduction

Validated on `vulcan` in a clean checkout at
`/home/admin/codex-runs/itb-engine-main-v2105`.

```bash
.venv/bin/python experiments/gw_alpha_likelihood_stub.py --out experiments/results/v2.105/gw_alpha_likelihood_stub.json
.venv/bin/python -m pytest tests/test_gw_alpha_likelihood_stub.py -q
.venv/bin/python -m ruff check experiments/gw_alpha_likelihood_stub.py tests/test_gw_alpha_likelihood_stub.py
.venv/bin/python -m pytest -q
```

Focused validation passed with 6 tests. Ruff passed. Full-suite validation
passed with 950 tests and one existing Starlette/httpx deprecation warning.
