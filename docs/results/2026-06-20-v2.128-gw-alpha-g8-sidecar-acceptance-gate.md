# v2.128 - GW alpha/G8 sidecar gate: executable, no real packet

## Question

v2.127 selected the next implementation step:

```text
obtain_or_publish_external_g8_sidecar_packet
```

Can we make the external G8 sidecar contract executable so future real packets
can be accepted or rejected reproducibly?

## Result

Artifact:
`experiments/results/v2.128/gw_alpha_g8_sidecar_acceptance_gate.json`

Current route status:

```text
g8_sidecar_acceptance_gate_ready_no_real_packet
```

Claimable discriminator now:

```text
false
```

## Method

The harness implements a sidecar evaluator for:

```text
primary source URL
external G8 numeric value or bound
engine G8 normalization and Jacobian
public G8 likelihood/covariance
cross-covariance with alpha or bounded independence proof
closed G8 systematics
shared EFT domain
framework projection and exclusion math
```

It runs two fixtures:

```text
synthetic_ready_g8_sidecar_packet
incomplete_g8_sidecar_packet
```

## Result Status

```text
acceptance_ready_sample_packets = [synthetic_ready_g8_sidecar_packet]
claim_ready_sample_packets = []
claimable_discriminator_now = false
```

The synthetic fixture proves the positive validation path, but it is explicitly
blocked from claim promotion:

```text
synthetic_fixture_not_real_source
```

The incomplete packet is rejected with actionable blockers, including:

```text
engine_g8_normalization_missing
public_g8_likelihood_or_covariance_missing
cross_covariance_with_alpha_missing
g8_systematics_not_closed
framework_pair_exclusion_math_missing
```

## Next Build Action

```text
run_gate_on_real_external_g8_sidecar_packet
```

## Interpretation

The repo can now validate the missing G8 object. This does not solve the
discriminator because no real external sidecar packet is available. The next
movement requires an actual public G8 packet, not another internal alpha
calculation.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_alpha_g8_sidecar_acceptance_gate.py --out experiments/results/v2.128/gw_alpha_g8_sidecar_acceptance_gate.json
.venv/Scripts/python.exe -m pytest tests/test_gw_alpha_g8_sidecar_acceptance_gate.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_alpha_g8_sidecar_acceptance_gate.py tests/test_gw_alpha_g8_sidecar_acceptance_gate.py
```

Passed:

```text
3 passed in 0.60s
All checks passed!
```

Full local suite:

```text
1068 passed, 7 skipped, 1 warning in 25.77s
```

## Vulcan Reproduction

Fresh clone with the v2.128 patch applied on `admin@192.168.4.178`:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -e '.[dev]' lalsuite
.venv/bin/python tools/build_index.py
.venv/bin/python experiments/gw_alpha_g8_sidecar_acceptance_gate.py --out experiments/results/v2.128/gw_alpha_g8_sidecar_acceptance_gate.json
.venv/bin/python -m pytest tests/test_gw_alpha_g8_sidecar_acceptance_gate.py -q
.venv/bin/python -m ruff check experiments/gw_alpha_g8_sidecar_acceptance_gate.py tests/test_gw_alpha_g8_sidecar_acceptance_gate.py
.venv/bin/python -m pytest -q
```

Before regeneration, the validation recorded SHA-256 hashes for
`docs/results/INDEX.md` and the v2.128 result JSON; after rerunning the index
builder and sidecar gate exporter, both files matched byte-for-byte.

Passed:

```text
docs/results/INDEX.md: OK
experiments/results/v2.128/gw_alpha_g8_sidecar_acceptance_gate.json: OK
3 passed in 0.47s
All checks passed!
1075 passed, 1 warning in 45.35s
```
