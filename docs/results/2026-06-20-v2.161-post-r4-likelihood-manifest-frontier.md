# v2.161 - Post-R4 likelihood manifest frontier: R4 is top diagnostic route, no claim

## Question

After v2.160 made the R4 shape-likelihood packet contract reusable, what is the
current discriminator frontier across R4 shape, g_8, native tower, Weyl/g_8,
parity, and R4 scale routes?

## Result

Artifact:
`experiments/results/v2.161/post_r4_likelihood_manifest_frontier.json`

Current route status:

```text
post_r4_frontier_diagnostic_ready_no_claim_ready_route
```

Claimable framework exclusions now:

```text
[]
```

Current in-repo promotion-ready routes:

```text
[]
```

## Frontier Ranking

Priority order:

```text
1. future_public_r4_shape_likelihood_ingestion
2. future_public_g8_measurement_ingestion
3. framework_specific_native_tower_search
4. weyl_g8_joint_frontier
5. gw_parity_operator_normalization_search
6. r4_symbolic_scale_resolution
```

Diagnostic-ready routes:

```text
future_public_r4_shape_likelihood_ingestion
r4_symbolic_scale_resolution
```

The top route is R4 shape-likelihood ingestion because v2.158-v2.160 now give
the engine a source-backed shape diagnostic, public-source search ledger, and
strict future packet manifest. That makes the route operationally ready for
future evidence, but not evidence-backed today.

## Claim Boundary

The top R4 route remains blocked by:

```text
public_r4_shape_likelihood_or_covariance_missing
maps_to_bresciani_r4_axes_missing
axis_normalization_declared_missing
excluding_discriminator_math_missing
```

The public search ledger also records missing packet capabilities across checked
sources:

```text
public_likelihood_or_covariance
maps_to_bresciani_r4_axes
axis_normalization_declared
excluding_discriminator_math
reproducible_data_or_code
```

`r4_ready_likelihood_packets_now` is empty, so the correct next artifact is to
ingest a future public R4 shape likelihood packet only if it passes the v2.160
manifest. Until then, R4 stays diagnostic and non-claiming.

Selected next build action:

```text
Ingest a future public R4 shape likelihood packet only if it passes the v2.160 manifest; otherwise keep the R4 diagnostic non-claiming.
```

## Local Validation

```bash
.venv/Scripts/python.exe experiments/post_r4_likelihood_manifest_frontier.py --out experiments/results/v2.161/post_r4_likelihood_manifest_frontier.json
.venv/Scripts/python.exe -m pytest tests/test_post_r4_likelihood_manifest_frontier.py tests/test_r4_shape_likelihood_packet_manifest.py -q
.venv/Scripts/python.exe -m ruff check experiments/post_r4_likelihood_manifest_frontier.py tests/test_post_r4_likelihood_manifest_frontier.py
```

Result:

```text
wrote experiments\results\v2.161\post_r4_likelihood_manifest_frontier.json
10 passed
All checks passed!
```

Full local suite:

```text
1251 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2161
base = 0c38fcd
```

Determinism:

```text
5fc496693ba46ac8d404e8b14ddbbcc0bc14265e8eee97a7d058cdf8d434ab0b  docs/results/INDEX.md
11a98bc7c24385d7b6ed279ce44d80ffdf0cb2599e56ce9f8d4a00603415d097  experiments/results/v2.161/post_r4_likelihood_manifest_frontier.json
```

Commands:

```bash
.venv/bin/python experiments/post_r4_likelihood_manifest_frontier.py --out experiments/results/v2.161/post_r4_likelihood_manifest_frontier.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.161/post_r4_likelihood_manifest_frontier.json
.venv/bin/python -m pytest tests/test_post_r4_likelihood_manifest_frontier.py tests/test_r4_shape_likelihood_packet_manifest.py -q
.venv/bin/python -m ruff check experiments/post_r4_likelihood_manifest_frontier.py tests/test_post_r4_likelihood_manifest_frontier.py
.venv/bin/python -m pytest -q
```

Result:

```text
10 passed
All checks passed!
1258 passed, 1 warning
```
