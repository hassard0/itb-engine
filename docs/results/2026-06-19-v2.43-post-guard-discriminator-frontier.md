# v2.43 - Post-guard discriminator frontier: live catalogue is still blocked earlier

**Date:** 2026-06-19
**Compute:** local run plus fresh Vulcan clone `~/codex-runs/itb-engine-main-v243a`
**Input:** v2.41 promotion guard and v2.42 frontier classifier
**Artifact:** `experiments/results/v2.43/post_guard_discriminator_frontier.json`

## Question

v2.41 and v2.42 installed and tested the tower-promotion guard. v2.43 asks:

> What does the real registered-framework frontier look like after the guard is wired in?

## Result

```json
{
  "registered_framework_count": 13,
  "tower_discriminator_claim_ready": [],
  "claimable_framework_exclusions_now": [],
  "promotion_guard_ready_frameworks": [],
  "promotion_guard_positive_control_blocked_frameworks": []
}
```

Frontier status counts remain:

| status | count |
|---|---:|
| `missing_native_tower_spectrum` | `8` |
| `reference_excluded_before_tower` | `3` |
| `scope_limited_reference_survivor` | `2` |

Every framework row now carries `tower_promotion_guard`, but the live catalogue is still
blocked before that gate: no reference-feasible in-scope registered framework supplies
native non-synthetic tower evidence.

## Command

```bash
python experiments/post_guard_discriminator_frontier.py \
  --out experiments/results/v2.43/post_guard_discriminator_frontier.json
```

Remote verification:

```bash
python tools/vulcan.py run \
  'set -e; cd ~/codex-runs/itb-engine-main-v243a; \
   .venv/bin/python -m pytest tests/test_post_guard_discriminator_frontier.py -q; \
   .venv/bin/python -m ruff check \
     experiments/post_guard_discriminator_frontier.py \
     tests/test_post_guard_discriminator_frontier.py; \
   .venv/bin/python experiments/post_guard_discriminator_frontier.py \
     --out experiments/results/v2.43/post_guard_discriminator_frontier.json'
```

## Bottom line

The promotion guard closes the false-claim path, but it does not itself create a
discriminator. The active blocker for the registered catalogue remains native,
non-synthetic tower evidence for an in-scope reference-feasible framework.
