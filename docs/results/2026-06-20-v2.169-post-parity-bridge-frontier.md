# v2.169 - Post-parity-bridge frontier: parity contract ready, no real bridge

## Question

After v2.168 defined the GW parity PPV/native-parameter to engine-axis bridge,
what is the updated global discriminator frontier?

## Result

Artifact:
`experiments/results/v2.169/post_parity_bridge_frontier.json`

Current route status:

```text
post_parity_bridge_frontier_no_claim_route
```

Claim-ready routes:

```text
[]
```

Current in-repo promotion-ready routes:

```text
[]
```

Source-side parity likelihood-ready routes:

```text
ng_gwtc3_kappa_at_100hz
callister_sgwb_kappaD_kappaz
```

Ready current operator bridges:

```text
[]
```

## Frontier Ranking

Priority order:

```text
1. future_public_r4_shape_likelihood_ingestion
2. external_spin4_or_detector_g8_measurement_packet_spec
3. registered_native_tower_adapter_authoring
4. weyl_g8_joint_frontier
5. gw_parity_operator_normalization_search
6. r4_symbolic_scale_resolution
```

Diagnostic-ready infrastructure routes:

```text
future_public_r4_shape_likelihood_ingestion
external_spin4_or_detector_g8_measurement_packet_spec
registered_native_tower_adapter_authoring
gw_parity_operator_normalization_search
r4_symbolic_scale_resolution
```

The parity route now has a bridge contract but remains blocked by the real
bridge object:

```text
ready_current_operator_bridge_missing
source_backed_operator_normalization
engine_axis_target
```

## Claim Boundary

The engine has sharper contracts for R4, G8, native tower, and GW parity. No
route currently has the external evidence or source-owned adapter needed for a
claim. Source-native parity likelihood material is useful, but it still cannot
be projected to `g_R2_parity/g_R3_parity` without a source-backed operator
normalization bridge.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/post_parity_bridge_frontier.py --out experiments/results/v2.169/post_parity_bridge_frontier.json
.venv/Scripts/python.exe -m pytest tests/test_post_parity_bridge_frontier.py tests/test_gw_parity_operator_bridge_spec.py tests/test_post_external_g8_spec_frontier.py -q
.venv/Scripts/python.exe -m ruff check experiments/post_parity_bridge_frontier.py tests/test_post_parity_bridge_frontier.py
```

Result:

```text
wrote experiments\results\v2.169\post_parity_bridge_frontier.json
16 passed
All checks passed!
```

Full local suite:

```text
1296 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2169
base = 9911730
```

Determinism:

```text
e05b4118309abf11673c1626816ef1d118ddfa250d4948a20a1a6c579a262cd3  docs/results/INDEX.md
7a0f9c898f9e9aaa4ac0cd713b6a71fe2aa29289278cce9180b43a97c26f1b72  experiments/results/v2.169/post_parity_bridge_frontier.json
```

Commands:

```bash
.venv/bin/python experiments/post_parity_bridge_frontier.py --out experiments/results/v2.169/post_parity_bridge_frontier.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.169/post_parity_bridge_frontier.json
.venv/bin/python -m pytest tests/test_post_parity_bridge_frontier.py tests/test_gw_parity_operator_bridge_spec.py tests/test_post_external_g8_spec_frontier.py -q
.venv/bin/python -m ruff check experiments/post_parity_bridge_frontier.py tests/test_post_parity_bridge_frontier.py
.venv/bin/python -m pytest -q
```

Result:

```text
16 passed
All checks passed!
1303 passed, 1 warning
```
