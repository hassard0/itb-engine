# v2.215 - The engine's source-backed R4 odd-parity ringdown sensitivity

## Question

v2.214 built the operator->QNM contraction machinery (decompose a modified potential in the
McManus (r_H/r)^j basis, contract with the published e_j). The missing physical input was a
SOURCE-BACKED modified potential for the R4 (quartic-curvature) sector and the transfer
coefficient at its radial power. Can the engine now compute a real beyond-GR ringdown
sensitivity?

## Result

Artifact: `experiments/results/v2.215/qnm_r4_sensitivity.json`.

Yes -- by combining two independent published results:

- **Modified potential** (Silva-Ghosh-Buonanno, arXiv:2205.05132, the repo's existing qEFT
  source): the leading odd-parity (axial) Regge-Wheeler correction from the quartic
  (dimension-8 = R4) operator is a SINGLE high power,
  `delta_V_2^- = -432 eta_2 (r_g/r)^10` (l=2).
- **Transfer coefficient** (McManus et al., PRD 99 (2019) 104077, Table 1, newly sourced
  this cycle): `r_H * e_10^- = 0.0036853 + 0.0065244 i`.

Because the R4 odd-parity `delta_V` is a *single* basis function at j=10, the contraction is
exact and needs only e_10 (no convergence series). With r_g = 2M = r_H (standard
gravitational radius), the v2.214 machinery decomposes it to `alpha_10 = -1728 eta_2`
(numerically cross-checked) and contracts:

```text
d(omega)/d(eta_2) = -1728 * e_10 = -3.184 - 5.637 i   (M=1)
```

So a positive quartic coupling eta_2 LOWERS the ringdown frequency (and changes the damping).
This is **the engine's own source-backed beyond-GR ringdown sensitivity for the parity-even
R4 sector** -- two independent published results fused through the validated contraction
machinery.

## What Changed

The QNM sub-program (v2.210 -> v2.215) now closes a loop: validated frequencies (v2.210/211),
the proven precision requirement (v2.212/213), the published-basis contraction route (v2.214),
and now a source-backed R4 sensitivity (v2.215). It also connects back to the existing R4
thread: the same 2205.05132 the repo pinned as `PARSPEC_SOURCE_URL` (v2.207-v2.209) provides
the `delta_V`, and the McManus e_10 provides an INDEPENDENT route to the QNM shift that the
repo's qEFT deformation coefficients (`delta_omega_qeft_0 = -0.2114`) report by a different
method.

Newly sourced e_j (odd-parity gravitational l=2 n=0): j = 0,1,2,3,4,10.

## Honest Scope (claim gate CLOSED on the number)

- The `r_g = 2M` normalization and the completeness of the single-`(r_g/r)^10` odd-parity
  form are taken from the search-surfaced expression; the exact convention and any additional
  terms live in the 2205.05132 appendix, which did not render in fetch. A different r_g
  convention (r_g = M) would rescale the result by 2^10, so **the precise NUMBER is not yet
  claim-grade**.
- Cross-validation against the repo's qEFT coefficient (`-0.2114`) needs the `eta_2 <-> gamma`
  normalization map (also in the appendix).
- This is the ODD-parity (axial) sector only; the even-parity (Zerilli) sector, systematics,
  and external review remain. The parity-ODD operator axis `g_R4_c3` stays dark (v2.209).
- **SOLID, claim-grade pieces:** e_10 is sourced; the R4 quartic odd-parity operator provably
  lives at j=10 in the McManus basis; the contraction machinery is exact and cross-checked.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_r4_sensitivity.py --out experiments/results/v2.215/qnm_r4_sensitivity.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_r4_sensitivity.py -q
```

Result: `alpha_10/eta2 = -1728.0  decomp_ok=True`; `d(omega)/d(eta_2) = -3.1841 -5.6371 i`;
`5 passed`.

## Next (v2.216)

Verify the r_g convention + full `delta_V` against the 2205.05132 appendix and derive the
`eta_2 <-> gamma` map to cross-validate against the repo's qEFT coefficients; and/or extend
to the even-parity (Zerilli) sector. Then connect the validated sensitivity back to the
v2.209 R4 discriminator gate.

## References

```text
Silva, Ghosh, Buonanno, PRD 107 (2023) 044030 (arXiv:2205.05132) -- modified RW potential
McManus, Berti, Macedo, Kimura, Maselli, Cardoso, PRD 99 (2019) 104077 -- e_j transfer coeffs
```
