# v2.459 - the sharp joint prediction of the axion dark energy: β ≠ 0 requires w0 > −1

Sharpening v2.458 (the parity axion is the dark energy *and* the birefringence source). The robust,
potential-**independent** content is not a precise β–w curve (that needs the axion potential shape) but the
**implication**: because one rolling axion sources both, a nonzero birefringence (β ≠ 0, requiring the field to
have rolled) forces the dark energy to be **dynamical and thawing** — w0 > −1, w ≥ −1 (canonical), never a pure
cosmological constant and never phantom.

```text
  beta != 0   <=>   w0 > -1  (dynamical, thawing dark energy)     [one rolling axion sources both]
```

## Current data — both hints present, co-occurrence supported

```text
  cosmic birefringence   beta = 0.34 +/- 0.09 deg  (> 0, ~3.6 sigma)  -- the field HAS rolled
  dark energy            DESI w0 ~ -0.83 (> -1)                        -- dynamical, not a pure CC
```

The same O(1) axion roll that gives the observed birefringence also gives a dynamical w0 > −1, and **both are
hinted** — so the picture is currently *supported in sign*. 5/5 checks pass.

## Sharp near-term falsifiers

- A confirmed **β ≠ 0 together with w0 = −1** (a pure CC gives no birefringence — the axion must roll to rotate
  polarization).
- A confirmed **w0 < −1** (a canonical axion cannot cross w = −1) — so the v2.454 DESI-CPL phantom-past tension is
  a **shared risk** to this unification.

## Honest consequence: no sub-mm fifth force

The dark energy is now an **ultralight axion** (m ~ H₀), whose Compton wavelength is the Hubble radius — so it
mediates **no sub-mm fifth force**. This *retires* the old "dark-energy scalaron → sub-mm fifth force" prediction
(v1.76–77), which was tied to the R² scalaron-as-dark-energy that v2.457 corrected away. The candidate's
dark-energy handle is now the **w0–β co-occurrence**, not Eöt-Wash. (The R² operator could still give a *separate*
short-range force at another scale — only the *dark-energy* sub-mm signal is retired.)

This completes the dark-energy self-correction arc: **v2.457** tempered a spurious operator-level unification →
**v2.458** found the genuine axion field-level unification → **v2.459** makes it a sharp, currently-supported,
falsifiable joint prediction and cleans up its consequences.

## Honest scope

The *implication* (β ≠ 0 ⇒ w0 > −1) is robust and potential-independent (it follows from "same rolling axion
sources both" + "canonical scalar has w ≥ −1"); the quantitative β–w0 *curve* is not computed. "Currently
supported" rests on two **hints**, not detections: β is ~3.6σ, and DESI's w0 > −1 is a CPL hint whose central fit
implies a phantom *past* (v2.454) that a canonical axion cannot accommodate — so "supported" means the *signs*
agree, while DESI's steep-wa/phantom-past trend is a live tension. The no-sub-mm consequence is a robust
order-of-magnitude fact and correctly retires the *dark-energy* sub-mm prediction (not all fifth forces).

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_axion_de_joint_test.py --out experiments/results/v2.459/qnm_axion_de_joint_test.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_axion_de_joint_test.py -q
```

Result: β ≠ 0 ⇒ w0 > −1; both hints present (supported); falsifiable by β + CC/phantom; no sub-mm force; `4 passed`.

## References

```text
this repo: v2.458 (axion = DE + birefringence), v2.457 (g_R2 bounds not is DE), v2.454 (thawing line + DESI phantom-past), v2.451 (beta ~ alpha_EM), v1.76-77 (old scalaron-DE sub-mm force -- retired)
physics: rolling-axion cosmic birefringence (Carroll); canonical scalar w >= -1; DESI 2024 w0; ultralight axion Compton wavelength ~ Hubble radius
```
