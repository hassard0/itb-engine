# v2.136 - Symbolic helicity projection fixture: guard plumbing works

## Question

v2.135 selected the next implementation step:

```text
implement_symbolic_helicity_projection_fixture
```

Can the engine take source-level `c_plus/c_minus` inputs, invert them into
Bresciani `g_R4_c1/c2/c3` axes, and pass the v2.133 projection guard while
remaining non-claiming?

## Result

Artifact:
`experiments/results/v2.136/symbolic_helicity_projection_fixture.json`

Current route status:

```text
symbolic_helicity_fixture_passes_projection_guard_nonclaiming
```

Claimable framework exclusions now:

```text
[]
```

## Fixture Scope

This is a coordinate-inversion fixture, not a string-theory derivation:

```text
fixture_is_source_backed_string_derivation = false
```

The remaining source gap is explicit:

```text
Replace the fixture c_plus/c_minus values with a source-backed string R4
helicity/tensor evaluation in a fixed four-dimensional frame.
```

## Inversion

Fixture input:

```text
c_plus = 0.3
c_minus = 0.1 + 0.05 i
```

Inverted Bresciani axes:

```text
g_R4_c1 = 0.2
g_R4_c2 = 0.1
g_R4_c3 = 0.05
```

Derived coordinates:

```text
g_R4_plus = 0.3
g_R4_minus_abs = 0.111803398875
```

Positivity:

```text
c1_nonnegative = true
c2_nonnegative = true
c3_square_bound_residual = 0.0775
passed = true
```

## Guard Result

The v2.133 guard accepts the projection packet:

```text
ready_for_framework_projection = true
projection_blockers = []
```

The same packet remains non-claiming:

```text
ready_for_framework_claim = false
claim_blockers = [
  discriminator_math_not_excluding,
  measurement_likelihood_missing_or_incomplete
]
```

## Interpretation

The algebraic and guard plumbing now works. If a future source-backed string R4
helicity/tensor calculation supplies `c_plus` and `c_minus`, the engine can
invert them into `g_R4_c1/c2/c3`, compute the derived guard coordinates, verify
positivity, and pass a projection packet through the R4 guard. That still does
not create a framework exclusion without a measurement likelihood and excluding
discriminator math.

## Next Build Action

```text
replace_fixture_with_source_backed_string_r4_helicity_evaluation
```

## Local Validation

```bash
.venv/Scripts/python.exe experiments/symbolic_helicity_projection_fixture.py --out experiments/results/v2.136/symbolic_helicity_projection_fixture.json
.venv/Scripts/python.exe -m pytest tests/test_symbolic_helicity_projection_fixture.py -q
.venv/Scripts/python.exe -m ruff check experiments/symbolic_helicity_projection_fixture.py tests/test_symbolic_helicity_projection_fixture.py
.venv/Scripts/python.exe tools/build_index.py
.venv/Scripts/python.exe -m pytest
```

Result:

```text
wrote experiments\results\v2.136\symbolic_helicity_projection_fixture.json
6 passed
All checks passed!
wrote docs/results/INDEX.md (247 notes)
1114 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone base:

```text
f34d7f3
```

Generated file hashes on Vulcan:

```text
e222c50de2c9717db53ac703a145ed5c307869cf55124883c68c287bf671e0e2  docs/results/INDEX.md
ba5f9c782ca034a1ffe88723fc02902710f169eb0683aed480d4ff8cb726bc80  experiments/results/v2.136/symbolic_helicity_projection_fixture.json
```

Commands:

```bash
.venv/bin/python experiments/symbolic_helicity_projection_fixture.py --out experiments/results/v2.136/symbolic_helicity_projection_fixture.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.136/symbolic_helicity_projection_fixture.json
.venv/bin/python -m pytest tests/test_symbolic_helicity_projection_fixture.py -q
.venv/bin/python -m ruff check experiments/symbolic_helicity_projection_fixture.py tests/test_symbolic_helicity_projection_fixture.py
.venv/bin/python -m pytest -q
```

Result:

```text
generated files were byte-stable
6 passed
All checks passed!
1121 passed, 1 warning
```
