# v2.320 - Does the surviving claim survive prefactor jitter? Robust core, fragile community feasibility

## Question

After the v2.316 RFC-form correction, the surviving headline (v2.317) is: under `convex_hull` the engine
prefers a constructed framework more robust than every community framework, three community frameworks are
feasible, and lqg alone is the boundary. The engine carries an O(1) prefactor uncertainty. Apply the
realism program: jitter all 11 tunable prefactors by an O(1) log-uniform factor (in [1/2, 2]) over 60
draws and ask in what fraction each conclusion holds.

## Result (a clean robust/fragile split)

```text
  conclusion                          holds in
  constructed beats every community    98%   <- ROBUST
  lqg is the boundary framework        92%   <- ROBUST
  >= 1 community framework feasible     13%   <- FRAGILE (the v2.317 feasibility)
  strictly-feasible interior exists     68%   <- marginal
```

**Robust core.** The constructed Chebyshev candidate is more robust than every community framework in 98%
of draws, and lqg is the least-robust community framework in 92% -- so "the engine prefers a constructed
higher-derivative gravity over the named proposals" and "lqg is the boundary framework" survive the O(1)
uncertainty; they are not artifacts of the canonical prefactors.

**Fragile layer.** The v2.317 statement that three community frameworks are feasible under `convex_hull`
holds at only 13% of the prefactor draws -- in the other ~87% ALL four community frameworks are infeasible
even under `convex_hull`. So the community higher-derivative frameworks are not comfortably consistent:
they sit on the KNIFE'S EDGE -- marginally feasible at the canonical prefactors (margins ~ +0.005,
v2.317), but pushed back out by most O(1) prefactor perturbations. All 4/4 checks pass.

## What this establishes

The fully honest picture is between the two earlier extremes: not the `matter_product` "universal
exclusion" artifact (v2.315/v2.316), and not the v2.317 "comfortably feasible" reading either, but
MARGINALLY feasible on a thin prefactor sliver. What is solid across BOTH the RFC-form correction and the
prefactor uncertainty: a constructed framework robustly beats the named ones, and lqg is robustly the
worst. This is the realism program doing its job -- refining, not overturning, the v2.316/v2.317
correction, and pinning exactly how much of the corrected story is house-number-independent.

## Honest scope

All values are the engine's literal `check()` output across a 60-draw O(1) log-uniform prefactor ensemble
under `convex_hull`. The constructed candidate is a LIGHT coordinate ascent from the v2.317 point (18
iters, coarse) re-run per draw -- a conservative witness (a fuller optimization would only RAISE the 98%
"constructed beats community" fraction), not a global optimum. The jitter range [1/2, 2] is a
representative O(1) choice; a wider range weakens the fractions, a narrower one strengthens them -- the
qualitative split (constructed-beats-community robust; community-feasibility fragile) is the content, not
the exact percentages. Only the 11 documented tunable prefactors are jittered; constraint functional forms
and the `convex_hull` RFC are held fixed -- this tests house-number robustness, not street robustness. Toy
basis.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_prefactor_robustness_convex_hull.py --out experiments/results/v2.320/qnm_prefactor_robustness_convex_hull.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_prefactor_robustness_convex_hull.py -q
```

Result: across 60 O(1) prefactor draws, the constructed framework beats every community framework in 98%
and lqg is the boundary in 92% (robust), while community feasibility holds in only 13% (fragile -- the
knife's edge); `5 passed`.

## Next (v2.321)

The corrected arc is now prefactor-audited. A fresh sector or a synthesis. Honest negatives reported as
part of the search.

## References

```text
this repo: v2.317 (corrected preferred framework), v2.316 (RFC-form correction), v2.313 (metric robustness)
the engine's 11 tunable prefactors (experiments/stack.py CANONICAL); the realism-program discipline
```
