# v2.447 - closing the primordial-observability question: the candidate has no cosmological-collider signal, so its inflation is observationally generic Starobinsky

A fresh probe that *closes* a question. The **cosmological collider** (Arkani-Hamed–Maldacena 2015) reads off
particles of mass m ~ H during inflation via oscillatory squeezed non-Gaussianity — needing m in a window ~(0.1 –
few) H (the signal is exp(−π m/H)-suppressed for m ≫ H). What states does the candidate have during inflation?

```text
  collider window:        m/H in (0.1, 5)
  swampland tower:        m/H ~ 625 ... 125,000   (species scale ~0.8 M_Pl descending to ~0.004 M_Pl, H ~ 6e-6 M_Pl)
                          -> FAR too heavy; exp(-pi m/H) astronomically small
  model-independent axion: m/H ~ 1e-3             (shift-symmetry protected, nearly massless)
                          -> FAR too light; a spectator, no oscillatory m~H signature
```

**No state lands in the collider window** — everything is either far too heavy (the tower) or far too light (the
axion). So the candidate has **no cosmological-collider signal**. (5/5 checks.)

## What this closes

Combined with the other two primordial channels — the chiral GW is Planckian-suppressed (Π ~ 10⁻⁶, v2.444) and the
scalar (n_s, r) is plateau-*class* (shared with generic Starobinsky, v2.442) — this **closes the
primordial-observability question**: the candidate's inflation is, at every achievable precision,
**indistinguishable from generic single-field Starobinsky**. Therefore **all** candidate-specific discrimination is
**late-time** — cosmic birefringence (parity), dark-energy w (the g_R2 plateau) — plus the multi-front
over-determination cross-checks (v2.442–443). *The early universe confirms the plateau class; it does not
fingerprint this candidate.*

This is a clean, honest closure with real value: it tells the observational program exactly **where not to look**
(primordial inflationary observables) and **where the leverage is** (late-time birefringence + dark energy + the
g_R2 and parity over-determination) — concentrating the falsification effort.

## Honest scope

An order-of-magnitude observability argument, not a computed non-Gaussianity. The m/H ratios use the toy
species-scale tower (proxy) and standard Starobinsky H_inf; the robust content is the huge separation from the
window (tower m/H ~ 10³–10⁵, axion m/H ≪ 1), which survives large uncertainty factors — the window is (0.1 – few)
H and the tower is >100× above it even at its lightest. The exp(−π m/H) suppression is the standard collider
Boltzmann factor. "Axion nearly massless during inflation" assumes its shift symmetry is unbroken at the
inflationary scale (standard for the model-independent axion, but the exact inflationary mass is model-dependent —
a mild caveat). A negative observability result, consistent with and completing the primordial-suppression picture.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_cosmological_collider.py --out experiments/results/v2.447/qnm_cosmological_collider.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_cosmological_collider.py -q
```

Result: tower m/H ~ 625–125,000, axion m/H ~ 1e-3, none in (0.1, 5) → no collider signal; `3 passed`.

## Next: primordial observability closed — leverage is late-time

The three primordial channels (scalar, tensor-parity, collider) are all closed as candidate-discriminators. The
falsification leverage is late-time. Next: a genuinely new axis, or continue. Bold swings, honest failures.

## References

```text
this repo: v2.444 (chiral-GW Planckian), v2.442 (n_s/r plateau-class), v2.441 (tower during inflation), v2.440 (species-scale tower), v2.434 (parity = model-independent axion)
physics: Arkani-Hamed-Maldacena 2015 (cosmological collider); exp(-pi m/H) Boltzmann suppression; Maldacena single-field consistency; shift-symmetric axion spectator
```
