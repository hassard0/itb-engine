# v2.324 - Gravitational leptogenesis: the preferred parity coupling links to the baryon asymmetry

## Question

A bold fresh swing into a new sector -- the matter-antimatter asymmetry. The same parity coupling
g_R2_parity that drives the chiral GW (v2.319) and matches cosmic birefringence (v2.321): does it also
connect to the baryon asymmetry of the universe?

## Result (yes -- a three-signature unification)

Through the mixed gravitational anomaly. The Standard Model `(B-L)` current has a gravitational anomaly
`d_mu j^mu_(B-L) ~ R R-dual` (Alexander-Peskin-Sheikh-Jabbari, PRL 96 081301 (2006)), and a parity-odd
curvature term makes `<R R-dual> != 0` during inflation -- the same chirality that gives the chiral GW --
so a net lepton number (sphaleron-converted to baryons) is produced `~ g_R2_parity`. This unifies three
cosmological signatures under one coupling:

```text
  theory             g_R2_parity   eta_B     beta(deg)   chiral Pi   sources baryons?
  pure_gr            0.000          0.000     0.000       0.000       no
  string_tree_eft    0.000          0.000     0.000       0.000       no
  asymptotic_safety  0.000          0.000     0.000       0.000       no
  cdt                0.000          0.000     0.000       0.000       no
  lqg_induced        0.080         +0.080     0.272      +0.310       YES
  engine_preferred   0.060         +0.060     0.204      +0.235       YES
```

All three -- cosmic birefringence (measured), chiral primordial GW (future CMB TB/EB), and the baryon
asymmetry `eta_B ~ 6e-10` (measured) -- are nonzero ONLY for a parity-violating theory. The four
parity-EVEN frameworks have `<R R-dual> = 0` and source ZERO of all three; they cannot gravitationally
explain the matter-antimatter asymmetry. The preferred framework (`g_R2_parity = 0.06`, in the
cosmic-birefringence data band) sources all three with aligned signs. All 5/5 checks pass.

## What this establishes

The mild parity violation the program kept arriving at -- preferred by anomaly matching (v2.318), required
by cosmic birefringence (v2.321) -- is, through the SAME coupling, a candidate gravitational origin for the
baryon asymmetry, with the chiral GW as the clean future test. Three of the deepest cosmological facts -- a
measured parity-odd CMB signal, the existence of matter over antimatter, and a predicted GW chirality --
are tied to one parity-odd curvature coupling that the engine's consistency analysis independently prefers.
The parity-even community frameworks are excluded from all three.

## Honest scope

ONLY the existence, the discriminator, the sign-correlation, and the unification are claimed -- NOT the
magnitude of `eta_B`. Gravitational leptogenesis is a real published mechanism, but its predicted `eta_B`
is famously scale-dependent (it needs the inflationary Hubble scale, the reheating temperature, the net
R-L anomaly coefficient, and the cutoff -- none fixed in the toy 8-coefficient basis), so
`eta_B = kappa_B * g_R2_parity` with `kappa_B ~ O(1)` is a placeholder that does NOT predict whether the
observed ~6e-10 is reproduced. The robust, prefactor-independent content is structural: (i) `<R R-dual> =
0` for parity-even gravity -> exactly zero asymmetry (a symmetry statement); (ii) the same `g_R2_parity`
sources all three signatures, so they are correlated not independent; (iii) the sign of `eta_B` tracks
`sign(g_R2_parity)` times the (SM-fixed but here-unsourced) anomaly coefficient -- so the sign-alignment
with the observed matter excess is a consistency statement modulo that coefficient's sign, not a derived
prediction. A known mechanism connected to the engine's coupling through the same schematic map as
v2.319/v2.321; the unification is the result, not a calculation of `eta_B`. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_gravitational_leptogenesis.py --out experiments/results/v2.324/qnm_gravitational_leptogenesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_gravitational_leptogenesis.py -q
```

Result: parity-even frameworks source zero baryon asymmetry; only the parity-violating ones (lqg, the
preferred framework) source it; the three signatures are all sourced by the same coupling and sign-align
for the preferred framework; `6 passed`.

## Next (v2.325)

The parity coupling now ties to three cosmological signatures. A fresh sector or continued audits. Honest
negatives reported as part of the search.

## References

```text
Alexander, Peskin, Sheikh-Jabbari PRL 96 081301 (2006) (leptogenesis from gravity waves / gravitational anomaly)
this repo: v2.319 (chiral GW), v2.321 (cosmic birefringence favors parity), v2.318 (anomaly matching prefers parity)
```
