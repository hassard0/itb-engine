# v2.335 - The parity sector shares an anomaly budget: the data squeezes the cubic parity to zero

## Question

A new seed -- the SECOND parity coupling `g_R3_parity` (cubic parity), ignored throughout the program (the
constructed theory sets it to zero, lqg has 0.04). Is it free, data-favored, or determined?

## Result (anomaly-budgeted away by the data-required leading parity)

The two parity couplings are coupled through the generalized gravitational anomaly inflow (the engine's
`anomaly_flow` constraint):

```text
  g_R2_parity^2 + 2 g_R3_parity^2  <=  rho_inflow * g_4 * g_R2      (a shared "anomaly budget")
  |g_R3_parity / g_R2_parity| ~ rho_match * (g_4 + g_6)            ('t Hooft ratio matching)
```

Mapping the feasible cubic-parity range as a function of the leading parity (matter sector fixed at the
constructed values):

```text
  g_R2_parity   g_R3_parity (max feasible)
  0.048         0.042
  0.056         0.038
  0.060         0.034
  0.064         0.030
  0.068         0.000   <- squeezed to zero
  0.076         0.000
```

As the leading parity rises across the cosmic-birefringence data window [0.048, 0.078], the feasible cubic
parity shrinks from ~0.042 at the lower edge to ~0 at the upper edge -- the boundary combination
`g_R2_parity^2 + 2 g_R3_parity^2` stays roughly constant (~0.0056), the signature of one shared budget. So
the data, by requiring a sizable leading parity, consumes the budget and squeezes the cubic parity toward
zero. All 5/5 checks pass.

## What this establishes

The constructed theory's `g_R3_parity = 0` is a consequence, not a choice: the data-required leading parity
leaves little anomaly budget for the cubic parity. This explains the v2.333/v2.334 finding of a SINGLE
stiff parity direction -- the parity sector is effectively ONE-parameter (the leading coupling) because
anomaly matching plus the cosmic-birefringence data jointly determine the cubic parity from it. The new
theory's parity violation is therefore genuinely a one-parameter, anomaly-locked feature: the leading
Pontryagin coupling carries it, and the cubic parity is pinned to ~zero by the data -- a tighter, more
economical parity structure than the two free couplings the basis nominally allows.

## Honest scope

The anomaly-inflow budget and the 't Hooft ratio matching are the engine's literal constraints
(`anomaly_flow.py`), and the squeeze of the feasible cubic-parity range is the engine's literal feasibility
verdict on a grid scan. The budget coefficient `rho_inflow` and the matching ratio `rho_match` are O(1)
prefactors, so the exact squeeze rate and the budget value (~0.0056) are convention-dependent; the robust,
structural content is that the two parity couplings share ONE anomaly budget
(`g_R2_parity^2 + 2 g_R3_parity^2` bounded) and that pushing the leading parity up -- which the
cosmic-birefringence data does -- squeezes the cubic parity down. The scan fixes the matter sector at the
constructed values (the cubic-parity range shifts with the matter couplings via the `g_4 g_R2` budget and
the `(g_4 + g_6)` matching ratio). "Effectively one-parameter" is a structural characterization of the
parity sector, contingent on the cosmic-birefringence data pinning the leading parity (v2.329 caveat:
without it the leading parity is free and the cubic parity has more budget). Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_parity_anomaly_budget.py --out experiments/results/v2.335/qnm_parity_anomaly_budget.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_parity_anomaly_budget.py -q
```

Result: the feasible cubic parity shrinks from ~0.042 to 0 as the leading parity rises across the data
window; the shared budget `g_R2_parity^2 + 2 g_R3_parity^2` is roughly constant (~0.0056); the constructed
`g_R3_parity = 0` is feasible; `6 passed`.

## Next (v2.336)

The parity sector is shown to be one-parameter (anomaly-locked). A fresh sector or continued audits, at a
deliberate pace. Honest negatives reported as part of the search.

## References

```text
src/itb/constraints/anomaly_flow.py (the shared anomaly budget + 't Hooft ratio matching)
this repo: v2.318 (anomaly prefers parity), v2.321 (data pins leading parity), v2.333/v2.334 (one stiff parity direction)
```
