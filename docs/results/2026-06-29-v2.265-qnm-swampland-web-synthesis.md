# v2.265 - Synthesis capstone: the swampland web (WGC + SDC + TCC + species scale), cross-verified

## Question

Four consecutive cycles built a swampland sub-arc -- the Weak Gravity Conjecture (v2.254), the
Swampland Distance Conjecture (v2.255), the Trans-Planckian Censorship Conjecture (v2.263), and the
Dvali species scale (v2.264). Are these four mutually consistent and connected, or four disconnected
slogans? This capstone cross-verifies them as one structure.

## Result

The four conjectures form one interlocking web, and **all 6/6 cross-program consistency checks pass**:

```text
  [PASS] planck_mass_conventions_consistent: M_full/M_reduced = 5.0103 vs sqrt(8 pi) = 5.0133
  [PASS] tcc_strictly_stronger_than_sdc:     r_TCC(N=60) = 7.4e-45 << r_SDC_safe = 0.002
  [PASS] both_push_r_down:                   r_TCC(60) and r_SDC_safe both < r_current = 0.036
  [PASS] species_scale_below_planck:         Lambda_sp(N=118) = 2.2e17 GeV = 0.092 M_Pl < M_Pl
  [PASS] wgc_cutoff_below_planck:            Lambda_WGC = g M_Pl, g = 0.303 < 1
  [PASS] light_tower_is_the_shared_object:   as Delta phi grows the SDC tower mass AND the species cutoff both fall
```

| conjecture | bounds | observable | direction |
|---|---|---|---|
| Weak Gravity (v2.254) | EFT cutoff `Lambda <~ g M_Pl` | charge/mass of states | cutoff below Planck |
| Distance (v2.255) | field range; tower `m ~ M_Pl e^{-alpha dphi}` | tensor ratio `r` (via Lyth) | `r` down (sub-Planckian) |
| Trans-Planckian Censorship (v2.263) | inflationary `H < M_Pl e^{-N}` | tensor ratio `r` | `r` unobservably small |
| Dvali species scale (v2.264) | QG cutoff `Lambda_sp = M_Pl/sqrt(N)` | number of light species | cutoff below Planck |

## What this establishes

The modules share a consistent Planck-mass bookkeeping (the WGC's full `M_Pl` and the inflation /
TCC / species reduced `M_Pl` differ by exactly `sqrt(8 pi)`, verified to <1%). The **light tower** is
the unifying physical object: the SDC says it descends with field distance, that growing tower IS the
species count `N` that lowers the Dvali cutoff `Lambda_sp = M_Pl/sqrt(N)`, and the SDC and TCC then
both push the tensor-to-scalar ratio `r` down -- with the TCC strictly stronger
(`r_TCC ~ 7e-45 << r_SDC ~ 2e-3 << r_current = 0.036`). The WGC and the species scale independently
say the gravitational EFT cuts off **below** the Planck mass. So "is this EFT in the swampland?" is
answered by one coherent structure -- the cutoff (WGC + species), the field range (SDC), and the
observable consequence (TCC: no detectable primordial GWs) -- not four disconnected slogans. This is
the session's fourth cross-verified synthesis capstone (after v2.242 strong-field, v2.250 BH-hypothesis,
v2.256 QG-phenomenology).

## Honest scope

A synthesis / cross-verification capstone: every check is a consistency relation among results already
established and caveated in v2.254/v2.255/v2.263/v2.264 (each a conjecture, not a theorem; `O(1)`
prefactors; `alpha ~ O(1)` tower exponent; a model-dependent `N(dphi)` law -- the species-count-from-
tower step uses an illustrative `p=1` density whose only robust content is the monotone DIRECTION). No
new bound is derived; the value is showing the four conjectures are mutually consistent and share one
mechanism (the light tower) and one bookkeeping (`sqrt(8 pi)` Planck conventions). A QG-consistency
structural result, not an engine constraint refit.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_swampland_web_synthesis.py --out experiments/results/v2.265/qnm_swampland_web_synthesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_swampland_web_synthesis.py -q
```

Result: 6/6 consistency checks pass; the full/reduced Planck masses differ by `sqrt(8 pi)`; the TCC is
the strongest `r` bound; `5 passed`.

## Next (v2.266)

The swampland web is now closed and cross-verified. Either (a) the user-authorized `g_R4` core-engine
extension, (b) the dedicated analytic Leaver solver, or (c) a fresh QG thread (the swampland arc is
complete, so a genuinely new direction -- e.g. back to the curvature/ringdown program or a new
observable sector).

## References

```text
Arkani-Hamed, Motl, Nicolis, Vafa (WGC), JHEP 06 (2007) 060
Ooguri, Vafa (Distance Conjecture), Nucl.Phys.B 766 (2007) 21
Bedroya, Vafa (TCC), JHEP 09 (2020) 123, arXiv:1909.11063
Dvali (species scale), Fortsch.Phys. 58 (2010) 528, arXiv:0706.2050
this repo: v2.254, v2.255, v2.263, v2.264
```
