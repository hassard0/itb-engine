# v2.271 - Synthesis capstone: the graviton-observables arc (v2.266-v2.270), cross-verified

## Question

Five consecutive cycles built a gravitational-wave / graviton-observable arc -- the graviton's mass
(v2.266), infrared structure (v2.267), polarization content (v2.268), propagation parity (v2.269), and
propagation distance (v2.270). Do they form one coherent structure, and are the modules mutually
consistent?

## Result

The five observables are the complete set of things a gravitational wave measures about the graviton,
mapping to five distinct properties, and all 5/5 cross-program checks pass:

```text
  observable                    graviton property            GR prediction
  graviton mass (v2.266)        MASS                         m_g = 0
  GW memory (v2.267)            INFRARED / soft charge       soft theorem
  GW polarizations (v2.268)     SPIN / helicity              2 tensor modes
  GW birefringence (v2.269)     PARITY                       no L/R split
  GW/EM distance (v2.270)       EXTRA DIMS / running M_Pl    D = 4, Xi_0 = 1
```

The modules share the helicity-2 backbone: v2.269's circular polarizations are built from EXACTLY
v2.268's two GR tensor modes (verified to 1e-12). The propagation sector factorizes cleanly by two
binary labels -- chromatic vs achromatic (frequency dependence) and parity-even vs parity-odd
(handedness):

```text
                    chromatic (freq-dependent)    achromatic (freq-independent)
   parity-even      mass dispersion (v2.266)      distance ratio (v2.270)
   parity-odd       birefringence (v2.269)        (none known)
```

so a GW propagation anomaly is diagnosable from its frequency dependence and handedness alone (mass
dispersion `~1/E^2` and birefringence `~f` both verified chromatic; the distance ratio is
frequency-free). And every test reduces to GR in its null limit (massless graviton -> no dispersion,
two polarizations, zero memory from oscillation, zero birefringence, `D=4` and `Xi_0=1` -> unit
distance ratio -- all verified).

## What this establishes

The graviton-observable arc is one complete, internally consistent structure: five GW measurements
completely partition into MASS / INFRARED / SPIN / PARITY / DIMENSIONS, all share one helicity-2
backbone, and the propagation sector has a clean chromaticity-x-parity map with one empty cell. This is
the session's fifth cross-verified synthesis capstone (after v2.242 strong-field, v2.250 BH-hypothesis,
v2.256 QG-phenomenology, v2.265 swampland).

## Honest scope

A synthesis / cross-verification capstone: every check is a consistency relation among results already
established and caveated in v2.266-v2.270 (representative couplings for the birefringence and distance
parametrizations, order-of-magnitude reproduction of the published mass and dimension bounds,
toy-waveform verification of the memory identity, idealized Michelson response for the polarization
rank). No new bound is derived; the value is showing the five observables are mutually consistent,
share one helicity-2 structure, and completely partition the graviton's measurable properties with a
clean chromaticity-x-parity map of the propagation sector. A QG / GW-structure result, not an engine
constraint refit.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_graviton_observables_synthesis.py --out experiments/results/v2.271/qnm_graviton_observables_synthesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_graviton_observables_synthesis.py -q
```

Result: the circular modes are built from the GR tensor modes (1e-12); mass dispersion is chromatic
`1/E^2`, birefringence chromatic `~f`, distance ratio achromatic; all GR null limits hold; `6 passed`.

## Next (v2.272)

The graviton-observable arc is closed and cross-verified. Either (a) a fresh QG thread (the stochastic
GW background spectrum, GW velocity birefringence, or a new sector), (b) the user-authorized `g_R4`
core-engine extension, or (c) the dedicated analytic Leaver solver.

## References

```text
Will, 'The confrontation between GR and experiment', Living Rev. Rel. 17 (2014) 4
Yunes, Yagi, Pretorius -- theoretical-physics implications of GW150914 / GW170817
this repo: v2.266 (mass), v2.267 (memory), v2.268 (polarizations), v2.269 (birefringence), v2.270 (distance ratio)
```
