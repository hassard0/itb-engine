# v2.126 - GW alpha/G8 joint audit: alpha ready, no current G8 component

## Question

v2.125 selected the next implementation step:

```text
supply_g8_joint_component
```

Can any current G8 source candidate be joined to the calibrated GW alpha packet
as the missing G8 component?

## Result

Artifact:
`experiments/results/v2.126/gw_alpha_g8_joint_component_audit.json`

Current route status:

```text
alpha_ready_g8_joint_component_missing_nonclaiming
```

Claimable discriminator now:

```text
false
```

## Method

The harness loads the v2.125 calibrated alpha packet and verifies that the alpha
side is ready for a G8 join:

```text
alpha_packet_native_adapter_ready = true
adapter_blockers = []
claim_blockers = [g8_joint_component_missing]
ready_for_g8_join = true
```

It then rechecks the current v2.99 G8 source candidates against the capabilities
needed for a join:

```text
external_numeric_measurement
engine_g8_normalization
joint_likelihood_or_covariance
closed_systematics_budget
framework_pair_exclusion_math
```

## Result Status

G8 source audit:

```text
candidate_count = 7
g8_axis_candidate_count = 2
join_ready_candidate_count = 0
g8_joint_component_supplied = false
```

Blocker counts:

```text
candidate_not_on_g8_axis = 5
closed_systematics_budget = 7
engine_g8_normalization = 1
external_numeric_measurement = 7
framework_pair_exclusion_math = 6
joint_likelihood_or_covariance = 1
```

The result remains nonclaiming because:

```text
g8_joint_component_missing
```

## Next Build Action

```text
external_spin4_or_detector_g8_measurement_packet_spec
```

## Interpretation

The alpha side is no longer the blocker. The remaining missing object is a real,
engine-normalized G8 measurement packet with likelihood/covariance, closed
systematics, and framework-exclusion math. Current candidates provide useful
formalism or secondary-axis material, but none supplies the G8 joint component.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_alpha_g8_joint_component_audit.py --out experiments/results/v2.126/gw_alpha_g8_joint_component_audit.json
.venv/Scripts/python.exe -m pytest tests/test_gw_alpha_g8_joint_component_audit.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_alpha_g8_joint_component_audit.py tests/test_gw_alpha_g8_joint_component_audit.py
```

Passed:

```text
4 passed in 0.59s
All checks passed!
```

Full local suite:

```text
1061 passed, 7 skipped, 1 warning in 26.67s
```

## Vulcan Reproduction

Fresh clone with the v2.126 patch applied on `admin@192.168.4.178`:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -e '.[dev]' lalsuite
.venv/bin/python tools/build_index.py
.venv/bin/python experiments/gw_alpha_g8_joint_component_audit.py --out experiments/results/v2.126/gw_alpha_g8_joint_component_audit.json
.venv/bin/python -m pytest tests/test_gw_alpha_g8_joint_component_audit.py -q
.venv/bin/python -m ruff check experiments/gw_alpha_g8_joint_component_audit.py tests/test_gw_alpha_g8_joint_component_audit.py
.venv/bin/python -m pytest -q
```

Before regeneration, the validation recorded SHA-256 hashes for
`docs/results/INDEX.md` and the v2.126 result JSON; after rerunning the index
builder and G8 audit exporter, both files matched byte-for-byte.

Passed:

```text
docs/results/INDEX.md: OK
experiments/results/v2.126/gw_alpha_g8_joint_component_audit.json: OK
4 passed in 0.48s
All checks passed!
1068 passed, 1 warning in 47.51s
```
