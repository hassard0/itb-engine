# v2.298 - The helicity-resolved curvature moment tower: deriving the dark parity-odd Riemann^4

## Question

SEVENTH SLICE / fresh swing of the new-theory arc. v2.292 gave the engine the parity-BLIND curvature
moment tower `g_R3^2 <= g_R2 g_R4`. But the engine splits the graviton into LEFT and RIGHT helicities,
each with its own positivity. Does resolving the curvature moment tower by helicity say anything new
about the new quartic operator -- in particular about its parity-odd component, which v2.209 found
"dark" (not source-backed)?

## Result (consistency derives the dark parity-odd Riemann^4)

Extending the engine's left/right helicity split to the quartic level gives two helicity-resolved
Cauchy-Schwarz towers:

```text
  (g_R3 + g_R3_p)^2 <= (g_R2 + g_R2_p)(g_R4 + g_R4_p)     (left helicity)
  (g_R3 - g_R3_p)^2 <= (g_R2 - g_R2_p)(g_R4 - g_R4_p)     (right helicity)
```

whose floors fix the parity-even `g_R4` and the parity-ODD `g_R4_p` together:

```text
  framework          P-viol   floor_L   floor_R   g_R4     g_R4_parity   ringdown splits
  string_tree_eft    no       0.1125    0.1125    0.1125   +0.00000      no
  asymptotic_safety  no       0.0667    0.0667    0.0667   +0.00000      no
  cdt                no       0.1023    0.1023    0.1023   +0.00000      no
  lqg_induced        YES      0.3042    0.3073    0.3057   -0.00153      YES
```

For the parity-conserving frameworks (`g_*_parity = 0`) the two helicity towers coincide, so
`g_R4_p = 0` and the result reduces exactly to the v2.292 parity-blind mandate -- no parity-odd quartic,
no ringdown splitting. But **lqg_induced**, the lone parity-violating framework
(`g_R2_parity = 0.08, g_R3_parity = 0.04`), has different left and right floors, so the helicity towers
**force a nonzero parity-odd Riemann^4 coefficient `g_R4_p = -0.00153`** -- the Bresciani `Im(K_minus)` /
`g_R4_c3` component that v2.209 found dark is here ILLUMINATED, its floor derived from the lower parity
couplings by consistency rather than imported. All 6/6 checks pass.

## What this establishes

A single idea -- resolve the curvature moment tower by graviton helicity -- extends the v2.292 `g_R4`
mandate, derives the previously-dark parity-odd quartic from consistency, and ties the new operator to a
concrete ringdown signature: because the left and right graviton modes carry different Riemann^4
deformations, the helicity towers PREDICT a parity-split ringdown for any parity-violating UV completion
-- exactly the isospectrality-breaking discriminator of v2.218, now forced by the moment structure
rather than assumed. This is the cross-connecting new structure the arc was for.

## Honest scope

The helicity-resolved towers are the left/right analog of the engine's existing helicity positivity
(`LeftHanded`/`RightHandedGravitonPositivity` at `g_R2`), extended to the new quartic `g_R4` -- exact
Cauchy-Schwarz given the helicity decomposition. The forced `g_R4_p` is the MANDATE FLOOR (a lower bound
from the two helicity towers, like the v2.292 `g_R4` floor), not a full determination of the
un-source-backed parity-odd operator; for lqg's small parity couplings it is small (`~1.6e-3`).
"Illuminates the dark component" means consistency DERIVES a floor for `g_R4_c3` from the lower parity
couplings -- it does NOT resolve the v2.209 sourcing gap (the operator's normalization / sign convention
still needs the literal dispersion derivation). The parity-split-ringdown link to v2.218 is structural;
the magnitude inherits the v2.209 dark-parity and O(1)-prefactor caveats. Only lqg is parity-violating
among the engine's frameworks. A new-engine-theory result. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_helicity_resolved_tower.py --out experiments/results/v2.298/qnm_helicity_resolved_tower.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_helicity_resolved_tower.py -q
```

Result: parity-conserving frameworks reduce to the v2.292 mandate (`g_R4_p = 0`, no split); lqg's
helicity floors differ, forcing a nonzero parity-odd `g_R4_p` and a ringdown split; `6 passed`.

## Next (v2.299)

The new-theory arc is broad. Options: (a) a synthesis capstone of the full arc (g_R4 extension ->
cross-sector characterization -> method-as-proposal -> helicity-resolved tower); (b) promote `g_R4`
(and now `g_R4_parity`) into the engine basis / frameworks; (c) a fresh swing.

## References

```text
this repo: v2.292 (parity-blind g_R4 tower), v2.209 (dark parity-odd g_R4_c3), v2.218 (isospectrality-breaking ringdown discriminator)
Caron-Huot et al 2024 (helicity / parity positivity); Bresciani et al arXiv:2504.12855 (K_plus / Re/Im K_minus R4 basis)
```
