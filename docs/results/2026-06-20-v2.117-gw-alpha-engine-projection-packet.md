# v2.117 - GW alpha engine projection packet: identity axis, still nonclaiming

## Question

v2.116 selected the next implementation step:

```text
close_systematics_budget_and_engine_projection_for_alpha_packet
```

Can we remove the missing engine-projection blocker by mapping the source-native
`alpha_bar_1`, `alpha_bar_2` basis into an explicit engine `gw_cubic_alpha`
axis, without incorrectly projecting it into `g8` or quadratic-curvature axes?

## Result

Artifact:
`experiments/results/v2.117/gw_alpha_engine_projection_packet.json`

Current route status:

```text
alpha_engine_projection_packet_ready_nonclaiming
```

Claimable discriminator now:

```text
false
```

## Method

The harness loads the v2.116 packet and replaces the source-native-only engine
strategy with:

```text
status = explicit_engine_projection
target_axis = gw_cubic_alpha
source_to_engine_jacobian = [[1, 0], [0, 1]]
framework_projection_strategy = framework_alpha_response_defined
```

This removes the v2.116 blocker:

```text
engine_projection_not_ready
```

## Result Status

Adapter state:

```text
engine_projection_ready = true
target_axis = gw_cubic_alpha
source_native_likelihood_export = true
source_parameter_covariance_numeric = true
source_parameter_constraints_numeric = true
adapter_blockers = [systematics_not_closed]
claim_blockers = [g8_joint_component_missing, systematics_not_closed]
```

The adapter still rejects promotion because:

```text
systematics_not_closed
```

The result remains nonclaiming because:

```text
g8_joint_component_missing
gw_cubic_alpha_axis_not_framework_discriminator_by_itself
likelihood_scale_not_calibrated_to_noise_evidence
systematics_budget_missing_or_open
```

## Next Build Action

```text
close_alpha_packet_systematics_budget
```

## Interpretation

The source-native alpha packet now has a valid engine projection into a dedicated
`gw_cubic_alpha` axis. This does not solve the discriminator: the axis is not
`g8`, the systematics budget remains open, and the likelihood scale is still a
diagnostic rather than calibrated noise evidence.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_alpha_engine_projection_packet.py --out experiments/results/v2.117/gw_alpha_engine_projection_packet.json
.venv/Scripts/python.exe -m pytest tests/test_gw_alpha_engine_projection_packet.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_alpha_engine_projection_packet.py tests/test_gw_alpha_engine_projection_packet.py
```

Focused validation passed with 5 tests. Ruff passed.

## Vulcan Reproduction

Validated on `vulcan` in a clean checkout at
`/home/admin/codex-runs/itb-engine-main-v2117`.

```bash
.venv/bin/python experiments/gw_alpha_engine_projection_packet.py --out experiments/results/v2.117/gw_alpha_engine_projection_packet.json
.venv/bin/python -m pytest tests/test_gw_alpha_engine_projection_packet.py -q
.venv/bin/python -m ruff check experiments/gw_alpha_engine_projection_packet.py tests/test_gw_alpha_engine_projection_packet.py
.venv/bin/python -m pytest -q
```

Focused validation passed with 5 tests. Ruff passed. Full-suite validation
passed with 1014 tests, 7 optional LALSuite skips under the standard dev
environment, and one existing Starlette/httpx deprecation warning.
