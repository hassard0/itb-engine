# v2.148 - Policy-scoped string_tree_eft R4 projection packet

## Question

v2.147 defined a non-claiming engine R4 shape unit. Can that policy be used to
build a source-provenance-clean `string_tree_eft` R4 projection packet that
passes internal projection gates while still failing framework-claim gates?

## Result

Artifact:
`experiments/results/v2.148/policy_scoped_string_tree_r4_projection_packet.json`

Current route status:

```text
policy_scoped_string_tree_r4_projection_packet_ready_nonclaiming
```

Claimable framework exclusions now:

```text
[]
```

## Packet

The packet registers a `string_tree_eft` R4 projection in the Bresciani spin-2
basis under the v2.147 engine unit policy:

```text
policy_id = engine_r4_shape_unit_v1
allowed_policy_use = internal_bresciani_basis_projection
absolute_string_alpha_prime_normalization_backed = false
claim_use_allowed = false
```

Projected coordinates:

```text
g_R4_c1 = 0.5
g_R4_c2 = 0.5
g_R4_c3 = 0
g_R4_plus = 1
g_R4_minus_abs = 0
```

## Guard Results

The v2.133 base projection guard accepts the packet for projection:

```text
ready_for_framework_projection = true
projection_blockers = []
```

The v2.137 strict source-provenance overlay also accepts it as source-backed
projection plumbing:

```text
ready_for_source_backed_framework_projection = true
strict_projection_blockers = []
truthy_synthetic_fixture_paths = []
```

The same packet is not claim-ready:

```text
ready_for_framework_claim = false
strict_claim_blockers = [
  discriminator_math_not_excluding,
  measurement_likelihood_missing_or_incomplete
]
```

## Remaining Gaps

The packet deliberately keeps these absolute-normalization gaps open:

```text
absolute_type_II_string_alpha_prime_R4_coefficient
engine_Lambda_R4_unit_conversion
source_backed_K_Russo_to_Kallosh_shape_bridge
```

This makes the packet useful for internal Bresciani-basis projection and
positivity algebra, but not for a physical string-scale prediction or framework
exclusion.

## Next Build Action

```text
register_policy_scoped_r4_adapter_without_claim_promotion
```

The next artifact should expose this packet through a registry-side adapter hook
while retaining the v2.133/v2.137 claim blockers.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/policy_scoped_string_tree_r4_projection_packet.py --out experiments/results/v2.148/policy_scoped_string_tree_r4_projection_packet.json
.venv/Scripts/python.exe -m pytest tests/test_policy_scoped_string_tree_r4_projection_packet.py -q
.venv/Scripts/python.exe -m ruff check experiments/policy_scoped_string_tree_r4_projection_packet.py tests/test_policy_scoped_string_tree_r4_projection_packet.py
```

Result:

```text
wrote experiments\results\v2.148\policy_scoped_string_tree_r4_projection_packet.json
6 passed
All checks passed!
1178 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2148
base = 4c0a6d1
```

Determinism:

```text
e7d09c3a36d54daaaf594436cb6246b18cd6e5602406e68f8b0b86ed81281ce1  docs/results/INDEX.md
46291cf4b5c42b54996ca55fbd87a4dbe52f0671d42dd1db8dd4677056158c6f  experiments/results/v2.148/policy_scoped_string_tree_r4_projection_packet.json
```

Commands:

```bash
.venv/bin/python experiments/policy_scoped_string_tree_r4_projection_packet.py --out experiments/results/v2.148/policy_scoped_string_tree_r4_projection_packet.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.148/policy_scoped_string_tree_r4_projection_packet.json
.venv/bin/python -m pytest tests/test_policy_scoped_string_tree_r4_projection_packet.py -q
.venv/bin/python -m ruff check experiments/policy_scoped_string_tree_r4_projection_packet.py tests/test_policy_scoped_string_tree_r4_projection_packet.py
.venv/bin/python -m pytest -q
```

Result:

```text
6 passed
All checks passed!
1185 passed, 1 warning
```
