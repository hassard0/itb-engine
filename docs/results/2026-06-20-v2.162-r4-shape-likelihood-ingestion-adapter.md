# v2.162 - R4 shape likelihood ingestion adapter: implementation-ready, no public packet

## Question

v2.161 made future public R4 shape-likelihood ingestion the top frontier route.
Can the engine implement the adapter now, so a future public packet can be
manifest-gated and scored without weakening the current claim guards?

## Result

Artifact:
`experiments/results/v2.162/r4_shape_likelihood_ingestion_adapter.json`

Current route status:

```text
r4_shape_likelihood_ingestion_adapter_ready_no_public_packet
```

Claimable framework exclusions now:

```text
[]
```

Ready public R4 likelihood packets now:

```text
[]
```

## Adapter Behavior

Route:

```text
future_public_r4_shape_likelihood_ingestion
```

Supported score axes:

```text
g_R4_c1
g_R4_c2
g_R4_c3
```

Supported likelihood status:

```text
public_covariance_matrix
```

The adapter:

```text
1. runs the v2.160 manifest gate,
2. extracts covariance-backed central values over g_R4_c1, g_R4_c2, and g_R4_c3,
3. compares the packet to the registered string_tree_eft R4 shape,
4. reports a chi-square/sigma diagnostic,
5. keeps framework claims disabled pending external adversarial review.
```

Synthetic controls:

```text
synthetic_complete_r4_shape_likelihood_packet
  adapter_ingestion_ready = true
  chi_square_to_string_tree_r4_shape = 0
  ready_for_framework_claim = false

synthetic_offset_r4_shape_likelihood_packet
  adapter_ingestion_ready = true
  chi_square_to_string_tree_r4_shape = 1
  sigma_distance_to_string_tree_r4_shape = 1
  ready_for_framework_claim = false
```

Current public slot:

```text
current_missing_public_r4_likelihood_slot
  adapter_ingestion_ready = false
  blockers = manifest_packet_gate_failed, shape_likelihood_score_unavailable
```

## Claim Boundary

Even a real-like complete packet remains non-claiming at this stage. Required
claim blockers include:

```text
framework_claim_controls_disabled
external_adversarial_review_missing
```

Synthetic controls also carry:

```text
synthetic_control_not_claim_evidence
```

Selected next build action:

```text
wire_real_public_r4_shape_likelihood_packet_when_source_exists
```

## Local Validation

```bash
.venv/Scripts/python.exe experiments/r4_shape_likelihood_ingestion_adapter.py --out experiments/results/v2.162/r4_shape_likelihood_ingestion_adapter.json
.venv/Scripts/python.exe -m pytest tests/test_r4_shape_likelihood_ingestion_adapter.py tests/test_r4_shape_likelihood_packet_manifest.py -q
.venv/Scripts/python.exe -m ruff check experiments/r4_shape_likelihood_ingestion_adapter.py tests/test_r4_shape_likelihood_ingestion_adapter.py
```

Result:

```text
wrote experiments\results\v2.162\r4_shape_likelihood_ingestion_adapter.json
11 passed
All checks passed!
```

Full local suite:

```text
1257 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2162
base = 445dc74
```

Determinism:

```text
60964d4dc4f43257e544f15239ce86a5b0bb8b0dcfed4b8294e00ff9ad8ff730  docs/results/INDEX.md
f54f4c42861e652ec5e3fecfb71b127eeeff67f1ed54813809962c02bc4016a7  experiments/results/v2.162/r4_shape_likelihood_ingestion_adapter.json
```

Commands:

```bash
.venv/bin/python experiments/r4_shape_likelihood_ingestion_adapter.py --out experiments/results/v2.162/r4_shape_likelihood_ingestion_adapter.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.162/r4_shape_likelihood_ingestion_adapter.json
.venv/bin/python -m pytest tests/test_r4_shape_likelihood_ingestion_adapter.py tests/test_r4_shape_likelihood_packet_manifest.py -q
.venv/bin/python -m ruff check experiments/r4_shape_likelihood_ingestion_adapter.py tests/test_r4_shape_likelihood_ingestion_adapter.py
.venv/bin/python -m pytest -q
```

Result:

```text
11 passed
All checks passed!
1264 passed, 1 warning
```
