# v2.259 - The cosmological constant problem: 'the worst prediction in physics', and the QG angle

## Question

A fresh fundamental-QG cycle, connecting to the engine's `cc_naturalness` experiment. The observed
dark energy has the density of a ~2.3 meV scale; quantum field theory predicts a vacuum energy
`~M_cutoff^4`. How big is the mismatch, and what does quantum gravity / the swampland say?

## Result

```text
  QFT cutoff           vacuum-energy overshoot = (M_cutoff / rho_Lambda^{1/4})^4
  Planck               7.9e122    <- the famous ~1e122, "worst prediction in physics"
  GUT                  5.7e111
  electroweak (TeV)    3.6e58
  QCD (0.2 GeV)        5.7e43
  electron mass        2.4e33
```

with `rho_Lambda^{1/4} = 2.3 meV` (observed). **There is NO cutoff at which naive QFT gets the vacuum
energy remotely right** -- even the electron mass overshoots by `~1e33`, the Planck scale by `~1e122`.
A consistent quantum-gravity theory must explain this ~120-order suppression (and why the value is
small, nonzero, and positive).

**The QG / swampland angle** sharpens it: the de Sitter conjecture (Obied-Ooguri-Spodyneiko-Vafa)
holds that a stable positive `Lambda` (de Sitter vacuum) may be FORBIDDEN in quantum gravity
(`|grad V| >= c V/M_Pl`), so the observed acceleration would be QUINTESSENCE (a rolling field), not a
true constant -- a falsifiable prediction (a time-varying dark-energy equation of state `w != -1`)
that ties the CC problem to the Swampland Distance Conjecture (v2.255) and the engine's
`cc_naturalness` / swampland sector. The CC problem is thus both the sharpest QG naturalness puzzle
and a place where the swampland program makes contact with observation.

## What this establishes

A self-contained, verifiable reconstruction of the cosmological constant problem (the vacuum-energy
overshoot across cutoffs) and its quantum-gravity framing (the de Sitter conjecture / quintessence
prediction), connecting the session's swampland work (v2.255) and the engine's `cc_naturalness` to
the deepest naturalness puzzle in fundamental physics.

## Honest scope

The vacuum-energy estimate `rho_vac ~ M_cutoff^4` is the NAIVE QFT scaling -- the true story involves
cancellations, cosmological-constant renormalization, SUSY (which would soften it to the
SUSY-breaking scale, still `~1e60` off), and the measure/anthropic and sequestering proposals; the
`~1e122` is the famous order-of-magnitude statement of the problem, not a precise prediction. The de
Sitter conjecture is CONTROVERSIAL (its constant `c` and even its validity are debated; many string
constructions claim metastable dS). The observed `rho_Lambda` is well-measured. Self-contained
reconstruction of a standard fundamental puzzle and its QG framing, not a new result. Parity-odd
`g_R4_c3` stays dark (v2.209).

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_cosmological_constant_problem.py --out experiments/results/v2.259/qnm_cosmological_constant_problem.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_cosmological_constant_problem.py -q
```

Result: Planck overshoot `~8e122`; even the electron-mass cutoff `~2e33`; de Sitter conjecture ->
quintessence; `5 passed`.

## Next (v2.260)

Either (a) the user-authorized `g_R4` core-engine extension (scoped v2.234, the highest-leverage open
item), (b) the deferred Teukolsky/Leaver solver, (c) a synthesis of the fundamentals thread
(thermodynamics + holography + CC), or (d) another QG probe.

## References

```text
Weinberg, Rev. Mod. Phys. 61 (1989) 1 -- the cosmological constant problem
Obied, Ooguri, Spodyneiko, Vafa (2018); Agrawal, Obied, Steinhardt, Vafa (2018) -- de Sitter conjecture
this repo: v2.255 (Swampland Distance Conjecture); engine cc_naturalness
```
