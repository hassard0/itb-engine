# v2.358 - Data-leverage audit: which of the four ingested-data constraints actually bind the constructed theory?

## Question

The program advertises "four ingested-data constraints." Which actually constrain the constructed theory,
and which merely document that an experiment probes a different sector?

## Result (only one binds -- with a recorded self-correction)

```text
  cosmic_birefringence_data  [BINDING]   signed-distance 0.013   (the parity channel -- tight, load-bearing)
  submm_gravity_yukawa_bound [VACUOUS(screened)/LOAD-BEARING(unscreened)]   (screening channel via config switch, v2.354)
  gw_speed_bound             [BLIND]     ratio-to-bound 2.3e-5   (frequency-blind to higher-derivative dispersion)
  gw_dispersion_bound        [SLACK]     signed-distance 0.64    (present but far from binding)
```

All 5/5 checks pass. Only **cosmic birefringence** actively binds in the standard (screened) configuration.
Sub-mm gravity is vacuous as configured but load-bearing when unscreened (a configuration switch, not a free
bind). The two GW-propagation constraints do not bind.

**Self-correction (recorded).** At 4-decimal display the gw_speed margin prints as `+0.0000`, which *looks*
like a knife-edge bind. It is not: the gw_speed bound is itself `5e-16`, and the constructed theory's
deviation is `~2e-5` of that (ratio-to-bound `2.3e-5`), so the margin is essentially the *full* bound --
maximally slack. The `+0.0000` is a display artifact of a tiny absolute scale. Higher-derivative gravity
gives a frequency-*suppressed* dispersion `~(E_GW/E_cutoff)^2 ~ 1e-20` at LIGO frequencies, not the
frequency-independent speed shift GW170817 tested -- so GW170817 is genuinely blind to this sector.

## What this establishes

The consistent+observed region's data leverage is essentially **one constraint** (birefringence) in the
screened configuration, plus submm-when-unscreened. The two GW constraints are honest sector-**documentation**
(which experiment probes which operator), not active discriminators. This sharpens the whole data story: the
program's single point of empirical failure is the birefringence detection (v2.329) *precisely because it is
the only data constraint that actually binds* -- the GW constraints cannot rescue or refute the parity
headline, and the screening channel's data role is a binary screened/unscreened switch, not a graded fit.

## Honest scope

The classification is exact (the engine's own margins / ratio-to-bound at the constructed point), and the
self-correction is genuine (gw_speed's `+0.0000` is the full `5e-16` bound, verified by ratio-to-bound
`~2e-5`). The thresholds (BINDING < 0.05 signed-distance, BLIND < 1e-3 ratio) are conventional cut points,
but the separations are large (birefringence 0.013 vs gw_dispersion 0.64 vs gw_speed 2e-5), so the verdicts
are not threshold-sensitive. This is the constructed *point's* data-leverage; over the family the
birefringence tightness varies but it remains the binding data constraint (v2.333: parity is the stiffest
direction). The gw_speed blindness is a physics statement from the engine's dispersion model
(frequency-suppressed `(E_GW/E_cutoff)^2`) -- robust by orders of magnitude, but it would bite for an
ultra-low cutoff below a few micro-eV (documented in the constraint). submm's screened/unscreened switch
carries the v2.354/v2.355 caveats. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_data_leverage_audit.py --out experiments/results/v2.358/qnm_data_leverage_audit.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_data_leverage_audit.py -q
```

Result: birefringence binding (signed-distance 0.013); gw_speed blind (ratio-to-bound 2.3e-5, the `+0.0000`
a display artifact); gw_dispersion slack (0.64); submm vacuous screened / load-bearing unscreened; `5 passed`.

## Next (v2.359)

A fresh sector or continued audits, at the <=10-minute cadence. Honest negatives reported as part of the
search.

## References

```text
this repo: src/itb/constraints/gw_speed.py (frequency-blind), gw_dispersion.py, cosmic_birefringence.py, submm_gravity.py
this repo: v2.329 (birefringence is the single point of failure -- here shown to be the ONLY binding data constraint), v2.354/v2.355 (screening config switch), v2.333 (parity is the stiffest direction)
```
