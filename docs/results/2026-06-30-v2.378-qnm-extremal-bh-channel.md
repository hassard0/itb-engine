# v2.378 - SWING (fresh sector): the theory predicts extremal black holes DECAY -- a fourth channel, WGC automatic from matter positivity

## Question (a new sector: black holes)

The program mapped three prediction channels (parity, ringdown, screening). Is there a fourth -- and does the
constructed theory make a definite black-hole prediction?

## Result (extremal BHs decay; WGC follows from matter positivity)

By the Cheung-Liu-Remmen / Reall-Santos theorem (engine's `wald_entropy_positivity`), the leading
higher-derivative shift to the near-extremal Reissner-Nordstrom entropy at fixed mass and charge is
`Delta S_ext = A g_C + B g_4` (A, B > 0), and `Delta S_ext > 0 <=> WGC` (the extremality bound moves so
`q_ext/m_ext > 1` and the black hole sheds charge).

```text
  constructed:  Delta S_ext = A*g_C + B*g_4 = 0.193 + 0.5*0.529 = 0.458 > 0   -> extremal RN BHs DECAY
  matter-only floor (g_C-independent):  B*g_4 = 0.265 > 0

  across frameworks:
    pure_gr            Delta S_ext = 0.00   (marginal -- g_4 = 0, extremal BHs exactly stable, the GR result)
    string_tree_eft    0.45   decays
    asymptotic_safety  0.35   decays
    lqg_induced        0.60   decays
    cdt                0.50   decays
```

All 5/5 checks pass. Every framework with `g_4 > 0` gives a positive shift (extremal BHs decay); only pure GR
(`g_4 = 0`) is marginal.

## What this establishes

A fourth prediction channel -- **black-hole extremality** -- and a structural claim: the constructed theory
predicts extremal RN black holes are **unstable** (`Delta S_ext = 0.458 > 0`), and this is **guaranteed by
matter positivity alone**. The matter term gives a positive floor `B*g_4 = 0.265 > 0`, so for *any* theory with
`g_4 > 0` (matter forward positivity -- an unavoidable unitarity condition) the shift is positive regardless
of the curvature sector. So the **WGC is not an extra assumption but a consequence of matter positivity** --
the constructed theory is "WGC-complete by construction". Quantum gravity's deepest self-consistency demand
(no exactly stable extremal remnants) is automatically met. Like screening (v2.359), this channel is
non-discriminating -- every positive-`g_4` higher-derivative gravity shares it -- so it strengthens the
theory's QG self-consistency credentials rather than distinguishing it (though it *does* separate all of them
from marginal pure GR).

## Honest scope

The formula `Delta S_ext = A g_C + B g_4` (A=1, B=0.5) is the engine's SIMPLIFIED encoding of the CLR /
Reall-Santos result -- source-cited (JHEP 10 (2018) 004; JHEP 04 (2018) 021) but with O(1) placeholder
coefficients; the real coefficients depend on the extremal background and full operator basis. `g_C` (Weyl^2)
is identified with `g_R2` in the engine, so the `g_C` contribution is toy; the `g_4` floor (0.265) is the
robust, basis-cleaner part. `Delta S_ext` is the leading correction in higher-derivative-expansion units
relative to the Bekenstein-Hawking entropy, not an absolute entropy. "WGC automatic from matter positivity" is
the CLR theorem's content (rigorous in structure -- positive coefficients times positive couplings -- toy in
the coefficient values). The channel is NON-DISCRIMINATING (verified across the frameworks). This is a
THEORETICAL channel, so it carries no birefringence caveat. Robust content: matter positivity guarantees
`Delta S_ext > 0`, so the theory predicts extremal-BH decay and satisfies the WGC by construction. Toy
coefficients, rigorous positivity structure.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_extremal_bh_channel.py --out experiments/results/v2.378/qnm_extremal_bh_channel.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_extremal_bh_channel.py -q
```

Result: constructed `Delta S_ext = 0.458 > 0` (extremal BHs decay); matter floor 0.265; all `g_4 > 0`
frameworks decay, pure GR marginal; `5 passed`.

## Next (v2.379)

The theory now has four channels (parity, ringdown, screening, BH-extremality). Next: a fresh swing or a new
consistency conjecture. Bold swings, honest failures.

## References

```text
this repo: src/itb/constraints/bh_entropy_positivity.py (Wald entropy / WGC), v2.340 (WGC extremal decay), v2.359 (screening generic -- the parallel non-discriminating channel), v2.356 (the three-channel map this extends)
physics: Cheung-Liu-Remmen JHEP 10 (2018) 004; Reall-Santos JHEP 04 (2018) 021 (Delta S_ext > 0 <=> WGC)
```
