# v2.288 - Synthesis capstone: the engine feasible-region investigation (v2.281-v2.287), cross-verified

## Question

Seven cycles turned the loop back from a long from-scratch phenomenology run to the engine itself and
mapped its feasible region. Is that investigation one coherent, self-consistent story -- and what does
it robustly conclude?

## Result

The seven-cycle arc forms one investigation, and all 7/7 load-bearing claims re-verify together through
the real `check()`/`Theory` API:

```text
  v2.281  engine encodes the reconstructed GW/swampland bounds; lqg the lone GW-sector failure
  v2.282  lqg's anomaly is curvature-driven (all 6 failures heal with curvature off)
  v2.283  only pure_gr strictly feasible; string/AS/cdt miss by repulsive_force alone
  v2.284  string/cdt sit on the WGC line; AS/lqg below it (matter product < g_R2)
  v2.285  a feasible higher-derivative witness exists -> region is not just GR
  v2.286  feasible region is a curvature wedge: g_R2 <= ~0.2, x = g_R3/g_R2 capped by positivity
  v2.287  g_R2 wall prefactor-robust; positivity verdict on the frameworks NOT robust

  cross-verification: 7/7 pass (witness feasible; inside-wedge feasible; above-x-ceiling infeasible;
  only pure_gr feasible; lqg fails forward-positivity at canonical; witness robust across the prefactor
  sweep; lqg's positivity verdict flips to satisfied at loose prefactors)
```

The honest split, stated up front:

- **Robust conclusions** -- the engine's feasible region is NOT just general relativity (a
  higher-derivative witness is feasible); it is a bounded curvature wedge whose `g_R2` ceiling (`~0.2`)
  holds for every prefactor, because it is set by anomaly cancellation and the repulsive-force
  conjecture, not the tunable positivity knobs; and the engine independently encodes the reconstructed
  GW/swampland phenomenology.
- **Canonical-only conclusions** -- the specific positivity ratio ceiling (`x ~ 0.83`, which moves to
  `[0.6, 1.2]` across the prefactor range) and therefore the lqg-vs-others positivity separation, whose
  verdict flips across the prefactor uncertainty.

## What this establishes

The whole engine-reconnection investigation reduces to one robust geometric fact -- a non-trivial but
bounded higher-derivative feasible region with a prefactor-robust `g_R2` wall -- plus a clearly-flagged
prefactor-dependent detail (the fine framework rankings). The realism program drew exactly the line it
exists to draw, and this capstone preserves that line rather than re-asserting the cleaner-looking
canonical picture. This is the session's seventh cross-verified synthesis capstone (after v2.242,
v2.250, v2.256, v2.265, v2.271, v2.277).

## Honest scope

A synthesis / cross-verification capstone: every check re-runs an already-established and caveated
result of v2.281-v2.287 through the real engine API; it adds no new constraint and changes no coupling.
The robust-vs-canonical split is the central honest content -- it PRESERVES the v2.287 demotion of the
positivity-ranking detail rather than re-asserting the cleaner v2.286 picture. Wedge boundaries use the
hand-built matter completion of v2.285/v2.286; the engine's couplings are the toy basis with O(1)
representative prefactors. A consistency result on the engine's feasible-region geometry, not a claim
about a physical theory.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_feasible_region_synthesis.py --out experiments/results/v2.288/qnm_feasible_region_synthesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_feasible_region_synthesis.py -q
```

Result: 7/7 cross-verification checks pass; the robust-vs-canonical split is recorded; the witness is
robust while lqg's positivity verdict is prefactor-dependent; only pure_gr is strictly feasible;
`6 passed`.

## Next (v2.289)

The engine feasible-region investigation is closed and cross-verified. The loop continues with a fresh
QG thread, the user-authorized `g_R4` core-engine extension (which would add a new wall to this wedge),
or the dedicated analytic Leaver solver.

## References

```text
this repo: v2.281, v2.282, v2.283, v2.284, v2.285, v2.286, v2.287
this repo: src/itb/engine.py, experiments/stack.py (CANONICAL prefactors + ranges)
```
