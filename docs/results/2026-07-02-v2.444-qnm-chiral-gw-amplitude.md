# v2.444 - computing the v2.443 crux: the chiral-GW amplitude is Planckian-suppressed (Π ~ 10⁻⁶), unobservable — an honest negative that tempers the fingerprint

v2.443 claimed the candidate's parity coupling makes the primordial GW spectrum chiral (a candidate-specific
inflationary signature) but flagged the **amplitude** as uncomputed and "plausibly suppressed." This cycle computes
it (order-of-magnitude) and resolves the crux honestly.

## The estimate

Chern–Simons GW chirality scales, in the weak/perturbative regime, as

```text
  Pi = (P_R - P_L)/(P_R + P_L)  ~  H_inf / M_CS
```

with M_CS the Chern–Simons mass scale. In the engine's normalization the parity coupling g_R2_parity ≈ 0.06 is an
O(1), Planck-scale coefficient of the dim-4 `R∧R̃` operator, so M_CS is **Planckian**
(~ M_Pl/√g_R2_parity ≈ 4 M_Pl). With H_inf ≈ 6×10⁻⁶ M_Pl (Starobinsky, v2.441):

```text
  Pi ~ H_inf / M_CS ~ 1.6e-6      vs  CMB tensor-parity threshold ~ 0.1   ->  ~4.8 orders BELOW
```

The only route to a large Π ~ O(1) is the **exponential-amplification** regime (one helicity tachyonically
produced), which needs a super-Planckian axion velocity / sub-Planckian CS scale the candidate's O(1) Planck-scale
parity coupling does not provide (and it would bring backreaction / strong coupling). (5/5 checks.)

## Honest verdict — tempering v2.443

The primordial chirality is **real in sign** (v2.443 stands as a sign/structure statement — the spectrum *is*
chiral, handedness locked to the birefringence) but **unobservable in amplitude** under the engine's
normalization. It does **not** give an observable break of the plateau-class degeneracy at the CMB after all. So:

- The candidate's inflation is, at achievable precision, **degenerate with generic Starobinsky in *both* the
  scalar (n_s, r) *and* the tensor-parity (Π) channels** — LiteBIRD tests the plateau *class*.
- The candidate's parity remains observable via its **late-time signature (cosmic birefringence)** — the
  established front — and via the multi-front over-determination cross-checks (v2.442–443), **not** via any
  primordial-inflationary observable.

This keeps the program's observational claims calibrated: I dreamed a fingerprint (v2.443), computed it, and found
it too small to see — reported as the honest negative it is.

## Honest scope

An **order-of-magnitude** estimate: Π ~ H/M_CS is the correct *parametric* scaling (Lue–Wang–Kamionkowski;
Alexander–Yunes), but the O(1) prefactor and k-dependence aren't computed, and mapping the engine's dimensionless
g_R2_parity to a physical M_CS is a **normalization assumption** (O(1) coefficient of the dim-4 operator ⇒
Planckian CS scale) — defensible but toy. The robust content is the *parametric* conclusion: a Planck-scale parity
coupling gives Π ~ H/M_Pl ~ 10⁻⁵, many orders below CMB sensitivity — surviving O(1) (even O(100)) prefactor
uncertainty. The escape hatch (exponential amplification, or a sub-Planckian axion decay constant f_a ≪ M_Pl)
could enhance it, but that's *outside* the engine's O(1) Planck-scale setup and would be a separate, lower-scale
model. So this is an honest negative **on the engine's normalization**, not a theorem that the chirality is
unobservable in every parity completion.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_chiral_gw_amplitude.py --out experiments/results/v2.444/qnm_chiral_gw_amplitude.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_chiral_gw_amplitude.py -q
```

Result: M_CS ~ 4 M_Pl, Π ~ 1.6e-6, ~4.8 orders below the CMB threshold; `4 passed`.

## Next: inflationary chirality closed (suppressed) — the parity's observable is late-time birefringence

The tensor-parity channel is real but unobservable under the engine's normalization. The candidate's discriminating
observable stays the late-time birefringence + over-determination. Next: a genuinely new axis, or consolidate. Bold
swings, honest failures.

## References

```text
this repo: v2.443 (chiral-GW fingerprint, amplitude flagged uncomputed), v2.441 (H_inf ~ 6e-6 M_Pl), v2.434 (parity = heterotic axion), v2.418 (g_R2_parity = single residual toy)
physics: Lue-Wang-Kamionkowski 1999; Alexander-Yunes review; Satoh-Soda (chiral inflationary GW); tachyonic-amplification regime; CMB tensor TB/EB sensitivity ~ Pi 0.1
```
