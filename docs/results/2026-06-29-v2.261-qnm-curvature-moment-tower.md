# v2.261 - The curvature dispersion tower as a Stieltjes moment sequence (Hankel positivity)

## Question

A genuinely novel theory advance extending v2.234 (which mandated `g_R4 >= g_R3^2/g_R2` from the
curvature dispersion tower). What is the FULL positivity structure of the higher-curvature tower --
the complete constraint hierarchy of which v2.234 is one piece?

## Result

In the EFT-hedron, the Wilson coefficients are MOMENTS of a positive spectral density, so for a
consistent UV completion the curvature couplings have a spectral representation
`g_R(k+2) = sum_i w_i x_i^k` with `w_i >= 0` -- a STIELTJES MOMENT SEQUENCE. The complete positivity
statement is that the HANKEL matrices are positive-semidefinite. For a constructed positive 3-state
spectral density (`couplings g_R2=1.00, g_R3=1.90, g_R4=4.90, g_R5=15.70, g_R6=56.50, ...`):

```text
  2x2 Hankel minor = g_R2 g_R4 - g_R3^2 = 1.2900   == the v2.234 bound (verified identical)

  size   Hankel PSD   shifted-Stieltjes PSD
  2x2    yes          yes
  3x3    yes          yes
  4x4    yes          yes
  -> all PSD = True

  swampland counterexample (g_R4 dropped 20% below g_R3^2/g_R2): 2x2 Hankel PSD = False
```

**The v2.234 result `g_R4 >= g_R3^2/g_R2` is exactly the 2x2 Hankel minor**, and the full tower (all
Hankel and shifted-Stieltjes matrices PSD, verified to 4x4) is the complete EFT-hedron constraint
hierarchy -- of which the engine's `dispersion_tower` (`g_6^2 <= g_4 g_8`) encodes a single 2x2 slice.
The converse is a sharp swampland criterion: a coupling sequence violating ANY Hankel minor has NO
positive spectral representation and so NO quantum-gravity UV completion.

## What this establishes

Promotes the v2.234 single-bound `g_R4` mandate to the full moment-sequence / Hankel-positivity
structure -- the rigorous theory behind the engine's positivity constraints, and a genuine
theoretical advance (not a textbook reconstruction): the higher-curvature couplings of any consistent
quantum gravity form a Stieltjes moment sequence, with the entire infinite hierarchy of Hankel
positivity bounds, and v2.234 is its leading minor.

## Honest scope

The moment-sequence / Hankel-positivity structure is the EXACT EFT-hedron statement for couplings
that are forward-limit moments of a positive (unitary) spectral density; the mapping of the engine's
specific curvature operators (`g_R2, g_R3, ...`) onto the moment index requires the literal
dispersion-relation derivation with its Gegenbauer/spin weights (the engine uses representative O(1)
prefactors, as its `dispersion_tower` docstring flags) -- so the INDEXING here (`g_R(k+2) <-> m_k`) is
the representative structural form, not the operator-exact moment map. The PSD checks and the
swampland counterexample are exact for the constructed sequence. A theory-structure result extending
v2.234, not a new bound on a measured coupling. Parity-odd `g_R4_c3` stays dark (v2.209).

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_curvature_moment_tower.py --out experiments/results/v2.261/qnm_curvature_moment_tower.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_curvature_moment_tower.py -q
```

Result: 2x2 Hankel minor == v2.234 bound; all Hankel + shifted-Stieltjes PSD (to 4x4); swampland
counterexample violates Hankel PSD; `5 passed`.

## Next (v2.262)

Either (a) the full Hankel hierarchy applied to the engine's actual framework couplings (the forced
g_R5/g_R6 ranges per framework), (b) the user-authorized `g_R4` core-engine extension (now with the
moment-tower structure to inform the constraint forms), (c) the dedicated Leaver solver, or (d) a
fresh QG probe.

## References

```text
Caron-Huot, Mazac, Rastelli, Simmons-Duffin, JHEP 07 (2021) 110 -- the EFT-hedron / moments
Bellazzini, Riva et al. -- positivity / moment problem for EFT coefficients
this repo: v2.234 (g_R4 dispersion-tower mandate); engine dispersion_tower
```
