# v2.87 - g8 derivation route decision: current sources retired

## Question

v2.86 found no source-backed operator identity from detector/energy-correlator
observables into engine `g_8`. This iteration turns that into a route decision.

## Result

Artifact: `experiments/results/v2.87/g8_adapter_derivation_route_decision.json`

Current route status:

```text
current_g8_adapter_derivation_retired_no_claim_ready_route
```

Claim-ready routes:

```text
none
```

## Decisions

| route | decision |
|---|---|
| `current_detector_formalism_direct_g8_adapter` | Retired for current sources. |
| `current_wilson_formalism_direct_detector_adapter` | Retired for current sources. |
| `public_energy_correlator_data_without_operator_identity` | Retired for current sources. |
| `new_source_backed_g8_operator_identity_search` | Retained as future source search. |
| `new_spin4_or_detector_g8_measurement` | Retained as cleanest measurement route. |
| `g8_formalism_source_archive` | Retained as non-promoting design material. |

## Interpretation

The current derivation sources are useful but non-promoting. They lack the
source-backed operator identity, public `g_8` projection, covariance,
systematics, and framework-exclusion math required by the adapter gate.

The next live route is therefore a direct spin-4/detector measurement packet,
unless a future source supplies the missing operator identity and covariance.

## Reproduction

```powershell
.\.venv\Scripts\python experiments\g8_adapter_derivation_route_decision.py --out experiments\results\v2.87\g8_adapter_derivation_route_decision.json
.\.venv\Scripts\python -m pytest tests\test_g8_adapter_derivation_route_decision.py -q
.\.venv\Scripts\python -m ruff check experiments\g8_adapter_derivation_route_decision.py tests\test_g8_adapter_derivation_route_decision.py
.\.venv\Scripts\python -m pytest -q
```

Focused validation passed with 4 tests. Full-suite validation passed with 850
tests and one existing Starlette/httpx deprecation warning.
