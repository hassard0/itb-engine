# v2.190 - R4 ParSpec engine-axis map contract

## Question

Can the v2.188 `ell_qEFT -> engine R4 axes` blocker be turned into an
executable packet contract, without inventing a source-backed ParSpec/qEFT map
that the repo does not yet have?

## Result

Artifact:
`experiments/results/v2.190/r4_parspec_engine_axis_map_contract.json`

Current route status:

```text
parspec_engine_axis_map_contract_ready_nonclaiming
```

Contract ready:

```text
true
```

Current v2.188 ParSpec packet axis-map ready:

```text
false
```

Current v2.188 likelihood attachment ready:

```text
false
```

Framework claims ready now:

```text
false
```

## Contract

The packet contract requires:

```text
source_axis = ell_qEFT_km
target_engine_axes = g_R4_c1, g_R4_c2, g_R4_c3
source_axis_power_policy
operator_basis_map
engine_axis_map
axis_normalization
likelihood_reference
event_set_policy
systematics
claim_controls
```

It is specifically a contract for mapping the ParSpec quartic-EFT length-scale
axis onto the engine-normalized R4 axes. It is not a claim that the map exists.

## Positive Control

The synthetic complete packet passes:

```text
source_bridge_ready = true
axis_map_ready = true
likelihood_attachment_ready = true
route_status = parspec_engine_axis_map_packet_ready_nonclaiming
```

It remains nonclaiming because:

```text
synthetic_control_not_claim_evidence
framework_claim_controls_disabled
external_adversarial_review_missing
```

The synthetic map uses formula placeholders such as:

```text
A_qeft_c1 * ell_qEFT_km^4 / Lambda_R4^8
```

Those placeholders are only a pass-branch control for the evaluator. They are
not source-backed qEFT physics.

## Current v2.188 Packet

The current v2.188 ParSpec bridge still has a valid source bridge:

```text
source = arXiv:2205.05132
doi = https://doi.org/10.1103/PhysRevD.107.044030
source_axis = ell_qEFT
source_axis_units = km
target_engine_axes = g_R4_c1, g_R4_c2, g_R4_c3
```

but fails the map contract:

```text
axis_map_ready = false
likelihood_attachment_ready = false
```

Map blockers:

```text
source_axis_power_policy_missing
operator_basis_map_missing
engine_axis_orientation_missing
axis_normalization_missing
```

Attachment blockers:

```text
public_parspec_qeft_likelihood_or_posterior_samples_missing
event_set_mismatch_gw170608_vs_gw150914_gw200129
calibration_and_waveform_systematics_not_exported
```

## Blocker Split

The v2.188 blocker:

```text
engine_r4_axes_to_parspec_qeft_length_map_missing
```

is now split into executable subpieces:

```text
source_axis_power_policy
operator_basis_map
engine_axis_orientation
axis_normalization
covariance_pushforward
```

This gives the next real source packet a precise acceptance target.

## Claim Boundary

The correct claim is:

```text
The repo now has a tested contract for a future ParSpec/qEFT length-scale to
engine R4-axis packet.
```

The incorrect claim would be:

```text
The repo has derived the real qEFT-to-R4 map or attached a public ParSpec
posterior to the engine axes.
```

No framework exclusions are claimable from this artifact.

## Validation

Focused validation:

```bash
.venv/Scripts/python.exe experiments/r4_parspec_engine_axis_map_contract.py --out experiments/results/v2.190/r4_parspec_engine_axis_map_contract.json
.venv/Scripts/python.exe tools/build_index.py
.venv/Scripts/python.exe -m pytest tests/test_r4_parspec_engine_axis_map_contract.py -q
.venv/Scripts/python.exe -m ruff check experiments/r4_parspec_engine_axis_map_contract.py tests/test_r4_parspec_engine_axis_map_contract.py
.venv/Scripts/python.exe -m pytest -q
```

Result:

```text
wrote experiments\results\v2.190\r4_parspec_engine_axis_map_contract.json
wrote docs/results/INDEX.md (301 notes)
7 passed
All checks passed!
1416 passed, 7 skipped, 1 warning
```

Local hashes:

```text
cec211f285bb09bd6770ce0b20593965724038b7f81647f88bc76c17ae27adf2  docs/results/INDEX.md
75988bcddde40f04e40ac6e30c62e91c8e1e39973798fa45e87c2dd14bd8376c  experiments/results/v2.190/r4_parspec_engine_axis_map_contract.json
```

## Vulcan Validation

Fresh clone plus full v2.190 patch:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2190-final
base = 28c263e
```

Commands:

```bash
git apply /home/admin/itb-v2190-final.patch
.venv/bin/python -m pip install -e ".[dev]"
.venv/bin/python -m pip install lalsuite
.venv/bin/python experiments/r4_parspec_engine_axis_map_contract.py --out experiments/results/v2.190/r4_parspec_engine_axis_map_contract.json
.venv/bin/python tools/build_index.py
.venv/bin/python -m pytest tests/test_r4_parspec_engine_axis_map_contract.py -q
.venv/bin/python -m ruff check experiments/r4_parspec_engine_axis_map_contract.py tests/test_r4_parspec_engine_axis_map_contract.py
.venv/bin/python -m pytest -q
```

Result:

```text
wrote experiments/results/v2.190/r4_parspec_engine_axis_map_contract.json
wrote docs/results/INDEX.md (301 notes)
7 passed
All checks passed!
1423 passed, 1 warning
```

Vulcan hashes:

```text
d2007434339aa0a8b519696114ac67b1109fe9332c47b785e4e4ff5dc5f7cb52  docs/results/INDEX.md
75988bcddde40f04e40ac6e30c62e91c8e1e39973798fa45e87c2dd14bd8376c  experiments/results/v2.190/r4_parspec_engine_axis_map_contract.json
```

The `docs/results/INDEX.md` hash differs from the Windows local hash because
`tools/build_index.py` writes platform-native newlines. The generated JSON
artifact hash matches exactly across local and Vulcan.

## Next Step

Follow-up [v2.191](2026-06-20-v2.191-r4-parspec-qeft-source-asset-audit.md)
audits the public arXiv source package and resolves the source-axis power
subpiece: the qEFT ParSpec deformation uses `p_qEFT = 6`, with source-backed
qEFT QNM coefficients and event bounds preserved in a tested JSON artifact.
That follow-up does not supply a machine-readable likelihood object or an
engine-normalized Bresciani R4-axis map.

The next artifact should satisfy one of the remaining real-data routes:

```text
1. derive a source-backed ParSpec/qEFT operator-basis and axis-orientation map
2. acquire public ParSpec/qEFT posterior samples or a likelihood grid
3. align the engine event set with GW150914/GW200129 or rerun the route
```

Until then, v2.190 is an executable contract, not a physics claim.
