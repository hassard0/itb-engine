# v2.299 - Synthesis capstone: the new-theory arc (v2.292-v2.298), cross-verified

## Question

The loop, after the user redirected it from reconstructing community physics toward inventing new QG
theory, ran a seven-cycle arc. Is it one coherent investigation, what did it produce, and is there a
result visible only across the whole arc?

## Result

The arc, with every load-bearing claim re-verified through its own modules (6/6):

```text
  v2.292  g_R4 (Riemann^4) engine-internal; the moment tower g_R3^2 <= g_R2 g_R4 mandates it
  v2.293  proposed the shared-spectrum cross-sector principle (calibrated ratio band)
  v2.294  rigorously DEMOTED it: the 2x2 tilted-Hankel is implied by the two towers (B>=0 by AM-GM)
  v2.295  the negative is structural (tilted-Hankel = H_m + t H_c, sum of PSDs); real content needs w monotone
  v2.296  third handle: w <= W gives a derived relative-coupling lower bound W_+
  v2.297  method-as-proposal made falsifiable: the carving bounds g_R4 per framework, excludes lqg
  v2.298  helicity-resolved tower DERIVES the dark parity-odd Riemann^4 g_R4_p from consistency
```

**Unifying result.** Every one of the four independent new structures singles out the SAME framework,
`lqg_induced`, as the consistency boundary:

```text
  framework          x_ratio  W_+     carved g_R4 band   g_R4_parity   verdict
  string_tree_eft    0.750    0.406   [0.115, 0.345]     +0.00000      inside
  asymptotic_safety  0.667    0.389   [0.070, 0.530]     +0.00000      inside
  cdt                0.682    0.408   [0.105, 0.370]     +0.00000      inside
  lqg_induced        1.000    0.800   EXCLUDED           -0.00153      BOUNDARY (all four)
```

lqg saturates the moment tower (`x = g_R3/g_R2 = 1`, the boundary), carries the largest cross-sector
relative coupling (`W_+ = 0.80`), is EXCLUDED by the carving (empty `g_R4` band), and is the ONLY
framework forced to carry a parity-odd quartic (`g_R4_p = -0.00153`, a ringdown split). Four orthogonal
new constraints, one convergent verdict -- a robust outcome of the new theory, not any single cycle's
artifact. All 6/6 cross-verifications hold.

## What this establishes

The arc's genuine deliverable is twofold: (1) a precise, HONEST characterization of what the engine's
moment / cross-sector structure can and cannot force -- with an over-claim proposed (v2.293) and then
rigorously demoted (v2.294), a structural negative proved (v2.295), the real content rescued (v2.295/96),
and a previously-dark coefficient derived (v2.298); and (2) a method -- swampland-complete EFT carving --
that produces falsifiable, region-valued QG predictions (v2.297) and robustly identifies the boundary of
consistency (this capstone's convergent lqg verdict). This is the session's eighth cross-verified
synthesis capstone and the close of the user-directed new-theory arc.

## Honest scope

A synthesis / cross-verification capstone: every check re-runs an already-established and caveated result
of v2.292-v2.298; it adds no new constraint. The convergent lqg verdict is robust ACROSS the four
structures, but each carries its own assumption (the cross-sector handles need the shared-spectrum /
monotone-`w` / bounded-`w` premises; the carving ceiling is O(1)-prefactor-dependent, v2.287; the
parity-odd `g_R4_p` is a mandate floor, not a full determination, and does not close the v2.209 sourcing
gap). So "lqg is the consistency boundary" is a statement about the engine's TOY encoding of lqg under
these structures -- consistent across them, NOT a claim about physical loop quantum gravity. The arc's
new theory is genuine but conditional and honestly bounded throughout, and the demoted over-claim
(v2.293 -> v2.294) is itself part of the deliverable. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_new_theory_arc_synthesis.py --out experiments/results/v2.299/qnm_new_theory_arc_synthesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_new_theory_arc_synthesis.py -q
```

Result: all seven cycles re-verify; the four new structures converge on lqg (saturated tower, largest
`W_+`, carving-excluded, forced parity-odd quartic) while the others sit inside on every axis; `6 passed`.

## Next

The new-theory arc is complete and cross-verified. The loop continues -- options: a fresh new-theory
swing in a different sector, promoting `g_R4`/`g_R4_parity` into the canonical engine basis, or returning
to a specific open thread (the dedicated analytic Leaver solver). Honest negatives reported as part of
the search.

## References

```text
this repo: v2.292-v2.298 (the new-theory arc); v2.234/v2.261/v2.262 (moment-tower foundations)
this repo: v2.209 (dark parity-odd g_R4_c3), v2.218 (parity-split ringdown), v2.287 (prefactor realism)
```
