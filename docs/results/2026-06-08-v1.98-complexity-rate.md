# v1.98 — Holographic complexity growth: do higher-derivative gravities violate the Lloyd bound?

**Date:** 2026-06-08
**Compute:** Vulcan. Formula/sign/citation confirmed with Dr. M.

---

## A new holographic observable

'Complexity = Action' (Brown–Roberts–Susskind–Swingle–Zhao 2016): the late-time growth
rate of holographic complexity is **dC/dt = 2M/(πℏ)** — the **Lloyd bound**, saturated by
AdS-Schwarzschild black holes. Higher-derivative / Gauss-Bonnet corrections *raise* the
rate and can **violate** the bound (dC/dt > 2M/πℏ for λ_GB > 0; Cai et al. 2016) — exactly
as Gauss-Bonnet makes η/s violate KSS (v1.67).

Toy mapping (Dr. M.-confirmed sign): **dC/dt (Lloyd units) = 1 + g_C**, driven by the
**Weyl² coupling g_C**. Crucially — the same fact as the v1.82 BH-entropy case — **g_R2
(Euler) is topological in 4d**, so it does *not* drive complexity growth (its Jacobian is
zero). Added as the `HolographicComplexityRate` observable and to the `itb predict`
fingerprint.

## Result — every dual super-saturates the Lloyd bound

| framework | g_C | dC/dt (Lloyd units) | Lloyd? |
|---|---|---|---|
| hořava_lifshitz | 0.89 | **1.89** | violated |
| discovered_parity_violating | 0.68 | 1.68 | violated |
| lqg_induced | 0.43 | 1.43 | violated |
| **discovered_data_driven** | 0.35 | **1.35** | violated |
| string_tree_eft | 0.25 | 1.25 | violated |
| asymptotic_safety | 0.18 | 1.18 | violated |
| … | | | |

**Every framework's holographic dual super-saturates the Lloyd complexity bound** (dC/dt
> 1), by 10–90%, because they all have positive Weyl² coupling — directly analogous to
the universal KSS violation in η/s (v1.67), but driven by a *different* coupling.

## Orthogonal to η/s — a genuinely new discriminator

Because complexity growth is driven by **g_C (Weyl²)** while η/s is driven by **g_R2
(Euler/R²)**, the two holographic observables order the frameworks **differently** whenever
a/c ≠ 1 — they disagree on **3 of 13** framework orderings. So holographic complexity is
**orthogonal** to η/s: a *new* holographic discriminator axis that separates theories the
viscosity does not. The engine's holographic sector now has *two* independent handles —
η/s (R²/Euler) and dC/dt (Weyl²) — mirroring the v1.71 Euler-vs-Weyl² basis split at the
level of observables.

## Honest scope

Toy κ=1 normalization; the robust content is the **ordering by g_C**, the **universal
Lloyd-bound super-saturation**, and the **orthogonality to η/s** — all resting on the
Dr. M.-confirmed structure (Weyl²-driven, Euler topological, Lloyd violation real;
Cai et al. 2016).

## What's committed

- `src/itb/gravitational_observables.py` — `HolographicComplexityRate` (dC/dt = 1 + g_C;
  zero g_R2 Jacobian). Added to the `itb predict` fingerprint.
- `experiments/complexity_rate.py` — per-framework dC/dt, the Lloyd-violation check, the
  η/s-orthogonality, and the figure (`complexity_rate.png`).
- `experiments/master_scorecard.py` — a "complexity dC/dt > 1" structural prediction row.
- `tests/test_complexity_rate.py` — 4 tests (Lloyd units, Euler-topological zero Jacobian,
  violation for g_C > 0, in fingerprint). Suite **524 green**.

## Headline

Every consistent framework's holographic dual **super-saturates the Lloyd complexity
bound** (dC/dt > 1), driven by the Weyl² coupling — and because Euler (g_R2) is
topological, this is **orthogonal to η/s**, giving the engine a second, independent
holographic discriminator. The viscosity orders by R², the complexity by Weyl²: the
Euler-vs-Weyl² split (v1.71) now shows up in the observables.

## Reproducibility

```bash
python experiments/complexity_rate.py
itb predict discovered_data_driven   # includes holographic_complexity_rate_lloyd_units
pytest tests/test_complexity_rate.py -q
```

*Citations: Brown–Roberts–Susskind–Swingle–Zhao, PRL 116 (2016) 191301; Cai–Ruan–Wang–
Yang–Peng, JHEP 09 (2016) 161 (Gauss-Bonnet action growth / Lloyd violation).*
