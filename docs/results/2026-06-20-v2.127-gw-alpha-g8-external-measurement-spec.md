# v2.127 - GW alpha/G8 external sidecar spec: contract defined, not satisfied

## Question

v2.126 selected the next implementation step:

```text
external_spin4_or_detector_g8_measurement_packet_spec
```

Can we turn the missing G8 component into an executable sidecar packet contract
that can be joined to the calibrated GW alpha packet?

## Result

Artifact:
`experiments/results/v2.127/gw_alpha_g8_external_measurement_spec.json`

Current route status:

```text
external_g8_sidecar_packet_specified_not_satisfied
```

Claimable discriminator now:

```text
false
```

## Method

The harness loads the v2.126 G8 audit and v2.125 calibrated alpha packet, then
emits a concrete external sidecar packet template. The template requires:

```text
source_url
source_type
observable_basis
g8_axis_normalization
g8_central_value_or_bound
statistical_uncertainty
systematic_uncertainty
covariance_or_likelihood
projection_to_engine_g8
cross_covariance_with_alpha
systematics_budget
shared_eft_domain
framework_projection
discriminator_math
synthetic_fixture
```

The acceptance checks require:

```text
primary source
external numeric measurement
engine G8 normalization
public likelihood or covariance
cross-covariance with alpha or bounded independence proof
closed G8 systematics
framework exclusion math
```

## Result Status

```text
alpha_packet_ready_for_sidecar = true
required_sidecar_field_count = 16
acceptance_check_count = 7
current_sidecar_available = false
```

The result remains nonclaiming because:

```text
real external G8 sidecar packet is not present
```

## Next Build Action

```text
obtain_or_publish_external_g8_sidecar_packet
```

## Interpretation

The remaining blocker is no longer ambiguous. The repo can validate and join a
proper G8 sidecar packet to the calibrated alpha packet, but it cannot invent
the external G8 measurement. A claim requires a real public source with
engine-normalized G8 likelihood/covariance, bounded systematics, and an alpha
cross-covariance or independence proof.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_alpha_g8_external_measurement_spec.py --out experiments/results/v2.127/gw_alpha_g8_external_measurement_spec.json
.venv/Scripts/python.exe -m pytest tests/test_gw_alpha_g8_external_measurement_spec.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_alpha_g8_external_measurement_spec.py tests/test_gw_alpha_g8_external_measurement_spec.py
```

Passed:

```text
4 passed in 0.63s
All checks passed!
```

Full local suite:

```text
1065 passed, 7 skipped, 1 warning in 27.04s
```

## Vulcan Reproduction

Fresh clone with the v2.127 patch applied on `admin@192.168.4.178`:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -e '.[dev]' lalsuite
.venv/bin/python tools/build_index.py
.venv/bin/python experiments/gw_alpha_g8_external_measurement_spec.py --out experiments/results/v2.127/gw_alpha_g8_external_measurement_spec.json
.venv/bin/python -m pytest tests/test_gw_alpha_g8_external_measurement_spec.py -q
.venv/bin/python -m ruff check experiments/gw_alpha_g8_external_measurement_spec.py tests/test_gw_alpha_g8_external_measurement_spec.py
.venv/bin/python -m pytest -q
```

Before regeneration, the validation recorded SHA-256 hashes for
`docs/results/INDEX.md` and the v2.127 result JSON; after rerunning the index
builder and sidecar-spec exporter, both files matched byte-for-byte.

Passed:

```text
docs/results/INDEX.md: OK
experiments/results/v2.127/gw_alpha_g8_external_measurement_spec.json: OK
4 passed in 0.48s
All checks passed!
1072 passed, 1 warning in 47.76s
```
