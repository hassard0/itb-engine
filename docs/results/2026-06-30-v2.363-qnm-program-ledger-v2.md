# v2.363 - The new-theory program ledger, v2: the three-channel state of the theory (v2.343-362 consolidation)

## Question

The v2.323 ledger classified the program's findings through v2.322. This session (v2.343-362) reorganized the
program around three prediction channels and surfaced two honest caveats. What is the re-verified, honestly-
tiered state of the new theory now?

## Result (re-confirmed core + tiered findings)

Live re-verification (check() calls, not assertions):

```text
  constructed theory+data ok:  [True, True]     (satisfies both)
  named frameworks both-ok:     all False        (pure_gr, string, AS, lqg, cdt -- none)
  channel falsifiers (live):    parity True, ringdown True, screening True
```

All 5/5 checks pass. The v2.322 headline holds: the constructed theory uniquely satisfies theoretical
consistency + all four data constraints; no named framework does. Parity is the sole observational
discriminator (beta=0 infeasible) and the screening mandate is generic across every non-GR framework.

### Robustness tiers

**ROBUST (structural, survive the toy basis)**
- constructed satisfies theory+data; no named framework does (v2.322, re-verified)
- parity is the sole observational discriminator; screening is generic (v2.359)
- the region is ~3D, non-convex, connected (v2.332/v2.333)
- the central prediction is well-posed: over-determined by a small theoretical core, invariant to slack drops (v2.361)
- cross-sector bridges: birefringence lower-bounds `g_4*g_R2` (v2.350); causality caps the ringdown floor (v2.351)
- three observationally-independent falsifiable channels: parity / ringdown / screening (v2.356)

**DATA-CONTINGENT (rest on the ~3.6-sigma birefringence detection, v2.329)**
- the parity prediction and its ~0.34-sigma fit (v2.360)
- the screening mandate's universality (its data-independent core survives, v2.355)
- the observational discrimination from the named frameworks (v2.359)
- cosmic birefringence is the only binding data constraint (v2.358)

**TOY-BASIS / SCHEMATIC (numbers illustrative, not sourced)**
- exact values: beta map, anomaly rho, Eot-Wash cap, coupling coordinates
- the ringdown MAGNITUDE is rank-1 schematic (qNM->R^4 unsourced, v2.336; pending deep-research)
- framework couplings are toy encodings, not real Veneziano/AS/CDT/LQG (v2.342)

**CAVEATS (honest limitations found this session)**
- the engine's margins are NOT cross-comparable (gw_speed ~5e-16); aggregations must restrict to comparable scales (v2.361), audited not to reach v2.341 (v2.362)
- screening is generic to higher-derivative gravity, not distinctive (v2.359)
- the constructed point is feasible-but-marginal under joint O(1) prefactor variation (~41%, v2.346)

## What this establishes

The single-sentence state: **the engine constructs a parity-deformed, curvature-trimmed string-like gravity
that uniquely fits current consistency + data, makes three falsifiable predictions (parity / ringdown /
screening), and whose distinctiveness and testability ride almost entirely on cosmic birefringence.** The
structural results survive the toy basis; the discrimination and the parity/screening predictions rest on the
single birefringence detection (the program's one point of empirical failure and only binding data
constraint); the numbers are toy/schematic; and the session's caveats are recorded.

## Honest scope

Every ROBUST-tier claim marked "live" is re-verified by check() here; the other tier entries are pointers to
their cycles, not re-derived (a ledger, not a re-run of every experiment). "ROBUST / survive the toy basis"
means the QUALITATIVE structure is basis-robust (an inequality direction, a feasibility, a dimensionality),
not that the numbers are; the tiering is a judgement informed by the per-cycle scopes, not a theorem. The
whole ledger is conditional on the toy-basis encodings + O(1) prefactors, and the DATA-CONTINGENT tier on the
birefringence detection being real. This updates the v2.323 ledger (which predates the three-channel
reorganization). Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_program_ledger_v2.py --out experiments/results/v2.363/qnm_program_ledger_v2.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_program_ledger_v2.py -q
```

Result: constructed uniquely satisfies theory+data; no named framework does; three channel falsifiers
live-verified; four robustness tiers assembled; `5 passed`.

## Next (v2.364)

A fresh sector or continued audits, at the <=10-minute cadence. The highest-value fresh input would be a
sourced qNM->R^4 map (the deep-research question) to make the ringdown magnitude quantitative. Honest
negatives reported as part of the search.

## References

```text
this repo: v2.323 (the first ledger, here updated), v2.356 (three-channel map), v2.359 (parity discriminates / screening generic), v2.354/v2.355 (screening mandate), v2.361/v2.362 (margin caveat + audit)
this repo: v2.322 (no framework fits both), v2.350/v2.351 (cross-sector bridges), v2.358 (birefringence the only binding data), v2.329 (the single point of failure), v2.317 (the constructed center)
```
