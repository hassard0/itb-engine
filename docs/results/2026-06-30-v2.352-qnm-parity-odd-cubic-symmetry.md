# v2.352 - Is g_R3_parity = 0 the verified center, or an untested assumption? (the program's one fixed coupling)

## Question

The program has held the parity-odd CUBIC coupling `g_R3_parity = 0` throughout -- it is not even in the
family sampler's KEYS (v2.333/v2.341 walk only 6 of the 7 couplings). Is that a justified choice or an
unexamined assumption?

## Result (verified center -- and the cubic is the one parity coupling left unpredicted)

Every constraint that reads `g_R3_parity` does so through an EVEN function of it:

```text
  generalized_anomaly_inflow:   g_R2_parity^2 + 2*g_R3_parity^2 <= rho g_4 g_R2     (g_R3_parity^2)
  t_hooft_anomaly_matching:     |g_R3_parity| <= rho_match (g_4+g_6) |g_R2_parity|  (|g_R3_parity|)
  parity_violating_cubic_bound: g_R3^2 + g_R3_parity^2 <= kappa g_4^2               (g_R3_parity^2)
  complexity_cutoff:            sum_i w_i g_i^2 <= c_max   (w=2 for g_R3_parity)     (g_R3_parity^2)
```

So the whole feasibility is invariant under `g_R3_parity -> -g_R3_parity`. Verified directly:

```text
  full-stack margins at +/-0.02:   0 of N asymmetric  ->  reflection-symmetric
  feasible window:                 [-0.035, 0.035]    center 0.0
  anomaly closed-form edge:        |g_R3_parity| <= sqrt((rho g_4 g_R2 - g_R2_parity^2)/2) = 0.0355
  g_R3_parity = 0 feasible?  Yes (central)     g_R2_parity = 0 feasible?  No (the contrast)
```

All 6/6 checks pass. The construction's `g_R3_parity = 0` is the exact Chebyshev center of a symmetric
window, not an assumption -- and the 6-coupling family sampler loses nothing by dropping it.

## What this establishes

A clean, honest asymmetry between the two parity-odd couplings. The parity-odd QUADRATIC `g_R2_parity` is
data-**pinned** away from zero (setting it to 0 is *infeasible*, because cosmic birefringence requires
`g_R2_parity >= 0.047`), whereas the parity-odd CUBIC `g_R3_parity` is **unpinned** (zero is feasible and
central; no datum breaks its reflection symmetry). So the theory makes exactly **one** parity-odd prediction
(the quadratic -- the birefringence headline) and **zero** for the cubic: the cubic is bounded
(`|g_R3_parity| <~ 0.036`) but unpredicted. A would-be second parity-odd prediction would require a
constraint or datum that breaks the `g_R3_parity` reflection symmetry.

## Honest scope

The reflection symmetry is **exact and structural** -- it follows from every `g_R3_parity`-reading
constraint being an even function, demonstrated by identical full-stack margins at `+/- delta` (not a fit).
So "`g_R3_parity = 0` is the center" and "the cubic is unpredicted" are robust against the toy-basis
*numbers*: they survive any reparametrization preserving the even structure (positivity/anomaly bounds are
generically even in a parity-odd coupling). The contingent parts are the window WIDTH (the edge 0.036 scales
with the anomaly prefactor `rho`, v2.344, and the other couplings) and the contrast resting on the
birefringence data being real (v2.329: if it is a systematic, `g_R2_parity = 0` becomes feasible too and
BOTH parity-odd couplings are unpredicted). The engine's `t_hooft_anomaly_matching` is encoded as an upper
BOUND with slack, not the equality its docstring motivates -- if it were an equality fixing
`|g_R3_parity|/|g_R2_parity|`, the symmetry would break and the cubic WOULD be predicted (up to sign); that
is the single most likely place a future constraint upgrade changes this verdict. Toy basis, O(1)
prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_parity_odd_cubic_symmetry.py --out experiments/results/v2.352/qnm_parity_odd_cubic_symmetry.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_parity_odd_cubic_symmetry.py -q
```

Result: full-stack margins identical at `+/- delta` (reflection-symmetric); window `[-0.035, 0.035]` centered
at 0 matching the anomaly closed-form; `g_R3_parity = 0` feasible/central while `g_R2_parity = 0` infeasible;
`5 passed`.

## Next (v2.353)

A fresh sector or continued audits, at the <=10-minute cadence. Honest negatives reported as part of the
search.

## References

```text
this repo: src/itb/constraints/cubic_parity.py + anomaly_flow.py + complexity_cutoff.py (the g_R3_parity-reading constraints, all even)
this repo: v2.321/v2.347 (g_R2_parity data-pinned), v2.335 (anomaly couples the parity couplings), v2.333/v2.341 (6-coupling family drops g_R3_parity), v2.329 (birefringence caveat)
```
