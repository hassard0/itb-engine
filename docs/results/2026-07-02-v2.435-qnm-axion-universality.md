# v2.435 - the axion-universality consequence: if the parity is the heterotic model-independent axion, it is *determined* — so heterotic predicts cosmic birefringence and the candidate loses its last free coupling

Following the bold UV identification (v2.434: candidate = heterotic string tree-EFT + its model-independent
axion), this cycle grounds it and draws out its strongest consequence.

## (a) Heterotic curvature-consistency (engine-checked)

```text
  g_R2 > 0  (positive Gauss-Bonnet sign -- the heterotic alpha' term):  yes
  g_R3 > 0:                                                              yes
  curvature moment tower g_R3^2 <= g_R2 g_R4  (feasible, g_R4 >= 0.042): yes
  parity g_R2_parity = 0.06 > 0 (positive handedness = CMB beta>0 sign): yes
```

The whole curvature+parity sign structure is heterotic-consistent.

## (b) The universality consequence

The heterotic **model-independent axion** (from the B-field) couples to `R∧R̃` with a **universal** coefficient —
fixed by the Green–Schwarz mechanism / the gravitational anomaly, the *same in every heterotic compactification*.
So if the candidate's parity coupling **is** this axion (the natural reading, since its `R∧R̃` coupling is the
leading gravitational parity term), then **the parity is determined by universal geometric data, not free.**

This is a major parameter reduction: the parity was the candidate's *one data-selected, rigor-uncageable degree
of freedom* — the "single residual toy" (v2.418). Under the heterotic-axion reading it becomes **fixed.** Two
consequences:
1. **Heterotic predicts cosmic birefringence** (given the string scale), rather than accommodating it — a
   birefringence measurement then tests a *prediction*, not a fit.
2. The candidate's genuinely-free continuous parameters **collapse** toward just the parity-even shape within its
   tight rigorous cage (v2.431) plus the overall string scale — the candidate approaches a **(near-)parameter-free
   heterotic prediction**, the strongest "solve"-toward statement the program can make.

**The whole arc converges on one object.** The parity — the coupling the rigor cage *cannot force* (v2.431), the
empirical verdict *tests* (v2.430), and the UV embedding *sources* (v2.434) — is, under heterotic universality,
exactly the thing that becomes *fixed*, turning the candidate's cosmic-birefringence signal into a genuine
prediction of a heterotic vacuum.

## Honest scope

The curvature-consistency is engine-checked; the universality consequence is a **physics argument**, not an
engine computation. Its crux is **contingent** on the parity being specifically the *model-independent* (universal)
axion — heterotic compactifications also have *model-dependent* (geometric) axions with non-universal couplings,
so if the candidate's parity is one of those it stays compactification-dependent (still heterotic, not fixed). The
model-independent reading is natural (its `R∧R̃` coupling is *the* leading gravitational parity term) but is an
identification, not a proof. "Heterotic predicts β" is structural: the β *magnitude* needs the string scale +
axion decay constant + cosmological field evolution — dimensionful inputs the engine lacks — so "predicts" means
"determined by universal data given the scale," not a computed number. "(Near-)parameter-free" is an aspiration:
the parity-even shape is *caged*, not fixed to a point, and the string scale + compactification choice remain, so
the parameter count *drops* (the parity leaves the free list) rather than reaching literally zero. All prior
caveats carry.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_axion_universality.py --out experiments/results/v2.435/qnm_axion_universality.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_axion_universality.py -q
```

Result: curvature heterotic-consistent; universality → parity determined → β predicted; `4 passed`.

## Next: the heterotic picture is coherent; the remaining work is a real heterotic vacuum computation

The bold arc (v2.433→435) has named the UV completion (heterotic + axion), grounded it (curvature signs + tower),
and drawn its strongest consequence (parity determined → β predicted → parameter reduction). Verifying the
magnitudes needs the heterotic compactification computation the engine can't do. Flagging for the user.

## References

```text
this repo: v2.434 (heterotic identification), v2.418 (parity = single residual toy), v2.431 (rigor cage; parity uncageable), v2.430 (verdict table), v2.386 (parity chirality positive handedness), v2.375 (curvature moment tower)
physics: heterotic model-independent axion (B-field) + universal Green-Schwarz R^R-tilde coupling (gravitational anomaly); model-dependent (geometric) axions non-universal; beta magnitude needs the string scale + axion decay constant
```
