# v2.313 - Is the engine-preferred framework metric-robust? Three robustness objectives agree

## Question

v2.312 constructed the engine's preferred framework as the point maximizing the worst-case constraint
margin, with the honest caveat that the raw worst-case margin MIXES the heterogeneous margin scales, so
the exact optimum is not canonical. This cycle tests that caveat head-on: reconstruct the preferred point
under three different robustness objectives and ask whether they agree.

## Result (metric-robust: all three land in the same region)

```text
  objective                                         g_4    g_6    g_8    g_R2   g_R3   parity
  raw min-margin (v2.312, scale-mixing)             0.584  0.391  0.395  0.186  0.145  0
  geometric Chebyshev (gradient-normalized, scale-free) 0.565 0.360 0.360 0.150 0.085 0
  analytic center (log-barrier interior point)      0.543  0.357  0.456  0.126  0.052  0
```

All three optima are strictly feasible (every margin positive), all three are parity-free, all three keep
the matter sector near the string values (g_4 ~ 0.54-0.58, g_6 ~ 0.36-0.39, g_8 ~ 0.36-0.46) while pulling
the curvature couplings below the string 0.2/0.15, and the three points agree to within **0.096** in every
coupling. The two principled metrics -- the scale-free geometric Chebyshev center and the log-barrier
analytic center -- agree with the raw v2.312 objective. All 5/5 checks pass.

## What this establishes

The v2.312 caveat does not undermine the conclusion. The exact coordinates shift a little between metrics
(the geometric center trims the curvature a bit more than the raw one; the analytic center sits between),
but the QUALITATIVE framework the engine prefers -- string-like matter sector, softened curvature, zero
parity -- is the same under the scale-free geometric metric as under the raw one. The engine's preferred
higher-derivative gravity is a robust feature of the carved region's interior, not an artifact of how the
margins were normalized. This is the honest validation v2.312 needed: the construction survives the change
of robustness metric.

## Honest scope

The geometric Chebyshev objective (min gradient-normalized signed distance) is the principled scale-free
answer and removes the unit-mixing concern of v2.312; the analytic center (sum of log signed-distances)
is a distinct, well-posed interior-point center included to bracket the metric choice. "Metric-robust"
means the three optima agree QUALITATIVELY (same orthant features: string-like matter, trimmed curvature,
parity-free) and to within ~0.1 in coordinates -- not that they are identical (they are not, and need not
be; the largest single-coupling spread is in g_8). Each optimum is an APPROXIMATE, grid-quantized,
seed-dependent argmax from multi-seed coordinate ascent, not a proven global optimum; the convergence is
empirical evidence of robustness, strong but not a theorem. (An ill-posed fourth objective tried first --
maximizing the raw SUM of signed distances -- collapsed to the origin via a penalty discontinuity and was
replaced by the log-barrier analytic center, which is the correct interior-point notion.) Default
38-constraint stack, couplings restricted to g >= 0; signed-distance margins are the engine's own
gradient-normalized distances. Toy basis, O(1) prefactors. A validation cycle for v2.312.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_preferred_framework_robustness.py --out experiments/results/v2.313/qnm_preferred_framework_robustness.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_preferred_framework_robustness.py -q
```

Result: all three robustness objectives yield strictly-feasible, parity-free, string-like-matter,
trimmed-curvature optima that agree to within 0.096 in every coupling; `6 passed`.

## Next (v2.314)

The engine-preferred framework is constructed (v2.312) and validated as metric-robust (this cycle).
Options: a fresh swing, or characterizing the preferred framework's phenomenology. Honest negatives
reported as part of the search.

## References

```text
this repo: v2.312 (engine-preferred framework), v2.283 (frameworks infeasible / pure_gr feasible)
Chebyshev center vs analytic center of a feasible region; gradient-normalized constraint distance
```
