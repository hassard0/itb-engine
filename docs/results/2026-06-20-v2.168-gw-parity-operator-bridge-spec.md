# v2.168 - GW parity operator bridge spec: source likelihoods exist, no engine bridge

## Question

The GW parity route has public source-native likelihood material, but no
engine-axis adapter. Can the missing PPV/native-parameter to engine parity-axis
bridge be made executable and claim-safe?

## Result

Artifact:
`experiments/results/v2.168/gw_parity_operator_bridge_spec.json`

Current route status:

```text
gw_parity_operator_bridge_spec_ready_no_real_bridge
```

Source-side likelihood-ready routes:

```text
ng_gwtc3_kappa_at_100hz
callister_sgwb_kappaD_kappaz
```

Engine-adapter-ready routes before bridge:

```text
[]
```

Ready current operator bridges:

```text
[]
```

Claimable framework exclusions now:

```text
[]
```

## Contract

Required bridge fields:

```text
label
source_url
source_type
source_native_likelihood
source_backed_operator_normalization
ppv_parameter_definition
engine_axis_target
sign_convention
unit_conversion
frequency_reference
covariance_or_samples
helicity_basis_harmonization
framework_exclusion_projection
claim_controls
```

Target engine axes:

```text
g_R2_parity
g_R3_parity
```

## Controls

Synthetic complete bridge:

```text
ready_for_engine_parity_bridge = true
ready_for_framework_claim = false
```

Current missing bridge slot:

```text
ready_for_engine_parity_bridge = false
```

The best public source-side rows remain nonpromoting because they still lack the
engine-axis map and dimensional/frequency normalization.

## Claim Boundary

This spec does not promote Ng, Callister, Jenks, Zhu, or the engine toy
observable into a framework claim. A valid bridge must provide a source-backed
operator normalization, unit conversion, frequency reference, helicity basis
harmonization, covariance/samples, and projection to `g_R2_parity/g_R3_parity`.
Claims remain disabled until external adversarial review completes.

Selected next build action:

```text
search_or_derive_source_backed_ppv_to_engine_parity_bridge
```

## Local Validation

```bash
.venv/Scripts/python.exe experiments/gw_parity_operator_bridge_spec.py --out experiments/results/v2.168/gw_parity_operator_bridge_spec.json
.venv/Scripts/python.exe -m pytest tests/test_gw_parity_operator_bridge_spec.py tests/test_gw_parity_adapter_readiness.py tests/test_gw_parity_route_decision.py -q
.venv/Scripts/python.exe -m ruff check experiments/gw_parity_operator_bridge_spec.py tests/test_gw_parity_operator_bridge_spec.py
```

Result:

```text
wrote experiments\results\v2.168\gw_parity_operator_bridge_spec.json
16 passed
All checks passed!
```

Full local suite:

```text
1291 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2168
base = 550ad06
```

Determinism:

```text
b20f4fe1fa712a99e0e46b044ec102d0dddcbdf3ef7787c01c3281e883a8fdad  docs/results/INDEX.md
3777fb6925e317006050a83a786677b3ace7ddc7c4da85d34e44df42b8314518  experiments/results/v2.168/gw_parity_operator_bridge_spec.json
```

Commands:

```bash
.venv/bin/python experiments/gw_parity_operator_bridge_spec.py --out experiments/results/v2.168/gw_parity_operator_bridge_spec.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.168/gw_parity_operator_bridge_spec.json
.venv/bin/python -m pytest tests/test_gw_parity_operator_bridge_spec.py tests/test_gw_parity_adapter_readiness.py tests/test_gw_parity_route_decision.py -q
.venv/bin/python -m ruff check experiments/gw_parity_operator_bridge_spec.py tests/test_gw_parity_operator_bridge_spec.py
.venv/bin/python -m pytest -q
```

Result:

```text
16 passed
All checks passed!
1298 passed, 1 warning
```
