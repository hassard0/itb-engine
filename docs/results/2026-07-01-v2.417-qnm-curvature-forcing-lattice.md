# v2.417 - the definitive feasibility lattice: the leading curvature coupling is forced by the matter × cubic-curvature pair (resolving both prior attributions)

## The move

Two prior cycles gave imprecise attributions for why g_R2 is bounded below at the candidate: v2.414 said "matter
sources gravity" (wrong — matter alone allows g_R2 → 0); v2.416 corrected that to "g_R3 and parity force g_R2,
within-gravity" (also imprecise — g_R3 alone is infeasible without matter, and parity does not force g_R2). This
cycle enumerates the **complete** feasibility lattice over the three sectors {matter, cubic-curvature g_R3,
parity} on the effective rigorous+implied core, settling it.

## The lattice

```text
  matter only                 :  g_R2 in [0.0, 0.452]    -> g_R2 ~ 0 OK (matter alone forces nothing)
  cubic g_R3 only             :  INFEASIBLE (all g_R2)   -> cubic curvature REQUIRES matter
  parity only                 :  INFEASIBLE (all g_R2)   -> parity REQUIRES matter
  cubic + parity (no matter)  :  INFEASIBLE (all g_R2)
  matter + g_R3               :  g_R2 in [0.108, 0.374]  -> FORCES g_R2 >= 0.108
  matter + parity             :  g_R2 in [0.0, 0.4]      -> g_R2 ~ 0 OK (parity does NOT force it)
  matter + g_R3 + parity      :  g_R2 in [0.108, 0.374]  -> forced (by the g_R3)
```

All 5/5 checks pass. Forcing constraints at matter+g_R3, low g_R2: graviton_forward_positivity +
cross_sector_efthedron.

## What this establishes

The leading curvature coupling g_R2 is rigorously forced positive by **exactly one thing — the cubic-curvature
coupling g_R3 acting together with matter** (via cross-sector amplitude positivity). Matter alone doesn't force
it; the cubic-curvature and parity couplings can't even *exist* without matter; and the parity coupling, even
with matter, doesn't force it. So the correct rigorous statement is **"matter × cubic-curvature jointly force the
leading curvature term"** — a genuine two-sector positivity bound, **neither "matter sources gravity" (v2.414)
nor "gravity self-forces" (v2.416)**. Because the lattice enumerates *all seven* sector-presence patterns, this
is complete and admits no further correction. Physically it's satisfying: an EFT that has both matter
self-interactions and a cubic-curvature (Riemann³) coupling *cannot* omit the leading curvature-squared term —
amplitude positivity ties the three together — whereas an EFT with only matter, or only parity atop matter, can.
The candidate (matter + g_R3 + parity) inherits its g_R2 ≥ 0.108 floor from its matter × g_R3 content.

## Honest scope

The lattice is at the constructed magnitudes (matter values, g_R3=0.09, parity=0.06); the *qualitative* pattern
(which sectors force / require which) is the robust content, while the specific floor 0.108 and range edges are
at those magnitudes. "Infeasible without matter" means infeasible across g_R2 ∈ [0,0.8] at the tested magnitudes.
This **refines v2.416's positive statement** — v2.416 was right to retract "matter sources gravity" but imprecise
to call the forcing "within-gravity self-forcing"; its core retraction stands, this pins the exact mechanism.
Carries the v2.411/412 rigor caveats.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_curvature_forcing_lattice.py --out experiments/results/v2.417/qnm_curvature_forcing_lattice.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_curvature_forcing_lattice.py -q
```

Result: matter-only free, cubic/parity need matter, matter+cubic forces g_R2≥0.108, matter+parity free;
`4 passed`.

## Next (v2.418): the forcing structure is settled -- continue swings

The leading-curvature forcing is now precisely mapped (matter × cubic-curvature). Next: a fresh swing. Bold
swings, honest failures.

## References

```text
this repo: v2.414 (matter-sources-gravity, wrong), v2.416 (retraction; positive part imprecise), v2.375 (curvature moment tower), v2.412 (effective rigorous core)
physics: cross-sector amplitude positivity tying matter and curvature (Caron-Huot-de Rham-Tolley-Zhou; Arkani-Hamed-Huang-Huang EFThedron)
```
