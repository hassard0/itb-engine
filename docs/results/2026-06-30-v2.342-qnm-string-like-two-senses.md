# v2.342 - The constructed theory is string-like in two independent senses: IR matter and UV completion

## Question

The trilogy (v2.338-v2.341) concluded the constructed theory's UV completion must be string-like (unitarity
+ the CEMZ higher-spin-tower requirement). Is its low-energy MATTER sector also string-like -- and if so,
how independently?

## Result (string-like at both ends)

The constructed theory's matter couplings `(g_4, g_6, g_8) = (0.53, 0.40, 0.40)` are CLOSEST to
`string_tree_eft` of all named frameworks:

```text
  framework           matter-sector distance from constructed
  string_tree_eft     0.029   <- closest
  cdt                 0.054
  lqg_induced         0.087
  asymptotic_safety   0.191
  pure_gr             0.774
```

So the theory is string-like in TWO independent senses, established by entirely separate arguments:
**(1) UV** -- the trilogy's unitarity + causality (a higher-spin Regge tower is required at the cutoff);
**(2) IR** -- the matter sector is closest to string in coupling space (distance 0.029, well inside the
next-nearest framework). All 4/4 checks pass.

## What this establishes

Both ends of the theory -- the matter it couples to and the UV it completes into -- are string-like, and
the two facts come from independent arguments (a coupling-space distance in the IR, a unitarity+causality
requirement in the UV). The theory's two DISTINGUISHING features are exactly the non-string pieces: a
TRIMMED curvature sector (g_R2, g_R3 pulled below the string values, forced by consistency -- the same
trimming that gives it the mildest ringdown, v2.336, and the most causality headroom, v2.339) and a MILD
PARITY VIOLATION (g_R2_parity ~ 0.06, absent at string tree level, favored by the cosmic-birefringence
data). So the engine's constructed new theory is, in effect, a **parity-deformed, curvature-trimmed
string-like gravity** -- string-like where consistency and the matter sector demand it, deformed exactly
where the data (parity) and the consistency boundary (trimmed curvature) push it. That is a coherent
identity for the new theory: not an arbitrary point, but the minimal parity+curvature deformation of a
string-like gravity that current data and consistency together select.

## Honest scope

The IR "closest to string" result is exact arithmetic on the encoded matter couplings, but "string" here
means the engine's TOY `string_tree_eft` encoding `(0.5, 0.4, 0.4)`, NOT the actual Veneziano /
Virasoro-Shapiro coefficients -- so the claim is "closest to the engine's string framework among the named
ones", a relative statement, not "matches real string theory's coefficient ratios" (which would require the
sourced string amplitude). The L2 distance in `(g_4, g_6, g_8)` is a convention (equal weighting). The UV
"string-like" sense is the trilogy's qualitative inference (unitarity + the CEMZ higher-spin-tower
requirement), not a constructed UV completion. The "parity-deformed, curvature-trimmed string-like gravity"
identity is a synthesis of v2.317 (string-like matter), v2.336/v2.339 (trimmed curvature), v2.321 (parity
from data), and v2.338/v2.339 (UV) -- a coherent reading, not a new computation. The parity piece rests on
the cosmic-birefringence data (v2.329 caveat). Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_string_like_two_senses.py --out experiments/results/v2.342/qnm_string_like_two_senses.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_string_like_two_senses.py -q
```

Result: the constructed matter sector is closest to string_tree_eft (0.029, vs cdt 0.054, lqg 0.087); the
theory is string-like in IR matter and (from the trilogy) UV completion; `5 passed`.

## Next (v2.343)

The new theory has a coherent identity (parity-deformed, curvature-trimmed string-like gravity). A fresh
sector or continued audits, at the <=10-minute cadence. Honest negatives reported as part of the search.

## References

```text
this repo: v2.338/v2.339 (string-like UV from unitarity+causality), v2.317 (string-like matter), v2.336 (trimmed curvature), v2.321 (parity from data)
```
