# v2.147 - R4 shape normalization policy: internal unit, no claims

## Question

v2.146 showed that the Russo/Gross-Witten K-convention bridge is still not
source-backed. Can the engine define an explicit non-claiming R4 shape unit so
internal Bresciani-basis algebra can proceed without enabling absolute
string-scale or framework-exclusion claims?

## Result

Artifact:
`experiments/results/v2.147/r4_shape_normalization_policy.json`

Current route status:

```text
engine_r4_shape_unit_policy_ready_nonclaiming
```

Claimable framework exclusions now:

```text
[]
```

## Policy

The policy defines:

```text
policy_id = engine_r4_shape_unit_v1
status = engine_convention_nonclaiming
```

One engine R4 shape unit is the v2.144 Kallosh/Bresciani maximally
supersymmetric R4 helicity shape:

```text
K_plus = 1
K_minus = 0
overall_R4_factor = 8
g_R4_c1 = 1/2
g_R4_c2 = 1/2
g_R4_c3 = 0
```

Allowed policy uses:

```text
internal_bresciani_basis_projection
positivity_and_unitarity_algebra
adapter_fixture_replacement
relative_shape_comparison
```

Disallowed policy uses:

```text
absolute_type_II_string_scale_claim
framework_exclusion_claim
measurement_likelihood_claim
wilson_coefficient_magnitude_claim
```

## Remaining Gaps

The following remain deliberately false:

```text
type_II_string_alpha_prime_units_source_backed
engine_lambda_r4_unit_conversion_source_backed
k_convention_bridge_source_backed
```

This means the policy can unblock internal adapters and algebra, but it cannot
be used as an absolute type-II string normalization, a likelihood packet, or a
framework exclusion.

## Next Build Action

```text
build_policy_scoped_string_tree_r4_projection_packet
```

The next artifact should create a `string_tree_eft` R4 projection packet that
passes the internal shape-normalization policy while still failing absolute
normalization and framework-claim gates.

## Local Validation

```bash
.venv/Scripts/python.exe experiments/r4_shape_normalization_policy.py --out experiments/results/v2.147/r4_shape_normalization_policy.json
.venv/Scripts/python.exe -m pytest tests/test_r4_shape_normalization_policy.py -q
.venv/Scripts/python.exe -m ruff check experiments/r4_shape_normalization_policy.py tests/test_r4_shape_normalization_policy.py
```

Result:

```text
wrote experiments\results\v2.147\r4_shape_normalization_policy.json
6 passed
All checks passed!
1172 passed, 7 skipped, 1 warning
```

## Vulcan Validation

Fresh clone:

```text
admin@192.168.4.178:/home/admin/itb-engine-main-v2147
base = 4d13bbe
```

Determinism:

```text
d3a11663b07f1e04afd2035b9b58aaf4c156a1e95e483adb4e596782b4759f54  docs/results/INDEX.md
0854196be035e511eb033f524d81e3135137097dadd98f989d2aaad71d4eb0f5  experiments/results/v2.147/r4_shape_normalization_policy.json
```

Commands:

```bash
.venv/bin/python experiments/r4_shape_normalization_policy.py --out experiments/results/v2.147/r4_shape_normalization_policy.json
.venv/bin/python tools/build_index.py
git diff --exit-code -- docs/results/INDEX.md experiments/results/v2.147/r4_shape_normalization_policy.json
.venv/bin/python -m pytest tests/test_r4_shape_normalization_policy.py -q
.venv/bin/python -m ruff check experiments/r4_shape_normalization_policy.py tests/test_r4_shape_normalization_policy.py
.venv/bin/python -m pytest -q
```

Result:

```text
6 passed
All checks passed!
1179 passed, 1 warning
```
