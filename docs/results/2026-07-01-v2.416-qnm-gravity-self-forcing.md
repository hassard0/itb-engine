# v2.416 - SELF-CORRECTION: "matter sources gravity" (v2.393/v2.414) was mis-attributed -- the rigorous g_R2 floor is forced by the higher-curvature (g_R3) and parity couplings, not matter

## How it surfaced

Probing whether amplitude positivity pins the leading curvature coupling g_R2 to a *matter-determined* window,
the rigorous-core lower bound came out **constant** (g_R2 ≥ 0.108) independent of the matter scale — a red flag.
Isolating the forcing variable overturns the v2.414 attribution:

```text
  (a) matter + g_R3=0.09 + parity=0.06 (the v2.414 test config):  g_R2 floor 0.108
  (b) matter, but g_R3=0 and parity=0:                            g_R2 floor 0.0   <-- matter forces NOTHING
  (e) matter only, g_R2=0.001:                                    FEASIBLE
  (f) g_R3=0.09 only (no matter, no parity), g_R2 -> 0:           VIOLATED (graviton positivity)
  (g) parity=0.06 only (no matter, no g_R3), g_R2 -> 0:           VIOLATED (parity-decomposed positivity)
```

All 5/5 checks pass.

## The correction

v2.414 tested matter-sources-gravity while **holding g_R3=0.09 and g_R2_parity=0.06 fixed**, and mistook the
floor *those* couplings impose for a matter effect. Corrected: **matter (g_4,g_6,g_8) alone does not rigorously
force the leading curvature coupling** — a theory with the full matter sector but no cubic-curvature and no
parity coupling can have g_R2 → 0. What *does* force g_R2 > 0 is the gravitational sector's **own higher
structure**: the cubic curvature coupling g_R3 and the parity coupling each independently require a nonzero
g_R2 via graviton / parity-decomposed positivity. So the correct rigorous statement is **"the higher-curvature
and parity couplings force the leading curvature term"** — gravity's structure is *self-forcing* under amplitude
positivity — **not "matter sources gravity."** The apparent matter→gravity link in the full stack came from the
toy anomaly (`g_4·g_6 = g_R2²`), which v2.414 itself showed is a removable 4D artifact; with the toy removed,
the matter→gravity forcing goes with it.

This **retracts** the v2.414 headline and corrects the v2.415 ledger row (`matter sources gravity [rigorous]` →
`the higher-curvature/parity couplings force the leading curvature term [rigorous]`). Everything else in the
de-toying arc stands: LQG excluded, matter dominance's WGC ceiling, extremal-BH decay, the a/c wedge, the moment
towers, the harmless speculative toys, the anomaly-as-removable-artifact — all untouched. Catching this
strengthens the ledger.

## Honest scope

Found by isolating the forcing variable (setting g_R3 and parity to zero), so it's a direct, reproducible
feasibility fact, not a sampled estimate. It retracts *only* the "matter sources gravity is rigorous" claim
(v2.414 + the v2.415 row); the other de-toying results stand. It remains true that at the candidate point (which
*has* g_R3 and parity) g_R2 is rigorously bounded below — **the candidate's g_R2 > 0 is rigorous** — only the
*attribution* to matter was wrong. Whether matter sources gravity in real physics (via matter loops generating
R²) is a separate, true RG statement the engine does not encode — so the retraction is about what the *engine*
establishes, not about matter-loop physics.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_gravity_self_forcing.py --out experiments/results/v2.416/qnm_gravity_self_forcing.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_gravity_self_forcing.py -q
```

Result: matter-only g_R2 floor 0.0 (feasible at g_R2→0); g_R3 and parity each force g_R2>0; `4 passed`.

## Next (v2.417): the ledger is corrected -- continue swings

The rigor ledger now reads correctly (gravity self-forcing, not matter-sourcing). Next: a fresh swing. Bold
swings, honest failures — this one was a failure caught and reported.

## References

```text
this repo: v2.414 (mis-attributed matter-sources-gravity, retracted), v2.393 (original), v2.415 (ledger row corrected), v2.411/412 (rigor core + implied, untouched)
physics: amplitude positivity within the gravitational sector (graviton forward positivity, cubic-graviton positivity, parity-decomposed positivity -- Caron-Huot et al); matter loops generating R^2 is a separate RG statement not encoded here
```
