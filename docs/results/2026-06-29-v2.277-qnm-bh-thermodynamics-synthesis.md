# v2.277 - Synthesis capstone: the black-hole thermodynamics sub-arc (v2.257-v2.276), cross-verified

## Question

Six cycles built a black-hole thermodynamics / quantum-geometry sub-arc -- entropy (v2.257), the
holographic bound (v2.258), greybody emission (v2.273), area quantization (v2.274), the Page curve
(v2.275), and the Hawking-Page transition (v2.276). Are they one coherent structure, and do the modules
agree?

## Result

They are one structure, and all 6/6 cross-program checks pass. The unifying object is the horizon AREA:

```text
  cycle   topic               the area is ...
  v2.257  thermodynamics      ENTROPY      (S = A/4)
  v2.258  holographic bound   the BOUND    (S <= A/4, saturated: Bekenstein = holographic = S_BH)
  v2.273  greybody            a RADIATOR   (barrier-filtered Hawking at the photon sphere)
  v2.274  area quantization   QUANTIZED    (dA = 4 ln3, entropy quantum ln3)
  v2.275  Page curve          the ISLAND   (remaining area/4 returns the information)
  v2.276  Hawking-Page        a PHASE      (free energy F(area) drives a transition)

  checks:
  [PASS] bekenstein_holographic_S_BH_all_equal       (v2.258 three-way saturation)
  [PASS] hawking_T_is_1_over_8piM_across_units        (T_H(M_Pl)/T_Pl = 1/8pi, SI == natural units)
  [PASS] entropy_area_law_S_prop_M2_T_prop_invM       (S ~ M^2, T ~ 1/M)
  [PASS] area_quantum_entropy_is_ln3                  (dA/4 = ln3, v2.274)
  [PASS] page_point_and_time_consistent               (peak at M0/sqrt2, t_Page = 1-2^-3/2)
  [PASS] greybody_unity_and_hawking_page_sign_change  (v2.273 -> 1, v2.276 F flips sign)
```

The Hawking temperature `1/(8 pi M)` is literally the same number in the SI treatment (v2.257) and the
natural-units modules (v2.273/274/276), verified via `T_H(M_Pl)/T_Pl = 1/8pi`; the area law scales as
`S ~ M^2`, `T ~ 1/M`, evaporation `~ M^3` consistently; and the Bekenstein and holographic bounds are
saturated by the black hole.

## What this establishes

"Black-hole thermodynamics" is not six topics but one -- the horizon area, read as entropy, bound,
quantum, radiator, island and phase. The modules are mutually consistent across SI and natural units,
sharing `S = A/4` and `T_H = 1/(8 pi M)`. This is the session's sixth cross-verified synthesis capstone
(after v2.242 strong-field, v2.250 BH-hypothesis, v2.256 QG-phenomenology, v2.265 swampland, v2.271
graviton-observables).

## Honest scope

A synthesis / cross-verification capstone: every check is a consistency relation among results already
established and caveated in v2.257-v2.276 (the area quantum's `ln3` is a source-backed input, the Page
curve is the phenomenological island model, the greybody is WKB-limited at low frequency, the
Hawking-Page result is AdS_4-specific). No new bound is derived; the value is showing the six treatments
are mutually consistent and are all the horizon area in disguise. The SI<->natural-units temperature
bridge and the thermodynamic scaling laws are exact. A QG / BH-structure result, not an engine
constraint refit.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_bh_thermodynamics_synthesis.py --out experiments/results/v2.277/qnm_bh_thermodynamics_synthesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_bh_thermodynamics_synthesis.py -q
```

Result: Bekenstein = holographic = S_BH; the Hawking temperature bridges SI and natural units
(`= 1/8pi`); `S ~ M^2`, `T ~ 1/M`; the area quantum carries entropy `ln3`; the Page point/time are
consistent; the greybody tends to 1 and the Hawking-Page free energy flips sign; `5 passed`.

## Next (v2.278)

The BH-thermodynamics sub-arc is closed and cross-verified. Either a fresh QG thread, the
user-authorized `g_R4` core-engine extension, or the dedicated analytic Leaver solver.

## References

```text
Bekenstein, 'Black holes and entropy', PRD 7 (1973) 2333
Hawking, 'Particle creation by black holes', CMP 43 (1975) 199
this repo: v2.257, v2.258, v2.273, v2.274, v2.275, v2.276
```
