# v2.292 - The g_R4 core-engine extension: the Riemann^4 operator becomes engine-internal

## Question

FIRST SLICE of the user-authorized new-theory arc (after the loop was redirected from reconstructing
community physics toward inventing new QG theory; plan: g_R4 -> cross-sector moment principle ->
method-as-proposal; mode: swing for breakthroughs). The engine's matter sector carries a dispersion
tower (`g_6^2 <= g_4 g_8`). Can the curvature sector's ringdown-active quartic operator be made
engine-internal, so the engine MANDATES it rather than importing the analysis?

## Result

The engine now carries the **Riemann^4 operator `g_R4`** -- the first ringdown-active curvature
operator (v2.233) -- and the matching curvature dispersion-tower constraints, new first-class engine
code in `src/itb/constraints/curvature_dispersion_tower.py`:

```text
  curvature_riemann4_positivity        :  g_R4 >= 0
  curvature_moment_tower_g_R4_mandate  :  g_R3^2 <= g_R2 * g_R4   (=> g_R4 >= g_R3^2/g_R2, the v2.234 mandate)
```

opt-in via `build_stack(include_curvature_tower=True)`. Its bite is real -- each curvature framework,
at its literature `g_R2, g_R3`, is INFEASIBLE against the new tower at `g_R4 = 0` and heals exactly at
the forced minimum:

```text
  framework          g_R2   g_R3   forced g_R4 min   feasible@0   feasible@min   mandate binds
  pure_gr            0.00   0.00   0.0000            yes          yes            no
  string_tree_eft    0.20   0.15   0.1125            NO           yes            YES
  asymptotic_safety  0.15   0.10   0.0667            NO           yes            YES
  lqg_induced        0.30   0.30   0.3000            NO           yes            YES
  cdt                0.22   0.15   0.1023            NO           yes            YES
```

The forced minima reproduce v2.234 exactly. All 5/5 checks pass, and the full suite stays green
(`1995 passed`) -- the opt-in design gives zero cascade.

## What this establishes

The engine's OWN positivity logic now mandates that any consistent UV completion carrying Ricci^2 and
Ricci^3 corrections must also carry a nonzero Riemann^4 -- the operator that actually deforms the
black-hole ringdown. What was an external analysis in v2.234 is now engine-internal structure: the
ringdown sector is no longer imported, it is forced by the engine's constraint geometry. This is the
authorized first step of the g_R4 arc, and the genuine new-theory content is that the curvature
dispersion tower (`g_R3^2 <= g_R2 g_R4`, the curvature analog of the matter `g_6^2 <= g_4 g_8`) is now
a live constraint the engine enforces.

## Honest scope

A genuine core-engine extension: `g_R4` and its two constraints are new first-class engine code, wired
opt-in into `build_stack`. The mandate is the v2.234 result (the curvature-sector Cauchy-Schwarz of the
v2.261 Stieltjes moment tower), now ENFORCED rather than analyzed; it inherits the engine's
representative-O(1)-prefactor caveat (the literal dispersion derivation carries Gegenbauer/spin weights
the toy basis sets to 1). The frameworks' source files are UNCHANGED this slice -- `g_R4` is applied on
the fly -- to avoid perturbing the `complexity_cutoff` and `distance_conjecture` margins; so "feasible
at the forced minimum" is against the curvature tower alone, not the full 38-constraint stack (that
integration is the next slice). The parity-odd `g_R4_c3` component (v2.209) stays dark. A
new-engine-theory result, not a reconstruction.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_gr4_engine_extension.py --out experiments/results/v2.292/qnm_gr4_engine_extension.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_gr4_engine_extension.py -q
.venv/Scripts/python.exe -m pytest -q   # full suite: 1995 passed
```

Result: the two curvature-tower constraints are opt-in wired; the mandate binds every curvature
framework at `g_R4 = 0` and heals at the forced minimum; the minima reproduce v2.234; full suite green;
`7 passed`.

## Next (v2.293)

The g_R4 arc continues: (a) promote `g_R4` into the framework encoders and the default stack (handling
the complexity/distance cascade), then (b) build the cross-sector moment principle -- propose that the
matter tower (`g_4/g_6/g_8`) and the curvature tower (`g_R2/g_R3/g_R4`) are ONE unified moment problem,
forcing inter-sector relations not in the literature, and test whether they are non-trivially forced.

## References

```text
this repo: v2.234 (the g_R4 mandate), v2.261 (curvature Stieltjes moment tower), v2.233 (Riemann^4 ringdown-active)
Caron-Huot, Mazac, Rastelli, Simmons-Duffin, JHEP 07 (2021) 110 (the moment / EFT-hedron tower)
new code: src/itb/constraints/curvature_dispersion_tower.py; experiments/stack.py (include_curvature_tower)
```
