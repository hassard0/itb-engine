# v2.99 - g8 joint-source discovery queue: next adapter build selected

## Question

v2.98 made the joint `g_8 + secondary_axis` gate executable. Instead of
accepting "no real packet" as a stopping point, which public sources can be
used to build toward that gate, and what adapter should be built next?

## Result

Artifact: `experiments/results/v2.99/g8_joint_source_discovery_queue.json`

Current route status:

```text
joint_source_queue_ready_next_adapter_build_selected
```

Claimable discriminator now:

```text
false
```

## Sources Checked

| candidate | role |
|---|---|
| <https://arxiv.org/abs/2504.12855> | `g_8` partial-wave formalism seed |
| <https://arxiv.org/abs/2407.08929> | GW170608 higher-curvature EFT measurement-analysis seed |
| <https://gwosc.org/GWTC-1/> | public GW170608 strain/reanalysis input |
| <https://github.com/gwastro/o2-bbh-pe> | public O2 BBH posterior-sample seed |
| <https://arxiv.org/abs/2507.17143> | observation-to-EFT dictionary |
| <https://arxiv.org/abs/2605.18212> | short-range `g_R2` secondary-axis seed |
| <https://arxiv.org/abs/2504.15005> | quadratic-Weyl `g_C` secondary-axis seed |

## Selected Build Route

The queue selects:

```text
gw_reanalysis_to_joint_secondary_packet
```

Inputs:

- `gwosc_gw170608_open_data_release`
- `liu_yunes_gw170608_higher_curvature_eft_2024`
- `bernard_eft_gravitational_wave_dictionary_2025`

Next build action:

```text
map_public_gw_reanalysis_parameters_to_engine_secondary_axis
```

Remaining missing objects for that route:

- `source_backed_g8_projection`
- `joint_engine_axis_likelihood_export`
- `framework_pair_exclusion_math`

## Interpretation

The blocker is now decomposed into buildable source-adapter tasks. No checked
source is claim-ready, but the next adapter target is explicit rather than an
open-ended stop condition: build a public-GW reanalysis secondary-axis adapter,
then re-run the v2.98 gate.

This still does not promote a physics claim. It turns the missing-packet problem
into a concrete implementation queue.

## Vulcan Reproduction

Validated on `vulcan` in a clean checkout at
`/home/admin/codex-runs/itb-engine-main-v299`.

```bash
.venv/bin/python experiments/g8_joint_source_discovery_queue.py --out experiments/results/v2.99/g8_joint_source_discovery_queue.json
.venv/bin/python -m pytest tests/test_g8_joint_source_discovery_queue.py -q
.venv/bin/python -m ruff check experiments/g8_joint_source_discovery_queue.py tests/test_g8_joint_source_discovery_queue.py
.venv/bin/python -m pytest -q
```

Focused validation passed with 6 tests. Full-suite validation passed with 914
tests and one existing Starlette/httpx deprecation warning.
