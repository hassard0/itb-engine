# v2.36 - Quintic promotion forecast: ownership would improve, exclusion would not

**Date:** 2026-06-19
**Compute:** local run plus fresh Vulcan clone `~/codex-runs/itb-engine-main-v236a`
**Input:** v2.34 quintic tower evidence candidate and v2.35 SDC-bound scope audit
**Artifact:** `experiments/results/v2.36/quintic_promotion_forecast.json`

## Question

v2.34 produced a schema-ready sourced tower candidate. v2.35 showed it is not an
asymptotic SDC test. v2.36 asks:

> Should the candidate be promoted into the framework catalogue?

Not as `string_tree_eft`. The source is one compactification, one scalar Laplacian
subtower, and a finite-range fit. Attaching it to the generic string-tree EFT framework
would overstate the source.

## Promotion Forecast

| mode | allowed | result |
|---|---:|---|
| attach to generic `string_tree_eft` | false | scope mismatch |
| register `string_tree_quintic_compactification` | true | evidence-owned but non-excluding |
| keep external candidate | true | no registry churn |

If a scoped quintic framework were registered:

```json
{
  "would_increase_registered_framework_count_to": 14,
  "would_have_native_tower_evidence": true,
  "expected_tower_verdict": "tower_allowed_by_predictive_spectrum",
  "would_create_framework_exclusion": false,
  "expected_frontier_status": "tower_evidence_present_but_not_excluding"
}
```

## Command

```bash
python experiments/quintic_promotion_forecast.py \
  --out experiments/results/v2.36/quintic_promotion_forecast.json
```

Remote verification:

```bash
python tools/vulcan.py run \
  'set -e; cd ~/codex-runs/itb-engine-main-v236a; \
   .venv/bin/python -m pytest tests/test_quintic_promotion_forecast.py -q; \
   .venv/bin/python -m ruff check \
     experiments/quintic_promotion_forecast.py \
     tests/test_quintic_promotion_forecast.py; \
   .venv/bin/python experiments/quintic_promotion_forecast.py \
     --out experiments/results/v2.36/quintic_promotion_forecast.json'
```

## Bottom line

Do not attach the quintic row to generic `string_tree_eft`. A scoped quintic framework
would be honest, but it would not advance the discriminator because the sourced row is
tower-allowed. The next scientific step should be asymptotic lightest-tower extraction
or a compactification source with `phi_tower` above the critical threshold.
