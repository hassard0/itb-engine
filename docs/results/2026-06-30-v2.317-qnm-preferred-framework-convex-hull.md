# v2.317 - The corrected preferred framework: re-establishing v2.312-315 under convex_hull

## Question

v2.316 showed the recent arc's feasibility verdicts used the deprecated `matter_product` repulsive-force
form and should use the recommended `convex_hull` form. Re-run the preferred-framework construction
(v2.312/v2.313) and the scorecard (v2.315) under `convex_hull`: which results survive, which change?

## Result (core survives; feasibility corrected; a parity reversal)

```text
  theory             geom margin   feasible   binding
  asymptotic_safety   +0.0192       yes        graviton_forward_positivity
  string_tree_eft     +0.0128       yes        graviton_forward_positivity
  cdt                 +0.0108       yes        bnossw_monogamy
  pure_gr              0.0000       yes        scalar_positivity_g4 (marginal)
  lqg_induced         -0.0474       no         cft_flat_space_bound
  --------------------------------------------------------------
  CONSTRUCTED center  +0.0331       yes        (g_4 0.53, g_6 0.40, g_8 0.40,
                                                g_R2 0.19, g_R3 0.09, g_R2_parity 0.038)
```

**1. The core survives.** The engine still prefers a distinct CONSTRUCTED framework -- the Chebyshev
center has geometric margin +0.033, more robust than every community framework (best community:
asymptotic_safety at +0.019). So "the engine points to a higher-derivative gravity more robust than the
named proposals" holds under the corrected encoding too.

**2. Feasibility corrected.** Three community frameworks are now feasible, with a clean robustness ranking
asymptotic_safety > string_tree_eft > cdt > pure_GR(marginal) > lqg_induced (the only infeasible one) --
replacing the "all community infeasible / only pure GR" framing of v2.312/v2.315.

**3. Parity reversal (the new finding).** The corrected preferred framework carries a MILD PARITY
VIOLATION (`g_R2_parity ~ 0.038`), and forcing parity to zero costs ~40% of the robustness
(`+0.033 -> +0.020`). Under the corrected encoding consistency mildly PREFERS a parity-violating theory --
the opposite of the parity-free preferred framework found under the deprecated form (v2.312/v2.313), which
we now see suppressed the parity direction via the spurious `g_4*g_6` product. All 5/5 checks pass.

## What this establishes

The corrected headline is sharper and more surprising than the original: the most-robustly-consistent
higher-derivative gravity is a string-like, MILDLY PARITY-VIOLATING theory distinct from every named
framework; among the named frameworks asymptotic safety is the most consistent while lqg alone is excluded
(via its outlier cubic curvature, v2.311). The v2.312 core claim survives the correction; the parity-free
and universal-exclusion sub-claims do not.

## Honest scope

All values are the engine's literal `check()` output under `build_stack(rfc_form='convex_hull')` (the
recommended form, v2.316). The constructed center is an approximate, seed-dependent multi-seed
coordinate-ascent argmax of the geometric (gradient-normalized) worst-case margin, not a proven global
optimum; its exact coordinates and margin are convention- and search-dependent. The robust, sign-based
claims: three community frameworks feasible / lqg alone infeasible, the constructed center strictly beats
every community framework, and the parity-zero optimum has strictly lower margin (so the mild parity
violation is a real feature, the 0.033-vs-0.020 gap being well above the grid step). The exact parity
magnitude (~0.04) and the close community ordering (0.019/0.013/0.011) depend on the O(1) prefactors. The
"parity reversal" is a statement about the two RFC encodings -- the deprecated form suppressed the parity
direction -- not a claim that nature is parity-violating. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_preferred_framework_convex_hull.py --out experiments/results/v2.317/qnm_preferred_framework_convex_hull.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_preferred_framework_convex_hull.py -q
```

Result: under convex_hull, AS > string > cdt feasible and lqg alone infeasible; the constructed center
(+0.033) beats every community framework; the corrected preferred framework has g_R2_parity ~ 0.038 and
forcing parity-free costs ~40% of the robustness; `5 passed`.

## Next (v2.318)

The corrected picture is established. A genuinely fresh sector is due. Honest negatives reported as part of
the search.

## References

```text
this repo: v2.316 (RFC-form correction), v2.312/v2.313 (preferred framework, deprecated form), v2.315 (scorecard, corrected)
engine: build_stack(rfc_form='convex_hull') -- the recommended repulsive-force encoding
```
