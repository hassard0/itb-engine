# v2.423 - CORE EXTENSION CC2: the AdS distance conjecture selects de Sitter

Second slice of the cosmological-constant sector (CC1 = v2.422). Adds the **AdS distance conjecture**
(Lüst-Palti-Vafa 2019): as |Λ| → 0 a tower becomes light, so with the tower at the Dvali species scale
`Λ_species = 1/√N` (the same `N = 1 + ν(|g_R2|+|g_C|+|g_R3|)` the species-scale bound counts), it becomes a
**floor** on the vacuum-energy magnitude, `|g_Lambda| ≥ c_AdS/N` (α=1/2). A bounded (not-parametrically-light)
tower forbids a parametrically small |Λ| — the swampland form of the CC naturalness puzzle. Opt-in; tagged
`sourced_proxy`.

## Result

```text
  candidate N = 1.566  ->  AdS floor |g_Lambda| >= 0.639   (c_AdS=1, alpha=1/2)
  EFT-validity / complexity ceiling on |g_Lambda| ~ 0.60
  => AdS floor (0.639) EXCEEDS the ceiling (0.60): the AdS branch is EMPTY
  allowed windows:  AdS branch = none ;  dS/Minkowski branch = [0.0, 0.193]
  dS-selection holds for c_AdS >~ 0.94 (O(1) threshold)
```

All 5/5 checks pass. Combined with CC1's refined-dS ceiling (`g_Lambda ≤ g_R2 ≈ 0.193`), the candidate's entire
admissible vacuum-energy window collapses to the **dS/Minkowski branch [0, 0.193]**.

## What this establishes

The two swampland conjectures plus EFT-validity **select de Sitter (or Minkowski)** for the candidate: it "wants
to be de Sitter," and it **cannot be anti-de Sitter**. The candidate's bounded tower (heavy-ish species scale,
N ~ 1.6) is exactly what forbids the small-|Λ| AdS vacuum — so the *same tower structure that makes the theory
string-like* (v2.375) also drives it to positive (dark-energy) or zero vacuum energy. The dS-selection is
**O(1)-robust** (holds for c_AdS ≳ 0.94, the threshold where the AdS floor drops to the EFT ceiling), not a
knife-edge — though c_AdS-dependent. Net CC arc so far: the candidate **accommodates dark energy** (CC1), is
**bounded above by its inflaton curvature** (CC1), and is **pushed off anti-de Sitter by its tower** (CC2) — a
coherent, if conjectural, dark-energy story from the swampland sector.

## Honest scope

Both CC constraints are **conjectural** swampland statements encoded as proxies (tagged `sourced_proxy`). "No AdS
vacuum" is an **O(1)-robust structural statement, not a theorem**: it says the AdS-distance floor and the
EFT-validity ceiling are incompatible at O(1) coefficients for the candidate's tower. `g_Lambda` is dimensionless,
so this is about the *structure/sign* of the vacuum energy (dS vs AdS admissibility), **not** the CC *magnitude*
problem (10⁻¹²⁰). N uses g_C=0 (not set in this coefficient dict); a nonzero g_C raises N and lowers the floor —
the qualitative "bounded tower forbids small AdS Λ" is the robust content. Opt-in, so no prior result changes.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_cc_ads_distance.py --out experiments/results/v2.423/qnm_cc_ads_distance.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_cc_ads_distance.py -q
```

Result: AdS floor 0.639 > EFT ceiling 0.60 → AdS empty, dS/Mink [0, 0.193]; dS-selection for c_AdS≳0.94;
`5 passed`. Full suite green (2693 passed).

## Next (CC3, v2.424): quintessence vs a true constant

Next slice: the quintessence-vs-cosmological-constant distinction — the refined dS conjecture favors a *rolling*
scalaron (quintessence) over a static Λ; the candidate's g_R2 scalaron can roll. Bold swings, honest failures.

## References

```text
this repo: v2.422 (CC1 refined-dS ceiling), v2.394 (species scale), v2.375 (string-like tower), src/itb/constraints/species_scale.py (N reused), src/itb/constraints/cosmological_constant.py
physics: Lust-Palti-Vafa 2019 (AdS distance conjecture); Dvali (species scale); Ooguri-Palti-Shiu-Vafa 2018 (refined dS)
```
