# v2.291 - The tidal deformability of a neutron star: the GW170817 observable and the EOS tension

## Question

Closes the loop between the relativistic stellar structure of v2.289/v2.290 (TOV, Buchdahl, maximum
mass) and the gravitational-wave program of v2.270 (GW170817). How deformable is the v2.290 neutron
star to a companion's tidal field, and is it consistent with what LIGO/Virgo measured?

## Result

Integrating the Hinderer l=2 metric-perturbation variable `y(r)` alongside the TOV background for the
v2.290 stiff `Gamma = 2` polytrope, then forming `k_2(y_R, C)` and `Lambda = (2/3) k_2 C^{-5}`:

```text
  M (Msun)  R (km)   C       k_2      Lambda
  0.98      16.60    0.087   0.137    18190
  1.41      15.36    0.136   0.090     1295   <- reference 1.4 Msun
  1.67      14.37    0.171   0.063      288
  1.82      13.58    0.198   0.047      105
  1.91      12.92    0.218   0.037       50
```

The reference `1.41 Msun` star has `k_2 = 0.090` and `Lambda = 1295` -- **above** the GW170817 upper
bound `Lambda_1.4 < 800` (measured `~190 (+390/-120)`). So the stiff EOS that v2.290 needed to reach
`M_max ~ 2 Msun` makes the `1.4 Msun` star too LARGE (`R = 15.4 km` vs the GW170817-implied
`R_1.4 <~ 13.5 km`) and is **disfavored** by the tidal measurement. All 5/5 checks pass (including the
check that the stiff polytrope is disfavored).

## What this establishes

This is the genuine, well-known equation-of-state tension, reproduced from first principles: the
heavy-pulsar maximum-mass data (PSR J0740, `M >= 2.08`, v2.290) demand a STIFF EOS, while GW170817's
small tidal deformability demands a SOFTer one at `1.4 Msun` -- and a single `Gamma = 2` polytrope
cannot do both. Real nuclear matter must be soft at intermediate density and stiff at high density.
`Lambda` falls steeply with mass (`~ C^{-5}`, verified), so the tidal signal preferentially constrains
the lighter, larger configurations. And a neutron star's Love number is NONZERO, in sharp contrast to
the v2.235 result that a black hole's vanishes exactly -- so a measured `Lambda` is itself an "is it a
black hole?" discriminator. The same TOV machinery connects the v2.289 compactness bound, the v2.290
maximum mass, and the v2.270 GW170817 observable, and shows honestly how the two data sets squeeze the
EOS from both sides.

## Honest scope

A first-principles RK4 integration of the standard Hinderer (2008) tidal equation on a TOV background,
with the TOY `Gamma = 2` polytrope (`K = 220 km^2`) of v2.290 -- so the numbers are REPRESENTATIVE of a
stiff EOS, not a microphysical prediction. The "disfavored by GW170817" conclusion is correct FOR THIS
TOY EOS (it really is too large at `1.4 Msun`); the broader heavy-pulsar-vs-tidal tension is the
published result, here illustrated rather than resolved. The `k_2` closed form and the y-integration
are exact for the chosen EOS (no surface-discontinuity junction term for a polytrope). The GW170817
`Lambda_1.4 ~ 190` / `< 800` and the PSR J0740 mass are cited published values. A classical-GR /
relativistic-astrophysics result, not an engine constraint refit.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_ns_tidal_deformability.py --out experiments/results/v2.291/qnm_ns_tidal_deformability.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_ns_tidal_deformability.py -q
```

Result: `k_2` in the NS range; `Lambda` falls steeply with mass; the reference `1.4 Msun` star has
`Lambda = 1295 > 800` (too large, disfavored by GW170817); `7 passed`.

## Next

Reorienting toward original quantum-gravity theory work (per user direction) rather than further
community-physics reconstructions.

## References

```text
Hinderer, 'Tidal Love numbers of neutron stars', ApJ 677 (2008) 1216
Abbott et al. (LIGO/Virgo), 'GW170817: ... neutron star radii and equation of state', PRL 121 (2018) 161101
this repo: v2.289 (TOV/Buchdahl), v2.290 (maximum mass), v2.235 (BH Love number = 0), v2.270 (GW170817)
```
