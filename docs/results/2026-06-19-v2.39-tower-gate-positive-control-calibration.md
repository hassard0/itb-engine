# v2.39 - Tower-gate calibration: positive controls fail, so no discriminator claim

**Date:** 2026-06-19
**Compute:** local run plus fresh Vulcan clone `~/codex-runs/itb-engine-main-v239a`
**Input:** v2.37 large-volume benchmark, v2.38 analytic KK benchmark, and the current tower gate
**Artifact:** `experiments/results/v2.39/tower_gate_positive_control_calibration.json`

## Question

v2.37 and v2.38 found primary-source benchmarks that cross the current tower threshold.
v2.39 asks:

> Are those threshold crossings evidence for a framework exclusion, or evidence that
> the tower gate is not calibrated as a universal discriminator?

They are calibration failures. The gate rejects known string-compatible decompactification
positive controls, so the gate cannot currently support framework exclusions.

## Result

```json
{
  "positive_control_count": 7,
  "positive_control_rejection_count": 7,
  "calibration_status": "tower_gate_fails_positive_control_calibration",
  "calibrated_discriminator_ready": false,
  "tower_discriminator_candidates_now": [],
  "claimable_framework_exclusions_now": []
}
```

Positive controls:

- one large-volume Calabi-Yau SDC benchmark from Blumenhagen/Klaewer/Schlechter/Wolf
- six analytic KK decompactification vectors from Aoufia/Castellano/Ibanez

All seven are mathematically excluded by the current tower gate at the audit's
`Delta_moduli = 1` benchmark. Since these are string-compatible controls, the correct
interpretation is:

> Do not promote tower-gate math exclusions into framework exclusions until the gate is
> recalibrated or scope-limited so it does not reject known string-compatible
> decompactification controls.

## Command

```bash
python experiments/tower_gate_positive_control_calibration.py \
  --out experiments/results/v2.39/tower_gate_positive_control_calibration.json
```

Remote verification:

```bash
python tools/vulcan.py run \
  'set -e; cd ~/codex-runs/itb-engine-main-v239a; \
   .venv/bin/python -m pytest tests/test_tower_gate_positive_control_calibration.py -q; \
   .venv/bin/python -m ruff check \
     experiments/tower_gate_positive_control_calibration.py \
     tests/test_tower_gate_positive_control_calibration.py; \
   .venv/bin/python experiments/tower_gate_positive_control_calibration.py \
     --out experiments/results/v2.39/tower_gate_positive_control_calibration.json'
```

## Bottom line

This is a stronger result than another candidate scan. The current tower gate is useful
as a stress test, but it is not a defensible quantum-gravity discriminator while it
rejects string-compatible positive controls. The next loop should recalibrate the tower
gate, not register a threshold-crossing framework.
