# v2.326 - The parity coupling is bounded below by both the swampland distance conjecture and the data

## Question

A rigorous follow-up tying the v2.325 active-core finding (the swampland distance conjecture is the TOP
carving constraint) to the parity finding (v2.321, cosmic birefringence requires nonzero parity). The
distance conjecture caps the coupling hierarchy -- does it interact with the data-demanded parity
violation?

## Result (a convergence: two independent lower bounds on the parity coupling)

```text
  theory               g_R2_parity   hierarchy (max/min)   parity-violating
  pure_gr              0.000          0.0                   no
  string_tree_eft      0.000          3.3                   no
  asymptotic_safety    0.000          4.0                   no
  cdt                  0.000          3.7                   no
  engine_constructed   0.060          8.8                   yes
  lqg_induced          0.080         15.0                   yes  (near the R_max ~ 20 limit)
```

The parity-EVEN frameworks have a tame coupling hierarchy (~3-4); the parity-VIOLATING ones are stretched
(constructed 8.8, lqg 15.0), because the small parity coupling sits far below the larger matter couplings.
This is exactly why the distance conjecture is the dominant carver (v2.325): it reacts to the hierarchy the
data-demanded parity violation creates. And it bites from below -- if the parity coupling shrinks too far,
`max/min` explodes, so the distance conjecture forbids `g_R2_parity < 0.027` (implied `R_max ~ 20`) on the
constructed matter sector. Cosmic birefringence independently requires `g_R2_parity >= 0.048` (v2.321),
tighter than the distance bound. All 5/5 checks pass.

## What this establishes

Both a swampland consistency condition (the distance conjecture) and a real measurement (cosmic
birefringence) push the parity coupling UP -- it cannot be too small -- a convergence that joins the
program's two dominant threads: the distance conjecture (the top carving constraint, v2.325) and the parity
violation (anomaly-preferred v2.318, data-required v2.321). The parity coupling is pinned not-too-small
from two independent sides, and lqg's near-limit hierarchy (15 vs R_max ~ 20) is yet another face of its
boundary status, alongside its outlier cubic `g_R3` (v2.311).

## Honest scope

The coupling hierarchies (max/min) are EXACT arithmetic on the encoded couplings; the distance-conjecture
lower bound and the implied `R_max` are the engine's literal `check()` output (`R_max ~ 20` is the engine's
O(1) prefactor for the distance conjecture, so the 0.027 bound scales with it). The robust, structural
content: parity violation stretches the coupling hierarchy (parity-even ~3-4 vs parity-violating ~9-15), so
the distance conjecture is the dominant carver because of the parity feature the data demands; and BOTH the
distance conjecture and cosmic birefringence lower-bound the parity coupling, with the data tighter. The
exact numbers (8.8, 15.0, the 0.027/0.048 bounds) depend on the constructed matter sector
(convention-dependent) and the `R_max`/beta-map prefactors; the DIRECTION of both bounds (lower) and the
convergence are robust. The cosmic-birefringence bound carries its ~3.6-sigma-detection and
order-of-magnitude-map caveats (v2.321). Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_parity_hierarchy_bound.py --out experiments/results/v2.326/qnm_parity_hierarchy_bound.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_parity_hierarchy_bound.py -q
```

Result: parity-violating theories have a stretched hierarchy (8.8, 15.0) vs parity-even (3-4); the distance
conjecture and cosmic birefringence both lower-bound the parity coupling (0.027 and 0.048), the data
tighter; lqg sits near the distance-conjecture limit; `5 passed`.

## Next (v2.327)

The active-core and parity threads are tied together. A fresh sector or continued audits, at a more
deliberate pace given the program's maturity. Honest negatives reported as part of the search.

## References

```text
this repo: v2.325 (distance conjecture is the top binder), v2.321 (cosmic birefringence lower-bounds parity), v2.311 (lqg g_R3 outlier)
the swampland distance conjecture (coupling-hierarchy bound); src/itb/constraints/swampland.py
```
