# v2.242 - Synthesis capstone: the black-hole strong-field program, cross-verified

## Question

This session built a large black-hole-physics program across ~30 cycles (v2.210-v2.241). Two things
are now worth doing: (1) GUARANTEE its internal consistency -- the independent modules must agree on
the shared Schwarzschild physics -- and (2) map it so it is navigable.

## Cross-program consistency (the verifiable content)

Independent modules computing the same quantity by different routes must agree. Re-importing the
headline modules and checking:

```text
  [OK] photon_sphere_Omega_c == 1/(3 sqrt3)
  [OK] eikonal_ringdown_freq(a=0) == Omega_c          (v2.240 -> v2.229 limit)
  [OK] eikonal_ringdown_damping(a=0) == Omega_c        (v2.241 Lyapunov -> v2.229 identity)
  [OK] shadow Omega_c * b_c == 1                        (v2.230)
  [OK] Schwarzschild ISCO == Kerr(a=0) ISCO == 6        (v2.237 == v2.239 limit)
  [OK] ISCO efficiency == Kerr(a=0) efficiency == 5.72% (v2.237 == v2.239 limit)
  [OK] WKB omega_220 ~ Berti reference                  (v2.210)
  [OK] BH tidal Love number == 0                        (v2.235)

  all_consistent = True
```

All eight pass: the photon-sphere `Omega_c = 1/(3 sqrt3)` appears identically in the shadow,
eikonal-frequency, and Lyapunov-damping modules; the Schwarzschild ISCO matches the `a=0` limit of
the exact Kerr formula; the WKB `omega_220` matches Berti; the BH tidal Love number is zero. If any
module had drifted, these would catch it.

## Program map (five themes)

```text
  v2.210-v2.228   R4 / qEFT ringdown sensitivity
                  in-house WKB+Riccati QNM solver -> source-backed R4 odd-parity sensitivity ->
                  overtone-DAMPING dominance -> resolvability + two-mode covariance + the overtone
                  covariance wall -> no-hair violation -> population N^-1/12 -> end-to-end reach

  v2.233-v2.234   Operator-sector bridge to the core engine
                  Schwarzschild Ricci-flat => g_R2/g_R3 ringdown-blind, the ringdown-active operator
                  is Riemann^4 (beyond the basis); the dispersion tower MANDATES g_R4 >= g_R3^2/g_R2

  v2.229-v2.232,  Geodesic strong-field + deformation null tests
  v2.237-v2.238   eikonal QNM <-> photon sphere; photon-sphere<->shadow (Omega_c=1/b_c); the
                  ringdown-frequency/shadow LOCKING vs independent damping; two-observable inversion;
                  ISCO/efficiency; the unified one-parameter fingerprint

  v2.235-v2.236   Tidal Love number (static response)
                  GR BH Love number EXACTLY ZERO (tail-free Legendre polynomial); any localized
                  deformation lifts it linearly (overlap-integral transfer function)

  v2.239-v2.241   Kerr generalization
                  spin SPLITS the locked observables and AMPLIFIES prograde efficiency 5.72%->42%
                  (AGN engine); full eikonal Kerr omega_lmn(a) from the photon orbit (frequency
                  rises, damping falls -> rising quality factor / near-extremal zero-damping modes)
```

## What this establishes

A verified-consistent, navigable synthesis of the strong-field program, unified by the photon orbit
and the deformation-response structure. It confirms the ~30 modules form one coherent body rather
than disconnected scripts, and it records the two larger efforts left honestly deferred: the precise
low-`l` Teukolsky/Leaver QNM solver (multi-session) and the `g_R4` core-engine basis extension
(awaiting user authorization).

## Honest scope

A SYNTHESIS / consistency capstone, not a new measurement: it re-verifies the shared baselines and
maps the program. The per-cycle honest caveats (eikonal large-`l` limits, static/Schwarzschild
baselines, illustrative deformations, un-sourceable absolute R4 normalization, representative O(1)
positivity prefactors) all carry. Parity-odd `g_R4_c3` stays dark (v2.209).

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_strong_field_synthesis.py --out experiments/results/v2.242/qnm_strong_field_synthesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_strong_field_synthesis.py -q
```

Result: 8/8 cross-program consistency checks pass (`all_consistent = True`); five-theme program map;
`4 passed`.

## Next (v2.243)

With the strong-field program consolidated, the open high-value directions are the two deferred
efforts -- (a) the user-authorized `g_R4` core-engine extension (fully scoped v2.234), or (b) the
Teukolsky/Leaver precise QNM solver -- or (c) a fresh thread (e.g. a return to the qEFT/ParSpec
ringdown data, or a different QG observable entirely).

## References

```text
this repo: v2.210-v2.241 result notes (docs/results/), docs/results/INDEX.md
```
