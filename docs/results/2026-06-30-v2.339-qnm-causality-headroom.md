# v2.339 - The constructed theory is causal with the most headroom: trimmed cubic curvature keeps it deepest inside the CEMZ bound

## Question

The companion to v2.338 (ghost/unitarity). The OTHER deep objection to higher-derivative gravity is
CAUSALITY: Camanho-Edelstein-Maldacena-Zhiboedov showed a graviton crossing a shock wave acquires a TIME
ADVANCE unless the cubic graviton coupling is bounded, `|g_R3| <= 0.8 sqrt(g_4 g_R2)`, or a higher-spin
tower intervenes at the cutoff. Where does the constructed theory sit, and why?

## Result (causal with the most headroom; trimmed cubic is the reason)

```text
  theory               g_R3   CEMZ bound   % of bound
  engine_constructed   0.090   0.256        35%   <- deepest inside
  asymptotic_safety    0.100   0.196        51%
  cdt                  0.150   0.278        54%
  string_tree_eft      0.150   0.253        59%
  lqg_induced          0.300   0.339        88%   <- nearest the causality edge
```

Every candidate is formally causal (all satisfy CEMZ), but the constructed theory sits at only ~35% of its
bound -- the deepest inside the causality region -- while lqg (g_R3 = 0.30, the outlier cubic) sits at ~88%,
right at the edge. The same trimmed cubic curvature that made the constructed theory consistent (the
smallest g_R3, and the smallest ringdown deviation, v2.336) also makes it the most CAUSAL. All 5/5 checks
pass.

## What this establishes

The constructed theory's defining feature -- trimmed cubic curvature -- is precisely what gives it the most
causality headroom; lqg's large cubic pushes it near a graviton time-advance. CEMZ's deeper content
sharpens this: even satisfying the bound, the EFT's causality REQUIRES a higher-spin tower at the cutoff
(string theory's Regge tower is the canonical example), so the constructed theory's UV completion must
contain higher-spin states -- pointing again to a string-like UV, the same one v2.338's ghost-freedom
analysis pointed to. Together, v2.338 and this cycle show the constructed theory clears BOTH deep
consistency requirements of higher-derivative gravity -- unitarity (no ghost) and causality (no
time-advance) -- with margin, and both point its UV completion toward unitary, string-like, higher-spin
physics. This is a CP-even, data-independent property (no cosmic-birefringence dependence).

## Honest scope

The CEMZ bound is the engine's representative O(1) encoding (`kappa = 0.8`, the geometric-mean form), so the
exact fractions-of-bound (35%, 88%, ...) are convention-dependent; the robust content is the ORDERING --
the constructed theory has the smallest cubic curvature and the most causality headroom, lqg the largest
and the least -- which is prefactor-independent (it follows from the g_R3 values and the common
`sqrt(g_4 g_R2)` scale). The "causality requires a higher-spin tower" statement is the standard
interpretation of the CEMZ result, not a computation of the tower scale here; "string-like UV" is the
pairing with v2.338's unitarity reading, a qualitative inference, not a constructed completion. All
candidates formally satisfy the bound; "most/least headroom" is a comparison, not a pass/fail. Toy basis,
O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_causality_headroom.py --out experiments/results/v2.339/qnm_causality_headroom.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_causality_headroom.py -q
```

Result: all candidates satisfy CEMZ; the constructed theory is deepest inside (35% of bound, smallest
cubic, most headroom) and lqg is nearest the edge (88%); `6 passed`.

## Next (v2.340)

The constructed theory clears both unitarity (v2.338) and causality (this cycle) with margin. A fresh
sector or continued audits, at the <=10-minute cadence. Honest negatives reported as part of the search.

## References

```text
Camanho-Edelstein-Maldacena-Zhiboedov 2016 (causality constraints on the graviton 3-pt coupling; higher-spin tower)
this repo: v2.338 (ghost-freedom/unitarity), v2.303 (CEMZ on the cubic), v2.336 (trimmed cubic -> mildest ringdown), v2.317 (constructed framework)
```
