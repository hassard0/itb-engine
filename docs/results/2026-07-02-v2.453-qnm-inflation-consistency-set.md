# v2.453 - the complete scale-independent inflation prediction set: n_s pins (r, n_t, α_s)

Continuing the dimensionless vein. v2.452 pinned r from n_s; but the R² plateau is a **one-parameter** family (the
e-fold number N), so a single observable fixes *all* the others through scale-independent relations:

```text
  N       = 2 / (1 - n_s)
  r       = 3 (1 - n_s)^2               (tensor-to-scalar, v2.452)
  n_t     = - r / 8                     (single-field tensor consistency)
  alpha_s = - 2 / N^2 = -(1-n_s)^2/2    (running of the scalar tilt)
```

None carries a scale or an O(1)-toy magnitude. At Planck's n_s = 0.9649 (N ~ 57):

```text
  r = 0.0037     n_t = -0.00046     alpha_s = -0.00062
```

5/5 checks pass.

## What this completes

The candidate's inflation makes a **complete set of scale-independent predictions from one number** — r ~ 0.0037
(LiteBIRD-testable), the tiny-but-definite tensor tilt n_t ~ −5×10⁻⁴ fixed by single-field consistency, and a
small negative running α_s ~ −6×10⁻⁴. Together with **β ~ α_EM** (v2.451), the candidate now has a
**scale-independent core** — the subset of falsifiable predictions that need neither the string scale nor any
O(1)-toy coefficient, the sharpest and most scale-clean part of its observational content.

## Honest scope

Standard single-field / Starobinsky consistency relations to **leading order in 1/N**, and **plateau-class** —
shared by any plateau/R²-like model, so they validate the plateau *class*, not uniquely this candidate (same
status as v2.442/v2.452). n_t = −r/8 is the generic single-field tensor consistency; r = 3(1−n_s)² and α_s = −2/N²
are the Starobinsky/plateau forms. Finite-N/reheating corrections shift the leading values, so the numbers carry
real theory uncertainty; the *relations* are robust to leading order. **Observability differs sharply**: r ~ 0.0037
is a genuine near-term target, but **n_t ~ −5×10⁻⁴ and α_s ~ −6×10⁻⁴ are below foreseeable sensitivity** (α_s below
the nominal CMB-S4 σ ~ 3×10⁻³) — "definite but effectively unmeasurable" predictions, honest to state, not
near-term discriminators. "Scale-independent core" is a framing of the toy-scale-free subset, not new physics.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_inflation_consistency_set.py --out experiments/results/v2.453/qnm_inflation_consistency_set.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_inflation_consistency_set.py -q
```

Result: n_s → (r, n_t, α_s) = (0.0037, −0.00046, −0.00062), N ~ 57, all scale-independent; `4 passed`.

## Next: the scale-independent core is assembled — dreaming on

β ~ α_EM + the full inflation consistency set = the candidate's toy-scale-free predictions. Bold swings, honest
failures.

## References

```text
this repo: v2.452 (r=3(1-n_s)^2), v2.451 (alpha_EM birefringence), v2.441 (Starobinsky), v2.442 (over-determination)
physics: Starobinsky 1980; single-field n_t=-r/8; alpha_s=-2/N^2; Planck 2018 (n_s=0.9649); LiteBIRD (r), CMB-S4 (alpha_s)
```
