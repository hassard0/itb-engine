# v2.432 - closing the "what would solve it" investigation: an honest over-determination negative + the quantified state of the five options

The user asked what would actually solve quantum gravity; the two options advanceable in-engine were #1
(empirical: make the candidate maximally testable) and #2 (rigor: how much rigor forces). Both are now quantified
(v2.429/430 correlated signature + verdict table; v2.431 rigorous cage). This cycle does two things.

## (a) Honest negative — over-determination

Tested whether rigor + the front correlations *over-determine* the candidate — i.e. whether measuring two of the
three fronts predicts the third (which would be the strongest possible falsifiability). **It does not:**

```text
  marginal parity width:              0.042
  parity width | g_4, g_R2 ~ candidate: 0.036
  narrowing factor:                   ~1.1x   (over-determined: False)
```

Conditioning the feasible island on the matter (g_4) and dark-energy (g_R2) fronts narrows the parity range by
only ~1.1× — because each front is pinned by its *own* data/constraints, not cross-predicted by the others. So
the three fronts are moderately correlated (v2.429) but **not over-determined**; the strongest falsifier remains
the **conjunction** (v2.430: the candidate requires all three; confirmed by 1 of 8 patterns), not "2 predict the
3rd." A genuine honest negative — the idea was worth testing and didn't hold.

## (b) The quantified state of the five options

```text
  #1 empirical       DECISION-READY   correlated signature -> verdict table (1/8 confirms); awaits ~2030 data
  #2 rigor           AT ITS CEILING   rigor cages the parity-even shape but cannot force the parity (rival is
                                       rigorously fine, v2.420); residual = dark g_8 scale + data-selected parity
  #3 real-norm       BLOCKED          needs a real amplitude/matching computation to de-toy the O(1) magnitudes
  #4 UV embedding    OPEN RESEARCH    needs matching the candidate to a specific string vacuum / fixed point
  #5 write-up        DONE             Research Report II (v2.428)
```

All 5/5 checks pass.

## What this establishes

The honest terminus of the autonomous program: **the two options advanceable inside the engine (#1 empirical,
#2 rigor) are now at their ceiling** — the candidate is maximally testable (a single correlated ~2030 measurement
renders the verdict) and maximally rigor-forced (the parity-even shape is caged; the parity provably cannot be
forced by rigor). Further progress toward *actually settling* the theory needs either the **~2030 data** (which
will render the verdict) or **external inputs the engine does not have** (a real amplitude computation for #3, a
UV embedding for #4). The candidate's fate is now a matter of *measurement*, not further carving.

## Honest scope

The over-determination test is a conditional-width comparison on a random-walk sample (others free), so the ~1.1×
is a sampled estimate; the robust content is that it's *close to 1* (no strong cross-prediction), not the precise
factor, and only in the g_4/g_R2→parity direction tested. The five-option "state" is a synthesis of prior cycles
(v2.428–431) with honest status tags, not a new computation for #3/#4/#5. "At its ceiling" means rigor cannot
force the parity and the parity-even cage is already tight — not that no further rigorous constraint could ever
be added. All the usual caveats carry (magnitudes O(1)-toy; the low-energy EFT is not the UV completion).

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_solve_synthesis.py --n 25000 --out experiments/results/v2.432/qnm_solve_synthesis.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_solve_synthesis.py -q
```

Result: over-determination ~1.1× (weak); the five-option state as tabulated; `4 passed`.

## Next: the in-engine solve-options are exhausted — holding for the user's steer on #3/#4 or a new direction

Both autonomously-advanceable paths are at their ceiling. The remaining moves (#3 real normalization, #4 UV
embedding) need external inputs; I can *scope* what each requires but cannot execute without them. Flagging for
the user.

## References

```text
this repo: v2.429 (correlated signature), v2.430 (verdict table), v2.431 (rigorous cage), v2.428 (Report II), v2.420 (parity-conserving rival = rigor ceiling)
physics: the low-energy EFT is testable + rigor-constrained; the UV completion and dimensionful magnitudes need inputs beyond amplitude-carving
```
