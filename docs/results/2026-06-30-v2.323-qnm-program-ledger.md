# v2.323 - The new-theory program ledger: robust findings, schematic predictions, honest corrections

## Question

After 31 cycles (v2.292-v2.322) with a major mid-course correction, the new-theory program has a clear,
data-grounded headline. A capstone synthesis: what does the program actually establish, and at what
robustness tier?

## Headline

The engine constructs the unique higher-derivative gravity consistent with current theory AND current data
-- string-like matter, trimmed curvature, mild right-handed parity violation -- where every named framework
is excluded on one axis (the four parity-even ones on data, lqg on theory).

## The ledger (robustness-tiered)

**ROBUST (encoding + prefactor independent) -- re-confirmed here, 6/6 checks:**
- v2.322 no named framework satisfies both theory and data; a constructed one does
- v2.322 parity-even gravity predicts `beta=0` -> excluded by cosmic birefringence (a symmetry statement)
- v2.320 the constructed framework beats every community framework (98% of O(1) prefactor draws)
- v2.320 lqg is the boundary framework (92%)
- v2.309 the curvature carving is one joint nested region (matter -> g_R2 -> g_R3 -> g_R4)
- v2.311 lqg's infeasibility traces to its outlier cubic curvature `g_R3` (2x peers)

**CONSISTENCY-MECHANISTIC (engine-internal, structural):**
- v2.314 the preferred framework is an amplitude-positivity vs anomaly/universality equilibrium
- v2.318 anomaly matching is the wall relieved by a parity-odd coupling -> prefers mild parity
- v2.304/v2.305 the consistent region is non-convex (bilinear gravitational bounds); a boundary layer
- v2.306 a finite EFT cutoff adds a Hausdorff ceiling positivity misses

**DATA-FAVORED (real measurement, schematic magnitude):**
- v2.321 cosmic birefringence (`beta=0.34+/-0.09 deg`, ~3.6 sigma) requires nonzero parity and favors it

**SCHEMATIC PREDICTION (sign/structure robust, magnitude not sourced):**
- v2.319 chiral primordial GW (CMB TB/EB) from the parity coupling -- right-handed
- v2.307 `n_s` is the CMB handle on the cubic curvature `g_R3`

**HONEST NEGATIVE / CORRECTION (carried forward explicitly):**
- v2.316 CORRECTION: "all community infeasible / universality decisive" (v2.312-315) was a deprecated
  `matter_product`-RFC artifact; `convex_hull` frees string/AS/cdt (knife-edge, v2.320)
- v2.308 the cubic and quartic curvature are DEGENERATE in `n_s`; the running cannot separate them
- v2.320 community feasibility is prefactor-FRAGILE (holds at ~13% of O(1) prefactor draws)

## What this establishes

The program's durable contribution is the structural dichotomy and the constructed both-consistent
candidate, with the parity finding triply motivated -- anomaly matching prefers it (mechanistic), cosmic
birefringence requires it (data), chiral GW would test it (prediction) -- all agreeing on the right-handed
sign. The robust tier is sign-/symmetry-based and prefactor-audited; the schematic tier has robust
sign/structure but unsourced magnitudes; the correction and negatives are explicit. This supersedes the
earlier syntheses (v2.299, v2.315), which predate the v2.316 correction and the data climax.

## Honest scope

A synthesis/audit, not a new bound: it classifies prior findings and re-confirms the robust tier with the
engine's literal `check()` output (convex_hull + all four data constraints). ROBUST claims are
sign-/symmetry-based and prefactor-audited (v2.320) or encoding-independent (the parity-even `beta=0`
exclusion); SCHEMATIC claims have robust sign/structure but unsourced magnitudes; the v2.316 correction and
the negatives are carried forward. The constructed candidate is a representative both-satisfying point
(existence, not a unique optimum). The whole program is in a toy 8-coefficient Wilson basis with O(1)
placeholder prefactors -- "the right streets, the wrong house numbers" -- so the durable content is
structural, not numeric. Toy basis, O(1) prefactors.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_program_ledger.py --out experiments/results/v2.323/qnm_program_ledger.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_program_ledger.py -q
```

Result: the robust tier re-confirms (constructed satisfies both theory and data; no named framework does;
parity-even data-excluded; lqg theory-excluded; the moment-floor carving holds; the parity lines agree in
sign); `5 passed`.

## Next (v2.324)

The program is synthesized at a natural milestone. A genuinely fresh sector, or continued audits. Honest
negatives reported as part of the search.

## References

```text
this repo: v2.322 (theory-vs-data capstone), v2.321 (cosmic birefringence), v2.320 (prefactor audit), v2.316 (RFC correction), v2.309-v2.318 (structure + mechanism)
```
