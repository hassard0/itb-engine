# v2.394 - SWING: the candidate theory's actual UV cutoff -- a Dvali species scale of ~0.72 M_Pl, tightly predicted

## Question

After 27 swings characterizing the consistent region, one central quantity was never computed: the **actual UV
cutoff** of the candidate QG EFT -- where does gravity go strongly coupled and the EFT break down?

## Result (a near-Planckian, tightly-pinned cutoff)

The engine encodes Dvali's species scale (`species_scale_bound`, class B): with N light species,
`Lambda_species/M_Pl = 1/sqrt(N)`, `N = 1 + nu(|g_R2|+|g_C|+|g_R3|)`, `nu=2`, `N_max=3`.

```text
  constructed:  N = 1.95 species  ->  Lambda_species = 0.716 M_Pl
  family:       N in [1.37, 2.36] ->  Lambda_species/M_Pl in [0.65, 0.86] (mean 0.74) -- tightly bounded
  species bound N <= 3 only ~62% saturated (max 79%); strong-coupling floor 1/sqrt(3) = 0.577 M_Pl
  validity ladder: cutoff 0.72 M_Pl  <  Weyl^2 ghost 2.28x cutoff ~ 1.6 M_Pl (v2.385)  -- self-consistent
```

All 5/5 checks pass. The theory is a **near-Planckian EFT**, valid to ~0.7 M_Pl before a tower of ~2 effective
species makes gravity strong.

## What this establishes

The answer to "where does this quantum-gravity EFT break down": **not at M_Pl, but at ~0.7 M_Pl**, lowered
modestly by its own light tower -- a sub-Planckian but near-Planckian cutoff that the consistent region pins to
a ~30% window. The species bound is not saturated (~62%), so the theory keeps its cutoff comfortably above the
strong-coupling floor. This closes the EFT's validity ladder self-consistently: the cutoff sits below the
Weyl^2 ghost (at 2.28x the cutoff ~ 1.6 M_Pl, v2.385), so the ghost is trans-cutoff exactly as required, and
the species tower is what UV-completes the EFT at 0.72 M_Pl. It grounds the tower/species picture (v2.375/388):
the ~2 species here are the low-lying end of the infinite log-convex tower, and the species bound is the
information-theoretic (holographic, class-B) face of the same tower the moment conditions (A/C) generate -- the
one place the least-carving B sector (v2.374) sets a physical scale.

## Honest scope

`Lambda_species = M_Pl/sqrt(N)` with `N = 1 + nu(|g_R2|+|g_C|+|g_R3|)` is the engine's TOY encoding of Dvali's
species scale (`nu=2`, `N_max=3` toy O(1); counting species by a linear sum of `|couplings|` is a proxy --
physically N is the number of tower STATES below the cutoff, not a coupling sum). So the specific 0.72 M_Pl and
the [0.65, 0.86] window are toy-basis and scale with `nu`; `g_C` is identified with `g_R2`. The ROBUST content
is structural: the cutoff is sub-Planckian (N > 1 always, since the anomaly forces `g_R2 > 0`, v2.393, so there
is always a partial species tower) but near-Planckian (N stays O(1) because matter dominance + scale rigidity
keep the couplings O(0.1), v2.389/390), and TIGHTLY bounded because the feasible couplings are -- a ~30% window,
not a free cutoff. The ghost-ladder consistency reuses v2.385's O(1)-schematic ghost-mass relation, so "~1.6
M_Pl" is schematic; the robust point is the ORDERING (cutoff < ghost). This is a computation within the toy
species encoding, not a first-principles derivation. Robust content: the candidate is a near-Planckian EFT with
a tightly-pinned sub-Planckian cutoff (~0.7 M_Pl in this encoding), set by its light tower, ghost safely above.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_species_scale_cutoff.py --n 25000 --out experiments/results/v2.394/qnm_species_scale_cutoff.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_species_scale_cutoff.py -q
```

Result: constructed cutoff 0.716 M_Pl; family [0.65, 0.86]; species bound ~62% saturated; ghost ~1.6 M_Pl >
cutoff; `6 passed`.

## Next (v2.395)

The EFT's validity ladder is closed. Next: a fresh swing or a new consistency conjecture. Bold swings, honest
failures.

## References

```text
this repo: src/itb/constraints/species_scale.py (Dvali species scale), v2.385 (ghost above cutoff), v2.375/388 (string-like tower / light-coupling count), v2.393 (anomaly forces g_R2>0 -> N>1), v2.389/390 (matter dominance / scale rigidity), v2.374 (B/information sector carves least)
physics: Dvali (species scale); van de Heisteeg, Vafa, Wiesner (species scale & the swampland)
```
