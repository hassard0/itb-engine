# v2.275 - The Page curve: the entanglement signature of black-hole unitarity

## Question

A fresh QG capstone of the black-hole-entropy thread (v2.257/v2.258/v2.273/v2.274), addressing the
information paradox head-on. As a black hole evaporates, does the von Neumann entropy of its Hawking
radiation rise forever (information loss) or come back down (unitarity)?

## Result

The fine-grained radiation entropy is the MINIMUM of two saddles (the quantum-extremal-surface /
island formula): the coarse-grained Hawking answer early, the remaining-horizon answer late,
`S_rad = min(S_rad^thermal, S_BH^remaining)`. With `S_BH = (M/M_0)^2 S_0` and entropy conservation
`S_rad^thermal = S_0 - S_BH`:

```text
  M/M0     Hawking (monotonic)   Page (min, unitary)
  1.000    0.000                 0.000
  0.900    0.190                 0.190     (rising, no-island saddle)
  0.707    0.500                 0.500     <- Page point: the two saddles cross
  0.500    0.750                 0.250     (falling, island saddle)
  0.250    0.938                 0.063
  0.010    1.000                 0.000     (information returned)

  Page point: M = M_0/sqrt2, S_fine = S_0/2
  Page time:  t/tau = 1 - 2^(-3/2) = 0.6464   (via the M^3 evaporation law, v2.257)
```

The Hawking curve rises monotonically to the full initial entropy `S_0` (information lost); the Page
curve rises to a peak at the Page point (`M = M_0/sqrt2`, exactly half the entropy radiated) and then
comes back DOWN to 0 (a pure final state, information returned). All 6/6 consistency checks pass. The
turnover -- the radiation entropy decreasing after the Page time -- is the entanglement signature of
unitary evaporation.

## What this establishes

The Page curve caps the BH-entropy thread: the switch from the 'no-island' to the 'island' saddle at
the Page time is the modern resolution of the information paradox (Penington; Almheiri et al.), and the
same horizon area that sets the v2.257 entropy, the v2.273 greybody emission and the v2.274 area
quantum is the late-time island that brings the information back out. One object -- the horizon -- runs
the whole story from thermodynamics to unitarity.

## Honest scope

This is the standard PHENOMENOLOGICAL Page model: the fine-grained entropy is taken as
`min(S_rad^thermal, S_BH)`, the island-formula RESULT, modeled directly rather than derived from a
replica-wormhole / gravitational-path-integral computation. The coarse-grained
`S_rad^thermal = S_0 - S_BH` uses entropy conservation and OMITS the Page/greybody factor (Hawking
radiation is `~1.4-1.6x` more entropic than the BH's entropy deficit, v2.273), which shifts the exact
Page time modestly earlier -- so `t_Page ~ 0.646 tau` is the clean-model value, not a precise number.
`S_BH = A/4` is semiclassical, `M^3` evaporation is the leading law, and unitarity (`S_fine -> 0`) is
the ASSUMPTION whose entanglement signature the Page curve IS -- the paradox's resolution is
illustrated, not proven. A QG-structure / information-paradox result, not an engine constraint refit.

## Validation

```bash
.venv/Scripts/python.exe experiments/qnm_page_curve.py --out experiments/results/v2.275/qnm_page_curve.json
.venv/Scripts/python.exe -m pytest tests/test_qnm_page_curve.py -q
```

Result: the Hawking curve is monotonic to `S_0`; the Page curve turns over at `M_0/sqrt2` with
`S_fine = S_0/2`; the island `min` prescription holds; the Page time is `1 - 2^(-3/2) ~ 0.646`;
information returns (`S_fine -> 0`); `7 passed`.

## Next (v2.276)

Either (a) a fresh QG thread (the Hawking-Page phase transition, replica-wormhole entropy, or a new
sector), (b) the user-authorized `g_R4` core-engine extension, or (c) the dedicated analytic Leaver
solver.

## References

```text
Page, 'Information in black hole radiation', PRL 71 (1993) 3743
Penington, 'Entanglement wedge reconstruction and the information paradox', JHEP 09 (2020) 002
Almheiri, Engelhardt, Marolf, Maxfield, 'The entropy of ... an evaporating black hole', JHEP 12 (2019) 063
Almheiri, Hartman, Maldacena, Shaghoulian, Tajdini, 'The entropy of Hawking radiation', RMP 93 (2021) 035002
this repo: v2.257/v2.258 (BH entropy), v2.273 (greybody), v2.274 (area quantization)
```
